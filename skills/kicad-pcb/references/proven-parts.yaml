# Proven-parts ledger — part selection as COMPOUNDING CAPITAL.
#
# One entry per FUNCTION; candidates are parts this pipeline has actually
# paid to verify. Consulted FIRST at D-SPEC (sourcing spike) and D-ESC
# (part selection); every newly verified part is harvested back at release.
#
# Rules that keep this honest:
# - Entries require PROVENANCE: status is one of
#     shipped        — ordered/released on a named board
#     designed-in    — schematic+escape verified on a named board, not yet
#                      ordered
#     incident       — a NEGATIVE entry: verified to be a trap; the caveat
#                      is the value
#     unresolved     — the function has no verified answer yet; the next
#                      board that needs it runs the sourcing spike and
#                      founds the entry
# - Provenance names boards (allowed) — NEVER projects/ paths (C-ISO).
# - NO stock counts or prices here — they rot in hours; jlc_stock checks
#   freshness at order time. The ledger stores IDENTITY + judgment.
# - escape blocks use the same schema as part.yaml (escape_check emits
#   them); tier names come from fab_tiers.yaml.
# - layout_refs: routed precedents consulted for the part's LOCAL layout
#   (datasheet layout-example section, manufacturer EVM, OSHWLab project) —
#   named so the next board starts from the same references instead of
#   re-searching. Study-then-re-derive; never imported copper (M3).

functions:

  battery-input-xt60:
    candidates:
      - mpn: XT60PW-M
        maker: AMASS
        lcsc: C98732
        package: XT60 horizontal PCB mount
        escape: {style: connector, pitch: 7.2, tier_required: jlc_2layer_default}
        status: shipped
        provenance: usb-power-3s v1.0-v1.3; re-verified usb-pwr-hub-3s 2026-07-21
        gotchas:
          - "PAD 1 IS '-' — polarity is a PART FACT; assert pad-1 net in
             audit_board (a reversed XT60 shipped once: spf fa0b9c1)"

  buck-controller-high-current:   # >3A output, controller + external FETs
    candidates:
      - mpn: LM5145RGYR
        maker: Texas Instruments
        lcsc: C485912
        package: VQFN-20 3.5x4.5 P0.5
        escape: {style: qfn, pitch: 0.5, tier_required: jlc_4layer_advanced}
        status: shipped
        provenance: usb-power-3s v1.0-v1.3 ("ADVANCED option REQUIRED" ORDER_README)
        gotchas:
          - "0.5mm VQFN: ADVANCED tier is REQUIRED (0.25/0.15 vias) — the
             cost is proven and orderable, decide it at D-TIER not at DRC"
      - mpn: LM5116MHX/NOPB
        maker: Texas Instruments
        lcsc: C13755
        package: HTSSOP-20-EP P0.65
        escape: {style: leaded, pitch: 0.65, escapes_worst_side: 8,
                 tier_required: jlc_4layer_standard,
                 conditions: [escape-corridor]}
        status: designed-in
        provenance: usb-pwr-hub-3s (cleanroom v3, 2026-07-21) — ERC 0, placed, routed
        gotchas:
          - "ESCAPE BUDGET WALL (ADR-0008): ~8 escapes on one 0.65mm side;
             at standard tier NO via fits between adjacent pins
             (0.65-0.3 drill = 0.35 < 0.5 hole-to-hole). escape_check v2
             grades standard CONDITIONAL on `escape-corridor` — reserve
             the lane at placement (floorplan `escape_corridors:`) or take
             the advanced tier. Standard-tier closure is UNPROVEN as of
             2026-07-21 (v3 stalled without the corridor)."
          - "No internal boot diode — external VCC->HB diode REQUIRED"
          - "VCCX: tie to 5V output for efficiency (gate drive drops to
             5V — check FET Rds(on) at Vgs=5V); GROUND if unused, never open"
        layout_refs:
          - "TI datasheet SNVS499I: Figure 7-1 / Table 7-1 worked 5V/7A
             reference design (pp. 21-30) + layout guidelines section —
             read in full by the 2026-07-21 design extraction"
          - "TI publishes an LM5116 EVM with full design files — pull the
             current EVM id from ti.com before the next board's placement"

  buck-integrated-high-current:   # integrated-FET bucks >3A
    candidates:
      - mpn: SY8368QNC
        maker: Silergy
        lcsc: C125897
        package: QFN3x3-10 P0.45-0.5
        escape: {style: qfn, pitch: 0.45, escapes_worst_side: 6,
                 tier_required: jlc_4layer_standard,
                 conditions: [outward-only-local]}
        status: shipped
        provenance: >
          BOTH outcomes are paid-for ground truth: xt60-usb-supply-rerun
          v1.0-v1.0.2 SHIPPED it x2 at STANDARD tier (measured on the sealed
          board 2026-07-21: one 0.2-0.25mm outward track per fine pad, zero
          vias between pads, PG sacrificed as NC, all escape nets terminated
          in adjacent passives); 3s-power-board-cleanroom v1/v2 (2026-07-20)
          STALLED on the same part when placement stranded those passives —
          the D-ESC/D-TIER gates exist because of that failure.
        gotchas:
          - "STANDARD tier is CONDITIONAL, not impossible: it requires
             single-side OUTWARD-ONLY escape — every fine-pad net local
             (passive hard against the pin, D-ADJ), fine side facing open
             copper, no escape crossings/layer drops, PG-class pins NC'd if
             unused. If any escape must cross or via down, it is advanced
             tier or a re-place. escape_check v2 (Phase F, 2026-07-21)
             grades this: with escapes_worst_side declared, standard is
             CONDITIONAL on `outward-only-local` (record the condition in
             the part.yaml or P-ESC fails); without the declared budget it
             stays UNCONDITIONAL advanced — both directions pinned in
             tests/t4_regressions.py"

  usb-c-receptacle-high-current:
    candidates:
      - mpn: TYPE-C-31-M-12A
        maker: HRO
        lcsc: C5337088
        package: USB-C 16P SMT+TH hybrid
        escape: {style: connector, pitch: 0.5, tier_required: jlc_2layer_default}
        status: designed-in
        provenance: usb-pwr-hub-3s (cleanroom v3, 2026-07-21); VBUS pads
          rated for high current per HRO datasheet
        gotchas:
          - "A1..B12 + SH pad names are ALPHANUMERIC — tscircuit drops the
             part SILENTLY (S-COUNT incident 2026-07-21). Run tsx_preflight,
             map pads in parity_padmap.txt BEFORE the first tsci build"

  # ------------------------------------------------------------------
  # usb-hub-3s harvest (cleanroom v4, 2026-07-21) — 9 function entries
  # from the released v1.0 board (DRC 0/0/0). Merged Phase F 2026-07-21;
  # one transcription fix vs the v4 worktree copy: TPS2557 tier_required
  # corrected to jlc_4layer_standard (qfn @ 0.65: 0.6 + 0.127 > 0.65 at
  # 2-layer — the v4 board's own part.yaml already said standard).
  # ------------------------------------------------------------------

  pd-source-5v5a:                 # compliant 5V/5A USB-C source
    candidates:
      # PREFER THIS for a 5V-ONLY port: a PURE PD-source PHY on an external 5V
      # buck rail — NOT a buck-boost SoC. Refutes the IP6559 entry's implicit
      # "you need a buck-boost" reading for fixed-5V ports (usb-hub-3s-v2
      # 2026-07-22: E-TOPO showed a 5V-only USB-C rail is a plain buck; the PD
      # part only does CC/BMC signalling + external-FET gate drive).
      - mpn: TPS25740ARGER
        maker: Texas Instruments
        lcsc: C544309
        package: VQFN-24 RGE 4x4 P0.5 (EP 2.7mm sq)
        escape: {style: qfn, pitch: 0.5, tier_required: jlc_4layer_advanced}
        status: designed-in
        provenance: usb-hub-3s-v2 (2026-07-22) — schematic gate GREEN (ERC 0,
          E-INV 15/15), NOT yet routed/ordered. Pure PD-source PHY + external
          back-to-back N-FETs; sits on a separate 5V/5A buck rail.
        layout_refs:
          - "TI SLVSDG8B Fig 19 (back-to-back dual-FET receptacle) + Fig 36 (basic schematic BOM)"
          - "SLVSDG8B Fig 63/66 (power-hub on external buck rail) + Sec 11 Fig 67 Kelvin ISNS"
          - "SLVUAP7A EVM Table 3 strap jumper truth (H3 = 5V-only/5A)"
        gotchas:
          - "STRAP TRAP: EN9V is active-LOW for enable-9V — tie it HIGH (to DVDD)
             for a 5V-ONLY contract. Low silently advertises 9V the buck can't supply."
          - "HIPWR/PSEL are 4-STATE ADC pins: '100k Rsel to GND' != 'direct short
             to GND'. 5V-only+5A needs HIPWR = 100k-to-GND (via-Rsel state), PSEL
             100k-to-GND. GD must be tied HIGH (VAUX) — internal pulldown, floats off."
          - "RECEPTACLE => BACK-TO-BACK dual FETs (Fig 19) + VBUS bulk >10uF; single
             FET is captive-cable only. GDNG is a charge-pump high-side driver."
          - "NRND lifecycle (all TPS2574x RGE, Jan-2026 addendum) — stock ~2974
             2026-07-22, order-day recheck MANDATORY; TI successor TPS65987D is a
             heavier config-tool part, NOT a pin-swap."
          - "OVP/UVP window at 5V is TIGHT (~[3.9, 5.8]V) — the feeding buck's
             transient overshoot/droop must stay inside it or the chip faults."
          - "VQFN-24 0.5mm 4-sided => jlc_4layer_advanced (via-in-pad EP). This is
             the ONLY advanced-forcing part on a 5V-only hub; a 3A plain-Rp port
             (no PD chip) would keep the board STANDARD tier."
      - mpn: IP6559-C
        maker: Injoinic
        lcsc: C5140592
        package: QFN-48 7x7 P0.5 EP5.4
        escape: {style: qfn, pitch: 0.5, tier_required: jlc_4layer_advanced}
        status: shipped
        note: "OVER-CAPABLE for a 5V-only port (buck-boost + 4 external FETs +
          inductor + 16A trunk). Correct ONLY for a TRUE multi-voltage PD port
          (5-20V). For fixed 5V, prefer TPS25740A above (usb-hub-3s-v2 finding)."
        provenance: usb-hub-3s (cleanroom v4, 2026-07-21) — routed DRC
          0/0/0, twin+pin-review verified, released v1.0; 100W buck-boost
          PD3.0 source SoC with e-marker read (5A only via PD per spec)
        layout_refs: ["IP6559 DS V1.4 Fig 8 (single-C reference)", "Fig 9 (Vconn e-marker drive)"]
        gotchas:
          - "PDO set is configured by ONE resistor on GPIO0 (DS note 1) whose
             value table is APP-NOTE-ONLY — populate as DNP + first-power PD
             analyzer check (ADR pattern), never guess the value"
          - "0.5mm QFN-48: advanced tier REQUIRED (0.25/0.15 POFV) — a
             48-pin 4-sided QFN has no outward-only conditional form"
          - "chip does NOT carry coil current (external H-bridge) — LX1/LX2
             chip pins are gate-return/sense class, thin taps are fine"
          - "stock is VOLATILE (62 on 2026-07-21) — order-day recheck is
             MANDATORY, alternates unverified"
      - mpn: TYPE-C-31-M-12A
        note: "see usb-c-receptacle-high-current for the matching receptacle"

  power-nfet-30v:                 # logic-level switching NFET, 30V class
    candidates:
      - mpn: AON6354
        maker: Alpha & Omega
        lcsc: C404363
        package: DFN 5x6-8L (PowerPAK SO-8 pattern)
        escape: {style: qfn, pitch: 1.27, tier_required: jlc_2layer_default}
        status: shipped
        provenance: usb-hub-3s (cleanroom v4, 2026-07-21) x7 (buck +
          buck-boost H-bridge + path switch), twin adjudicated, released
        gotchas:
          - "KiCad PowerPAK_SO-8_Single merges drain leads+paddle into ONE
             pad '5' — jlc_twin reports PAD-MISMATCH best=none vs JLC's
             5-8+EP9 split; adjudicate, the copper coincides"
          - "escape style is 'qfn' in checker vocabulary (DFN/SON ring) even
             though the body says DFN — declaring 'dfn' fails P-ESC"
          - "rejected sibling NCEP40T15AGU: Vth 4V max with no 4.5V Rds spec
             — not logic-level drivable from a 5V gate rail"

  reverse-polarity-pfet-20v:
    candidates:
      - mpn: AON6403
        maker: Alpha & Omega
        lcsc: C2760089
        package: DFN 5x6-8L (PowerPAK SO-8 pattern)
        escape: {style: qfn, pitch: 1.27, tier_required: jlc_2layer_default}
        status: shipped
        provenance: usb-hub-3s (cleanroom v4, 2026-07-21) — 3S battery
          reverse-polarity high-side switch, Vgs +-20V covers 12.6V direct
        gotchas: ["same merged-pad-5 twin adjudication as AON6354"]

  usb-power-dist-switch-2ch-class:   # per-port 5V current limit switch
    candidates:
      - mpn: TPS2557DRBR
        maker: Texas Instruments
        lcsc: C130056
        package: VSON-8 DRB 3x3 EP
        escape: {style: qfn, pitch: 0.65, tier_required: jlc_4layer_standard}
        status: shipped
        provenance: usb-hub-3s (cleanroom v4, 2026-07-21) x3 — RILIM 36.5k
          -> 2.72-3.29A window derived from DS equations (DETAIL_DESIGN)
        gotchas:
          - "ILIM window is WIDE (DS min/typ/max equations) — derive BOTH
             ends; the max must stay under the upstream rail budget"

  usb-dcp-controller:
    candidates:
      - mpn: TPS2513ADBVR
        maker: Texas Instruments
        lcsc: C473910
        package: SOT-23-6 DBV
        escape: {style: leaded, pitch: 0.95, tier_required: jlc_2layer_default}
        status: shipped
        provenance: usb-hub-3s (cleanroom v4, 2026-07-21) — dual-channel
          DCP auto-detect, one chip per two A-ports
        gotchas:
          - "the 2.7/2.7V 'Apple 2.4A' Divider3 mode is the A-VARIANT ONLY
             (device-options table). The non-A TPS2513 tops out at ~2.1A
             Apple modes — a fresh-context pin review caught the non-A on a
             BOM claiming A behavior (2026-07-21). Order C473910, not C44770"

  usb-a-receptacle:
    candidates:
      - mpn: KH-AF90DIP-112
        maker: Shenzhen Kinghelm
        lcsc: C503996
        package: USB-A female right-angle THT
        mates: receptacle
        escape: {style: connector, pitch: 2.0, tier_required: jlc_2layer_default}
        status: shipped
        provenance: usb-hub-3s (cleanroom v4, 2026-07-21) — vendored
          footprint from the vendor drawing (O1.0 pins 2.5/2.0/2.5, O3.0
          shells 13.24); jlc_twin fit 0.00mm vs JLC's own model
        gotchas:
          - "USB-A contacts are a 1.5A-class system — there is no '2.5A
             rated' classic A receptacle; 2A/2.4A DCP use is industry
             practice, record the disposition (I2R ~0.19W at 2.5A), do not
             silently pass it"
      - mpn: 1001-011-01101
        maker: CNC Tech
        lcsc: null
        mates: plug
        status: incident
        provenance: usb-hub-3s (cleanroom v4, 2026-07-21) — caught at
          fresh-context pin review, NEVER ordered (ADR-0006)
        gotchas:
          - "this is a USB-A MALE PLUG ('USB 4P AM SMT' on its own drawing)
             rated 1.5A, present in KiCad's official library alongside
             receptacles. Every distributor listing that calls a USB-A part
             a 'connector' must be gender-checked against the DRAWING TITLE
             — footprint+netlist+silk can all be consistently wrong together.
             Record `mates: plug|receptacle` in part.yaml; the release gate
             pins the pin-review artifact per connector (t4_regressions)"

  fuse-holder-mini-blade:
    candidates:
      - mpn: "3568"
        maker: Keystone
        lcsc: C5249699
        package: MINI blade fuse clip (single)
        escape: {style: connector, pitch: 3.4, tier_required: jlc_2layer_default}
        status: shipped
        provenance: usb-hub-3s (cleanroom v4, 2026-07-21)
        gotchas:
          - "C5249699 is ONE clip; the KiCad Fuseholder_Blade_Mini_Keystone_3568
             footprint is the TWO-clip holder — order qty 2 per board, and
             expect a twin PAD-MISMATCH (granularity, not geometry)"

  buckboost-power-inductor-10uH:
    candidates:
      - mpn: MWSA1707S-100MT
        maker: Sunlord
        lcsc: C5240401
        package: 17.2x17.2 shielded
        escape: {style: connector, pitch: 12.0, tier_required: jlc_2layer_default}
        status: shipped
        provenance: usb-hub-3s (cleanroom v4, 2026-07-21) — 12.7A peak
          derivation in DETAIL_DESIGN; rejected APH1265T100M (DCR 16.5mR,
          Isat 12.5A < peak)
        gotchas: ["17.2mm body: floorplan for it FIRST — it forced a board
             growth 110x82 -> 110x90 when added late"]

  usb2-esd-array:
    candidates:
      - mpn: USBLC6-2SC6
        maker: ST
        lcsc: C7519
        package: SOT-23-6
        escape: {style: leaded, pitch: 0.95, tier_required: jlc_2layer_default}
        status: shipped
        provenance: usb-hub-3s (cleanroom v4, 2026-07-21) x3 — pass-through
          pairs 1-6/3-4 in-line with the port data nets
        gotchas: []

  audio-opamp-dual-low-noise:
    candidates:
      - mpn: OPA1678IDR
        maker: Texas Instruments
        lcsc: C192421
        package: SOIC-8 3.9x4.9 P1.27
        escape: {style: leaded, pitch: 1.27, tier_required: jlc_2layer_default}
        status: shipped
        provenance: crow-array-pod v1.0/v1.1; re-verified crow-mic-pod v1.0
        layout_refs:
          - "TI D0008A land/stencil drawing 4214825/C 02/2019 (pad-span 5.4mm vs KiCad IPC 4.95mm — twin PAD-GEOM adjudication evidence)"
        gotchas:
          - "twin PAD-GEOM d0.47mm vs JLC CAD is the KiCad-IPC vs TI-datasheet
             SOIC-8 pad-length split — leads land on both; adjudicate, don't
             re-footprint"
          - "rotation DB '^SOIC-',270 is assembly-zero truth; twin's EDA-frame
             fit says 90 — keep 270, eyeball JLC preview pin-1 dot"

  esd-2ch-bidirectional-sot553:
    candidates:
      - mpn: TPD2E2U06DRLR
        maker: Texas Instruments
        lcsc: C1972959
        package: SOT-553
        escape: {style: leaded, pitch: 0.5, tier_required: jlc_2layer_default}
        status: shipped
        provenance: crow-array-pod v1.0/v1.1; crow-array-central v1.0; re-verified crow-mic-pod v1.0
        gotchas:
          - "alternate code C51886141 exists for the same MPN"

  magnetic-transducer-smd-5v:
    candidates:
      - mpn: CMT-8504-100-SMT-TR
        maker: CUI Devices
        lcsc: C22359707
        package: 8.5mm SMD transducer (vendored 4-pad footprint)
        escape: {style: connector, pitch: 3.0, tier_required: jlc_2layer_default}
        status: shipped
        provenance: crow-array-pod v1.0/v1.1; re-verified crow-mic-pod v1.0
        gotchas:
          - "JLC stock chronically THIN (182 on 2026-07-18, 104 on 2026-07-21)
             — order-day re-check mandatory; Digi-Key hand-solder fallback"
          - "inductive load: needs flyback/TVS clamp at the driven end when
             switched (crow pod ADR: SS14 populated + SMAJ6.0A empty position)"

  rj45-jack-shielded-tht:
    candidates:
      - mpn: RJHSE-5384
        maker: Amphenol
        lcsc: C9900035627
        package: RJ45 right-angle shielded, LEDs, THT (KiCad RJ45_Amphenol_RJHSE538X)
        escape: {style: connector, pitch: 1.02, tier_required: jlc_2layer_default}
        status: shipped
        provenance: crow-array-pod v1.1; crow-array-central v1.0 (x8); re-verified crow-mic-pod v1.0
        layout_refs:
          - "Amphenol catalogue RECOMMENDED PCB LAYOUT p.4 RJHSE-548X + p.6
             RJHSE-538X-04, April 2015 ed. (hole-for-hole match to the KiCad
             footprint, independently re-read twice)"
        gotchas:
          - "C99* code = consign-only placeholder, permanently stock 0, NO
             EasyEDA CAD — twin needs an evidence-backed NO-CAD adjudication;
             hand-solder line"
          - "NPTH o3.25 board-lock posts: DRC hole clearance 0.25 > routed
             copper clearance — fence the posts with keepouts before KRT or
             collect hole_clearance violations"
          - "mating face is on the SNAP-POST side of the hole field; LED
             tails mark the REAR (a face-side mixup survived one review)"
          - "dense GND tail field starves thermal spokes on 2-layer pours —
             set zone connection SOLID for the jack GND tails"

  electret-capsule-wired:
    candidates:
      - mpn: AOM-5024L-HD-R
        maker: PUI Audio
        lcsc: none
        package: 9.7mm electret capsule, wire leads to 2.54mm pads
        escape: {style: connector, pitch: 2.54, tier_required: jlc_2layer_default}
        status: shipped
        provenance: crow-array-pod v1.0/v1.1; re-verified crow-mic-pod v1.0
        gotchas:
          - "not in JLC catalog — hand-wire (Digi-Key 668-1538-ND); capsule
             solder limit ~2s per lead"
          - "3.9k bias from RC-filtered rail (100R+100u) — bias string on the
             FILTERED node, op-amp on the raw rail (PSRR covers it)"

  # ---- mixed-signal-audio-hub harvest (crow-recorder-central v1.0, sealed
  #      2026-07-22): USB-audio-recorder core. All escape blocks verified by
  #      escape_check at the board's declared tier; pin maps verified by
  #      fresh-context reviews; topology verified by the v1.0 red-team. ----

  usb-audio-soc-xcore:
    candidates:
      - mpn: XU316-1024-TQ128-I24
        maker: XMOS
        lcsc: C6938291
        package: TQFP-128, 14x14mm body, 0.40mm pitch, 4.7mm exposed GND paddle (pin 129)
        escape: {style: leaded, pitch: 0.4, tier_required: jlc_6layer_smallvia}
        status: shipped
        provenance: crow-recorder-central v1.0
        gotchas:
          - "STOCK IS THE HARD PART: JLC assembly stock is chronically 0 — it
             is a CONSIGNMENT / global-sourcing line (ADR-0013). Plan the
             consign at commission, not at order (D-SPEC sourcing spike)."
          - "0.4mm TQFP-128 does NOT close full routing at 4L — the escape
             geometry alone hints 4L-advanced, but real DRC needed 6L +
             SMALL-VIA (0.30/0.15) via-in-pad on the EP and inner rings
             (ADR-0008 escalation, ADR-0009 small-via, D-TIER ADR-0012)."
          - "EP (pin 129) = GND: vendored footprint carries the EP pad + a
             4x4 (16) thermal-via grid per XMOS 14.4; the duplicate pad-129
             numbering trips jlc_twin PAD-MISMATCH — adjudicate (artifact),
             electrical coverage is the 129-pin fresh-context pin review."
          - "Power domains: VDD core 0.9V (buck), VDDIO banks 3.3V, VDDIOB18
             + USB_VDD18 on 1.8V. LV_x_N strap pins HIGH (3V3) select 3.3V IO
             mode — must match the VDDIO rail or the bank mis-drives."
          - "JTAG (TDI/TDO/TMS/TCK) sits on the 1.8V IOB bank (abs-max 1.98V)
             — bring-up must use a 1.8V-level probe, not 3.3V."
        layout_refs:
          - "XMOS xcore.ai TQ128 datasheet Fig 22 (package) + §14.3/14.4
             (EP + 16-via 4x4 grid); board escape re-derived, copper not imported"

  audio-adc-8ch-tdm:
    candidates:
      - mpn: PCM1865DBTR
        maker: Texas Instruments
        lcsc: C181312
        package: TSSOP-30 (DBT), 0.5mm pitch
        escape: {style: leaded, pitch: 0.5, tier_required: jlc_4layer_advanced}
        status: shipped
        provenance: crow-recorder-central v1.0 (2 units, TDM daisy = ch1-4 / ch5-8)
        gotchas:
          - "CLOCK TRAP: drive MCLK into SCKI (pin 15, 3.3V) and TIE XI
             (pin 10) TO GND — XI abs-max is 2.1V; feeding MCLK into XI
             overstresses it (PCM1865 part.yaml)."
          - "Two-chip TDM: share BCLK/LRCK/MCLK via SOURCE-SERIES 33R links
             (one 33R per shared clock at the driver); I2C address strapped
             by AD pin (GND/3V3 -> 0x4A/0x4B), MD0=GND selects I2C control."
          - "Split supply: AVDD on the QUIET analog rail (own LDO, 3V3A),
             DVDD/IOVDD on the digital 3V3; join only at GND. AVDD isolation
             is only as good as its local bulk+HF decoupling."

  buck-1a5-sot563:
    candidates:
      - mpn: AP61102Z6-7
        maker: Diodes Inc
        lcsc: C5224055
        package: SOT-563, 6-lead, 0.5mm pitch
        escape: {style: leaded, pitch: 0.5, tier_required: jlc_2layer_default}
        status: shipped
        provenance: crow-recorder-central v1.0 (5V->3V3 and 5V->0V9 core)
        gotchas:
          - "EN doubles as MODE: EN tied to VIN = PFM (light-load tones);
             drive EN < VIN-200mV from logic for FORCED-PWM (lower ripple —
             the audio-board choice). This board runs PFM (digital rails +
             LDO-buffered analog make it tolerable) but forced-PWM is the
             cleaner default for a mixed-signal board."
          - "VIN abs-max 6.5V, internal OVP stops switching at 6.3V but does
             NOT protect the VIN pin from over-voltage — the 5V rail needs
             its own OVP if the input can exceed 6.3V."
          - "Sequence a dependent rail with PG: 0V9 core EN <- 3V3 buck PG,
             so the core comes up after its IO rail."
          - "Keep the VIN input cap <2mm from VIN/GND — the hot loop is the
             EMI/ripple driver (red-team L2: 6mm was flagged P1)."

  clock-buffer-2ch-lvcmos:
    candidates:
      - mpn: NC7NZ34K8X
        maker: onsemi (Fairchild)
        lcsc: C232798
        package: US8 (MO-187 CA), 0.5mm pitch
        escape: {style: leaded, pitch: 0.5, tier_required: jlc_2layer_default}
        status: shipped
        provenance: crow-recorder-central v1.0 (MCLK fanout to 2 ADCs)
        gotchas:
          - "3-input triple buffer used as a 1->2 fanout: tie all inputs to
             MCLK_SRC, take Y1/Y2 to the two loads, leave the unused output
             open with its input still driven."

  analog-ldo-quiet-3v3:
    candidates:
      - mpn: XC6227C331PR-G
        maker: Torex
        lcsc: C6035451
        package: SOT-89-5
        escape: {style: leaded, pitch: 1.5, tier_required: jlc_2layer_default}
        status: shipped
        provenance: crow-recorder-central v1.0 (5V->3V3A analog rail for 2x PCM1865 AVDD)
        gotchas:
          - "CE (pin 1) is active-high WITH internal pulldown (type C):
             floating CE = OFF. Tie CE to VIN for always-on."
          - "SOT-89-5 twin trap: JLC's fetched footprint numbers BOTH the
             center lead and the tab '2' (dup pad) -> PAD-MISMATCH artifact,
             adjudicate; tab = pin 2 = VSS on GND (Torex ETR03054-006)."
          - "VIN abs-max 6.5V, no OVP — same 5V-rail OVP caveat as the bucks."

  ldo-1v8-200ma:
    candidates:
      - mpn: TLV70018DDCR
        maker: Texas Instruments
        lcsc: C79924
        package: SOT-23-5 (DDC / X2SON alt)
        escape: {style: leaded, pitch: 0.95, tier_required: jlc_2layer_default}
        status: shipped
        provenance: crow-recorder-central v1.0 (3V3->1V8 for XU316 VDDIOB18/USB_VDD18)
        gotchas:
          - "On the BOM as the in-stock 1V8 LDO; the pin-compatible TCR2LF18
             (Toshiba) is the exact-MPN fallback (ADR-0015). Fed from 3V3,
             not 5V — comes up right after 3V3 so it is never the last rail."
