# STATUS beacon — pluto-cal-switch (live head of the journal)

<!-- reader parses from here down -->
stage:   06-ROUTING-8-VIOLATIONS-8-UNCONNECTED-NOT-PROMOTED
step:    "THE PIPELINE REACHES seed_stubs BY ITSELF (prep -> route --race 4 -> import --no-fill -> taps -> stitch, EXIT 0, `21 pin(s) served, 32 segments, 0 refused`, no hand unfill) and the DRC residue is down from 83/8/0 to 8/8/0 across three MEASURED, CLASSIFIED reductions. THE CHAIN IS STILL NOT PROMOTED AND route.final: IS STILL COMMENTED OUT — 0/0/0 is the gate and promoting a dirty chain seals it."
measure: "ALL MEASURED 2026-07-30, UNPIPED, REAL EXIT CODES. FULL DRC (kicad-cli pcb drc --severity-all --refill-zones --schematic-parity) = 8 / 8 / 0, DOWN FROM 83 / 8 / 0 on the first full run this board has ever had, in three classified steps: 83 -> 16 (the mask aperture) -> 8 (the tap widths). THE 67 THAT WENT AWAY WERE ONE DEFECT, NOT 67: every solder_mask_bridge finding (12 distinct pairs) was inside U_SW1/U_SW2 and was the SAME single mask aperture the BGS12WN6 footprint deliberately carries — the datasheet land (Rev 2.9 Fig 5) has a 0.15 mm land-to-land gap, below JLCPCB's 0.2 mm mask-dam minimum, so per-pad openings are UNMANUFACTURABLE. Declared to KiCad as `allow_soldermask_bridges` in the footprint that OWNS the fact, not waived downstream. THE 8 track_width FINDINGS ARE ALSO GONE AND WERE ALSO NOT ROUTING DEFECTS: all eight were widths `route.yaml taps:` DECLARES (4x 3V3_SW at 0.25 vs PWR 0.4; 3x USB die-end incl. the B.Cu hop vs USB_DP 0.33; 1x at 0.20 inside scoped_taper_u_mcu) with no rule area joining the two files. Closed by three NEW bounding-box rule areas (tap_3v3_sw1, tap_3v3_sw2, tap_usb_die) rather than by widening taper_u_sw1/taper_u_mcu, which would have relaxed RX_PLUTOn's impedance width and the whole 3V3 net. VIOLATIONS REMAINING, CLASSIFIED: starved_thermal 3 (J_USB.6, U_HDR_ESD.4, C_CTRL1.2 — 1 spoke against a 2-spoke minimum); via_dangling 3 (2 GND + 1 HDR_STATE_GPIO — prune_stitch_dangling was ADDED and ran, and did NOT clear them, so they are ROUTER vias, not stitcher vias, and the pass is correctly scoped; the HDR_STATE_GPIO one is part of a single-net cluster with the track_dangling and one unconnected); clearance 1 (VREG_VOUT vs 3V3 on In2.Cu at 0.0727 mm against 0.15); track_dangling 1 (HDR_STATE_GPIO). UNCONNECTED 8, AND THEY ARE FOUR DIFFERENT DEFECTS, NOT ONE STORY (this is the pluto-rx2-8way lesson landing on this board): 4x GND pad -> GND zone / pad (U_SW1.2, U_SW2.2, C_DV1.2, C_IO3.2 — these are pad_rescue's 4 unserved of 92); 1x RF_CTRL_SW2 U_SW2.6 to a 0.0200 mm stub; 2x VREG_VOUT (U_MCU.50 to a 0.1414 mm In2 stub, U_MCU.45 to U_MCU.50) — the ONLY two that are the inherited D-BACK; 1x HDR_STATE_GPIO track-to-track, which pairs with the track_dangling. HOLE_TO_HOLE 0.5 -> 0.25 AND THAT UNBLOCKED THE STITCH GATE: 5 escape vias walled in at 0.5 (gaps 0.282/0.350/0.373/0.404/0.450) and ALL are legal at the declared tier's published 0.25. It is arithmetic, not density: two adjacent escape vias on the RP2040's 0.400 mm pitch at the tier's 0.15 mm drill are pitch-drill = 0.250 mm apart EXACTLY, so at 0.5 every adjacent-pin QFN-56 escape via was a violation BY CONSTRUCTION — while ADR-0010 raised the tier to advanced with that very escape as justification #1. floorplan.yaml's own note pre-authorised this ('if a future escape genuinely walls in at 0.5 ... that is the moment to spend it'); ORDER-TIME OBLIGATION recorded. RACE: 4 concurrent chains, ALL FOUR identical at unconnected=3 violations=10, so the residue is STRUCTURAL, not stochastic — do not re-race it hoping. WIDTH 0.36 mm holds: rf_copper VERIFIES 14 paths / 32 segments / 18 nets / 0 vias and D4 SPREAD = 0.000000 mm = 0.0000 ps BY CONSTRUCTION. Schematic side green: E-INV 42/42, E-ADR 10/10, S-NETMERGE 48/48, S-COUNT 4/4, parity 0/49 nets over 263 nodes. audit_board still EXIT 1 on I9 (23 refdes silk labels), PRE-EXISTING and not waived."
state:   working
op_pid:  none
next:    "FIVE VIOLATIONS AND EIGHT UNCONNECTED REMAIN, ALL NAMED, NONE A RE-RACE (all four race candidates were identical, so the residue is STRUCTURAL). (1) THE HDR_STATE_GPIO CLUSTER IS ONE DEFECT WEARING THREE CLASS NAMES: track_dangling (3.6 mm free end) + via_dangling + the track-to-track unconnected are all one net whose chain the `rest` wave left broken. drop_dangling max_length is 1.0 and this tail is 3.6, so it is not sweepable — it wants a tap, not a bigger broom. Fix it as ONE thing. (2) via_dangling x2 on GND: ROUTER vias, not stitcher vias — prune_stitch_dangling was added, ran, and correctly did not touch them. (3) starved_thermal x3 (J_USB.6, U_HDR_ESD.4, C_CTRL1.2): 1 spoke against a 2-spoke minimum; raise spoke count or solid-connect those three pads. (4) clearance x1: VREG_VOUT vs 3V3 on In2.Cu at 0.0727 mm. (5) pad_rescue 4 unserved of 92 -> 4 GND unconnected; U_SW1.2/U_SW2.2 are the BGS12WN6 SOLE ground pin whose OUTWARD via ARCHITECTURE sec.10.7 already specifies, so they want an explicit seed stub with a via, not a rescue ring. (6) U_MCU.45/.50 VREG_VOUT LAST: the untried remedy is a tap through the UPPER corridor y[30.880,31.740] with a B.Cu hop, NOT a placement change — the named x-slot IS the documented load-bearing USB pair escape corridor. THEN promote to 03_src/route/, set route.final:, commit."
updated: 2026-07-30T00:00:00
