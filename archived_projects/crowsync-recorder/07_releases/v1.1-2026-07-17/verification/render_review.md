# Render review — crowsync-recorder v1.1 (schematic-only change)

v1.1's schematic is generated by the schwriter2 LAYOUT ENGINE: structure-only
declarations (regions/rows/chains), every coordinate computed from text
ENVELOPES ("envelopes are the schematic's courtyards", canon S6). Fab files
byte-identical to v1.0-2026-07-16.

- 51 cells, 13 real wires: USB D+ entry (J1->D1 ESD), D+ series->pullup
  node, VBUS filter->pin cap, XTO->load cap, LDO OUT->output cap, mic bias
  FB->reservoir cap, mic series R->coupling cap->preamp IN+ (2 wires),
  feedback node R11->R12 + gain leg R12->C20, amp out->VINL coupling cap,
  PPS divider chain R14->R15->R16 (2 wires).
- Collision-free BY CONSTRUCTION and verified twice: the engine's internal
  S-OCCL asserts 0 at emission; policy_audit's independent label-aware
  S-OCCL confirms 0 (was 34 waived in v1.0 — waiver REMOVED, cleared not
  waived). Section boxes disjoint by construction (packed).
- Full-page + quadrant-crop visual inspection (orchestrator, PNG at
  130dpi): no double-printed plates, no box collisions, wires legible,
  regions in reading order 1-6, no_connect crosses visible on all 9
  sanctioned floats (J1 A8/B8, U3 pin 4, U1 pins 5/6/7/15/16/25).
- Gates independently re-verified: ERC 0 TOTAL (v1.0 baseline of 51
  endpoint_off_grid warnings eliminated — schwriter2 is 1.27-grid-exact),
  board DRC 0/0/0 with schematic parity on the UNTOUCHED board (pcb sha
  unchanged c764ee86), netlist node-for-node identical to the pre-port
  schematic (44 nets / 165 nodes), policy_audit full mode zero FAIL.
- S6 readability: the mic signal path, PPS divider, LDO output, VBUS
  filter and crystal load are now DRAWN; remaining fan-out label-based by
  design (T3). Grade: READABLE for story paths.
- Known label-only spots (documented engine compromises, T3 fallback):
  C14->U3 VIN (facing nets differ across the cap: engine chains may not
  re-net), J2/J3 JST headers (single-sided L-pin symbols — no right-side
  pin to chain from), C9->U1 VINL and C10->U1 VINR (codec is in region 2,
  chains are row-local). Same class as laser v1.6's C2->U2 note.
