    date: 2026-07-27
    subject: smc0985-cooksense interposer v1.1 (pre-seal staging archive)
    reviewer: FIX-PASS targeted render re-confirmation (canon "Verification scoping")
    context-given: the regenerated render pair + twin renders + twin_overlay +
                   missing_models + CPL; the v1.0 fresh-context verdict carried
                   forward on a silk state measured to differ in ONE glyph
    verdict: PASS

# RENDER REVIEW — interposer v1.1

## THE RENDER IS PROVED FAITHFUL BEFORE IT IS READ (canon A-RENDER)

A render review is worthless on a picture nobody has proved is the board.
`twin_overlay.py` measures every mounted body in PIXELS out of `twin_top.png`
and compares it against the position the BOARD implies (mesh bbox × JLC's own
model transform × placement) — so it cannot agree with a wrong mount by
construction. Run on this archive's own render (`verification/twin_overlay.md`):

    calibration      17.8189 px/mm x, 17.8308 px/mm y, anisotropy 0.9993 (tol 0.02)
    board edge       9.950..64.050 x, 9.950..56.050 y mm
    courtyards       27 drawn; 0 footprints with no courtyard on either layer
    COVERAGE         1 measured / 1 refs with an expected body
                     (0 unresolvable, 0 resolvable-but-unmeasured, 0 no-model)
    J_KEY_MATRIX     centre delta 0.165 mm, outward 0.049 mm, courtyard
                     excursion 0.084 mm, tolerance 1.00 mm
    OVERLAY OK

Coverage is 1/1 rather than partial because this board machine-places exactly
one part.

## What is new in the picture, measured rather than eyeballed

`verification/copper_identity.txt`: F.Silkscreen differs from v1.0 by 50 atoms
out of 5368, ALL of them inside a single 0.514 × 0.900 mm character cell at
x 44.286–44.800, y 12.009–12.909 — the version digit in the board caption
(`v1.0` → `v1.1`). 5328 atoms are unchanged: no refdes, no functional label, no
pin-1 numeral, no isolation warning moved. Every other layer is geometrically
identical. **So v1.0's fresh-context render review verdict applies unchanged,**
and its four P2s stand exactly as dispositioned (I6/I7/I8 RECORDED as next-rev,
I1 FIXED pre-v1.0-seal).

Confirmed by eye on the regenerated pair, against the v1.0 disposition ledger:

| check | result | evidence |
|---|---|---|
| board caption reads **v1.1** | YES | `render_top_bare.png`, `twin_top.png` |
| "1" and "10" silk numerals present OUTSIDE both ZIF housing outlines (v1.0 I1 fix) | YES, both rows | `render_top_bare.png` |
| square (RECT) pin-1 pad visible on both ZIF rows | YES | `render_top_bare.png` |
| J_KEY_MATRIX body sits on the WEST edge with its mouth facing OFF-BOARD | YES | `twin_top.png`, `twin_iso_nw.png` |
| the 10 pass-through lines run as columns, TP rows on-column | YES | `render_top_bare.png` |
| bottom side carries no components | YES, bare | `render_bottom_bare.png`, `twin_bottom.png` |
| "FLOATING KEYPAD DOMAIN — NO GND / NO CHASSIS BOND" legible | YES | `render_top_bare.png` |
| no silk-over-pad collision, no pad off-board | none seen | both bare renders |

## The two bodiless footprints are now ABSENT FROM THE POPULATION, not merely bodiless

v1.0's `missing_models.txt` listed both 10FDZ-BT as CPL placements with no 3D
body (1 of 3 bodies mounted). That is the usb-hub-3s class of misreading waiting
to happen — "bodiless" and "not placed" look identical in a render. In v1.1 the
two ZIFs are DECLARED not_assembled and are off the CPL, so:

    # GENERATED by jlc_twin.py NO-BODY pass
    # source of the population set: 1 CPL placements (fab/cpl.csv)
    # bodies mounted: 1/1
    (none — every CPL designator resolves a 3D body)

A-BODY is now 1/1 with nothing to interpret. The ZIF bodies are absent from the
twin render because JLC has no model for a part JLC is not placing — which is
the correct picture of what will be assembled.

## Findings

No new findings. No P0, no P1. The four v1.0 P2s (TP label stagger, no back-side
silk ID, J_KEY_MATRIX refdes near the edge, and the pin-1 marker already fixed)
are unchanged and remain next-rev candidates in the v1.0 disposition ledger,
carried forward in `verification/dispositions.md`.

PASS for release.
