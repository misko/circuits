# STATUS beacon — cooksense (MAIN board; live head, OVERWRITTEN each transition)

History is append-only in `journal/<stage>_cooksense.md`; this is only the
current frame. Read by `skills/kicad-pcb/scripts/pcb_status.py`, graded against
the tree by `skills/kicad-pcb/scripts/status_beacon_check.py` (canon M-BEACON).

<!-- reader parses from here down -->
stage:   V1.7-SEALED-2026-07-30--65C-ENVELOPE-DECLARED--4-LENSES-SOUND--SOURCING-BLOCKED-1--REMEASURED-2026-07-31-STOCK-0--BENCH-GATE-WRITTEN-NOT-RUN
step:    "**SEALED: `07_releases/cooksense-v1.7-2026-07-30/`.** 2-commit seal — source S `1d7a41e8f3bd225f521e49100df9f985665ce1e0` (git_dirty false, `release_git_dirty.py` RAW_EXIT 0), seal commit `2fd753172dba6376027a3dd91613efc4c3017ddb` adding only the release dir + `SUPERSEDED.md` on v1.6. MANIFEST stamped with a sha256 table over all 122 files. **THE FIRST cooksense RELEASE THAT IS NOT DO-NOT-ORDER BY CONSTRUCTION** — v1.0/v1.1/v1.3/v1.4/v1.5/v1.6 all carry the `pinout12` relay land against a `pinout13` part. **IT IS SEALED AND IT IS NOT ORDERABLE TODAY, AND THOSE ARE TWO DIFFERENT STATEMENTS.** Board md5 `9f4fd5fae810f40a52b1035df727243c` never moved — the tenth pass sealed what the ninth declined, and nothing physical changed between them. ADR-0029 declares the 65 C ambient + the mandatory bench gate; ADR-0030 corrects WHY."
measure: "**ALL FOUR FRESH-CONTEXT LENSES: `design_verdict: SOUND`, `order_verdict: BLOCKED-SOURCING`, ZERO P0 between them** (topology 0P0/2P1, layout 0P0/2P1, pin 0 FAIL over 243/243 footprints, render 0P0/4P1), each written to its OWN filename so none could overwrite another. THERMAL at the DECLARED 65 C, MEASURED by me from CITED constants only: `Tj` 107.0795 C, margin **17.9205 C citable / 13.27...16.37 C carrying the board's other 0.958 W**; at 75 C the same forms are 7.92 and 3.27...6.37. Dropout at 65 C **+19.5 mV** worst case (INHERITED, ADR-0028 ladder; slope -0.31 mV/K, so narrowing bought +1.4 mV). **THE BINDING PART IS THE REED RELAY AT -20...+70 C, NOT THE LDO** — the UNIQUE minimum `t_op` across all 47 dossiers, found by THREE of four lenses; 5.00 C of margin at 65 C and -5.00 C at 75 C. GATES, UNPIPED, raw exit codes: DRC **0** (0 violations / 0 unconnected / 0 parity, `--exit-code-violations`, BOTH halves) / standalone-archive DRC **0** outside the repo / ERC 0 errors 411 warnings / policy_audit FAIL=0 **0** (PASS=28 WAIVED=6 HUMAN=6 N-A=5) / release_freshness **0** (DESIGN PASS, SOURCING BLOCKED-1, FRESHNESS PASS) / E-TOPO **0** (PD 410 mW vs the ratcheted 497 mW, 82 %) / E-INV 168/168 / E-ADR 12/12 / M-BOUND **0** (7 CITED) / G-ORPHAN 315/315 / A-POP PASS / A-BODY 206/206 / A-POS worst 0.00050 mm / F-LEGIBLE 60 / M-BOM PASS / M-DEPEND PASS / S-COUNT 4/4 over 239 refdes / **M4 PASS RAW_EXIT 0, 16 waivers graded**. SOURCING measured 2026-07-30 21:29 local with both controls: `C265111` **5 / MOQ 21** (fifth reading, 0->5->0->5->5, MOQ 21 at every one — **the blocker has never been the stock**), `C587657` **70 / MOQ 1** (130->80->70->70, single-source, no drop-in fallback)."
state:   done
op_pid:  none

next:    "**THE USER HAS DECIDED, AND THE NEXT MOVE IS A BENCH HOUR, NOT ANOTHER CATALOG READ.** (0) **THE INSTRUMENT IS WRITTEN AND NOT RUN: `01_docs/gh08-clone-mate-confirm.md`.** The user chose to bench-check the SHOU HAN clone `C42376901` in preference to spending a copper revision on the top-entry `C133062`. That file carries the WHOLE gate: EXACTLY WHAT TO ORDER — every line MOQ 1 and in stock, **$4.79 for the bench set**, MEASURED 2026-07-31 14:40 local with both controls (`C5620` 5206 / `C99999999` no exact row over 25 fuzzy neighbours), cached at `06_build/cache/gh08_mate_articles_2026-07-31.json` TTL 6 h: `C42376901` x10 ($0.0831), **genuine JST `GHR-08V-S` = `C485357` x5** ($0.0683, stock 9518), **genuine JST `SSHL-002T-P0.2` = `C189897` x100** ($0.0132, stock 641115), and **`C133062` x5 bought ONLY AS A GENUINE-JST CONTROL to measure against — NEVER as a board part** (same GH mating interface per eGH p.3/p.4, so it gives every force and resistance number a genuine twin on one rig; `C265111` cannot serve as the control because you cannot buy it, which IS the problem); plus **GH1-GH7, each with a numeric threshold and the source of that threshold** (retention >=4.0 N = the clone's OWN published 0.5 N/pin x 8, JST's `eGH.pdf` publishes NO force figure at all; insertion <=24 N = its own 3 N/pin; contact resistance <=30 mOhm = its own 20 mOhm plus a crimp/wire budget). **DO NOT ADOPT `C42376901` UNTIL §5 READS PASS.** `BLOCKED-SOURCING` is the honest state and adopting a substitute to green a verdict is precisely the move this project has already paid for. The staged `part.yaml` is a FENCED BLOCK in §7.4, deliberately NOT a `02_parts/` directory — a dossier for a part not on the board is a stale entry under `02_parts/contracts.md`, and the contracts ratchet sits EXACTLY at its 2646 ceiling so an unpermitted path FAILS rather than accruing quietly. **TWO FINDINGS NOT IN ADR-0031, both MEASURED this pass: (a) THE VENDOR DRAWING NUMBERS NO CIRCUITS** — sheet 1's plan view is left-right SYMMETRIC, no '1', no chamfer, and the 1/2/3 on the isometric are BOM item callouts (Housing/PIN/FITTING NAIL), so circuit 1's end rests on JLC's EasyEDA footprint alone. A GH plug keys on a **FACE**, not an END, so a mirrored clone shroud would **mate and latch while reversing the row**, landing the pod's 3V3_SW feed on `SHIELD_DRAIN` and its GND on `TH_PORT` — a live short of the switched rail to shield through `Q_SWA`, on a board that assembles and powers up. That is **GH3, a hard stop**, and it is the exact shape of commit `16c54169`. **(b) contact resistance is a WORKMANSHIP screen here, not a circuit limit**: MEASURED off `cooksense.net`, `R_REF0` 10k runs from `3V3_ANALOG` which IS `U_ADC.15/16` (VDD **and** VREF), so the divider is truly ratiometric and only the NTC leg crosses the connector, through two contacts. With R25=10k / B25-85=3987 (sensitivity 0.044853 /K), 20 mOhm moves a reading by **8.9e-5 K**, JST's worst after-test 50 mOhm by **2.2e-4 K**, and the circuit-derived condemn level is **22.4 Ohm per contact** — a margin of ~1120x. **(c) a v-NEXT silk item this turned up**: the ONLY pin-1 mark at `J_THERM_A/B` is a 0.99 mm silk tick at x=27.065, west side only, no east counterpart — add a dot or a '1' numeral on the revision that is happening anyway. **(d) A BLOCKER THE v1.8 CUT INHERITS, FOUND BY DRY-RUNNING THE FIXED EXPORTER RATHER THAN DISCOVERING IT COLD (`gh08-clone-mate-confirm.md` §7.6): `export_jlc_package.py` RAW_EXIT **2** on A-ROT — `C160403` (`J_ESTOP`, JST SH SM03B-SRSS-TB) has **ZERO rows in `jlc_lcsc_rotations.csv` and has NEVER had one** (`git log --all -S C160403` over that path returns nothing; control `C5620` = 4 rows). v1.7's `fab/cpl.csv` line 71 nonetheless ships `J_ESTOP,C160403,...,90.0`, the A-ROT tightening ('silence is now a FAIL') landed 2026-07-25 vs the 2026-07-30 seal, and NO `rotations_unsourced.csv` travelled in `fab/` — so the escape hatch was used and the worklist did not ship. It is NOT unevidenced (`twin_overlay_lowres.md` line 47: `0deg @0.00mm`, margin 0.205) but the exporter explicitly refuses jlc_twin as the authority (canon M1). **NOT dispositioned here — 07_releases is immutable and release integrity is not this gate's call. What IS certain: the v1.8 cut BLOCKS at the exporter until a real measured row exists, and `jlc_lcsc_rotations.csv` is a FLEET file under `skills/`, outside this board's partition. Do NOT reach for `--allow-unsourced-rotations` a second time.** (1) **RE-MEASURED 2026-07-31 (ADR-0031, commit `0a3a5ab1`; re-confirmed unchanged at 14:40 local) — THE BLOCK DID NOT CLEAR, IT DEEPENED: `C265111` reads stock **0** (was 5), MOQ **21** unchanged, and `canPresaleNumber` **-1285** — the only negative in the candidate set, reading as ~1285 units of committed demand queued ahead of any incoming reel. The sequence is 0->5->0->5->5->**0** over SIX readings in 72 h with MOQ 21 at every one. **NO v1.8 WAS CUT; the two owed fixes (task #53 `kicad_prl`/bijection, exporter-produced `fab/`) STAY OWED and still ride the next cut.** Re-measure again on order day — the reading is volatile and its cache TTL is 6 h. Candidate sweep done: `C42376901` (SHOU HAN, stock 5868 / MOQ 1) remains THE USER'S purchasing decision and now has a real VENDOR DRAWING corroborating its land plus a ratings row (-25..+85C, wider at both ends than the DIP05 that binds, so the swap does not move the envelope) — its MATE/RETENTION against a genuine GHR-08V is still UNVERIFIED and is a BENCH check, not a catalog read; `C133062` (genuine JST, stock 32068 / MOQ 1) is the standing fallback but is TOP-ENTRY = a copper revision; `C54110154` (XYECONN) REJECTED — no datasheet exists at all, and brass contacts. (2) ⚠️ **`C587657` (Molex 436500224, `J_PWR`) IS NOW THE THINNEST LINE ON THIS BOM AND THE NEXT SOURCING BLOCK IF ANY. RE-MEASURED 43** (130->80->70->70->**43**, i.e. **-67 % over five readings**), presale headroom **12**, MOQ 1. 1/board x 5 boards = 5 needed, so it STILL CLEARS the floor and **is NOT blocking and must not be reported as one.** But it is **SINGLE-SOURCE WITH NO DROP-IN**: the only alternate the sweep found, `C293740`, is THROUGH-HOLE = a footprint change = copper. **Re-specify before it reaches the build quantity, not after it reaches zero — and the cheapest moment to act is the bench order itself**: `gh08-clone-mate-confirm.md` §1 line E buys 10 ($0.6471 each) alongside the bench articles, so a JLC stock-out cannot strand the build. Recorded there as well as here because that file is the one a human will be holding. (3) The JLC order-preview HUMAN GATE for every ref in `verification/rotation_human_gate.txt` (A-POL single-channel). (4) **F-ECHO** — save JLC's resolved BOM and diff it back; it is the one gate on the far side of the upload. (5) **ORDER_README §7b B1-B6 IS MANDATORY BEFORE THE BOARD IS TRUSTED ABOVE BENCH CONDITIONS** — and passing it reopens at most 65->70 C, NOT 75 C: above 70 C needs re-rating or replacing twelve `DIP05-1A72-13L`, a BOM change and a NEW BOARD REVISION. **v-NEXT WORK ORDER** (all carried, none blocking): 6 more vias in the EXISTING `U_LDO` tab pad (-18.0 C/W, +8.4 C, NO BOM change — the cheapest item anywhere on this board) beside ADR-0027's deferred 5 V pour (+28.1 mV); the eFuse-cell placement (pull `C_EFIN`, `R_OVT`/`R_OVB`, `R_ILM` hard against `U_EFUSE` pins 3/2/7); the `ANALOG SENSE` silk off `Q_SWDRVA` pad 3 (34.5 % ink, measured); a `J_ESTOP` 2-3 bridge legend in silk; `D_ESD_IN` -> a 6.0/6.8 V stand-off SOD-323 (ADR-0028 D5). **OWED SKILL PATCHES P15-P18, P20, P21, P22 — FILED, NOT IMPLEMENTED** (`skills/` outside this partition); P19 LANDED as `1b70262a`. **P22 is the one worth doing first**: where `power_tree.yaml` declares an ambient, sweep `02_parts/*/part.yaml` `t_op` over the refdes actually on the board, name the minimum, and FAIL when the declared ambient meets or exceeds it — four lines, and it would have found in milliseconds what three frontier lenses each spent a full pass finding. **INTERPOSER FINDING, REPORTED NOT ACTED ON:** `03_src/rules/` holds FIVE undeclared symlinks into `../cooksense/rules/`, so flat-path gates grade COOKSENSE while believing they read a project-wide file. MEASURED: `interposer-v1.1-2026-07-27`'s SHIPPED bytes are SOUND (its `fab/cpl.csv` correctly omits `J_MEMBRANE`+`J_CN1_JUMPER`, its BOM carries them, zero cooksense refdes leaked, and its shipped `assembly_coverage.txt` reads `declared=2` = its OWN file) — **no supersede is warranted**. The contamination is in TODAY'S READERS: re-running A-POP on that sealed release now returns 18 findings naming cooksense's `K_U3..K_U6`, and `policy_audit --board interposer` resolves the COOKSENSE release series. Do not delete the symlinks alone — unsafe for `policy_audit`/`export_jlc_package` until those resolve per-board."
updated: 2026-07-31T14:55:00
