# Pi USB port switch — schematic source

This TSX is the single electrical source for four independent inline USB
channels. Each channel supports hardware-controlled full-off, power-only and
power-plus-data states. Raspberry Pi VBUS and header supply pins are NC; the
board uses a separate regulated 5.2 V input and common ground.

There is deliberately no firmware in this project.
