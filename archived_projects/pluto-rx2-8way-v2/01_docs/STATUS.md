# STATUS beacon — the live head of the journal

This file is the ONE place a coordinator reads to know, in a glance, where this
board is between gates. It is OVERWRITTEN (not appended) at every transition —
the append-only history lives in `journal/<stage>.md`; this is only the current
frame. Rewrite it at every stage enter/finish, every iterate, and IMMEDIATELY
BEFORE and AFTER every long blocking op (see SKILL.md "Journal discipline").

Read by `skills/kicad-pcb/scripts/pcb_status.py`. Everything below the fence is
`key: value` (one per line); `#` lines and blanks are ignored by the reader.

Multi-board projects: one beacon PER board, named `STATUS-<board>.md`
(mirroring the per-board `journal/<stage>.md` suffix). A single-board
project uses this bare `STATUS.md`.

## Schema

| field | meaning | vocabulary |
|---|---|---|
| `stage` | which pipeline stage the board is in | `commission` \| `parts` \| `schematic` \| `placement` \| `routing` \| `verify` \| `seal` |
| `step` | the specific thing happening RIGHT NOW, one line | free text |
| `measure` | the last MEASURED numbers (gate output, counts) — never hope | free text; the rebuild loop tees its last line here |
| `state` | the coordinator's traffic light | `working` (progressing) \| `blocked` (STOPPED, escalated to coordinator) \| `done` (this stage's gate is green) |
| `next` | what happens on the next transition | free text |
| `op_pid` | pid of the running long op, or empty when idle | integer or empty |
| `updated` | when this frame was written, ISO-8601 local | `YYYY-MM-DDTHH:MM:SS` |

`state: working` + a fresh `updated` + a live `op_pid` = progressing (coordinator
POLLS, does not interrupt). `state: working` + a STALE `updated` + no live
`op_pid` = STALLED (the reader flags it). `state: blocked` = a decision or
D-BACK wall the agent has PUSHED up — the coordinator acts. `state: done` =
terminal for this stage.

<!-- reader parses from here down -->
stage:   verify
step:    "RE-STAGED from the rebuilt board, and the DELTA is MEASURED object by object rather than described. 06_build/staging/ exists again, assembled from the producers (fab set from export_jlc_package.py at 628ee3d4 - nothing hand-copied or renamed, M-HANDFIX). Control for every comparison is an ISOLATED WORKTREE at 9af663f0, so the pre-fix artifacts are regenerated by the SAME tools as the post-fix ones. NOT SEALED, and two blockers stand: A-EVID FAIL=4 (the four review documents, deliberately absent until a fresh lens reads THIS board) and git_dirty: true."
measure: "DELTA vs 9af663f0 (what the four lenses graded), CLASSIFIED. SCHEMATIC IDENTICAL MODULO UUIDs - canonical sha256 EQUAL (2f335996...), 476 UUID tokens each, 387 distinct, ZERO shared; the 2 non-UUID diff lines are both the string '  )'. NETLIST 40 nets / 130 nodes both sides, net symdiff 0, node symdiff 0, netclass reassignments 0; canonicalised .net files differ on ONE line, the (source) path. PCB: 8 vias moved (0.0349 x6, 0.0347 x2, all GND 0.25/0.15, 0 unpaired) + min_hole_to_hole 0.25 -> 0.315. EVERYTHING ELSE IDENTICAL: 32 footprints incl. full pad tables, 199 tracks, 0 arcs, 6 zones (fill areas equal to 4 dp), 4 graphics, 51 board texts, 88 footprint texts, 4 netclasses, 40 nets. GERBERS: 4 copper layers (32 commands each) + PTH.drl (16 lines) CHANGED; mask/paste/silk/edge/NPTH UNCHANGED as command multisets; bom.csv + cpl.csv BYTE-IDENTICAL. METHOD NOTE: gerber BYTES are not comparable run-to-run - regenerating the pre-fix gerbers from the pre-fix board gave 13322 differing lines that are ONLY flash ORDER; compare multisets. HEADLINES: fence 1.1769/1.1910 22 arm-sides 0 OVER UNCHANGED (RAW EXIT 0); band-free 2.2142 UNCHANGED (the RF lens says 2.2152 - a 0.0010 reimplementation difference, measured the same on BOTH boards); In1.Cu 3437.1569 mm2 = 93.9% + 0 track segments UNCHANGED; arm spread 0.1657 UNCHANGED; via census 3446 @ 0.25/0.15, GND 3433 + 40 PTH = 3473 UNCHANGED; BOM 11 / CPL top 27 bottom 0 UNCHANGED; DRC 0/0/0 CLASSIFIED both halves. MOVED: min hole-to-hole 0.3016/0.2366 -> 0.3265/0.2615, sub-floor pairs 8 -> 0. TWO HEADLINES MOVED THAT ARE NOT THE FIX: (1) ERC is 0/213 (124 endpoint_off_grid + 89 lib_symbol_issues) and the PRE-FIX schematic ALSO measures 213 today - the archived 209 was never a property of either board. (2) seed_stubs 28 -> 36 declared vias and 8 of them sit at d=0.0002-0.0006 mm, not 0.0000, because the emitter snaps to the micron and the new entries are declared to 4 decimals - so the RF lens s6 claim 'all at d = 0.0000, exactly' no longer holds as worded (0.6 um against a 125 um via radius). ARCHIVE: standalone DRC 0/0/0 RAW EXIT 0 outside the repo (git rev-parse confirms no repo there; that run regenerated a .kicad_prl IN THE COPY); MANIFEST bijective BOTH ways by my own walker 68 rows / 68 files / 0 / 0 / 0, sha256sum -c RAW EXIT 0 over 68 rows, with a known-bad fixture proving the walker catches what sha256sum -c structurally cannot; A-EVID FAIL=4 / 29 present; policy_audit FAIL=1 HUMAN=6 N-A=7 PASS=31 (the FAIL is A-POP MANIFEST-UNDECLARED against the PROJECT; A-POP against THIS ARCHIVE is PASS RAW EXIT 0; S-OCCL now PASS)."
state:   working
next:    "A FRESH SCOPED LENS ROUND against 06_build/staging/, and the delta above SCOPES IT: the schematic and the netlist did NOT move, so the schematic/netlist lens's SOUND/ORDER verdict does not need re-deriving; the RF lens's fence, band-free, In1.Cu, arm-spread and via-census numbers are all UNCHANGED and only its s6 seed-stub EXACTNESS claim moved; the fab lens's own finding is the one this work closed; the release-integrity lens has the genuinely new material. DO NOT close A-EVID by copying the pre-fix reviews into verification/ - the previous revision's argument that 'an absent required artifact is worse than a stale one' is WITHDRAWN in ORDER_README s1: a stale review at a contract-required path is read by every name-based check as THIS release's verdict. TWO THINGS OWED AND CARRIED INTO THE ARCHIVE, NOT CLAIMED DONE: (1) the tap's 440 ohm is overstated 1.61x - only R_T1=220 is at the junction, R_T2 sits 1.180 mm / 15.10 deg at 6 GHz downstream, measured floor 273.85 ohm / RL 21.45 dB not 440 / 25.39; still SOUND because the defect it replaced was 5.80 ohm / RL 1.71 dB (47x), but floorplan.yaml's intent block owes the correction, and the same cause makes the silk's -20.26 dB about 1 dB optimistic at 6 GHz. (2) THE VENDOR QUESTION IS NOT SETTLED: the tight class is a VIA against a PTH PAD and JLC publishes NO mixed-class rule (Q&A #693 unanswered); 0.315 makes the board honour its OWN declared tier at max material and nothing more - ORDER_README s7 item 4 carries the exact question to put to JLC in writing before ordering. ALSO BLOCKING THE SEAL: git_dirty true, and it is NOT this board - projects/pluto-rx2-8way-v2/ is CLEAN, but four SHARED generic-backend scripts are uncommitted and THREE of them changed AFTER the board file was written (one at 14:44, while this archive was being staged), so rebuild_all.sh today is not the driver that produced source/*.kicad_pcb. Recorded in MANIFEST.txt, not stamped away."
op_pid:
updated: 2026-07-31T15:05:00
