# Render review — usb-power-3s v1.2 (schematic-only change)

v1.2's schematic is generated by the schwriter2 LAYOUT ENGINE: structure-only
declarations (regions/rows/chains), every coordinate computed from text
ENVELOPES ("envelopes are the schematic's courtyards", canon S6). Fab files
byte-identical to v1.1-2026-07-16 (which REQUIRED the JLC ADVANCED small-via
option — carried unchanged in this MANIFEST/ORDER_README).

- 96 cells, 25 real wires (22 declared chains + 3 engine auto-wires):
  battery(+)->fuse->TVS entry; front-end HGATE network U1->Q2 plus the
  engine-derived VSW source tie; UVLO/OV ladder R1->R2->R3; buck EN
  divider R4->R5; per-buck HO gate network U->Q1 + SW tie, SW->inductor
  Q2->L1, FB divider R3->R4, comp RC leg R5->C8, CC3-RC2 leg C10->R6;
  PGOOD sequencing divider R21->R33->R34; 3x TPS2557 ILIM chains U->RILIM;
  USB-C Rp R19->J5.CC1.
- Collision-free BY CONSTRUCTION and verified twice: the engine's internal
  S-OCCL asserts 0 at emission; policy_audit's independent label-aware
  S-OCCL confirms 0 (was 65 waived in v1.1 — waiver REMOVED, cleared not
  waived). Section boxes disjoint by construction (packed, A1 sheet).
- Full-page + sextant-crop visual inspection (orchestrator, PNG at
  100dpi): no double-printed plates, no box collisions, wires legible,
  regions in reading order 1-7, no_connect crosses visible on all 13
  sanctioned floats (U2/U3 pins 7/9/15/16, U4/U5/U6 FAULT, J5 A8/B8).
- Gates independently re-verified: ERC 0 errors with warnings within the
  documented baseline (endpoint_off_grid 96 -> 0 — schwriter2 is
  1.27-grid-exact; isolated_pin_label 1 = PGOOD_B, baselined), board DRC
  0/0/0 with schematic parity on the UNTOUCHED board (pcb sha unchanged
  81a01c2e), netlist node-for-node identical to the pre-port schematic
  (68 nets / 303 nodes), policy_audit full mode zero FAIL.
- S6 readability: input protection, front-end gate path, ladder/dividers,
  buck gate/switch/FB/comp networks and port ILIM chains now DRAWN;
  remaining fan-out label-based by design (T3). Grade: READABLE for story
  paths.
- Known label-only spots (documented engine compromises, T3 fallback):
  U1.1 DGATE->Q1 gate (both pins left-side — no facing orientation),
  R20->J5.CC2 (one chain per row adjacency; CC1 got the drawn wire),
  buck ILIM R{A,B}2/C{A,B}4 taps (LM5145 ILIM pin is left-side),
  USB-A port feeds U->J (ILIM chain took the adjacency). Same class as
  laser v1.6's C2->U2 note.
