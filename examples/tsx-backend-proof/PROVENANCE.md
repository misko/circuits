# provenance

Snapshot of `projects/cook-loadcell/03_tscircuit/backend_proof/`
(`build_from_tsx.sh` + `NOTES.md`) as of commit `7e3742d`, extracted
2026-07-21 so that skills reference `examples/`, never a live project.

This is EVIDENCE, frozen: the run that proved the tscircuit→KiCad backend
drives a real board to DRC 0/0/0 + board parity 0 with no per-board adapter.
It is not maintained as runnable against the current tree — the live,
regenerable path is `skills/kicad-pcb/scripts/tsx_to_board.sh`. If the
backend is re-proven, add a NEW snapshot; do not edit this one.
