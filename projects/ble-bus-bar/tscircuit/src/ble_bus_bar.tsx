// ble-bus-bar — tscircuit render (SCAFFOLD / placeholder)
// Fab-of-record: ../../04_kicad/ble_bus_bar.kicad_pcb (~104 parts). Author this
// board node-for-node from that netlist, then `gen_tscircuit.sh` renders it and
// writes verification/parity.md. Canon S-DSL: KiCad stays authoritative.
export default () => (
  <board width="10mm" height="10mm">
    {/* TODO: author ble_bus_bar from the KiCad netlist (components, nets, footprints, JLC parts) */}
  </board>
)
