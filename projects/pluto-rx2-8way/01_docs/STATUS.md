# STATUS beacon — pluto-rx2-8way (live head of the journal)

<!-- reader parses from here down -->
stage:   routing
step:    "STAGE 6 STOPPED AT A MEASURED D-BACK, NO CHAIN PROMOTED, 04_kicad NEVER WRITTEN TO. The stage opened by REPAIRING THE SPEC (landed, commit ec6ecda): the standing '+/-0.10 mm ROUTED arm length' obligation is WITHDRAWN with its arithmetic (13.19 deg/mm on JLC04161H-7628, so 0.10 mm = 1.3 deg, inside PE42482A-X's own published 13.2 deg part-to-part window and below ADR-0006(d)'s ~2 deg/fillet mounting term, and KRT's length matching is diff-pair shaped so nothing can deliver it), and replaced by the board's first-ever `length_match: RF_RADIAL_STAR` declaration in 03_src/rules/nets.yaml - 8 congruent radials, topology chain, no_vias, congruent_pads, max_spread_mm 1.0 as a DRIFT ceiling. The matched set is EIGHT and not nine because ADR-0006 already said the tapped RF8 path is 'unequal by construction'. E-NETREF ghosts 4 -> 1: SW_VDD -> {3V3, anchor_pins ['8']} at 2.873/3.0, ABM8 XOUT -> {XOUT_XTAL, anchor_pins ['3']} at 3.618/6.0, and SW_LS DELETED rather than re-pointed at GND (which would have measured 6.956 mm, a real number about the wrong thing) with its obligation moved to the new audit_board I8 via-at-pad check. THEN ROUTING HIT TWO STRUCTURAL WALLS, both arithmetic, neither fixable from this stage."
measure: "W1 THE LAUNCH: PE42482A-X's vendor land is 0.60x0.30 at 0.50 pitch, so a neighbouring GND land's near edge is 0.350 mm from the RF centreline; a 0.36 mm track needs 0.180+0.200 = 0.380. DEFICIT 0.030 mm, and the max width that leaves the land at 0.2 clearance is 0.30 mm = the pad width = 55.3 ohm against the derived 50.5. MEASURED: 6 of 11 rf nets route, 5 FAIL (ANT2, ANT3, ANT6, ANT7, RX2_OUT - the pins with a GND land on BOTH flanks), KRT naming it exactly ('boxed in by static obstacles (neighboring pads + clearance), not by congestion'). The vendor land itself sits at EXACTLY the 0.200 floor, so the launch has zero margin by construction. KRT's power-net neck-down CAN make the textbook taper (measured on ANT5: 0.75 mm narrow + a stepped 0.232/0.264/0.296/0.328 taper + 16.513 mm at 0.36) but falls back to re-routing the WHOLE net narrow, order-dependently; grid 0.05 + neck 0.2 routes 11/11 with every arm at 0.2 mm = 67 ohm, which generate_rules' own RF50_width rule fails. The <=0.17 clearance relaxation is REFUSED on a measured reason: the stitch fence would then sit 0.15-0.17 mm from a 0.36 mm arm over 0.2104 mm prepreg (g/h ~ 0.8) and detune the PURE-MICROSTRIP 50.5 ohm the width was derived from - clearance on this board is electrical in exactly one place, the fence gap, which needs >=0.2 while the launch needs <0.2. W2 THE ANGLES: KRT is OCTILINEAR (every measured segment is H, V or exactly 45 deg) and only THREE of the nine radials (135/225/315 = ANT1/ANT4/ANT5) sit on a 45-degree multiple. The other six are 15 deg off every routable direction, where the shortest octilinear path is cos15+0.4142*sin15 = 1.0731x the radius: +1.3058..1.3238 mm each. The placement's 0.3237 mm spread (4.27 deg) becomes a 1.6475 mm octilinear MINIMUM spread (21.73 deg), so this morning's 1.0 mm ceiling is UNREACHABLE BY CONSTRUCTION - and the one chain that routed all nine measures spread 2.237 mm = 29.5 deg. ADR-0007's 'equal length BY CONSTRUCTION' is true of the placement and FALSE of the copper. THE FIX IS IN THE SAME TABLE: the arms are two tight clusters, three at 17.7784 and six spanning 19.1618..19.4259 (only 0.1390 mm apart), so +1.45 mm on the three 45-degree arms collapses the group to ~0.14 mm, BETTER than the placement's 0.3237."
state:   stuck
op_pid:  none
next:    "TWO UPSTREAM DECISIONS ARE OWED BEFORE STAGE 6 CAN FINISH, and routing now would ship a board whose central claim fails its own new gate. (W1) Either extend generate_rules_generic.py to emit a SCOPED CLEARANCE rule (scoped_floors: emits track_width only today) so the QFN launch can run at the 0.09 tier floor inside a named rule area while the arms keep 0.2 - the general fine-pitch-launch exemption - or write an ADR pricing a 0.30 mm / 55.3 ohm launch neck. (W2) Either revise ADR-0007 to move J_ANT1/J_ANT4/J_ANT5 to r ~ 21.5 (their flange gap to the 30-degree neighbours GROWS to 10.85 mm and J_ANT1 stays 8.9 mm from its M3 hole, but it breaks the r=20.000 ring assertion in audit_board I3 and ADR-0007's R=20 derivation, so it is an ADR revision and not a floorplan tweak), or build the inter-net skew equaliser copper_length_audit's docstring already records as absent - three deliberate 1.45 mm jogs is the whole job. FOUR PATCHES PROPOSED, NONE MADE (skills/ off-limits): scoped_clearance in generate_rules; neckdown_length/neckdown_taper_length in route_and_stitch's _KRT_FLAGMAP (without them a tapered launch is not declarable, canon M3); PF-KRT must FAIL a route.common.fab_tier outside {standard, advanced} (it printed 'config is tier-consistent' over the invalid 'jlc_4layer_advanced' that killed the first wave, because it returns unless the value is exactly 'advanced'); and copper_length_audit should compute the OCTILINEAR FEASIBILITY BOUND max(dx,dy)+0.4142*min(dx,dy) from the PADS, which would have refused the 1.0 mm ceiling at authoring time instead of at the fifth KRT run. CARRIED FORWARD UNCHANGED: policy_audit FAIL=2 (R-DRC 4/99/0 and R-THERM, both structural because there are still no tracks and no vias), the four dispositioned P-ADJ-UNREACHED budgets now 3 of 42, the ABM8 GND pair at 2.457/2.291 mm, and the ORDER-DAY DFM judgement that 61 of 64 refdes emit a 0.1200/0.1125 mm silk stroke below JLC's published 0.15 while only the ten port captions clear it."
updated: 2026-07-29T21:48:14
