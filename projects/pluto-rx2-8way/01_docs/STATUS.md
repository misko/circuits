# STATUS beacon — pluto-rx2-8way (live head of the journal)

<!-- reader parses from here down -->
stage:   routing
step:    "STAGE 6: THE RADIAL STAR IS ROUTED, MEASURED AND PROMOTED. 03_src/route/r6.kicad_pcb is committed and route.final: is set (canon M3); the recipe that produced it is fully declarable in 03_src/route.yaml, which is what blocked promotion on 2026-07-29. BOTH walls fell to route.yaml keys. W1 (the launch): the wall was NOT width alone — a 20-point width x clearance sweep plus a grid sweep shows that at KRT DEFAULT grid_step 0.1 NOTHING routes the five boxed pads at ANY width (0.30/0.25/0.20 all fail), because the RF land centres sit at odd multiples of 0.05 (y=45.25, 46.25...). With grid_step 0.05 the rf wave routes 11/11 at the FULL 0.36 mm impedance width on a clearance of 0.14 — the least relaxation that routes with margin (0.145 routes, 0.15 does not; a 0.005 mm quantisation cliff). stitch.clearance stays 0.2, so the via fence gap route.common.clearance guards is untouched. NECK-DOWN IS REFUTED as a mechanism here: --power-nets/--neckdown routes 11/11 and delivers 149.832 mm of RF copper at 0.25 mm and 0.000 mm at 0.36, because KRT restores width only where the NARROW-PLANNED path has wide clearance. W2 (octilinear length): elongation: meander + length_match_group make R-LEN-OCT pass for the stated reason, and meander_amplitude — not tolerance — is the knob."
measure: "R-LEN PASS: realized group spread 0.3236 mm = 1.975 ps = 4.27 deg at 6 GHz against the 1.0 mm DRIFT ceiling, 8/8 member paths measured, every member 0 vias / 0 branch / 1 component / 2 ends so no_vias and topology chain are VERIFIED FROM COPPER. Arms 19.6369 (RF1/4/5) / 19.8018 (RF3/6/RFC) / 19.9604 (RF2/7) mm, 158.237 mm of arm copper, ALL of it at 0.36 mm. 0.3236 IS THE GEOMETRIC RESIDUE, not a router minimum: the Euclidean pad spread this board derives is 0.3238 mm, so the elongation recovered the ENTIRE 1.4966 mm octilinear penalty. Amplitude sweep: 1.0 -> 1.5586, 0.8 -> 1.1586, 0.5 -> 0.6549, 0.3 -> 0.3236, 0.2 -> 0.3236; length_match_tolerance {0.15,0.10,0.05} moved it by EXACTLY ZERO at all five amplitudes, so the 2026-07-29 attribution of 1.1586 to the tolerance is measured false (it is the amp-0.8 row). All four race candidates returned IDENTICAL RF geometry, so the published number does not depend on the lane that won. GATES: policy_audit FAIL=2 HUMAN=6 N-A=10 PASS=24 WAIVED=2 (same counts as the unrouted baseline, different content). R-DRC 88/28/0 — parity 0, unconnected 99 -> 28. Violations split cleanly: 49 clearance are RF-vs-GND at 0.166-0.194 mm (40 of them RF track vs an SMA PTH post, NOT the QFN land as predicted), 21 clearance are digital VIA-vs-pad/via at 0.175/0.1817 in the MCU field, 17 track_width (10 real: 3V3 power taps necked to 0.1998 by KRT power_tap_neckdown; 7 are a 0.2 um rounding artifact, 0.1998 against a 0.2 floor), 1 starved_thermal. copper_length_audit PASS. count_parity 4/4 over 64 refdes. waiver_provenance PASS 3/22. status_beacon_check PASS. audit_board FAIL(1) I8: U_SW.1 nearest GND via 0.863 mm against the 0.5 mm bound — pad_rescue rings start at 0.6, so its first candidate site is already outside the bound."
state:   blocked
op_pid:  none
next:    "DRC IS NOT 0/0/0 AND THE BIGGEST REMAINDER IS NOT MINE TO FIX. (1) THE 49 RF CLEARANCE FINDINGS ARE STRUCTURAL: routing a 0.36 mm arm out of PE42482A-X land REQUIRES sub-0.2 clearance (measured: 0.36 mm needs <= 0.145), so the board needs a LAUNCH-LOCAL scoped CLEARANCE rule. generate_rules_generic.py emits scoped_floors as track_width ONLY — there is no clearance constraint — so this cannot be said from source today. That is patch #4 from the 2026-07-29 escalation and it now has a measured requirement behind it. Until it exists the choice is a 20 mm-long 58 ohm arm or an evidenced waiver, and the waiver is the better of the two. (2) tier_preflight BLIND SPOT, found by using it: eff_route_clearance() reads only route.common.clearance and never scans route.waves[], so it printed 0 FAIL / config is tier-consistent over a wave routing at 0.14 under a 0.2 mm DRC floor — precisely the defect class PF-ROUTE-CLR and PF-RULES-CLR exist to catch. Same shape as the PF-KRT defect this board found on 2026-07-28. (3) G-ORPHAN FAILS on length_match.<G>.elongation: needs a row in skills/pcb-design/templates/contracts/03_src/rules/contracts.md naming copper_length_audit.py, and PROVEN_FLOOR 241 -> 242. (4) MINE AND NOT DONE: 28 unconnected (USB_DP/DM, SW_V1-4, QSPI_SD0-2, VBUS, XIN, XOUT_XTAL, USB_CC1/2, DVDD_1V1, 2 GND) are digital-wave congestion; the 21 via clearance findings are the same field. Both want a placement look before more router effort (CLAUDE.md: a routing failure is usually a placement problem). (5) audit_board I8: add a ring below 0.5 to stitch.pad_rescue.rings, or a seed stub, for U_SW.1. (6) OWED DERIVATION, deferred deliberately by the coordinator: route.yaml edge_band 0.7 is 3.33x substrate height against an external RF rule of 5x (1.052 mm here). MEASURED and it does not bind any RF net: the closest RF pad to a board edge is 4.731 mm = 22.5x h, and arms run inward from there, so the 5x rule is satisfied by geometry with margin. Record the derivation; do not change the number. CARRIED, NOT MINE TO LOSE: 61 of 64 refdes emit 0.1200/0.1125 mm silk stroke below JLC published 0.15 (order-day DFM), the ABM8 GND pair at 2.457/2.291 mm, and the four dispositioned P-ADJ-UNREACHED budgets."
updated: 2026-07-30T07:48:36
