# STATUS beacon — pluto-rx2-8way (live head of the journal)

<!-- reader parses from here down -->
stage:   routing
step:    "STAGE 6 STOPPED AT A MEASURED D-BACK. NO CHAIN PROMOTED, route.final: still absent, 04_kicad carries no tracks and was never written to. THE SPEC REPAIR LANDED FIRST (ec6ecda): the '+/-0.10 mm ROUTED arm length' obligation is WITHDRAWN with its arithmetic (13.19 deg/mm on JLC04161H-7628, so 0.10 mm = 1.3 deg, inside PE42482A-X's own published 13.2 deg part-to-part window, below ADR-0006(d)'s ~2 deg/fillet mounting term), replaced by the board's first `length_match: RF_RADIAL_STAR` in 03_src/rules/nets.yaml (8 congruent radials, chain, no_vias, congruent_pads, max_spread_mm 1.0 as a DRIFT ceiling); the matched set is EIGHT not nine because ADR-0006 already called the tapped RF8 path 'unequal by construction'. E-NETREF ghosts 4 -> 1 (SW_VDD -> {3V3, anchor_pins ['8']} 2.873/3.0; ABM8 XOUT -> {XOUT_XTAL, anchor_pins ['3']} 3.618/6.0; SW_LS DELETED rather than pointed at GND, whose 6.956 mm would have been a real number about the wrong thing, with its obligation moved to the new audit_board I8 via-at-pad check). THEN TWO WALLS, BOTH ARITHMETIC. W1: the vendor land is 0.60x0.30 at 0.50 pitch so a neighbouring GND land's near edge is 0.350 mm from the RF centreline, and a 0.36 mm track needs 0.180+0.200 = 0.380 — DEFICIT 0.030 mm, max landable width 0.30 mm = the pad width = 55.3 ohm against the derived 50.5. 6 of 11 rf nets route; ANT2/ANT3/ANT6/ANT7/RX2_OUT fail, exactly the pins with a GND land on BOTH flanks. W2: KRT is OCTILINEAR and only 3 of the 9 radials (135/225/315) sit on a 45-degree multiple, so the other six cost 1.0731x the radius (+1.31 mm each) and the placement's 0.3237 mm spread becomes a 1.6475 mm octilinear MINIMUM — the 1.0 mm ceiling is unreachable BY THE ROUTER'S MOVE SET, not by its stochasticity. ADR-0007's 'equal length BY CONSTRUCTION' is true of the placement and FALSE of the copper."
measure: "BOTH WALLS HAVE EXISTING FIXES AND THE BLOCK IS FIVE MISSING CONFIG KEYS. W2 MEASURED SOLVED BY HAND: route.py's --length-match-group (repeatable fnmatch patterns) + --length-match-tolerance + --meander-amplitude, backed by length_matching.py's trombone meanders, reported '8 nets (0 diff pairs, 8 single-ended), target=19.83mm' and took the group spread from 2.237 mm to 1.1586 mm (29.5 -> 15.28 deg at 6 GHz), 11/11 rf nets routed, 0 failed, min_clearance_used 0.2 — NO clearance relaxation anywhere, and the residual is the 0.15 mm tolerance I passed rather than a floor. THIS REFUTES copper_length_audit.py's own docstring, which says KRT's machinery is diff-pair shaped and 'a different problem with no tool behind it here', and that sentence is in the file that grades this board. W1's mechanism is the sibling's (ce70658, same wall, same day): permissive rule areas in floorplan.yaml (deny: [], precedent cook-hub u7_taps, F.Cu only) + scoped_floors: in nets.yaml, bounded in SPACE to the land so a 0.45 mm neck is lambda_g/61 and lumped. IT LEGALISES A NECK BUT DOES NOT MAKE KRT PRODUCE ONE: the power-net neck-down falls back to re-routing the WHOLE net at the neck width, so every arm returns at 0.25 mm (58 ohm) and fails generate_rules' own RF50_width rule outside the area — detectable, not silent, still not a route. THE <=0.17 mm clearance relaxation is REFUSED on a measured reason and not by quoting route.yaml: the stitch fence would then sit 0.15-0.17 mm from a 0.36 mm arm over 0.2104 mm prepreg (g/h ~ 0.8) and detune the PURE-MICROSTRIP 50.5 ohm the width was derived from — clearance is electrical on this board in exactly one place, the fence gap, which needs >=0.2 while the launch needs <0.2. UNROUTED STATE, honestly: policy_audit FAIL=2 HUMAN=6 N-A=10 PASS=24 WAIVED=2 (R-RULES N-A -> PASS, the route input now carries its own netclasses, canon R1); the two fails are R-DRC 4/99/0 and R-THERM, both structural with no tracks and no vias. copper_length_audit UNREACHED, 0 of 8 member paths measured. audit_board PASS 8 groups / 13 measurements, I8 UNREACHED at 0 GND vias and saying so. count_parity 4/4 over 64 refdes. waiver_provenance PASS, P-ADJ-UNREACHED now 3 of 42 (was 6 of 43). E-NETREF 119/120. tier_preflight 0 FAIL / 1 WARN. contracts_audit 0 violations. ALSO FIXED: `adr: 0007` is OCTAL in YAML 1.1 and printed as ADR-7 — the sibling hit the same defect as adr: 0011 -> 9, both from the unquoted field in copper_length_audit.py --schema."
state:   stuck
op_pid:  none
next:    "FIVE PATCHES PROPOSED, NONE MADE (skills/ off-limits, four siblings live there), in cost order. (1) route_and_stitch_generic.py _KRT_FLAGMAP: add neckdown_length, neckdown_taper_length, length_match_group, length_match_tolerance, meander_amplitude. Five keys, mechanical, and it is what makes the measured 2.237 -> 1.159 mm improvement DECLARABLE — I produced it by hand on the command line, and a promoted chain whose recipe is not in route.yaml is a canon-M3 violation wearing a green gate, which is why nothing was promoted. (2) copper_length_audit.py docstring: correct the claim that no inter-net skew tool exists, and consider computing the OCTILINEAR FEASIBILITY BOUND max(dx,dy)+0.4142*min(dx,dy) from the PADS — it would have refused the 1.0 mm ceiling at authoring time this morning instead of at the fifth KRT run. Also quote `adr:` in --schema. (3) escape_check.py (the sibling asks for the same gate): grade min(landable track width) per pad against its netclass floor, from pad geometry alone; on this board that single question is W1 and nothing in the pipeline asks it. (4) generate_rules_generic.py: a scoped CLEARANCE constraint alongside scoped_floors' track_width, so 'tier floor inside the QFN launch, 0.2 mm everywhere else' is sayable from source. (5) tier_preflight PF-KRT: FAIL a route.common.fab_tier outside {standard, advanced} — it printed 'config is tier-consistent' over the invalid 'jlc_4layer_advanced' that killed the first wave, because it returns unless the value is exactly 'advanced'. THE ONE DECISION THAT IS NOT A PATCH, and it is ADR-0007's to make: with the meander pass available, equalising UP to ~19.4 mm is now the cheap path and the jack ring can stay at r = 20.000 — the alternative recorded at 19:40 (move J_ANT1/J_ANT4/J_ANT5 to r ~ 21.5, which grows their flange gap to 10.85 mm and keeps J_ANT1 8.9 mm from its M3 hole) is no longer needed and should be dropped rather than half-remembered. CARRIED FORWARD: the four dispositioned P-ADJ-UNREACHED budgets, the ABM8 GND pair at 2.457/2.291 mm, and the ORDER-DAY DFM judgement that 61 of 64 refdes emit a 0.1200/0.1125 mm silk stroke below JLC's published 0.15 while only the ten port captions clear it."
updated: 2026-07-29T21:53:07
