# STATUS beacon — pluto-rx2-8way (live head of the journal)

<!-- reader parses from here down -->
stage:   routing
step:    "STAGE 6b DONE — THREE BOUNDED FIXES ON A ROUTED, PROMOTED BOARD, AND THE RF DELIVERABLE SURVIVED ALL OF THEM. Nothing here re-rolled the star: every verification went through 03_src/rebuild_reuse.sh (deterministic replay of the promoted chain 03_src/route/r6.kicad_pcb), never rebuild_all.sh --reroute, and copper_length_audit was re-run after each rebuild rather than assumed. (1) R-SCOPE ADOPTED: seven permissive rule areas in floorplan.yaml keepouts + seven scoped_clearances entries at 0.14 in rules/nets.yaml, PLUS the half that had to come first — default_clearance 0.2mm and an explicit clearance: 0.2mm on all six classes, because every class was riding generate_rules_generic's HARDCODED 0.2 and you cannot bound a relaxation of a floor nobody declared. (2) stitch.pad_rescue.rings gained a 0.15 first candidate. (3) the 17 track_width findings are CLASSIFIED in route.yaml and deliberately not touched."
measure: "R-DRC 88/28/0 -> 39/28/0. All 49 RF clearance findings CLEARED, zero remaining; unconnected UNCHANGED at 28 because no copper moved. THE REGION SCHEME IS THE FINDING: the 49 were predicted to be QFN-land findings and are not — 9 are arm-vs-U_SW-GND-land (all exactly 0.1700 mm) and 40 are arm vs an SMA PTH GROUND POST 20 mm away at the JACK end (0.1659-0.1936; J_ANT2/3/6/7 and J_RX2 seven each, J_RX1 five), so a launch-only region would have licensed 9 of 49 and left the board at 79. J_ANT1/4/5/8 measure clean and get NO region: an isolation relaxation has no downstream grader, so one declared for symmetry rather than for a measurement is the inherited-waiver defect. 0.14 (not the 0.145 routability threshold) because KRT PACKED TO 0.14 and a 0.145 floor fails that copper by 5 um; worst pair on the board is 0.1659 (RX1_MAIN vs J_RX1 post 2), so 0.14 clears it by 0.0259 mm and stays 0.05 mm above the tier's 0.09 min_space. P-LAND 8 -> 3 and the 5 MODEL-REFUTED lines -> 0: U_SW.2/.4/.15/.17/.22 now read landable 0.420 @ clearance 0.140 (rule scoped_clr_rf_launch) against a 0.360 floor — MEASURED 0.420, the prediction was 0.436. escape_check's routed cross-check went 5 pads WIDER-than-model -> 0. The 3 left are U_MCU.23/.46/.47, out of scope. tier_preflight 1 FAIL -> 0 FAIL / 2 WARN: PF-RULES-CLR cleared by the explicit declaration, which UNBLOCKS PF-ROUTE-CLR (it returns before it), and PF-ROUTE-CLR then WARNs exactly as R-SCOPE prescribes. audit_board FAIL(1) -> PASS: U_SW.1 nearest GND via 0.863 mm -> 0.418 mm against the 0.5 mm bound (rings are measured from the pad BOUNDING-BOX EDGE, so 0.6 was 0.75-0.90 mm from the centre and the FIRST candidate was already outside the bound; 0.15 bounds every angle at <= 0.45). THE 17 track_width: 8 ARTIFACT / 9 REAL, not the 7/10 previously carried — the misfiled one is the single 0.3998 3V3 segment, which is the SAME 0.0002 mm on a different floor. Mechanism NAMED: KRT _neck_terminal_grazes uses allowed_half = d - clearance - 1e-4, so every width it emits is 2e-4 mm low; the board carries 17 tracks at 199800 nm and 1 at 399800 nm and nothing else off-grid. The 9 real are 3V3 taps at U_MCU's pads, 0.2002 mm short = 0.2 mm of genuine neck plus the same margin, 3.2936 mm of copper = 8.10 mOhm, costing 4.05 mOhm / 0.61 mV at the declared 0.15 A; even necked, 0.1998 mm is 9.1x the IPC-2221 ampacity need (0.0219 mm). policy_audit FAIL=2 PASS=24 WAIVED=2 (R-DRC 39, R-THERM U_LDO.4) unchanged. copper_length_audit PASS — RF_RADIAL_STAR realized spread 0.3236 mm = 1.975 ps = 4.27 deg at 6 GHz against the 1.0 mm ceiling, IDENTICAL before and after every rebuild. G-ORPHAN PASS 303/303, no new contract row needed."
state:   ready
op_pid:  none
next:    "THE REMAINING 39 ARE TWO PILES AND NEITHER IS A ROUTER PROBLEM. (a) 21 clearance + the 28 unconnected are ONE MCU-field congestion problem, D-BACK declared with the measurement: 8 escapes on the RP2040's north 0.400 mm side into a 3.2 mm band = exactly 8 x (0.25 + 0.15), with no escape_corridors: declared for U_MCU. That is a STAGE-5 PLACEMENT fix (a routing failure is usually a placement problem) and must not be answered with more router effort. (b) 17 track_width, classified in 03_src/route.yaml: 8 are a 0.2 um KRT artifact that must NOT be given copper — the pipeline's own width_floor pass uses a 1000 nm dead band, five times the deficit, so two gates disagree about tolerance and the board is not the place to settle it (proposed skills-side patch below); 9 are real 3V3 taps whose honest remedy is a bounded scoped_floors width relaxation over the U_MCU 3V3 pad field carrying the measured IR numbers — deliberately NOT taken, because that pad field is the same geometry (a) is about to re-place. 1 starved_thermal (C_XTAL2.2, 1 spoke of 2). PROPOSED SKILLS PATCHES, reported not applied: (i) tier_preflight PF-RULES-CLR `return`s before PF-ROUTE-CLR, so on any board whose classes ride the 0.2 hardcode the R-SCOPE downgrade is UNREACHABLE — its own fix line recommends scoped_clearances and then cannot credit them; (ii) the 0.2 um width artifact has two graders with two different tolerances (p_width_floor's 1000 nm dead band vs DRC's exact floor) and no owner. CARRIED, NOT LOST: 61 of 64 refdes emit 0.1200/0.1125 mm silk stroke below JLC's published 0.15 (order-day DFM), the ABM8 GND pair at 2.457/2.291 mm, the four dispositioned P-ADJ-UNREACHED budgets, and route.yaml edge_band 0.7 (measured NOT binding: closest RF pad to an edge is 4.731 mm = 22.5x substrate height). DO NOT SEAL; the review battery has not been run."
updated: 2026-07-30T09:40:00
