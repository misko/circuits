# DISPOSITIONS — usb-hub-3s-v3 review findings ledger

The living decision register across ALL reviews (08_reviews contract). Started
2026-07-23 with the post-seal external v1.3 review; findings from the earlier
red-team/fix-confirmation reviews were dispositioned inside their release
verifications (v1.0-v1.3 `verification/` + SUPERSEDED.md chains) before this
ledger existed and are not retro-transcribed — their reviews remain the
evidence of record.

Severity vocabulary: P0/P1/P2 (SKILL.md stage 7). Every finding is a CLAIM,
independently re-verified against artifacts before disposition.

## Review status index — WHICH VERDICTS ARE STILL LIVE

Two archived reviews carry `verdict: DO-NOT-ORDER` in their header block.
**Both are DISCHARGED.** Review files are APPEND-ONLY EVIDENCE (08_reviews
contract) — a superseded verdict is dispositioned here, in the mutable index,
never banner-edited into the evidence. Read this table before reading a verdict
line as a live blocker.

| review file | verdict | applies to | STATUS as of v1.5-2026-07-25 |
|---|---|---|---|
| `2026-07-25_v1.4_pcba-audit_assembly.md` | **DO-NOT-ORDER** | **v1.4-2026-07-23** | **SUPERSEDED — discharged by v1.5-2026-07-25.** Its P0 (PCBA-1, C1/C2 reversed) is fixed; all 15 findings carry a disposition below. v1.4 carries `SUPERSEDED.md` naming this review. |
| `2026-07-22_v1.0_redteam_topology.md` | **DO-NOT-ORDER** | **v1.0-2026-07-22** | **SUPERSEDED — discharged three releases ago.** Re-reviewed in `2026-07-22_v1.0_redteam_topology_rereview.md` and fixed in v1.1-2026-07-23; v1.0 carries `SUPERSEDED.md`. Nothing in it is open against the current board. |
| `2026-07-22_v1.0_redteam_layout.md` | ORDER | **v1.0 copper ONLY** | **DOES NOT COVER THE SHIPPED COPPER.** The v1.5 fresh lens measured the difference: 10 footprints added after v1.0 (C53, C54, D5, F2, Q6, Q7, R30, R34, R35, SW1) and tracks 642 → 1061, none of which this review mentions (FL15-2). Superseded as the layout lens of record by the row below. |
| `2026-07-25_v1.5_redteam_layout.md` | *(see file)* | **v1.5-2026-07-25 (copper as sealed since v1.3)** | **the layout/thermal/power-integrity lens of record.** Commissioned specifically to close FL15-2 — the first such lens ever run against the shipped copper, focused on the never-reviewed discrete VBUS protection chain. |
| `2026-07-22_v1.0_redteam_topology_rereview.md` | ORDER (prose only) | v1.0 topology re-review | discharges the v1.0 DO-NOT-ORDER above. **Header block incomplete** (2 of 5 required keys — FL15-12), so it is not cited as the topology lens of record; the row below is. |
| `2026-07-23_v1.3_redteam_fresh-lens.md` | ORDER | v1.3-2026-07-23 | **the topology/protection lens of record** — the last full fresh-context lens on this copper, which is unchanged since. Carries a complete header block. |
| `2026-07-25_v1.5_fresh-lens_staged.md` | **ORDER** (0 P0, 3 P1, 9 P2) | **v1.5-2026-07-25** | zero-context lens over the STAGED v1.5 release, run before sealing. Independently re-derived the four CPL cells and ran its own rotation fit over the whole board: **107/107 parts with a cached JLC footprint agree with the shipped CPL to <1°.** All 12 findings dispositioned below. |
| `2026-07-26_v1.8_pin-review.md` | PASS | v1.8-2026-07-26 | first pin review this board ever ran. Superseded as the pin lens of record by the v1.9 row below (same copper, but the v1.9 review is against the release that ships). |
| `2026-07-26_v1.8_render-review.md` | PASS | v1.8-2026-07-26 | first render review this board ever ran. Superseded as the render lens of record by the v1.9 row below. |
| `2026-07-27_v1.9_pin-review.md` | **PASS-WITH-NOTES** (0 P0, 1 P1, 8 P2) | **v1.9-2026-07-27** | **the pin lens of record.** 122 refs / **372 pins all read and adjudicated**; PCB pads cross-checked pad-by-pad against the netlist by an independently written comparator, **372/372 agree, 0 mismatches**. Findings PR-1..PR-9 below. |
| `2026-07-27_v1.9_render-review.md` | **PASS** (0 P0, 3 P1, 6 P2) | **v1.9-2026-07-27** | **the render lens of record.** Independently confirmed the pour on all four layers and derived both feedback dividers from the netlist before reading any design doc. Findings RR-1..RR-9 below. |
| `2026-07-27_v1.9_redteam_fix-lens.md` | **ORDER** (**0 P0**, 1 P1, 8 P2) | **v1.9-2026-07-27** | **the integrated red-team lens of record — topology/protection/ratings AND layout/thermal/PI AND fix confirmation in one pass.** Four instruments, each stated so it can be disagreed with; the stranded-island check was proven able to FAIL by three negative controls, and the pour was re-counted from the SHIPPED GERBERS by a parser that does not use pcbnew. Findings P1-1, P2-1..P2-8 below. |

**The current orderable release is `07_releases/v1.9-2026-07-27/`.** Every
earlier release is DO-NOT-ORDER — v1.6, v1.7 and v1.8 because their gerbers
carry **no copper pour on any layer**, v1.5 and earlier for the A-POS datum
defect.

## 2026-07-27 — the three v1.9 pre-seal lenses

**Run against the STAGED v1.9 archive, before the seal, which is the whole
point:** `07_releases/` becomes immutable at the seal commit, so a finding here
costs an EDIT and the same finding afterwards costs a SUPERSEDE. Every "fixed"
row below was fixed **in this release**, not deferred to the next one.

Verdicts: **PASS-WITH-NOTES / PASS / ORDER. No P0 in any of the three.**

### Red-team integrated fix lens — P1-1, P2-1 … P2-8

| id | finding (one line) | severity | verification | disposition |
|---|---|---|---|---|
| **P1-1** | **E-OFF stored quiescent draw understated 2.6× — the two LM5116 UVLO dividers are permanently across VIN and were never in the budget** | **P1** | **CONFIRMED, re-derived from the shipped netlist rather than from the review.** `R6+R7` = 49.9k + 6.98k = **56.88 kΩ** VIN→GND on buck-A, `R15+R16` identically on buck-C. `SW1` gates only ENABLE (pad 1 = T1→GND, pad 2 = COM→ENKILL, pad 3 unconnected; **no pole touches VBAT/VBAT_F/VIN**), so both strings conduct whenever the XT60 is mated. `12.6/56 880 = 221.5 µA` each = **443.0 µA** absent from a declared 271 µA. Corrected **714 µA typ / 744 µA countable worst**. Storage life 292 d / 233 d, not 769 d / 615 d | **FIXED IN SOURCE (canon M3), and it was the gate that was broken, not the board.** `03_src/rules/power_tree.yaml` now declares **714** with the full term-by-term derivation, both unbounded terms NAMED (D2/R1 zener leg 0-12 µA; C1/C2 polymer leakage — no leakage entry in its `part.yaml` at all) rather than silently zero. **THE SERIOUS PART WAS THE GATE:** ORDER_README bench **Q6 declared "PASS ≤ 300 µA", which a correctly-built board CANNOT meet** — a gate that cannot pass is the same defect class as a gate that cannot fail. Q6 re-based to **≤ 1.00 mA** with a stated derivation (worst-good 744 µA; weakest-possible-bad 1461 µA at VIN 9.0 V / Vf 2.4 V; `sqrt(744×1461) = 1042` → 1.00 mA, 1.34× above good, 1.46× below bad) **plus a 1.00-1.45 mA INDETERMINATE band with a discrimination step** (lift D8 and re-measure), because the two unbounded terms make a single number dishonest. Root cause reported upstream: `power_topology.py grade_off_control()` checks only that `quiescent_ua` is DECLARED, never that it reconciles with the netlist |
| **P2-1** | **CSG is not a Kelvin connection — DETAIL_DESIGN sec.2.5's "no shared trunk copper enters the sense loop" is falsified by measurement** | P2 | **CONFIRMED, and the tap re-measured here independently with pcbnew:** `R10.1` sits on the **GND net at (75.175, 47.000)**, **3.73 mm** from `RS1.2` (72.000, 48.962) — it taps the PLANE, not the shunt terminal. Buck-C identical (`R19.1`, 3.73 mm from `RS2.2`). SOR on the GND copper puts **0.359 mΩ** (A) / **0.381 mΩ** (C) of shared trunk copper inside the sense loop, plus 0.124 / 0.174 mΩ on the CS side | **FIXED — the prose AND the derived limit.** A claimed Kelvin connection is exactly what a later reviewer builds on. New `DETAIL_DESIGN` **sec.2.5a** withdraws the sentence verbatim, tabulates the measurement, and carries the corrected limit: buck-A `0.483 mΩ × 6 A = 2.90 mV` on a 60 mV full-scale signal = **+4.8 %** → **I_LIM 11.0 A is really ≈10.5 A**; buck-C +5.6 % → ≈10.4 A. Still 1.75× the rail load and still clears TI eq. (11), so no design change. Also recorded: the shared path is inductive and carries the PULSED return, and there is **no differential filter** — R9/R10/R18/R19 are 0 Ω and no cap exists between CSF_x and CSGF_x. **NEXT-REV** work order: CSG to the shunt's own pad + the CS RC filter the 0 Ω links were provisioned for |
| **P2-2** | buck-A's pour is 2.7-3.2× more resistive than the mirrored buck-C cell, and the declared switch-node margin is method-dependent | P2 | CONFIRMED as reported: SOR reads `SW_A Q2.S→L1.1` **0.774 mΩ** vs `SW_C` **0.287 mΩ**, and `CS_A` **6.578** vs `CS_C` **2.083 mΩ**. Resistance-equivalent width for SW_A is **4.241 mm = 0.96×** the IPC-2221 4.399 mm, where `nets.yaml`'s summed cross-section says **1.38×** — the two methods disagree by 44 %. Cause localised at 0.05 mm raster: both CS pours neck to **0.300 mm over ≈0.8 mm** where the CSGF/COMP escapes cross | **RECORDED WITH THE MEASUREMENTS, COPPER DEFERRED — deliberately.** v1.9 exists to fix the pour; **re-routing voids every verdict this release just collected**. `03_src/rules/nets.yaml` SWITCH_NODE now STATES THE METHOD WITH THE NUMBER ("1.38× by summed cross-section, 0.96× by resistance-equivalent width on SW_A") and says WHY they disagree — a 0.8 mm neck barely moves a cross-section SUM and triples a resistance, so a cross-section sweep is blind to it **by construction**. Thermally closed either way: **37.9 mW** on SW_A over 281.7 mm², **143 mW** on CS_A. **NEXT-REV:** widen the CS neck, and find out why two mirrored cells differ 3× — nobody knows, and nobody had measured it before 2026-07-27 |
| **P2-3** | high-side gate loops are 25-34 nH, against the AON6354 `part.yaml` layout note asking for short, direct gate traces | P2 | CONFIRMED as reported: `HO_A` 20.253 mm (changes layer mid-run, 2 vias), `HO_C` 24.151 mm; mean same-layer gap to SW copper 1.966 / 2.510 mm; enclosed loop ≈40 / 61 mm² → **25-34 nH**. With Ciss ≈ 4 nF and a 2-3 Ω driver, **Q ≈ 0.8-1.0** (near-critically damped), ring 14-16 MHz. LO loops are fine (0.787 mm mean gap, coplanar in the CS pour) | **RECORDED, COPPER DEFERRED** — same reason as P2-2. Q ≈ 1 reads as switching-loss/EMI, **not** shoot-through. The uncomfortable part is not the number but that the `nets.yaml` GATE class justifies its 0.300 mm on dI/dt loop area **and nobody had measured the loop** — now measured, and the figure is in the class comment. Bench: **gate Q3 already scopes both SW nodes**; adding V_gs at Q2/Q4 is a next-rev scope item. **NEXT-REV:** route HO/SW as a pair |
| **P2-4** | **the R-THERM waiver's evidence describes a board that no longer exists** | P2 | **CONFIRMED, and re-measured here independently** (via centre inside the pad polygon, pcbnew, on the sealed v1.9 board): **U2.21 = 7 GND vias, U11.21 = 7 GND vias** — against a waiver saying *"U11.21 … 1 direct via (vs 3 on the sister EP U2.21)"* and carrying a next-rev work order that is **already done**. The same waiver's dissipation figures still cited the superseded 15.5 A Q1 / 5 A Q6 envelope. Also measured: `U3.9/U4.9/U5.9` (TPS2557 EPs) have **exactly 1 via each** and were covered by a waiver that never named them | **FIXED — the waiver body is rewritten in `03_src/rules/policy_waivers.yaml`.** A waiver needs evidence, and **stale evidence is the same failure mode as no evidence**; this one had outlived four releases (first raised as **RL-11** against v1.5, where the rewrite was DEFERRED — that deferral is what let it ship three more times). Now: measured via counts, dissipation re-derived at the CURRENT envelope (Q6 at the Pi 4's **3 A** = 39 mW, not 5 A), the completed work order marked done, and **the three TPS2557 EPs NAMED with the numbers that make them acceptable** (140 mW at 2 A / 259 mW at the 2.72 A ceiling, ≈48.7 °C/W → 7-13 °C) instead of being silently inside the blast radius. One genuinely OPEN next-rev order remains: a ≥4× 0.3 mm array under the three TPS2557 EPs |
| **P2-5** | **the hardware port ceiling is 2.72 A, not 2.5 A, and that number "appears nowhere"** | P2 | CONFIRMED and reproduced: `R20/R21/R22 = 36.5 kΩ` → `I_OS(min) = 127981/36.5^1.0708` = **2717 mA**; the TPS2557 is guaranteed **not to limit below** it. On the 0.800 × 13.554 mm B.Cu feed that is **ΔT 19.4 °C** and wants **1.132 mm** — not the 9.6 / 16.0 °C pair the file stated for 2.0 / 2.5 A. Aggregate 3 × 2.72 = **8.16 A** on a 6 A rail, below the 11 A valley limit, so **nothing intervenes** | **FIXED — the number is now in three places a reader will actually meet it:** `nets.yaml` VBUS `current:` (which now reads "2 A continuous per port budget; **HARDWARE CEILING 2.72 A** … nothing enforces 2 A or 2.5 A"), `DETAIL_DESIGN` sec.3, and `ORDER_README` section 6. **Survivability checked term by term rather than asserted:** L1 I_rms 10 A / I_sat 15.2 A ✓, RS1 `8.16² × 10 mΩ` = **0.67 W** in a 1 W 2512 ✓, F1 ≈7.2 A of a 10 A blade ✓. The recorded v-next widening is re-sized on **2.72 A (≈1.15 mm)**, not on 2.5 A. **Stated openly:** A-AMP still GRADES 2.0 A (the first number in `current:`) because 2.72 A would FAIL — that choice is now written down beside the number instead of being hidden by it, and bench **Q4b** measures the real rise |
| **P2-6** | **E-MARGIN was never computed for the 5VA / USB-A rail**, which feeds three known 2 A loads | P2 | CONFIRMED — `power_tree.yaml` and `DETAIL_DESIGN` sec.4 ran E-MARGIN only on 5VC. The reviewer computed it: **PASS +151.8 mV** at the receptacle, **+7.8 mV** if the two 30 mΩ USB-A mating contacts are charged to the board | **FIXED — and wired into the MACHINE gate, not just written down.** `power_tree.yaml`'s USB-A rail now declares `load_uv_threshold: 4.75` (USB 2.0 §7.2.1, at the connector) and `ir_budget_mohm: 18.1`; **E-MARGIN now grades 2 rails instead of 1, both PASS.** The rail's `vout_min/vout_max` were also synced from the bare nominal **5/5** to the tolerance-inclusive **5.032/5.273** — the same defect 5VC had fixed in v1.5 and this rail never did. **The 18.1 mΩ is labelled an EFFECTIVE rail-level resistance, not a resistance between two points**, and why is stated: three of the four terms are per-port at 2 A and only the pour carries 6 A, so it is defined as `108.5 mV / 6 A` to make the gate's own arithmetic reproduce the worst-port drop. Full derivation in `DETAIL_DESIGN` **sec.4a**, including the +7.8 mV contact case and the 1 m 28 AWG cable that would put a device at 4.04 V |
| **P2-7** | **the board declares no stackup**, so every ampacity claim's copper weight is a fab default | P2 | CONFIRMED — `usb_hub_3s_v2.kicad_pcb` and `.kicad_pro` contain **zero** `stackup` entries. JLC's 4-layer 1.6 mm default is 1 oz outer / **0.5 oz inner**, so In1 GND and In2 VIN carry **half** the copper "1 oz external" arithmetic assumes | **FIXED AS A DECLARATION, board-file change DEFERRED with the cost stated.** The assumed stackup is now written in `nets.yaml` (beside the plane cross-sections, as the reviewer suggested), `DETAIL_DESIGN`'s envelope block, and **`ORDER_README` section 1b as an ORDER-FORM OBLIGATION** — which is where it actually binds, since the gerbers do not carry it. It changes no conclusion (every IPC-2221 figure quoted is an outer-layer one; PWR_IN's 1.99× is attributed to VBAT on F.Cu, correctly), but the weight is PINNED instead of inherited. **NOT put in the board file in v1.9:** that would change the sealed payload the three v1.9 reviews were run against, for a parameter the gerbers do not carry. Declaring it in `floorplan.yaml` is a next-rev work order |
| **P2-8** | **5VA's worst corner 5.273 V exceeds the 5.25 V ceiling sec.2.11 cites as its own reason not to raise the rail** | P2 | CONFIRMED and re-derived here: `1.215×1.015 × (1 + (3.92×1.001)/(1.21×0.99))` = **5.2731 V**, against USB 2.0 §7.2.1's 4.75-5.25 V downstream-port window. sec.5.3 catches the **USBLC6** side of the same number (+23 mV over U8/U9/U10's characterised point) and dispositions it; **nothing caught the USB-2.0-spec side** | **ADJUDICATED: the ceiling is right, the corner is right, and it is a REAL (small) margin finding — one number, two independent limits, only one of them ever checked.** Recorded in `DETAIL_DESIGN` sec.2.11 so the sentence stops contradicting sec.4's own table, bounded honestly: **no-load, all-tolerance-extremes** corner only (any real port load pulls it back inside), **+23 mV = 0.44 %** against a 5 % band. **NO HARDWARE CHANGE** — and R42 is **not** a lever here, it is a **5VC** strap, so the note that "R42 already exists" does not apply to this rail. If a future revision wants the corner inside 5.25 V the lever is R4/R13 at ±0.1 %, which alone gives **5.237 V**. Lowering 5VA would spend the +151.8 mV of P2-6 slack |
| **OQ-1** | *(recorded as an OPEN QUESTION, not a finding)* the fix lens's own SOR reads **3.009 mΩ** for the three 5VC delivery segments against RL-2's **9.32 mΩ** and the **12 mΩ** carried in the budget | — | **A 3× DISAGREEMENT BETWEEN TWO MESH SOLVES ON IDENTICAL COPPER, WHICH NEITHER SIDE COULD RECONCILE.** Both are SOR; the v1.9 solver was validated against analytic bars at 0.4-5 % LOW, so it is not a scale error in its core. It is in the **safe direction** — the shipped budget is the pessimistic one — which is the only reason it is not a finding | **WRITTEN DOWN AS UNRECONCILED, in `power_tree.yaml` beside `ir_budget_mohm` and in `DETAIL_DESIGN` sec.4.** Not papered over and not averaged away: if 3.009 mΩ is right the budget is conservative by ≈9 mΩ (27 mV at 3 A) and the slack is **+276.6 mV** rather than +244.2 mV. **Two solvers disagreeing 3× on the same copper is settled by the bench, not by whichever number reached the file first** — gates **Q2/Q5** measure the delivered voltage, and this stays OPEN until they do |

### Found during the pre-seal pass itself, by neither review

| id | finding (one line) | severity | verification | disposition |
|---|---|---|---|---|
| SEAL-1 | **the gerber zip size stated in six shipped documents was wrong by 4 bytes** | P2 | found while re-verifying the reviews' own numbers rather than copying them: `stat` on `fab/usb_hub_3s_v2_gerbers.zip` reads **394 534 B**, while `MANIFEST.txt`, `ORDER_README.md` §0, `copper_restoration_proof.md`, `freshness_exceptions.txt`, `01_docs/STATUS.md`, `01_docs/CHANGELOG.md` **and v1.8's `SUPERSEDED.md`** all said **394 530 B** | **FIXED in all seven.** Four bytes changes no conclusion — the zip is still 4.45× v1.8's 88 692 B and the difference is still the copper — but the size is offered to the reader as *"the crudest possible check, and you can run it without any tooling"*. **A check a reader can run in five seconds must not disagree with the reader's own `ls`**, or the first thing they learn about this archive is that its numbers are approximate. Corrected before the seal made it permanent |

### Pin review — PR-1 … PR-9

| id | finding (one line) | severity | verification | disposition |
|---|---|---|---|---|
| PR-1 | `pin_audit.py` silently produced an EMPTY dossier for **U2 and U11**, the two 21-pad LM5116s — the highest-consequence parts on the board | **P1** | confirmed: the BOM MPN is `LM5116MHX/NOPB` and the tool does `parts / mpn / "part.yaml"`, which resolves to `02_parts/LM5116MHX/NOPB/part.yaml` — nonexistent. It falls through to `ymap = {}`, prints no warning and **exits 0**. The hazard was known one level down and never propagated: `02_parts/LM5116MHX-NOPB/part.yaml:4` literally says *"'/' in MPN not usable in a path"* | **REPORTED UPSTREAM, not fixed here — `skills/` is owned by concurrent agents** (`skills/kicad-pcb/scripts/pin_audit.py:130`). **The board is unaffected:** the reviewer re-read SNVS499I Table 4-1 directly and adjudicated all 42 LM5116 pins from the datasheet. But this is the canon "a gate that cannot fail is worthless" pattern in review tooling — the instrument the protocol depends on failed silently, exactly where it mattered most |
| PR-2 | `pin_audit.py:124-125` excludes **every 2- and 3-pin part** from dossier coverage unless `--refs` is passed | P2 | confirmed: 22 dossiers on a 122-ref board, with no note that 100 refs were skipped. The omitted set is D1-D5, D8-D12, J1, C1/C2, RS1/RS2, L1/L2, F2, SW1, Q7, Q8 — **precisely the repeat-offender class this project's own `part.yaml` files flag** | **REPORTED UPSTREAM** (shared gate). Locally mitigated by the ORDER-PREVIEW HUMAN GATE, which already carries D1-D5 polarity, C1/C2 polarity, J1 XT60 polarity and R12 by value — the parts the dossier silently skipped are the parts the human gate explicitly covers |
| PR-3 | **every pin in the exported netlist is typed `passive`** (364 + 8 no-connect of 372), so KiCad ERC's electrical-type matrix is **structurally incapable of firing** | P2 | confirmed: the `.kicad_sch` contains no `(type …)` tokens at all. A "0 ERC violations" result therefore carries **no pin-type information whatsoever**. The board's own pin-type sanity is fine — verified by hand: both gate drives fan out to exactly one gate, no net has two drivers, no net has a single member — **but that was verified in the review, not by the gate** | **RECORDED — a converter property, reported upstream.** It is a real limit on what this board's ERC evidence means, and it is now stated rather than left for the next reader to assume ERC covered something it cannot. The MANIFEST's ERC line already reports 221 `lib_symbol_issues` warnings from converter-synthesised symbols; this is the same root |
| PR-4 | **U2.11 / U11.11 (DEMB) tied directly to GND** — R_DEMB = 0 Ω, an intentional departure from the TI worked design this project declares it adopts, **with no written derivation** | P2 | confirmed: SNVS499I says *"When R_DEMB = 0 Ω, the LM5116 will always run in diode emulation"*, and TI's Fig. 7-1 / Table 7-1 fit **R8 = 10 kΩ** for exactly the opposite. `grep -i "demb\|diode emulation" DETAIL_DESIGN.md` → **0 hits**, against that file's own rule *"a value in the schematic with no line here is UNJUSTIFIED"* | **FIXED — `DETAIL_DESIGN` sec.2.10a written.** Both bucks run in **permanent diode emulation** and never operate fully synchronously; the DCM boundary is re-derived (`I_pp = 1.79 A` → DCM below ≈0.9 A/rail). It is legal, datasheet-sanctioned, and arguably right on a battery board — **but it is a CHOICE, and an undocumented choice is indistinguishable from an oversight.** Now documented as a choice, with its cost, and pointed at bench gate **Q3**, which already scopes the boundary. Fitting a 10 kΩ DEMB resistor is a BOM+board change → next-rev decision item |
| PR-5 | the USBLC6 "pass-through" claim in sec.5.2 asserts a property **the netlist cannot carry** | P2 | confirmed and agreed with the reviewer's reading: ST doc 11265 Rev 5 Fig. 1 labels pins 1/6 both `I/O1` and 3/4 both `I/O2` — each pair is ONE internal node — so `U8.1 = U8.6 = DP_A1` is electrically correct. But once both pads are on one net, **nothing in the netlist, ERC or DRC can distinguish an in-line clamp from a stub on a tee** | **FIXED — the claim is DEMOTED to what it actually is.** sec.5.2 now states the one-internal-node fact and says plainly that in-line-ness is a **pure LAYOUT property with no gate behind it** on this board. A routing check for it is a gate gap reported upstream, not something this release invents locally. The wiring was never wrong; the sentence was |
| PR-6 | **R42 is on `fab/bom.csv` with no row in `fab/cpl.csv`**, and the mechanism rests on an uncited assumption about the fab's behaviour | P2 | confirmed: BOM-only = `[F1, R42, SW1]`, CPL-only = `[]`. If JLC's desk resolves the DFM query by *placing* R42, shipped 5VC becomes 5.249 V instead of 5.352 V and E-MARGIN's worst-min drops 5.227 → 5.125 V | **ALREADY COVERED, verified rather than assumed.** ORDER_README section 1 names R42 **DNP by design, on the BOM so JLC ships one loose, off the CPL**, and human-gate item **P7** REJECTS the order if *"JLC 'helpfully' adds placement data for any of them"*. All three refs also carry dated evidence in `03_src/rules/assembly.yaml`, and A-POP grades `{BOM}−{CPL}` against that file exactly. The assumption the reviewer flagged is therefore backed by a human gate that can fail |
| PR-7 | U12's V_BUS above the USBLC6's 5.25 V characterised point — **already recorded** | P2 (recorded) | confirmed by re-reading the ST datasheet rather than the write-up: Table 1 "Absolute ratings" lists only V_PP, T_stg, T_j, T_L — **no V_BUS entry** — so sec.5.3's reasoning holds as written | **NO NEW ACTION** — carried at `DETAIL_DESIGN` sec.5.3 and bench gate **Q9**, with R42 as the trim lever. Listed so the coverage is complete, per PCBA-4 / PCBA-15 above |
| PR-8 | the "unused channel-2 pins may float" claim cites a sentence belonging to a **different device variant** | P2 | confirmed: the only *"can be grounded or left floating"* line in SLVSBY8D is in the **TPS2514x** pin table, where pins 3/4 are genuine `N/C`. On the dual **TPS2513A** that is fitted, pins 3/4 are real `DP2`/`DM2` detection I/O | **FIXED — the citation is withdrawn and the claim restated as what it is.** `DETAIL_DESIGN` sec.3 now labels it an **engineering judgement, not a datasheet permission** (an unused BC1.2 detection channel has no port to characterise and idles), assesses the risk as low, and records the honest label *"unqualified by the datasheet"*. Grounding DP2/DM2, or fitting a single-channel TPS2511/TPS2513 for the third port, is a next-rev tidy |
| PR-9 | the vendored USB-A footprint's **pin 1 = VBUS is INHERITED**, not read from the vendor drawing | P2 | confirmed: the Kinghelm drawing's PCB pattern does **not number** the four signal holes and the pattern is geometrically symmetric, so the drawing alone cannot establish which end is pin 1. The footprint is a byte-level derivative of KiCad's `USB_A_Stewart_SS-52100-001_Horizontal`, so pin 1 = VBUS comes from the KiCad library convention plus the USB-A contact standard. Also: the vendored footprint carries **no F.SilkS geometry at all** | **RECORDED, and it belongs on the human gate — consequence if wrong is VBUS↔GND swapped on all three ports.** ORDER_README human-gate item **P8** already requires every polarized part's magenta pin-1 marker to match our silk in the JLC preview, and the reviewer's conclusion agrees with `part.yaml`. Naming J2/J3/J4 explicitly in the mechanical confirm list alongside SW1's throw direction is a next-rev ORDER_README improvement, not a v1.9 blocker |

### Render review — RR-1 … RR-9

| id | finding (one line) | severity | verification | disposition |
|---|---|---|---|---|
| **RR-1** | **a FAILING gate ships inside the release while the MANIFEST reports the same gate as clean** | **P1** | **CONFIRMED, and the reviewer root-caused it with the right instrument — mtime.** `verification/assembly_coverage.txt` ended `A-POP: FAIL (1)` on `MANIFEST-UNDECLARED`, while `MANIFEST.txt` plainly carries `not_assembled:  F1, R42, SW1`. `assembly_coverage.txt` 11:43:54 vs `MANIFEST.txt` 12:04:45 — **A-POP read the MANIFEST 21 minutes before it was written**, so on a first build this check can never pass | **FIXED — and fixed by ORDERING, which is where the defect was.** Re-run against the staged MANIFEST: **`A-POP: PASS (every unpopulated part is declared with evidence)`**, 129 footprints / 119 placements / 10 unpopulated (3 declared), A-POS worst 0.00050 mm. The archived file is now the re-run, and it is regenerated **AFTER the MANIFEST stamp** — where the seal procedure already puts `policy_audit` and `release_freshness_check` for exactly this reason. Gate gap reported upstream: `assembly_coverage.py` has no way to say *"manifest not yet stamped"*. Full adjudication: `verification/gate_adjudications_v1.9.md` §3.1 |
| **RR-2** | **`missing_models.txt` is false, and it is the file a reviewer is told to trust** | **P1** | **CONFIRMED in consequence, and re-measured here:** set-differencing `twin_report.csv` gives **46 LCSC codes, 45 with a `MODEL-REG*` row, exactly one without — `C2984354` (R12)**, whose only row is `FETCH-FAILED`. Yet `missing_models.txt` says `bodies mounted: 122/122` and `(none — every CPL designator resolves a 3D body)`, and the MANIFEST repeated it | **ADJUDICATED AND CORRECTED WHERE IT CAN BE TRUSTED — the generated file is NOT hand-edited.** The file is answering a different question than the one asked of it: the NO-BODY pass measures *"does this ref render a body"* (true — R12 renders its **KiCad** footprint's body), not *"does JLC have a model"* (false for R12). **v1.5 of this board shipped a HAND-AUTHORED `missing_models.txt` claiming zero gaps while seven placements rendered nothing**, which is precisely why the header says *do not edit*. So the correction lands in the **MANIFEST `twin:` line**, which now names R12 and the caveat, and in `gate_adjudications_v1.9.md` §3.2. Consistent with ORDER_README gate **P5**, which already records the C2984354 404 as genuine absence. Gate gap reported upstream: NO-BODY conflates "a body renders" with "JLC has a model" |
| **RR-3** | the USB-C rail ships **above the port ceiling**; worst static corner 229 mV over, and it applies to **every attached device**, not only U12 | **P1** | **CONFIRMED in fact, ADJUDICATED on which limit applies.** The reviewer derived both dividers from the netlist alone before reading any design doc and matched the release's numbers exactly (5.352 / 5.248 / 5.151 V). But **5.25 V is USB 2.0's downstream-port number; a USB Type-C SOURCE is permitted `vSafe5V` = 4.75-5.5 V**, so 5.479 V is **inside** the Type-C window by 21 mV | **RECORDED, with the limit corrected and the finding made SHARPER, not softer.** New table in `DETAIL_DESIGN` sec.4 pairs each corner with the limit that actually governs it. The honest statement is **narrower than "over the port limit" and worse than "only U12"**: 5.479 V is **+229 mV above the Raspberry Pi 4's own recommended-input maximum** (5.0 V ±5 %) while staying **521 mV below** its 6.00 V absolute maximum. Also in `ORDER_README` section 5. That is a supervised-prototype posture consistent with ADR-0002's best-effort OV story, and it is exactly what **R42** (DNP, shipped loose) and gate **Q9** exist to retire if the bench says so |
| RR-4 | **51 image artifacts the A-RENDER report names are not in the release** (`twin_top_courtyard_overlay.png` + 50 per-ref crops), so its central claim cannot be re-checked from the release alone | P2 | confirmed by enumerating every `.png` cited in `twin_overlay_top.md` and testing each for existence — none of the 51 are present. Compounding it, coverage is **53 measured / 121 refs with an expected body**, so render faithfulness is established for 44 % of the board (the coverage number IS disclosed; the missing crops were not) | **RECORDED — and the disclosure gap is the real finding, not the crops.** The A-RENDER verdict does not rest on the crops: `gate_adjudications_v1.9.md` §1 adjudicates all 29 flags by CLASS with the mechanism for each (18 MLCC segmentation slivers, 6 PowerPAK MOUNT-FALLBACK, 5 connector overhangs), and every class is independently reproducible from `twin_report.csv` + the board. Shipping 51 debug crops into an immutable archive is not the fix; **naming them as not-shipped is**, and the MANIFEST now does. Next-rev: have `twin_overlay.py` cite only artifacts it also writes to the release |
| RR-5 | `twin_report.csv` **contradicts itself** on the indicator LEDs — `POLARITY-FIT` says never trust the fitted angle, then `ROT-DB-SUGGEST` proposes `C2296,180` | P2 | confirmed; the shipped board is **correct** (`jlc_lcsc_rotations.csv` carries `C2296,0` / `C2297,0` from a hand-measured two-channel derivation, and `fab/cpl.csv` ships D8-D12 at 0.0). But the release contains a **machine-readable instruction that would flip all five LEDs**, sitting next to prose saying not to | **ALREADY ADJUDICATED AND HUMAN-GATED — this is the single most-defended item in the release.** ORDER_README human-gate **P1** rejects the order if any LED row reads 180.0, states that KiCad and JLC number this part's terminals **oppositely**, and names the `ROT-DB-SUGGEST` line as *"a known tool inconsistency, adjudicated REJECTED — its own marking channel disagrees with its own pad fit"*. The reviewer independently reached the same conclusion. **A 180° LED ships every indicator dark, which on a bench is indistinguishable from a dry joint** — which is why it is gated by a human and not by a tool that contradicts itself |
| RR-6 | the silkscreen next to the battery connector reads **`J1 FUSE 10A MINI`** on one baseline, so F1's caption reads as J1's value | P2 | confirmed from the bare render: the free text is at (35, 50) and F1's body at (35, 56), so it is nominally F1's caption, but on the printed board it lands on J1's line — and a via breaks the string between `FUSE` and `10A` | **RECORDED, silk change DEFERRED — it is copper (silk is a plotted layer) and v1.9 must not change the payload.** Bounded: it is a legibility defect, not a polarity or value defect, and **J1's actual identification is human-gated** — ORDER_README gate **P2** is a MANDATORY XT60 polarity check that names pad 1 = NEGATIVE from `part.yaml` and requires a human with the connector. **NEXT-REV:** move the F1 caption below F1's body and off J1's baseline |
| RR-7 | `pdf/assembly.pdf` p1 is **unusable in the dense areas** and **does not mark DNP** — F1, SW1 and R42 are drawn exactly like the 119 placed parts | P2 | confirmed by rendering the page at 150 dpi: around U2/U11 the value, LCSC and refdes layers overprint into an unreadable block; around C1/F1 four LCSC codes print on top of one another | **RECORDED.** The DNP half is fully covered in text — ORDER_README section 1 tables all three refs with why, human-gate **P7** rejects the order if JLC adds placement data for any of them, and `03_src/rules/assembly.yaml` carries dated evidence per ref — so **nothing about the population set depends on this PDF**. The legibility half is a plot-configuration next-rev item (separate layers or a larger sheet). Not a blocker: the CPL, not the assembly drawing, is population truth |
| RR-8 | `pdf/schematic.pdf` has **no sheet frame, title block, revision or date**, and draws every FET and diode as an unmarked box | P2 | confirmed by rendering at 140 dpi and at 400 dpi: readable only when zoomed, where pin names/numbers and net labels are all **correct**; Q3/Q5 render as plain rectangles with `S1 S2 S3 / D G`, D3 as a rectangle with `K / A` | **RECORDED — and it is a deliberate ADR consequence, not a regression.** ADR-0002 requires `pdf/schematic.pdf` to be **tscircuit's OWN render**, not a KiCad re-render; v1.6-v1.8 regressed to Eeschema and v1.9 restores it. tscircuit's renderer does not draw sheet frames or device-type symbols. **The correct schematic of record for reading device polarity is `source/usb_hub_3s_v2.kicad_sch`**, which ships in the archive. Upstream feature request, not a local fix |
| RR-9 | **J5 is the tightest of the five connector fits** — expected body 0.216 mm proud of the board edge, an order of magnitude tighter than J2/J3/J4's 2.46 mm | P2 | confirmed from `twin_overlay_top.md`'s expected column (measured, not from pixels): J5 reaches y = 112.266 against an edge at 112.050 | **RECORDED, and it is already on the human gate.** Usable for an edge-mount USB-C, but it is the one connector with no clearance for a cable overmold. ORDER_README human-gate **P8** requires a rotation-and-datum sweep in the JLC 3D preview; **J5 is worth one deliberate look there.** Next-rev: pull J5 back ~0.5 mm or trim the edge cut-out |

## 2026-07-23_v1.3_external-user_full.md (received post-seal; examined pre-seal state)

| id | review file | finding (one line) | severity | verification | disposition |
|---|---|---|---|---|---|
| EXT13-1 | 2026-07-23_v1.3_external-user_full.md | §1: v1.3 is a staged, blocked release — gate iii + seal pending | P0 (at review time) | confirmed for the PRE-SEAL state the reviewer saw; overtaken when received — seal completed same day (source S=01983b0, seal=d137e00; fresh lens VERDICT ORDER archived 2026-07-23_v1.3_redteam_fresh-lens.md) | fixed — overtaken by seal d137e00; the demanded gate-iii fresh red-team + 2-commit seal are exactly what completed |
| EXT13-2 | 2026-07-23_v1.3_external-user_full.md | §2: R30 still C2933195 = 3.09 kΩ, not 100 kΩ | P0 (at review time) | confirmed for the pre-seal state; overtaken — sealed v1.3 BOM has R30 on C25803 (100 k, merged into the R1/R8/R17 row), sealed verification/bom_source_check.txt PASS incl. the semantic value-vs-ledger leg; forbidden-code sweep in the fresh lens found no C2933195 anywhere | fixed — v1.3 gate ii-b commit 01983b0 (pre-seal); reviewer's recommended part C25803 is the one shipped |
| EXT13-3 | 2026-07-23_v1.3_external-user_full.md | §3: divider worst-case math omits R13/R4 ±1% (C5126242 = FRC0603F1211TS, ledger row 150); true 5VC range ≈5.227-5.479 V, not 5.27-5.43 V | P1 | confirmed — ledger decode FRC0603F**1211**TS = 1.21 k ±1% ("F" series = 1%); recomputed: 5VC min 5.227 V / max 5.479 V (R12 ±0.1%, R13 ±1%, Vref ±1.5%); low-corner headroom 597 mV vs 440 mV IR budget = still PASS with 157 mV slack; USB-A top corner 5.273 V slightly above the 5.25 V ceiling (R3 C728591 is 0.1%) | fixed — v1.4-2026-07-23 ORDER_README ships the tolerance-inclusive worst-case table + no-load ceiling criteria; hardware option (0.1% R13/R4) recorded as a next-rev choice, not required (margins still pass) |
| EXT13-4 | 2026-07-23_v1.3_external-user_full.md | §4: README SW1 fallback-header polarity REVERSED ("COM-T1 shunted = ON") | P1 | confirmed — tsx SW1 `connections={{ pin1: "net.GND", pin2: "net.ENKILL" }}` (pin1=T1, pin2=COM); power_tree off_control: grounding ENKILL turns BOTH bucks off + opens Q6. COM-T1 shunted therefore = OFF | fixed — v1.4-2026-07-23 ORDER_README: "COM-T1 shunted = OFF; shunt removed = ON" + continuity-verify step in the bench gate |
| EXT13-5 | 2026-07-23_v1.3_external-user_full.md | §4b: README calls F1 "KH-AF90DIP-112" (the USB-A connector family) | P2 | confirmed — sealed fab/bom.csv row 38: `C5249699,F1,Fuseholder_Blade_Mini_Keystone_3568`; KH-AF90DIP-112 is the USB-A receptacle footprint family | fixed — v1.4-2026-07-23 ORDER_README names F1 = Keystone 3568 mini-blade holder, C5249699 |
| EXT13-6 | 2026-07-23_v1.3_external-user_full.md | §5: F1 + SW1 on fab/bom.csv but absent from cpl.csv → JLC upload shows 2 unmatched designators | P2 | confirmed — bom.csv rows 32 (SW1 C2939728) + 38 (F1 C5249699); neither refdes in cpl.csv (intentional hand-solder, FP_EXCLUDE_FROM_POS_FILES) | fixed — v1.4-2026-07-23 ORDER_README packaging note: mark F1/SW1 DNP/not-assembled in the JLC order review; purchasing list incl. the 10 A MINI blade fuse element |
| EXT13-7 | 2026-07-23_v1.3_external-user_full.md | §6: fail-high protection is best-effort, not deterministic; acceptable for supervised prototype | P2 | confirmed and already recorded — this IS the documented OV posture (Option 2, BRIEF A3/D3, ADR-0002; escalation boundary verbatim in ORDER_README/MANIFEST) | recorded — consistent with the user-recorded decision; reviewer concurs with the acceptance; text kept VERBATIM in v1.4 |
| EXT13-8 | 2026-07-23_v1.3_external-user_full.md | §7: tighten bench pass criteria (VBUSC@5A ≥5.00 V, cable-end ≥4.80-4.85 V hot, no-load ceiling, load-release overshoot, get_throttled) | P1 | confirmed reasonable — criteria strictly tighten the existing Q1-Q5 gate; no conflict with recorded decisions | fixed — v1.4-2026-07-23 ORDER_README adopts the tightened Q1-Q5 criteria + adds R30 visual/ohmmeter pre-power check, no-load ≤5.45 V firm ceiling, 5 A→0 A overshoot capture, SW1 continuity logic check, vcgencmd get_throttled monitoring |

## 2026-07-25_v1.4_pcba-audit_assembly.md (pre-order PCBA audit of sealed v1.4; drove v1.5)

| id | review file | finding (one line) | severity | verification | disposition |
|---|---|---|---|---|---|
| PCBA-1 | 2026-07-25_v1.4_pcba-audit_assembly.md | §1: C1/C2 (100uF/35V POLARIZED polymer, across the 9.0-12.6V 3S pack) on CPL at 270.0 where the measured value is 90.0 — 180deg REVERSED; they vent at first power-up | **P0** | confirmed by direct measurement, independent of jlc_twin's (then-negated) jlc_offset: our CP_Elec_6.3x7.7 pad1(VIN,'+') local x=-2.700 vs JLC C2982822 pad1 x=-2.670 -> offset 0, fit rms 0.030mm @0 vs 5.370mm @180 (179x). Polarity cross-checked against JLC's OWN silk primitives: two crossed filled polys centred (-2.706,1.518) draw a '+' over THEIR pad 1, a lone bar at (+2.706,1.518) draws '-' over pad 2 — JLC's library states pad1=positive, and our pad1 is on VIN. Resolver reproduces the defect exactly (with row -> 90.0/lcsc; without -> 270.0/name) | fixed — v1.5-2026-07-25 CPL C1/C2 270.0 -> 90.0; root-caused at source in repo commit 9078ad9 (per-LCSC row for C2982822). v1.4 carries SUPERSEDED.md naming this finding |
| PCBA-2 | 2026-07-25_v1.4_pcba-audit_assembly.md | §1: J1 (AMASS XT60PW-M, polarized battery input) on CPL at 90.0 where the measured value is 0.0 — 90deg off | P1 | confirmed — FOUR-pad fit (2 blades + 2 anchors) in the common KiCad local frame with the pcbnew-verified operator: rms 0.0000mm @270 offset, and at 90 the two anchors miss by 12.0mm. Root cause: the name-DB pattern `^AMASS_XT60PW-M` is START-ANCHORED and the board uses a vendored `XT60PW-M_EdgeTrim`, so NO rule fired and the offset silently defaulted to 0 | fixed — v1.5 CPL J1 90.0 -> 0.0; source fix repo commit 95a8180. J1 additionally added to the ORDER-PREVIEW HUMAN GATE (see PCBA-11) |
| PCBA-3 | 2026-07-25_v1.4_pcba-audit_assembly.md | §1: Q7 (BSS138 gate inverter for the Q6 VBUS reverse-block) on CPL at 270.0 where the measured value is 180.0 — 90deg off | P1 | confirmed — 3-pad asymmetric fit vs JLC C78284: rms 0.062mm @180, 1.95mm @270; decisive with no numbering assumption. Would have mis-landed G/S/D: Q6 never enable-gated -> Pi rail dead, or master-off fails to open Q6 (loses the RT-T4 reverse-feed protection). Root cause: name-DB `^SOT-23` = -90 is wrong for this part | fixed — v1.5 CPL Q7 270.0 -> 180.0; the per-LCSC row was re-verified 2026-07-25 against the corrected operator (repo commit e0d735c) |
| PCBA-4 | 2026-07-25_v1.4_pcba-audit_assembly.md | §1: U12 (USBLC6-2SC6) V_BUS pin runs 5.352V nominal / 5.479V worst corner against a 5.25V characterized standoff | P1 | confirmed, and RE-GROUNDED against the datasheet read directly (ST Doc ID 11265 Rev 5, 02_parts/USBLC6-2SC6/USBLC6-2SC6_ST.pdf p.2): Table 1 "Absolute ratings" carries NO V_BUS limit; 5.25V is the TEST CONDITION for I_RM (typ 10nA / max 150nA); V_BR min = 6V at 1mA. So the exceedance is +102mV nominal / +229mV worst corner over the characterized point, and the worst corner still sits 521mV BELOW the minimum breakdown knee. Mode = elevated reverse leakage (bounded ~150nA x 30 at Tj 125C ~ 4.5uA ~ 25uW), not breakdown. ALSO FOUND: U8/U9/U10 (same C7519) on 5VA reach 5.273V worst corner = +23mV over the same point | accepted + measured (user decision 2026-07-25) — R12 NOT changed (lowering 5VC spends the E-MARGIN headroom: low corner 5.227V vs a 440mV IR budget = 157mV slack). v1.5 bench gate Q1 now RECORDS measured VBUSC and VBUSA values, and the derating ships as an EVIDENCE-BACKED waiver in the v1.5 MANIFEST (canon M4) |
| PCBA-5 | 2026-07-25_v1.4_pcba-audit_assembly.md | §1: the population set had no machine-readable home — `not_assembled` was PROSE in the MANIFEST that nothing read (canon A-POP) | P1 | confirmed — no `03_src/rules/assembly.yaml` existed. Measured set: 114 footprints - 4 mounting holes = 110 BOM parts = 108 CPL placements + 2 declared not-assembled (F1, SW1); board `excl_pos = {F1,H1,H2,H3,H4,SW1}` agrees with the CPL; 108/108 rows `top`, zero bottom-side footprints | fixed — v1.5 adds `03_src/rules/assembly.yaml` (service incl. THT, sides, fiducials, build_quantity 5, both not_assembled entries with dated evidence, exempt_prefixes). The MANIFEST `not_assembled:` line is now GENERATED from that one file |
| PCBA-6 | 2026-07-25_v1.4_pcba-audit_assembly.md | §1: F1 (fuse holder) stays off the CPL although JLC THT assembly is now being ORDERED for J1-J4 | P2 | confirmed — F1 was excluded in v1.1 because jlc_twin measured `best=none` (JLC has no placement model for the 2-clip THT holder; re-confirmed 2026-07-25). That rationale is weaker once THT assembly is bought. Moving F1 onto the CPL needs `FP_EXCLUDE_FROM_POS_FILES` cleared at source + a board regen | deferred — v1.5 is a declared NO-COPPER-CHANGE release; recorded in `03_src/rules/assembly.yaml` (F1 `disposition:`) and ORDER_README section 2 as a next-revision item |
| PCBA-7 | 2026-07-25_v1.4_pcba-audit_assembly.md | §1: `01_docs/DETAIL_DESIGN.md` did not exist although three sealed part.yaml files cite sec.1 / sec.2 / sec.5 as authority | P2 | confirmed — AON6403 part.yaml:27 (sec.1), LM5116MHX-NOPB part.yaml:48 (sec.2), USBLC6-2SC6 part.yaml:25 (sec.5). The sec.5 citation is the stated authority for the C-port ESD choice AND is wrong twice over: it names LESD5D5.0 (the board fits USBLC6-2SC6 / C7519) and describes a "20V C port" that v3 does not have | fixed — v1.5 writes `01_docs/DETAIL_DESIGN.md` covering sec.1 / sec.2 / sec.5, with the PCBA-4 U12 deviation recorded in sec.5 against the measured 5.352V nominal / 5.479V corner |
| PCBA-8 | 2026-07-25_v1.4_pcba-audit_assembly.md | §1: v1.4's ORDER_README mentions the JLC order preview ZERO times, while 12 twin findings are waived on exactly that gate | P1 | confirmed by count — `grep -ci preview` on v1.4's ORDER_README = 0. The 12: 7 POLARITY-CHECK rows (D1,D2,D3,D4,D5,**C1,C2**) whose own text says "verify via the JLC order preview", plus 5 `twin_adjudications.yaml` entries naming it (L1,L2 PAD-GEOM; D5 PAD-GEOM; SW1 PAD-GEOM; R12 FETCH-FAILED). C1/C2 — the PCBA-1 P0 — are on that list: the one check that could have caught a reversed polarized cap before payment was waived to a gate that did not exist in writing | fixed — v1.5 ORDER_README gains an **ORDER-PREVIEW HUMAN GATE** section, each item with what to look for and what rejects it: C1/C2 polarity, J1 XT60 polarity, D1/D5 TVS polarity, L1/L2, SW1 pitch, R12 value |
| PCBA-9 | 2026-07-25_v1.4_pcba-audit_assembly.md | §1: `assembly.yaml` schema has no `through_hole:` key and no `reason:` term for "catalogued and on the BOM, but hand-fitted because its land pattern / placement model is unconfirmed" | P2 | confirmed while filling the file in — the closed vocabulary (`not_in_catalog\|consign\|user_supplied\|dnp_by_design\|mechanical\|test_point`) has no fit for either of THIS board's two entries, which are the true reason for both | deferred — raise as a SKILL change (template + `03_src/rules/contracts.md` + `assembly_coverage.py` in one change, per the repo structure-governance rule). Not invented locally: v1.5 declares THT inside `service:` and states the exact reason in each `evidence:` block |
| PCBA-10 | 2026-07-25_v1.4_pcba-audit_assembly.md | §1: v1.4's `fab/bom.csv` ships an EMPTY MPN column on all 43 rows | P2 | confirmed — code-without-MPN is what leaves JLC's matcher at "No Part Selected" / "qty 0, no price" and forces mid-order manual confirmation (jlcpcb-fab order-review empirics) | fixed — v1.5 builds `lcsc_mpn_map.csv` from the live stock-check attribute data; **43/43 rows carry an MPN**. Cross-checked (canon M1) against the independently datasheet-authored `02_parts/` directory names: **26/26 hard-part MPNs matched, 0 `02_parts` dirs unaccounted for** |
| PCBA-11 | 2026-07-25_v1.4_pcba-audit_assembly.md | §1: jlc_twin raises one `MODEL-REG` on J1 (body centre 14.3mm off courtyard) | P2 | confirmed as a FALSE ALARM, verified the way the tool instructs: the XT60 body's own bbox centre is 4.10mm from its model origin (WRL bbox -13.15..+4.95mm) because the housing extends back from the blade face, so a bbox-centre metric is meaningless here. Leads-on-pads checked directly — the render was calibrated on the four mounting holes and J1's four pad centres overlaid on `twin_top.png`: all four land on the model's own features (2 blade bores on pads 1/2, 2 housing posts on the anchors) | recorded — false alarm, NO rotation override; the rotation stands on the PCBA-2 four-pad fit. **BUT** geometry cannot check which blade is '+': `02_parts/XT60PW-M/part.yaml` records "PAD 1 IS NEGATIVE — polarity is a PART FACT" and the fleet ledger records a reversed XT60 shipping once, so J1 is a MANDATORY item on the v1.5 ORDER-PREVIEW HUMAN GATE |
| PCBA-12 | 2026-07-25_v1.4_pcba-audit_assembly.md | §1: v1.4's `verification/` ships no twin report and no stock evidence | P2 | confirmed — 7 files present, neither a `twin_report.csv` nor any stock evidence, although the 07_releases contract lists both and the archive is required to stand alone | fixed — v1.5 archives `verification/twin_report.csv` (231 rows) + `verification/stock_check.txt` + `verification/stock_check.json` INSIDE the release, all hashed in the MANIFEST table |
| PCBA-13 | 2026-07-25_v1.4_pcba-audit_assembly.md | §1: two BOM lines have thin stock ceilings and v1.4's stock table named no availability alternates | P2 | confirmed — measured 2026-07-25 at `--min-stock 5`, verdict PASS (43/43 OK). Tightest floor(stock/qty): C473910 TPS2513ADBVR 75/2 = **37 boards**; C5337088 TYPE-C-31-M-12A 90/1 = 90; C408523 MWSA1206S-6R8MT 450/2 = 225. All clear build_quantity 5 by >=7x, so no block — but v1.4's table listed only the three Extended *correctness* risks (R12/F2/D5), unsorted, with no availability alternates | fixed — v1.5 ORDER_README stock table rebuilt **sorted by floor(stock/qty), tightest first**, each row carrying a named alternate; order-day recheck of C473910 + C5337088 called out explicitly |
| PCBA-14 | 2026-07-25_v1.4_pcba-audit_assembly.md | §1: the Basic/Extended split was never priced before the order | P2 | confirmed — measured 2026-07-25: **12 Basic / 31 Extended** of 43 BOM lines; ~31 per-reel setup fees | fixed — split recorded in the v1.5 MANIFEST gate table and in the ORDER_README stock section, so the feeder bill is known before the order rather than at it |
| PCBA-15 | 2026-07-25_v1.4_pcba-audit_assembly.md | §1: U12's V_BUS transil breaks down (V_BR 6.00V min) BELOW D5's breakdown window (V_BR 6.67-8.15V), so on a slow VBUSC rise the ESD array conducts before the TVS that is supposed to be the over-voltage element | P2 | confirmed from the two datasheets side by side (ST Doc ID 11265 Rev 5 Table 2 for U12; 02_parts/SMBJ6.0A/part.yaml for D5, both re-read 2026-07-25). Bounded: normal operation is unaffected (the 5.479V worst static corner is 521mV below U12's V_BR MINIMUM), and the window only opens in a buck-fail-high, which ADR-0002 already declares protected on a BEST-EFFORT basis only | recorded — written into 01_docs/DETAIL_DESIGN.md sec.5.4 so nobody reads "D5 protects VBUSC" as covering U12, and so a future revision adding active OVP knows U12 is part of what the present design spends. No change to v1.5 |

## 2026-07-25_v1.5_fresh-lens_staged.md (zero-context lens over the STAGED v1.5, pre-seal)

Verdict **ORDER**; **0 P0**, 3 P1, 9 P2. The lens independently re-derived the
four CPL cells five ways and ran its own point-set rotation fit over the whole
board (**107/107 parts with a cached JLC footprint agree with the shipped CPL to
<1 deg**), so the release's central claim is confirmed by a second method. Every
finding below is a defect in this release's own PAPERWORK or archive, not in the
board — which is exactly what a pre-seal lens is for.

| id | review file | finding (one line) | severity | verification | disposition |
|---|---|---|---|---|---|
| FL15-1 | 2026-07-25_v1.5_fresh-lens_staged.md | `verification/manifest_selfcheck.txt` is 0 bytes — the MANIFEST cites it as evidence for a gate that therefore cannot fail | P1 | confirmed — the file was created as an empty placeholder so the sha256 table could be generated over it, and never written. "A gate that cannot fail is worthless" (CLAUDE.md) | fixed — the self-check is now RUN and its real output written to `verification/manifest_selfcheck.txt`, including the bootstrap it cannot escape (its own row is stamped last) and the command to re-run it independently |
| FL15-2 | 2026-07-25_v1.5_fresh-lens_staged.md | the MANIFEST claimed "both red-team lenses stand from the last copper change" — FALSE: the only layout/thermal lens is dated 2026-07-22 against the v1.0 copper, and 10 footprints were added afterwards (C53, C54, D5, F2, Q6, Q7, R30, R34, R35, SW1; tracks 642 -> 1061) | **P1** | confirmed by the lens's own pcbnew diff of the v1.0 board vs the shipped board. The whole discrete USB-C VBUS protection chain (Q6/Q7/R30/F2/D5) and both SW-node snubbers had **never** been layout- or thermally reviewed. This is an inherited gap: v1.3 and v1.4 sealed with the same claim | fixed — a FRESH layout/thermal/power-integrity red-team lens was commissioned and run against the shipped copper before sealing: `08_reviews/2026-07-25_v1.5_redteam_layout.md`, archived in `verification/`. The MANIFEST claim is rewritten to state what is actually true of each lens |
| FL15-3 | 2026-07-25_v1.5_fresh-lens_staged.md | seven artifacts the 07_releases contract's validate list requires IN `verification/` were absent (both redteam lens files, `render_{top,bottom}_bare.png`, `missing_models.txt`, `pin_review.md`, `render_review.md`, `audit.txt`), replaced by pointers into `08_reviews/` — the "outsource the archive" pattern the same contract forbids | P1 | confirmed against `07_releases/contracts.md` | fixed (partly) — v1.5 now ships `audit.txt` (16 polarity / 19 proximity / 4 edge / 114 silk checks, PASS), `render_top_bare.png`, `render_bottom_bare.png`, `missing_models.txt`, and VERBATIM copies of all three red-team/fresh lens reviews. `pin_review.md` / `render_review.md` are NOT fabricated for a release whose copper is byte-identical to v1.3's; their absence is stated explicitly in the MANIFEST rather than papered over |
| FL15-4 | 2026-07-25_v1.5_fresh-lens_staged.md | the "MEASURED board-wide minimum pad-to-pad centre distance 0.600 mm" backing `fiducials: none` is WRONG — it is 0.500 mm (J5 USB-C, A5<->B7) | P2 | confirmed by re-measurement: smallest non-zero distance between two DISTINCT-numbered pads = **0.500 mm** at J5 (A5<->B7, A5<->B8); 0.600 mm is the next (U3/U4/U5 VSON-8); some pairs read 0.000 mm because they are ONE physical pad carrying two pin names. **The conclusion survives** — 0.500 > 0.400, so no local fiducials are owed | fixed — corrected in `03_src/rules/assembly.yaml`, the MANIFEST and ORDER_README, with the 0.000 mm dual-pin-name pairs explained so the number cannot be mis-derived again |
| FL15-5 | 2026-07-25_v1.5_fresh-lens_staged.md | the "157 mV slack" E-MARGIN figure omits the **1.2x derate** the project's own gate applies; the honest slack is **69 mV** | P2 | confirmed against `power_topology.py` (`DEFAULT_MARGIN = 0.20`; grade is `headroom >= ir_budget x 1.20`): headroom 597 mV, raw IR 440 mV, required 528 mV -> **69 mV**. 157 mV comes from comparing against the UN-derated 440 mV and appears in the v1.4 paperwork | fixed — 69 mV is now the reported number in DETAIL_DESIGN sec.4, ORDER_README and the MANIFEST, with the derate shown. `03_src/rules/power_tree.yaml` also synced from the Vref-only 5.27/5.43 corners to the tolerance-inclusive **5.227/5.479** (the harvest item v1.4's STATUS carried); E-MARGIN re-run: still PASS |
| FL15-6 | 2026-07-25_v1.5_fresh-lens_staged.md | bench gate Q5's floor (>=4.80-4.85 V) sits ABOVE the design's own worst-case cable-end estimate (4.79 V) | P2 | confirmed arithmetically | fixed — Q5 now states this is DELIBERATE and says why: 4.79 V is the quadruple-worst corner (Vref low AND R13 high AND F2 fully hot AND a marginal cable) while Q5 is a requirement on the DELIVERED system. Explicit instruction added not to re-label a sub-floor reading as a pass on the strength of that corner |
| FL15-7 | 2026-07-25_v1.5_fresh-lens_staged.md | MANIFEST payload breakdown said "source x13" (components summed to 21, not 20); `source/` holds 12 files | P2 | confirmed by `find` — 3 fab + 4 pdf + 12 source + 1 3d = 20 | fixed — MANIFEST corrected to "source x12" |
| FL15-8 | 2026-07-25_v1.5_fresh-lens_staged.md | shipped `verification/policy_audit.md` read `FAIL=1` against a MANIFEST claiming "0 FAIL" | P2 | confirmed, and the lens verified the cause is EXACTLY the two declared-pending stamp items (`git_sha: PENDING-S`, and the self-referential hashes of `policy_audit.md` / `release_freshness.txt`) and nothing else | fixed — policy_audit is re-run AFTER the git_sha stamp and its final 0-FAIL output is what ships; the seal report states the measured result |
| FL15-9 | 2026-07-25_v1.5_fresh-lens_staged.md | `01_docs/DETAIL_DESIGN.md` is cited ~15x by the release but is not IN the archive (nor is the W-U12 datasheet or the XT60 part.yaml) | P2 | confirmed against the contract's completeness test ("someone with this directory and no network can...") | fixed — `DETAIL_DESIGN.md` is copied VERBATIM into `verification/`. The two datasheets are deliberately NOT copied (they are third-party PDFs); the MANIFEST names them by document ID + revision + the sha256 recorded in their `part.yaml` so a reader can identify the exact document |
| FL15-10 | 2026-07-25_v1.5_fresh-lens_staged.md | DETAIL_DESIGN sec.2.4 says "< 2 %" where the value is 2.08 %; sec.4's corner formulas have ambiguous operator precedence as written (all RESULTS correct) | P2 | confirmed by recomputation — 6/(4 x 250 kHz x 32 uF) = 0.188 V = 2.08 % of 9.0 V | fixed — sec.2.4 now states 2.08 % / 1.73 %; sec.4 formulas parenthesized with a note that the inner parentheses are load-bearing |
| FL15-11 | 2026-07-25_v1.5_fresh-lens_staged.md | the twin's `MODEL-REG` on J1 has no `twin_adjudications.yaml` entry | P2 | confirmed; the lens independently re-confirmed it is a false alarm (4-pad fit rms 0.0000 mm) | fixed — entry added to `03_src/rules/twin_adjudications.yaml` carrying the full measurement, and explicitly labelled NON-BLOCKING (MODEL-REG is not in the twin's critical set) so it cannot be mistaken for a gate waiver that cannot fail |
| FL15-12 | 2026-07-25_v1.5_fresh-lens_staged.md | `2026-07-22_v1.0_redteam_topology_rereview.md`, cited as a standing ORDER lens, has no header block (2 of 5 required keys; its verdict is prose only) | P2 | confirmed against the 08_reviews contract | recorded — review files are APPEND-ONLY EVIDENCE and are not banner-edited; the gap is recorded here and the MANIFEST cites the v1.3 fresh lens (which DOES carry a proper header block with `verdict: ORDER`) as the topology lens of record for this release |
| PCBA-16 | 2026-07-25_v1.4_pcba-audit_assembly.md | §1: the release ARCHIVE was not self-contained — `source/fp-lib-table` pointed at `${KIPRJMOD}/../03_src/lib/*.pretty`, OUT of the release and into the mutable project tree, so the standalone archive did NOT re-measure DRC clean | **P1** | confirmed by running the 07_releases contract's own completeness test on the STAGED v1.5: `cd source && kicad-cli pcb drc --severity-all --refill-zones` -> **6 violations, all `lib_footprint_issues`** (J1-J5 on `usb_hub_3s`, SW1 on `Button_Switch_THT`). The vendored `.pretty` folders WERE shipped; the fp-lib-table just never referenced them. Fails identically on sealed v1.3/v1.4 (same `source/` bytes). The v1.3/v1.4 MANIFESTs additionally CLAIM `custom-lib uris ${KIPRJMOD}/<lib>.pretty`, which is what the file should have said and did not | fixed — v1.5's archive fp-lib-table now reads `${KIPRJMOD}/usb_hub_3s.pretty` / `${KIPRJMOD}/Button_Switch_THT.pretty`; re-measured standalone: **0 violations / 0 unconnected** (`verification/standalone_archive_drc.json`). COST STATED: `source/fp-lib-table` is consequently the ONE payload file not byte-identical to v1.4 — the identity claim is **19 files**, not 20, and the difference is a library-path config line, not copper. A stray gitignored `source/usb_hub_3s_v2.kicad_prl` (regenerated by any `kicad-cli` board open) was also removed |

## 2026-07-25_v1.5_redteam_layout.md (layout / thermal / power-integrity lens — the FIRST ever run against this copper)

Verdict **ORDER**; **0 P0**, 5 P1, 7 P2. Commissioned to close FL15-2: the only
prior layout lens was written against the **v1.0** board, before the entire
discrete VBUS protection chain (Q6/Q7/R30/F2/D5) and both SW-node snubbers
existed. **No P0 — no re-spin is justified.** The five P1s land in ORDER_README
section 3a (build notes B1-B4) and the next-rev work order.

| id | review file | finding (one line) | severity | verification | disposition |
|---|---|---|---|---|---|
| RL-1 | 2026-07-25_v1.5_redteam_layout.md | PMID's F.Cu pour is bisected by the 0.200 mm QG gate route, so the entire 5 A USB-C rail crosses Q6→F2 through TWO 0.30 mm vias in series — a sole path with zero redundancy | P1 | confirmed by polygon booleans (PMID F.Cu = 2 disjoint outlines, 18.28 mm² and 27.30 mm²) AND independently by KiCad's own connectivity engine (deleting either via takes the net 0 → 1 unconnected), AND visibly in the kicad-cli F.Cu plot. NOT thermal: ΔT ≈ 0.85 K — the reviewer says so explicitly rather than overclaiming | deferred — ORDER_README section 3a build note **B2**; watch for upward VBUSC droop drift during the Q2 8-24 h soak. Next-rev: route QG on B.Cu so PMID is one pour, and stitch the Q6→F2 span with ≥4 vias. No copper change in v1.5 |
| RL-2 | 2026-07-25_v1.5_redteam_layout.md | the 5 A path's board copper is 3-4x the documented allowance, cutting the E-MARGIN slack the project already called "thin" by roughly two-thirds | **P1** | confirmed by numerical mesh solve (SOR, 0.3 mm cells, F.Cu+B.Cu+vias; solver validated against analytic bars and found to read 10-20 % LOW): 5VC L2.2→Q6 **2.198 mΩ** + PMID Q6.S→F2.1 **4.914 mΩ** + VBUSC F2.2→J5 **2.209 mΩ** = **≥9.32 mΩ**, true ≈10.4-11.6 mΩ, against the `board pours ~3 mOhm` carried since v1.3 | **fixed in the numbers, deferred in the copper** — `03_src/rules/power_tree.yaml` now declares the measured value conservatively (board 12 mΩ, `ir_budget_mohm: 97`, was 88). E-MARGIN re-run: **PASS**, slack **15 mV** (597 mV headroom vs 582 mV required). DETAIL_DESIGN sec.4, ORDER_README and the MANIFEST all carry 15 mV and the three-figure history (157 → 69 → 15 mV), each step having removed an optimistic assumption without the hardware changing. **15 mV of paper slack is not a margin to ship on — gates Q2/Q5 measure the delivered voltage and THAT is the qualification** |
| RL-3 | 2026-07-25_v1.5_redteam_layout.md | H3 mounting hole straddles the 5VA/GND pour boundary — an M3 screw head or washer bridges the 6 A USB-A rail to GND through solder mask only, on BOTH outer layers | **P1** | confirmed by radial scan: H3 = 3.20 mm NPTH at (106.0, 24.0), hole edge r = 1.60 mm; **5VA copper starts at r = 1.80 mm AND GND copper starts at r = 1.80 mm, on F.Cu and B.Cu**. A typical M3 screw head spans ~3.0 mm radius, so it sits on both | deferred with a MANDATORY build instruction — ORDER_README section 3a build note **B1**: fit a NYLON screw + nylon washer at H3, or leave H3 unfitted; if a metal fastener is required, use an insulating shoulder washer. Mask is not an insulator under clamping force. Next-rev: pull both pours back from H3 |
| RL-4 | 2026-07-25_v1.5_redteam_layout.md | J5's four VBUS contacts are unequally fed — the right-hand pair reaches the board through a single via, so the left pair carries 60 % of the current | P1 | confirmed — J5.A4/B9 sit on VBUSC F.Cu island #0 (direct pour from F2.2); J5.A9/B4 sit on island #1 (15.66 mm²) whose only link is the via at (122.970, 103.000), sole path confirmed by connectivity-engine removal (0 → 1 unconnected). Measured split **2.91 A / 1.90 A = 60.5 % / 39.5 %** → two contacts at **1.46 A** vs the 1.25 A/contact a four-way split implies | deferred — within the connector's rating; ORDER_README build note **B3**, and it is part of why Q5 measures at the cable end rather than trusting the model. Next-rev: stitch the second VBUSC island properly |
| RL-5 | 2026-07-25_v1.5_redteam_layout.md | twelve sole-path power/sense vias with no redundancy; one (5VA) is FAIL-HIGH — losing it disconnects buck-A's FB divider and VOUT sense, driving 5VA toward V_IN into three USB-A ports | **P1** | confirmed by connectivity-engine removal, 16 vias tested, **12 break their net**: PMID x2, VBUSC x1, VBUSA1/2/3 x3, 5VA x2, 5VC and others. The fail-high one is 5VA at (97.925, 46.000) | deferred with a bench catch — ORDER_README build note **B4**: gate **Q1's no-load reading catches an open FB immediately** (5VA must sit in 5.03-5.28 V), so Q1 must not be skipped. Next-rev: double every sole-path power via |
| RL-6 | 2026-07-25_v1.5_redteam_layout.md | F2 (0.775 W — the hottest element on the C path) has ZERO thermal vias on either pad, with an 8:1 copper asymmetry between its input and output sides | P2 | confirmed by exact polygon containment: F2 pads 10.43 mm² each, F.Cu only, **0 vias in either**; input side (PMID island #1) **27.30 mm²** vs output side (VBUSC #0) **218.45 mm²**; estimated rise **31-39 °C**. Also 0 vias in L1/L2's 17.82 mm² pads | recorded + folded into a gate — inside rating, so no action at order time; ORDER_README section 3a names F2 as **the first place to point the IR camera in gate Q4**. Next-rev: via-stitch the PMID side of F2 |
| RL-7 | 2026-07-25_v1.5_redteam_layout.md | VBUSA1/2/3 each run 8.5 mm of BARE 0.500 mm B.Cu track outside the pour, at the class floor and hot in the current-limit case | P2 | confirmed — 0.500 × 0.035 = 0.0175 mm² cross-section; IPC-2221 external ΔT ≈ 21 °C at 2 A, ~65 °C at the TPS2557 current limit | recorded — the TPS2557's own 2.72-3.29 A limit bounds the exposure and the condition is a fault, not steady state. Next-rev: widen or pour the VBUSA runs |
| RL-8 | 2026-07-25_v1.5_redteam_layout.md | the CS/CSG pairs are not true Kelvin — CSG taps the shared GND return pour 3.93 mm from the shunt, and the two halves differ 2.3x in length | P2 | confirmed — R10.1 at (75.17, 47.00) is 3.93 mm from RS1.2 (72.00, 48.96) in the pour carrying the full LS return; ~2 % current-sense error | recorded — a 2 % error on a CURRENT-LIMIT threshold (not a regulation loop) is immaterial: I_LIM 11.0 A vs a 6.89 A steady peak. Next-rev: tap CSG at the shunt pad |
| RL-9 | 2026-07-25_v1.5_redteam_layout.md | both buck feedback/VOUT sense paths are long B.Cu runs (43.50 mm and 37.50 mm) referenced to the VIN plane, crossing under their own power stage | P2 | confirmed — 5VA: 174 collinear 0.400 mm B.Cu segments at y = 46.000, x = 54.425→97.925, passing under RS1 and the CS_A pour; 5VC the same at y = 86.000 | recorded — bench-observable: gate **Q3** scopes both SW nodes through startup, shutdown and 0→5 A→0 steps, which is where FB pickup would show. Next-rev: move FB sense to F.Cu over the GND plane |
| RL-10 | 2026-07-25_v1.5_redteam_layout.md | ~211 mm² of switch-node copper on B.Cu per buck sits directly over the VIN plane, injecting dV/dt into VIN | P2 | confirmed — SW_A B.Cu 211.41 mm², SW_C 211.78 mm²; B.Cu↔In2 spacing 0.2 mm → **40.2 pF and 40.3 pF** onto the VIN plane (63.5 / 78.3 pF total per node) | recorded — the R34/C53 and R35/C54 snubbers are POPULATED BY DEFAULT for exactly this class, and gate Q3 tunes them on the bench. Next-rev: keep the SW trunk on F.Cu only |
| RL-11 | 2026-07-25_v1.5_redteam_layout.md | the R-THERM waiver's prose is STALE and over-reaches onto parts it never measured | P2 | confirmed against the shipped copper: the waiver says U11.21 has "1 direct via (vs 3 on the sister EP U2.21)" and carries a next-rev work order to add a ≥4x 0.3 mm array under BOTH LM5116 EPs — **measured, U2.21 and U11.21 each already have 7 GND vias inside the exposed pad** (3×2+centre at ±0.85 / ±2.1 mm). Likewise VBAT_F already has 195.6 mm² of B.Cu + 7 vias | recorded — two v1.0 work orders (L-2, L-3) are ALREADY IMPLEMENTED and were never marked done, so the waiver was describing a board that no longer exists. Noted in ORDER_README section 3a so nobody "fixes" what is already fixed. Rewriting `policy_waivers.yaml` prose is deferred to the next revision (it is source, and v1.5 changes no gate outcome) |
| RL-12 | 2026-07-25_v1.5_redteam_layout.md | LM5116 timing/compensation parts sit 5-6 mm from their pins against a datasheet asking for "as close as possible" | P2 | confirmed — RT 5.50 mm, RAMP 5.82 mm, SS 5.11 mm, COMP 6.45 mm (routed 10.8/13.4 mm), UVLO 5.46 mm, FB span 9.37 mm (routed 20.8 mm); all on F.Cu over the solid In1 GND plane with AGND-side returns | recorded — mitigated by the solid GND reference under every one of them; the loop is bench-verified by gate Q3, and both bucks are the v1-proven cell that has been ordered before. Next-rev: tighten the control cluster in `floorplan.yaml` |

## v1.12-2026-07-28 — J5 land pattern (FIX-PASS)

| id | review file | finding (one line) | severity | verification | disposition |
|---|---|---|---|---|---|
| TC-1 | 2026-07-28_v1.12_targeted-confirmation.md | J5's land pattern disagreed with the part in every release v1.0–v1.11: pad row 1.445 mm from the alignment line vs 1.070, pad length 1.450 vs 1.140 — 16 contacts each 0.220 mm inboard of their land (80.7 % overlap), 0.530 mm of land and open stencil aperture past the lead end, on a 0.5 mm pitch, on a part that cannot self-align | P0 | confirmed — three independent sources agree and the board is the outlier: HRO `TYPE-C-31-M-12A` REV A sheet, HRO `TYPE-C-31-M-12` REV A sheet, and JLC/EasyEDA `C5337088` (1.070102 / 1.14000). Raster re-read of the sheet at 600 dpi: 1.0654 / 1.1415 | fixed — 07_releases/v1.12-2026-07-28; footprint AUTHORED from the sheet; `verification/j5_geometry_gate.txt` 127 checks 0 failures on two instruments sharing no code, RED-verified against the v1.11 footprint |
| TC-2 | 2026-07-28_v1.12_targeted-confirmation.md | the emitted CPL placement datum for J5 was 0.2024 mm off JLC's own model origin, telling the machine to set the connector north of where its own Ø0.50 alignment posts sit (0.075 mm radial clearance) | P0 | confirmed — JLC `C5337088` model origin +1.30492 mm from the alignment line; v1.11 emitted +1.10250; v1.12 emits +1.30500. `export_jlc_package.placement_datum()` computes the pad-centre bbox centre, so wrong pads gave a wrong datum | fixed — 07_releases/v1.12-2026-07-28, as a consequence of TC-1 with no separate edit; `fab/cpl.csv` differs from v1.11's in exactly one row |
| TC-3 | 2026-07-28_v1.12_targeted-confirmation.md | `jlc_twin` — the gate whose job is comparing our footprint to JLC's — reports `C5337088,J5,OK,fit=0.00mm` character-identical on the defective and the corrected board | P1 | confirmed — `pads_of()` drops unnumbered pads so the NPTH alignment holes never enter the fit, and `centered()` removes the centroid so the comparison is translation-invariant by construction | deferred — reported as a gate gap and already queued; deliberately NOT patched inside a board fix (a `skills/` change does not belong in the same commit, and a gate rewritten by the agent it is about to grade is not a gate) |
| TC-4 | 2026-07-28_v1.12_targeted-confirmation.md | `A-POS` is blind to TC-2 for the sibling reason: it recomputes the placement datum from the same board it grades | P1 | confirmed — v1.11's MANIFEST reads "A-POS datum: worst residual 0.00050 mm"; the number is true and says nothing about TC-2. Canon M1, checker and checked sharing a method | recorded — the comparison with force is ours-vs-JLC's and v1.12 ships it (`verification/j5_geometry_gate.txt`, MANIFEST); making it a gate is a `skills/` change |
| TC-5 | 2026-07-28_v1.12_targeted-confirmation.md | sibling projects `usb-hub-3s` and `usb-hub-3s-v2` carry the byte-identical defective footprint, and `usb-hub-3s` has a SEALED release carrying it | P1 | confirmed — md5 `7aebd381d4637d498f8d9fdf9a6ea0ab` on all three projects' copies; `usb-hub-3s/07_releases/v1.0-2026-07-21` | deferred — out of scope for this release; recorded in v1.12's CHANGELOG and MANIFEST, and spun off as its own work item. Each board needs its own release, gates and review |
| TC-6 | 2026-07-28_v1.12_targeted-confirmation.md | a land pattern had no checker of any kind in this repo — DRC grades copper against copper and a wrong-but-self-consistent pattern passes | P2 | confirmed — the defect survived twelve sealed releases, DRC, `escape_check`, two pin reviews, two render reviews, four red-team lenses and an adversarial full audit | recorded — partly closed by `verification/j5_geometry_gate.txt`, the first thing here that reads a vendor drawing's numbers and compares them to emitted copper. It is per-board, not a fleet gate; named as a remaining gap |
| TC-7 | 2026-07-28_v1.12_targeted-confirmation.md | front shell slot offset was 0.53 (stock-KiCad inheritance), shell pad width 1.00, slot centre span 8.64 — the sheet and JLC say 0.50, 0.90 and 8.65 | P2 | confirmed — raster 0.4985 / 0.894 / 8.6510; JLC 0.500126 / 0.90000 / 8.64972. The sheet's `8.64` is real but is the FRONT VIEW's part LEG span, a different dimension from the layout view's slot centres | fixed — 07_releases/v1.12-2026-07-28. 30 µm, 100 µm and 5 µm respectively; corrected because a land pattern authored from a drawing should carry the drawing's numbers |
| TC-8 | 2026-07-28_v1.12_targeted-confirmation.md | alignment hole ⌀0.65 (v1.11's clearance-end choice) is no longer available at the corrected datum | P2 | confirmed — KiCad's own shape engine: land A1/B12 to NPTH hole is 0.1944 mm at ⌀0.65 against this board's `min_hole_clearance` 0.200 (DRC fails, built and measured), 0.2194 mm at ⌀0.60 | fixed — changed to ⌀0.60, the sheet's nominal and JLC's own value; the ⌀0.65 build is retained as the gate's known-bad fixture. Recorded as a DECISION in the footprint `descr`, `part.yaml`, MANIFEST and CHANGELOG |
| TC-9 | 2026-07-28_v1.12_targeted-confirmation.md | the staged archive failed the 07_releases completeness test on its first run — 12 `lib_footprint_issues` from an `fp-lib-table` copied verbatim from `04_kicad/` | P2 | confirmed — its URIs read `${KIPRJMOD}/../03_src/lib/`, which from inside `source/` points outside the archive. Canon V-REL-FPLIB | fixed — rewritten to `${KIPRJMOD}/<lib>.pretty`, byte-identical to v1.11's sealed form; re-measure DRC 0/0/0. `verification/standalone_archive_check.txt` |

### v1.12 fresh-context lens (`2026-07-28_v1.12_fresh-lens_staged.md`, ORDER-WITH-CONDITIONS, 0 P0)

| id | review file | finding (one line) | severity | verification | disposition |
|---|---|---|---|---|---|
| FL-1 | 2026-07-28_v1.12_fresh-lens_staged.md | the release's severity narrative was a tautology: `lead_overlap.txt` DEFINED the contact tail as the vendor's recommended LAND, so "0.220 mm inboard / 80.7 % overlap / 0.530 mm past the lead" were land-vs-land ratios wearing a lead's name | P1 | confirmed — re-measured independently from JLC's own mesh anchored on the part's datum: posts dia 0.5000 at x=±2.8900 (span 5.7800, = the sheet), housing 7.35 + tail 0.4000 = mesh depth 7.7500. **Solderable tail 1.070…1.470 from the alignment line; 100 % on its land in v1.11 AND v1.12.** Real defect: inverted fillet balance (heel 0.350 vs the vendor's 0.570), +27.2 % paste, 0.700 mm of open aperture past the tail tip on a 0.5 mm pitch | fixed — `verification/lead_overlap.txt` rewritten from the mesh; corrected in MANIFEST, CHANGELOG, ORDER_README, `02_parts/TYPE-C-31-M-12A/part.yaml`, v1.11's `SUPERSEDED.md`, the render note, the targeted-confirmation review, and **the footprint `descr`** — which forced a board rebuild, since `descr` lives inside the `.kicad_pcb` and every future release's `source/` |
| FL-2 | 2026-07-28_v1.12_fresh-lens_staged.md | `verification/policy_audit.md` absent from the staged archive although the MANIFEST cites it and the 07_releases contract requires it | P1 | confirmed — it existed only at `06_build/policy_audit.md` | fixed — generated into the archive at the stamp step, which is where it belongs (it must grade the STAMPED manifest) |
| FL-3 | 2026-07-28_v1.12_fresh-lens_staged.md | MANIFEST said `A-POP: PASS` while the shipped `assembly_coverage.txt` said `A-POP: FAIL (MANIFEST-UNDECLARED)`; the archived file predated the MANIFEST | P1 | confirmed — file mtimes 15:22 vs 15:48 | fixed — the archived `assembly_coverage.txt` is now the re-run taken AFTER the stamp, which is the ordering the MANIFEST already declared and the staging had not yet honoured |
| FL-4 | 2026-07-28_v1.12_fresh-lens_staged.md | `gerber_payload_delta.txt` asserted "every geometric change lies inside the J5 corner" directly above its own count of 494 coordinates that do not | P2 | confirmed — the file contradicted itself; the physical claim is nonetheless sound, bounded by `copper_xor.txt` at 0.000969 mm² | fixed — conclusion reworded to defer to the SHAPE_POLY_SET area bound and to explain why a coordinate diff cannot distinguish a moved feature from zone-filler vertex churn |
| FL-5 | 2026-07-28_v1.12_fresh-lens_staged.md | `min_hole_clearance: 0.200` — the number Decision 2 was presented as turning on — has no fab provenance; `fab_tiers.yaml` models no hole-to-copper capability at any tier | P2 | confirmed — set in `floorplan.yaml` with no source comment, unlike its neighbour `hole_to_hole: 0.5` | recorded — decision UNCHANGED but its evidence re-weighted: ⌀0.60 is carried by the vendor's nominal and JLC's own ⌀0.59999, not by the DRC number. 0.2194 mm is inherent to the vendor's own layout. Hole-to-copper filed as a missing `fab_tiers.yaml` field |
| FL-6 | 2026-07-28_v1.12_fresh-lens_staged.md | the third source disagrees on the REAR shell slot and no document said so — EasyEDA gives all four slots 2.00/1.70 where both sheets give the rear pair 1.70/1.40 | P2 | confirmed — own EasyEDA re-parse; the sheet's rear-right slot is dimensioned 1.70/1.40 | recorded — no change: the footprint follows the SHEET and the MANIFEST's source column already said "sheet" for that row. The disagreement is now stated. Benign: JLC's mesh puts the rear leg at 0.800 long, so 1.40 leaves 0.30 mm/end against v1.11's 0.20 |
| FL-7 | 2026-07-28_v1.12_fresh-lens_staged.md | the F.Courtyard was not regenerated and is now stale — north margin over the numbered-pad copper 0.500 → 1.030 mm | P2 | confirmed — own pcbnew measurement on both boards | recorded, declared in the MANIFEST — deliberately NOT changed: byte-identical body graphics are what make the mating-face claim provable. Oversized is conservative; DRC 0/0/0 and audit proximity PASS. Next revision re-derives it |
| FL-8 | 2026-07-28_v1.12_fresh-lens_staged.md | J5's twin metrics moved and were undeclared (MODEL-REG 0.05 → 0.33 mm; overlay centre 0.962 → 0.992, courtyard excursion 0.049 → 0.326) | P2 | confirmed — diff of the two releases' twin files; all are consequences of FL-7 | recorded, declared in the MANIFEST. "Identical to v1.11" was true of the 29-ref SET, not of J5's numbers |
| FL-9 | 2026-07-28_v1.12_fresh-lens_staged.md | "deliberate" was asserted for the 5.600 mm mating face with no decision record behind it | P2 | confirmed — no note in `floorplan.yaml` or `DETAIL_DESIGN.md`; the gap is 0.19 mm = 3.8× the sheet's own ±0.05 | fixed — reworded to "pre-existing and untouched" in every document, with the direction of the error stated (mouth overhangs 0.680 where the vendor intends 0.490 — more plug clearance, not less) |
| FL-10 | 2026-07-28_v1.12_fresh-lens_staged.md | the MANIFEST's three-source table cited the base `-12` sheet, which is not in the archive, without saying so | P2 | confirmed — `02_parts/TYPE-C-31-M-12A/` holds the `-12A` PDF only | fixed — all three source rows now footnoted with where they live; the `-12` row carries its sha256 and is marked NOT IN THIS ARCHIVE |
| FL-11 | 2026-07-28_v1.12_fresh-lens_staged.md | `jlc_twin`'s blindness is not only `pads_of()` + `centered()`: JLC names its pads differently, so the by-name correspondence set is degenerate | P1 | **confirmed and REFINED** — the lens read the set as EMPTY; measured here it is EIGHT names (A5–A8, B5–B8) and **all eight are COLLINEAR** (their y = 2982.205, ours = −3.670, one value each). After `centered()` the fit is eight points on a line against eight points on a line, carrying no information on the only axis this defect lives on | deferred — recorded in the MANIFEST and CHANGELOG with the measurement. A fix that only stops dropping unnumbered pads will NOT close it; the gate needs a numbering-free landmark fit. `skills/` change, out of scope for a board fix |
| FL-12 | 2026-07-28_v1.12_fresh-lens_staged.md | v1.11's SHIPPED `twin_report.csv` grades `C25744` for R28/R29 — the code v1.11's own BOM had already replaced with `C60490` | P2 | confirmed by the lens; v1.12's is correct | recorded — NOT a v1.12 finding. v1.11 is sealed and immutable; noted so the observation is not lost, and v1.12 incidentally corrects it |
