# v1.3 FIX-CONFIRMATION REVIEW — usb-hub-3s-v3

    subject: usb-hub-3s-v3 v1.3-2026-07-23 (staged, pre-seal)
    date: 2026-07-23
    reviewer: board lead (targeted delta confirmation, canon "Verification scoping": fix-pass leg)
    context-given: full-tree
    verdict: PASS

**Scope:** targeted confirmation that the v1.3 fix delta vs sealed v1.2 —
EXACTLY {R12, D5, R30} — is correctly and completely implemented in the
RESTAGED release archive (post R30 fix), plus the protection setpoint math.
The integrated zero-context fresh lens is a SEPARATE review (same date,
`2026-07-23_v1.3_redteam_fresh-lens.md`).

## Measured gates at this review (all re-run on the restaged v1.3 archive)

- **DRC 0/0/0** (severity-all + refill-zones + schematic-parity; note: the
  taps/stitch pass is stochastic on the FIRST rebuild_fast after a converter
  regen — run #1 gave 3/0/0 [HO_A via vs SW_A clearance], run #2 gave 0/0/0;
  final board re-verified). `verification/drc.json`
- **ERC 0 errors** (severity-all, grid-mode converter sch). `verification/erc.json`
- **E-INV 24/24** vs the freshly exported netlist (the 15:54 pre-R30 netlist
  was stale and was re-exported).
- **M-BOM bom_source_check PASS incl. the SEMANTIC value-vs-ledger leg** —
  the R30 VALUE-MISMATCH row is GONE. `verification/bom_source_check.txt`
- **jlc_twin 88 OK / 235 checked, exit 0**, zero unadjudicated criticals.
- **policy_audit FAIL=1 = M-REL only** (stale MANIFEST sha table, expected
  pre-stamp; re-cleared at the stamp step).

## Delta item 1 — R12 wrong-part (v1.2 order blocker) → CONFIRMED FIXED

- BOM row: `4.12kΩ,R12,R_0603_1608Metric,,C2984354` (diff vs v1.2: was C2933210
  = FRC0603F3741TS = 3.74 kΩ).
- Setpoint recomputed from BOM values: Vout = 1.215 × (1 + 4120/1210) =
  **5.352 V**; headroom to Pi-5 UV 4.63 V = **722 mV nominal** ≥ the E-MARGIN
  worst-case need **528 mV** (6 A × ir_budget 88 mΩ); E-MARGIN gate PASS
  (worst-low corner 640 mV per power_tree derivation).
- Twin: C2984354 = ADJUDICATED-FETCH-FAILED (EasyEDA 404, genuinely absent —
  probed directly; standard 0603 land; same-spec alt C861436 has R0603 CAD).
  Evidence-backed adjudication carried in `twin_adjudications.yaml`.

## Delta item 2 — D5 directionality → CONFIRMED FIXED

- BOM row: `C113976,D5,D_SMB,,C113976` (was C140903, JLC-catalog BIDIRECTIONAL).
- C113976 = SMBJ6.0A **UNIDIRECTIONAL** DO-214AA, catalog-verified; netlist
  polarity cathode(pad1)=VBUSC / anode(pad2)=GND asserted by the floorplan
  D5.1=VBUSC + P-POL pad-1 gate.
- Twin: fit 0.21 mm, MODEL-REG-OK; PAD-GEOM 0.42 mm land-extent preference
  adjudicated (same class as the D1 SMBJ15A SMA artifact; order-preview confirm).

## Delta item 3 — R30 wrong-part (this pass's blocker) → CONFIRMED FIXED

- v1.2/old-staging BOM: `100kΩ,R30,...,C2933195` where the vetted ledger says
  C2933195 = FRC0603F3091TS = **3.09 kΩ** (the 2nd wrong-part the semantic
  M-BOM gate caught; v1.2 SUPERSEDED addendum `688a8af`).
- v1.3 BOM: R30 now IN the catalog-verified row `100kΩ,"R1,R17,R30,R8",...,
  C25803` (44 → 43 grouped lines). C25803 = UNI-ROYAL 0603WAF1003T5E,
  ledger-verified 100 kΩ ±1 % (JLC Basic); offline MPN decode `1003` E96 =
  100×10³; family cross-checks 0603WAF1002T5E=10k / 0603WAF1004T5E=1M /
  0603WAF3091T5E=3.09k decode consistently. CONVERGENCE: R1/R8/R17 — the
  board's other three 100 k 0603s — already auto-resolved to C25803.
- tsx: `supplierPartNumbers={{ jlcpcb: ["C25803"] }}` baked (the R12 pattern)
  + DO-NOT-USE C2933195 comment; verified end-to-end tsx → circuit.json →
  converter sch → netlist → BOM.
- Q6 gate-drive margins at the REAL 100 k (AON6403 STATIC table, Vgs(th)
  −1.2/−1.7/−2.2 V min/typ/max @ −250 µA): OFF/back-feed |Vgs| =
  (Q7 Idss 0.5 µA + Q6 IGSS 0.1 µA) × 100 k ≈ **60 mV**, 20× below
  |Vgs(th)|min → Q6 OFF, body diode reverse-biased → blocks (RT-T4 holds).
  ON: Vgs = **−5.35 V** > |Vgs(th)max| 2.2 V and past the −4.5 V Rds spec
  point → fully enhanced. Pull-up waste 5.352 V / 100 k = **54 µA (0.29 mW)**
  vs ~1.7 mA at the wrong 3.09 k — the v1.2-addendum waste note is RESOLVED.
- Twin: C25803 @ R30 fit 0.08 mm, MODEL-REG-OK.

## Diff-verified completeness

`diff(sort v1.2 bom.csv, sort v1.3 bom.csv)` shows EXACTLY the three-part
delta {R12: C2933210→C2984354, D5: C140903→C113976, R30: C2933195→C25803
(row merge)} — no other BOM change. CPL 108 placements, SW1 + F1 absent
(grep-verified). Netlist topology and routing unchanged (same promoted KRT
chain).
