# Journal — v1.4 docs-only supersede of v1.3 (2026-07-23)

## 2026-07-23 — external review received POST-SEAL; user chose docs-only supersede

- context: v1.3 sealed (S=01983b0, seal=d137e00), electrically correct. A
  user-supplied external review arrived AFTER the seal, examining the PRE-SEAL
  staged state. Orchestrator triage vs the sealed archive: §1 (unsealed) + §2
  (R30) OVERTAKEN by the seal; three findings LIVE in the sealed DOCS (SW1
  shunt polarity REVERSED in ORDER_README, F1 misnamed KH-AF90DIP-112, margin
  math omitting R13/R4 ±1%); one packaging note (F1/SW1 BOM-not-CPL). User
  decision (option b): docs-only supersede — NO copper or BOM change.
- did: archived the review VERBATIM with provenance header ->
  08_reviews/2026-07-23_v1.3_external-user_full.md; created the project's
  first 08_reviews/DISPOSITIONS.md (EXT13-1..8; prior reviews' findings live
  in their release verifications, not retro-transcribed).
- re-verified every confirmed finding MYSELF from source (not from the review):
  tsx SW1 `pin1: net.GND, pin2: net.ENKILL` + power_tree off_control =>
  COM-T1 shunted = OFF (v1.3 README said ON — reversed); BOM row 38
  `C5249699,F1,Fuseholder_Blade_Mini_Keystone_3568`; ledger row 150 C5126242 =
  FRC0603F1211TS = 1.21k ±1% => tolerance-inclusive 5VC 5.2274-5.4790 V
  (was Vref-only 5.272-5.432), low-corner headroom 597 mV vs 440 mV budget
  (PASS, 157 mV slack), 5VA top corner 5.2729 V > 5.25 V intent (R3 C728591 is
  0.1%, R4 is the 1% part); cpl.csv grep: F1/SW1 absent (intentional).
- NO non-docs defect surfaced during re-derivation — the docs-only premise
  holds (the stop-and-report tripwire never fired).

## 2026-07-23 — staged v1.4-2026-07-23

- did: staged 07_releases/v1.4-2026-07-23/ per the 2-commit seal procedure
  step 0. fab/ source/ 3d/ pdf/ COPIED from sealed v1.3 and verified
  **22/22 files sha256-IDENTICAL** (sorted find|sha256sum diff = empty) — the
  identity IS the release's premise. New: MANIFEST (release_type = docs-only
  supersede, identity declared), ORDER_README (5 fixes: shunt polarity, F1
  name, tolerance-inclusive rail table + USB-A note + no-load/overshoot
  ceilings, DNP packaging instructions + purchasing list, TIGHTENED Q0-Q7
  bench gate; OV-posture section 5 body diff-verified VERBATIM from v1.3 —
  user-recorded decision untouched), fresh verification/.
- verification scoping (canon "Verification scoping", cited): docs-only
  fix-pass — NO fresh review lens; there is no new electrical state to
  review. Targeted confirms instead:
  verification/2026-07-23_v1.4_docfix_confirmation.md re-derives each fix
  from SOURCE evidence (tsx pin trace, BOM row, ledger decode + corner
  arithmetic, CPL grep). v1.3's electrical battery stands for the unchanged
  payload and is NOT re-claimed.
- gates on staging: bom_source_check PASS (staged bom.csv vs circuit.json +
  02_parts + ledger 135 codes); release_freshness_check = 9 STALE
  (fab/pdf identical to v1.3) — EXPECTED AND INTENTIONAL, evidence-backed
  waiver verification/freshness_waiver.md; final run at stamp uses the gate's
  own `--allow-identical` edge-case flag x9 with all other checks enforced.
- **HARVEST FLAG:** release_freshness_check.py needs a
  `--docs-only-supersede <predecessor>` mode asserting whole-payload fab/pdf
  identity instead of 9 per-file waivers. A builder is delivering this in
  parallel — NOT waited for, NOT used here.
- also flagged for next-rev doc sync: power_tree.yaml's 5.272 V worst-low is
  the Vref-only corner; the tolerance-inclusive derivation now lives in the
  v1.4 ORDER_README (E-MARGIN verdict unaffected: PASS either way).
- next: 2-commit seal — source commit S (08_reviews + journal + beacon) ->
  stamp MANIFEST git_sha=S + sha256 table + policy_audit FULL + freshness
  re-run -> seal commit (release dir + CHANGELOG + v1.3/SUPERSEDED.md ONLY)
  -> check-ignore sweep LAST.

## 2026-07-23 — SEALED: v1.4-2026-07-23

- did: 2-commit seal per the 07_releases contract. Source commit S = 764c7c7
  (review archive + DISPOSITIONS + journal + beacon; `git diff --cached`
  foreign-path check clean). Stamp: MANIFEST git_sha=S git_dirty:false;
  sha256 table computed in passes (27 -> 29 -> 30 files as the stamp-time
  verification outputs landed); manifest self-check 29/29 bidirectional
  pre-selfcheck-inclusion, final 30/30 (selfcheck's own entry verified by
  policy_audit M-REL's independent re-hash). policy_audit FULL run x2 —
  0 FAIL (PASS=28, WAIVED=2, HUMAN=6, N-A=2), M-REL PASS over the FINAL
  manifest, shipped copy byte-identical to 06_build. release_freshness_check
  PASS with --allow-identical x9 (the gate's doc-only edge-case flag; waiver
  evidence in verification/freshness_waiver.md), confirm re-run
  byte-identical. check-ignore sweep on the release dir: clean (no ignored
  droppings). Seal commit bc3855e = release dir + CHANGELOG + v1.3
  SUPERSEDED.md ONLY.
- result: **v1.4-2026-07-23 SEALED + ORDERABLE** (docs-only supersede;
  order from v1.4; v1.3 remains valid sealed evidence for the identical
  payload, its README superseded).
- seal lifetime note: v1.3's seal was superseded ~1h after cutting — by a
  review RECEIVED post-seal but WRITTEN pre-seal. The "reviews run pre-seal"
  rule cannot catch a review that arrives late; the docs-only-supersede path
  (payload identity + targeted confirms, ~zero regen cost) is the cheap
  remedy and worked as designed.
