# journal — v1.12 J5 footprint geometry fix

## 2026-07-28 14:20 — start
- did: opened the CONFIRMED J5 footprint defect. Read CLAUDE.md, the 07_releases
  contract (2-commit seal), CHANGELOG v1.11/v1.10, STATUS beacon, every contracts.md
  on the path, and the v1.11 release read-only. Confirmed `03_src/lib/usb_hub_3s.pretty/
  TYPE-C-31-M-12_EdgeTrim.kicad_mod` is the KiCad stock `USB_C_Receptacle_HRO_TYPE-C-31-M-12`
  minus three F.SilkS lines (diff vs /usr/share/kicad/footprints/Connector_USB.pretty/
  is exactly 3 removed fp_line records) — so it inherits stock geometry wholesale.
- result: J5 = usb_hub_3s:TYPE-C-31-M-12_EdgeTrim at board (120.000, 109.000), rot 0,
  F.Cu. Edge_Cuts bbox 19.950..150.050 x 19.950..112.050 (board 20..150 x 20..112).
  So the alignment-hole line sits at board y=106.400 and the south board edge at
  y=112.000 -> alignment-to-edge = 5.600 mm. That number is the MATING FACE and is
  what decision 1 must not disturb.
- next: reproduce the three sources independently before touching anything.

## 2026-07-28 14:35 — iterate 1 (independent measurement of the vendor drawing)
- did: rendered `02_parts/TYPE-C-31-M-12A/TYPE-C-31-M-12A_LCSC.pdf` p.1 at 600 dpi
  and measured the "RECOMMEND P.C.B LAYOUT (COMPONENT SIDE)" view as a RASTER, with
  my own PIL code — no KiCad, no EasyEDA, no shared code with any gate. Calibrated the
  scale on FOUR independent horizontal pad-centre spans (2.50 / 3.50 / 4.80 / 6.40),
  which agree to 0.15%: S = 105.1268 px/mm. Then intensity-weighted sub-pixel centroids
  of every horizontal feature line.
- result (measured / nominal-on-sheet):
    pad length                 1.1415 / 1.140
    pad centre -> alignment    1.0654 / 1.070      <-- THE DATUM
    pad inner edge -> align    0.4946 / 0.50
    pad outer edge -> align    1.6361 / 1.64       (1.64-0.50 = 1.14, 0.50+1.14/2 = 1.07)
    front slot ctr -> align    0.4985 / 0.50       <-- sheet says 0.50, NOT 0.53
    slot separation            4.1811 / 4.18
    front slot outer height    1.9822 / 2.00
    rear  slot outer height    1.6772 / 1.70
    slot pad width             0.894  / 0.90       <-- sheet says 0.90, NOT 1.00
    slot drill width           0.604  / 0.60
    slot centre span           8.6510 / 8.65       <-- layout view says 8.65
    alignment hole dia         0.5994 / 0.60
    alignment -> PCB EDGE      5.7938 / 5.79
  The wide-pad spans calibrated on the narrow-pad pitch alone read 4.8095 and 6.4095 —
  i.e. 4.80/6.40, and 4.90/6.50 is refuted by 9.5 px (0.09 mm), far outside noise.
  Front view (separate view, same sheet): `2-{\O}0.50` posts, `5.78` post span,
  `8.64` LEG span; top view `16-0.20+/-0.05` contacts.
- next: parse EasyEDA C5337088 with my own parser as the third source.

## 2026-07-28 15:05 — iterate 2 (third source + the two decisions)
- did: parsed `.easyeda_cache/C5337088.json` -> `result.packageDetail.dataStr.shape`
  with my own splitter (EasyEDA unit = 10 mil = 0.254 mm). 60 shapes: 12 RECT lands,
  2 HOLE, 4 OVAL multi-layer SH pads.
- result (units x 0.254):
    land h 4.4882 -> 1.14000      land w 1.1811 -> 0.29999 / 2.3622 -> 0.59999
    A5<->A8   9.841 -> 2.49961    B8<->B5  13.780 -> 3.50012
    A4<->B4  18.897 -> 4.79984    A1<->B1  25.196 -> 6.39978
    HOLE r 1.1811 -> dia 0.59999  hole span 22.756 -> 5.78002
    LAND ROW -> HOLE ROW  4.213 -> **1.070102**   <-- the datum, third source
    SH pad 3.5433 x 7.8740 -> 0.90000 x 2.00000   drill r 1.1811 / len 6.6929
                                                  -> 0.600 x 1.700
    SH span 34.054 -> 8.64972     SH front->rear 16.457 -> 4.18008
    SH front -> hole row  1.969 -> 0.500126
  THREE SOURCES AGREE on 1.070 and on 1.140. TWO DISAGREEMENTS with the brief's
  "already correct" list, both measured, both small, both taken from the sheet+JLC:
    * front slot offset: sheet 0.4985 (raster) / JLC 0.500126 -> **0.50**, not 0.53.
      The sheet's own `0.50` dimension terminates on the pad-INNER-EDGE line, and the
      front slot centre measures 0.4 px (0.004 mm) from that same line — they are one
      line. 0.53 is a stock-KiCad inheritance with no support in either sheet.
    * slot centre span: layout view 8.6510 (raster) / JLC 8.64972 -> **8.65**. The
      8.64 in the brief is real but is a DIFFERENT dimension: the FRONT VIEW's
      `8.64` is the PART's leg span, not the recommended layout's slot centres.
  A THIRD delta the brief did not list: shell pad width **0.90**, not 1.00 (sheet
  dimensions it as `0.90`/`0.60` at the rear-left slot; JLC 3.5433 -> 0.90000).
- DECISION 1 — WHICH FEATURE MOVES: **the pad row moves; the body does not.**
  The alignment holes + 4 shell slots are one rigid body-referenced set and in v1.11
  that set is internally SELF-CONSISTENT and correct (span 5.78, separation 4.18);
  only the pad row is displaced. Moving the holes instead would fix the separation
  equally but would carry the connector body — and therefore the USB mouth — 0.375 mm
  north, away from the board edge, changing a MATING INTERFACE that eleven sealed
  releases of mechanical review are built on. Measured and preserved: alignment line
  at board y=106.400, south edge y=112.000 -> **alignment-to-edge 5.600 mm, IDENTICAL
  before and after** (sheet nominal 5.79; the 0.19 mm is v1.11's pre-existing, settled
  edge position and this fix does not touch it). F.Fab, F.CrtYd, F.SilkS and the 3D
  model reference are left BYTE-IDENTICAL, which is what makes "the mating face did
  not move" provable rather than asserted. Cost: 16 lands move 0.375 mm south.
- DECISION 2 — ALIGNMENT HOLE DIAMETER: **0.65 -> 0.60 (CHANGED).** 0.65 was a
  defensible choice IN THE OLD GEOMETRY — top of the sheet's own +/-0.05 band, bought
  post-entry clearance against a phi 0.50 moulded post. At the CORRECTED datum it is
  no longer free: MEASURED with KiCad's own shape engine, land A1/B12 to NPTH hole is
  **0.1944 mm at 0.65** against this board's `min_hole_clearance` 0.200 (DRC fails),
  and **0.2194 mm at 0.60** (clears). Built both and measured both — see RED 2 below.
  The lateral-play argument also reverses at the true geometry: `16-0.20+/-0.05`
  contacts on 0.30 lands leave 0.05 mm/side, which a 0.60 hole on a 0.50 post exactly
  preserves and a 0.65 hole exceeds. Post entry is the vendor's own trade and they
  answered it with 0.60 +/-0.05; JLC, who drills this board AND places this part,
  ships 0.600 in their own C5337088 package.
- next: gate the authored footprint two ways, then red-verify both.

## 2026-07-28 15:20 — iterate 3 (two-instrument geometry gate + two red-verifies)
- did: wrote a gate with two instruments that share no code — (A) my own
  s-expression tokeniser over the raw .kicad_mod TEXT, compared against
  hand-retyped drawing numbers; (B) `pcbnew.FootprintLoad` + KiCad's own shape
  engine. A never imports pcbnew; B never reads the file as text (canon M1).
- result: **GEOMETRY GATE PASS — 127 checks passed, 0 failed.** KiCad's shape
  engine independently measures min land<->NPTH-hole clearance **0.2194 mm**.
- RED 1 (the gate must be able to fail): fed it the v1.11 footprint exactly as
  sealed (`git show HEAD:...`). GATE FAIL, naming the defect:
      PAD ROW -> ALIGNMENT   got 1.4450  want 1.0700  d=+0.3750
      pad length (x16)       got 1.4500  want 1.1400  d=+0.3100
      wide-land x (x8)       got 3.2500  want 3.2000  d=-0.0500  (and 2.4500/2.4000)
- RED 2 (the diameter decision must be falsifiable): same corrected footprint,
  alignment hole reverted to 0.65, nothing else changed.
      KiCad shape engine: land<->NPTH clearance = **0.1944 mm** (nearest land A1)
      -> FAIL B: 0.1944 < min_hole_clearance 0.200
- next: rebuild. ROUTING NOTE recorded before running it: J5's nets are NOT
  KRT-routed. route.yaml `waves.exclude` lists CC1/CC2/DPC/DMC verbatim ("J5 USB-C
  corner: CC + interleaved D+/D- edge pads defeat KRT (v1 finding) -> deterministic
  taps own them"), and VBUS/GND are pour-owned. VERIFIED, not assumed: the promoted
  chain `03_src/route/final_chain.kicad_pcb` holds 504 items on 44 nets with
  **0 items on CC1/CC2/DPC/DMC/VBUSC/GND and 0 endpoints anywhere in the J5 corner
  box x114-126 y102-112**. So rebuild_fast.sh's stated validity condition ("valid
  ONLY while KRT-routed pins do NOT move") HOLDS, and the correct router for the
  affected area is the deterministic tap pass, which re-derives from pad positions
  on every build. Re-rolling KRT would re-route 504 stochastic items this fix does
  not touch — replacing a reviewed route with an unreviewed one and destroying the
  FIX-PASS diff. Baseline: 04_kicad md5 83af8e5a5596a51cf139dd06e8903d47 == v1.11's
  sealed source.

## 2026-07-28 15:35 — iterate 4 (build; and a SECOND defect found on the way)
- did: rebuilt via rebuild_fast.sh. First pass came back DRC 0/0/1 — one
  schematic-parity issue, and it was MINE: I had put the datasheet reference into
  the footprint's `Datasheet` PROPERTY, which propagates to the board, while the
  schematic symbol's field is empty. Reverted Datasheet and Description to "" (the
  facts live in `descr`, which is not parity-compared, and in 02_parts/). Rebuilt.
- result: `AUDIT PASS: 21 polarity, 19 proximity, 4 edge, 129 silk checks` —
  including I-EDGE J5. Promoted chain imported: 484 segments, 20 vias.
- SECOND DEFECT, found while checking whether the CPL would move: **the CPL
  PLACEMENT DATUM for J5 was also wrong, and by a mechanism the brief did not
  name.** `export_jlc_package.placement_datum()` emits the centre of the bounding
  box of the PAD CENTRES, because JLC places a part so that ITS OWN model origin
  lands on Mid X/Y. Measured:
      JLC C5337088 model origin   +1.30492 mm from the alignment line
      v1.11 emitted CPL datum     +1.10250  -> **0.2024 mm OFF**
      v1.12 emitted CPL datum     +1.30500  -> **0.00008 mm** (80 nm)
  So v1.11 did not merely mis-shape the lands: it told the placement machine to
  put the connector 0.2025 mm NORTH of where its own alignment posts sit, and the
  posts have only 0.075 mm radial clearance in v1.11's 0.65 holes. That is the
  crow-recorder-central-v2 v1.4 J2 class, named verbatim in placement_datum()'s
  own docstring. It follows automatically from the land-pattern fix; no separate
  edit was needed. CONSEQUENCE FOR THE RELEASE SHAPE: fab/cpl.csv is NOT
  byte-identical to v1.11 — J5's Mid Y moves 107.5025 -> 107.7050.
  WHY NO GATE SAW IT: A-POS recomputes the datum from the SAME board it grades,
  so it is self-consistent by construction (v1.11's MANIFEST reads "worst residual
  0.00050 mm"). Checker and checked shared a method — canon M1.
- next: DRC gate, then the classified board delta.

## 2026-07-28 15:20 — iterate 5 (board green; delta classified four ways)
- did: rebuild #2 with the Datasheet/Description properties reverted.
- result: **DRC 0/0/0** at --severity-all --refill-zones --schematic-parity.
  AUDIT PASS 21/19/4/129. ERC 0 errors / 221 warnings (all lib_symbol_issues,
  identical to v1.11). Netlist parity 0 by three tools: shipped .net vs v1.11's
  sealed .net (122 comp / 73 nets / 372 nodes, identical), board-vs-board
  372/372, and DRC's own parity leg.
  NEAR-MISS RECORDED: the first parity script I wrote matched `(net (code` on one
  line, found **0 nets in both files**, compared them equal and printed
  `PARITY: 0`. That is the jlc_twin exit-0 shape — a hollow pass. KiCad 10 writes
  `(net` and `(code ...)` on separate lines. The shipped parser now ASSERTS a
  non-zero census in both inputs before it may report agreement, and parity.md
  says so out loud.
- DELTA, four independent instruments:
  * pcbnew classification: Edge_Cuts IDENTICAL; 0 footprints moved; exactly ONE
    footprint's pads changed (J5, 22 records); tracks 908->907 with 7 added /
    8 removed and **0 outside the J5 corner box**; vias 293->293 with 9/9 and
    **0 outside**; track delta confined to CC1/CC2/DPC/DMC — the four tap nets.
  * SHAPE_POLY_SET XOR on the saved filled polygons: F.Cu 3.5905 / In1 10.0333 /
    In2 9.4501 / B.Cu 5.0280 = **28.1019 mm2** of copper moved, of which
    **0.000969 mm2 (0.0034%)** lies in regions not wholly inside the J5 corner —
    174 zone-filler slivers, largest 78 x 16 um.
  * gerber TEXT: 13 members, Edge_Cuts.gm1 and B_Paste.gbp byte-identical after
    the timestamp strip. A LINE diff on a copper gerber is noise (the filler
    re-emits regions in arbitrary order) — measured that too rather than
    asserting it: 494 coordinates differ outside the box, and their
    nearest-neighbour distribution in the other payload is 748/1027 within
    0.001 mm on B.Cu. Recorded, then superseded by the XOR above, which is
    representation-free.
  * render before/after of the J5 corner, same plot command on both boards.
- fab payload: bom.csv **byte-identical**; cpl.csv **one row** (J5 Mid Y
  -107.502 -> -107.705); board md5 83af8e5a... -> 35ba862e..., the first change
  since v1.9.
- next: gates + the fresh-context lens, then the 2-commit seal.

## 2026-07-28 15:30 — iterate 6 (gate battery against the STAGED archive)
- did: staged 07_releases/v1.12-2026-07-28/ (58 verification files) and ran the
  battery against it.
- result: A-ROT 119/119 sourced. A-POS worst residual 0.00050 mm (Q6), tol 0.05.
  A-BODY bodies mounted 119/119. F-PAYLOAD OK 5 checks, F-POUR 17/87/1/1 G36
  regions on the SHIPPED zip. F-LEGIBLE OK 48 checks. M-BOM PASS 46 lines / 0
  without LCSC. A-STOCK PASS 46/46 (C5337088 catalog **84**, down from 104 at
  v1.11 — and JLC's assembly side allocated 0 at the real upload; the gate now
  prints its own scope caveat). rules_audit PASS 0 fails / 32 checks.
  P-FACT FAIL 1 (KT-0805Y/D8) — pre-existing, adjudicated, unchanged.
  A-RENDER twin_overlay top: 29 unfaithful refs, COVERAGE 53/121 — IDENTICAL to
  v1.11's 29 and 53/121, carried adjudication; --side bottom REFUSES, which is
  the correct result on a 119/119-top board and is v1.11's result too.
- **jlc_twin, run as instructed, and its verdict is the finding**:
      v1.11 (DEFECTIVE board): C5337088,J5,OK,fit=0.00mm
      v1.12 (CORRECTED board): C5337088,J5,OK,fit=0.00mm
  Character-identical, on two boards differing by 0.375 mm on the datum the gate
  exists to check. The blindness is now DEMONSTRATED rather than argued.
- freshness dry run: 5 findings. 3 are one legitimate identity (schematic.pdf,
  unchanged because the .kicad_sch is byte-identical to v1.11's — sha256
  f007c55a..., git diff empty), 1 is the other (bom.csv — no part changed), and
  1 was a real gap (policy_audit.md missing) which was FIXED not waived. TWO
  --allow-identical exceptions, each with its measurement, in
  verification/freshness_exceptions.txt. v1.11 needed seven.
- next: fresh-context lens against staging, then seal.

## 2026-07-28 15:45 — iterate 7 (the completeness test caught a real staging defect)
- did: ran the 07_releases contract's completeness test — `kicad-cli pcb drc` from
  INSIDE the staged `source/` directory, on nothing but the archive.
- result: FIRST RUN **12 violations, all lib_footprint_issues** ("The footprint
  library 'usb_hub_3s' is not enabled"). I had copied `04_kicad/fp-lib-table`
  verbatim, and its URIs read `${KIPRJMOD}/../03_src/lib/<lib>.pretty` — which
  from inside `source/` points outside the archive, making the two VENDORED
  `.pretty` dirs sitting right beside it unreachable. Canon V-REL-FPLIB, the
  exact defect that rule exists for. Rewrote to `${KIPRJMOD}/<lib>.pretty`; the
  result is byte-identical to v1.11's sealed source/fp-lib-table (diff clean),
  which cross-checks the form. RE-RUN: **DRC 0/0/0**. Recorded in
  verification/standalone_archive_check.txt rather than quietly fixed.
  Also stripped the `usb_hub_3s_v2.kicad_prl` kicad-cli drops on every board
  open — the release dir must contain nothing the MANIFEST does not account for.
- next: fresh lens verdict, then seal.

## 2026-07-28 15:55 — iterate 8 (I mutated a SEALED release, and put it back)
- did: pre-seal `git check-ignore` sweep over the whole project.
- result: found `07_releases/v1.11-2026-07-27/source/usb_hub_3s_v2.kicad_prl`,
  mtime 15:34 — **I created it**, by running `kicad-cli pcb export svg` against
  v1.11's sealed source board to make the before/after J5 render. kicad-cli
  writes a `.kicad_prl` beside any board it opens. That is a mutation of an
  IMMUTABLE directory. It is gitignored so it would never have been committed,
  and it is in no MANIFEST, but the contract does not say "do not commit to a
  sealed release", it says do not TOUCH one.
  Removed it, and the pre-existing one in v1.1 (mtime 2026-07-23, an earlier
  session's — the same trap RESUME.md's watch-outs already name).
  RE-VERIFIED that v1.11 is intact rather than assuming: **MANIFEST self-check
  76/76 sha256 verify, 0 bad, 0 files present but unlisted** (SUPERSEDED.md and
  MANIFEST.txt excluded, as the contract's own rule requires).
  LESSON for next time: open a sealed board READ-ONLY via pcbnew.LoadBoard, or
  copy it to scratch first. kicad-cli is not a read-only tool.
- also: moved `rotation_human_gate.txt` from the release ROOT into
  `verification/`, where the 07_releases contract template puts it (beside
  bom_echo_gate.txt, the other half of the same human gate). v1.6..v1.11 each put
  it at the root and each carries a standing C-ALLOW violation for it; this
  release does not add a seventh. Noted in the MANIFEST.
- MANIFEST sha256 table layout confirmed against v1.11's: plain `sha256sum`
  output, `<hash>  <path>`, NO leading whitespace. (My first self-check regex
  assumed leading whitespace and matched 0 of 76 rows — which is exactly the
  M-REL matcher bug the v1.10 entry records. Caught it because a 0/0 census is
  not a pass.)

## 2026-07-28 16:05 — iterate 9 (verified the THIRD source myself instead of citing it)
- did: the "HRO -12 sheet says the same thing" claim came from the brief and from
  pluto-rx2-8way's footprint descr. Both are second-hand. It is load-bearing —
  if the two HRO part numbers had DIFFERENT land patterns, the stock footprint
  might have been right for one of them — so I read the drawing.
- result: HRO `TYPE-C-31-M-12` REV A 2020.12.08 (sha256 6ae33d50ac47820114661138
  dc4e4659f40fdc5d4f485b46685dfec338da1a9e), title block confirms PART NO.
  TYPE-C-31-M-12. Its "RECOMMEND P.C.B LAYOUT (COMPONENT SIDE)" view carries a
  DIMENSION-FOR-DIMENSION IDENTICAL pattern: 8-0.30, 4-0.60, 0.50, 1.50, 1.64,
  2.00/1.70, 1.70/1.40, 0.60/0.90, phi0.60, 2.50, 3.50, 4.80, 5.78, 6.40, 8.65,
  4.18, 5.79, same "+/-0.05" note. **Ruled out as an M-12 vs M-12A variant
  difference.** Recorded in the CHANGELOG with the sha256 and with the caveat
  that this PDF is a WORKING COPY, not a committed artifact — the dossier this
  project owns is the -12A sheet, which is the part actually bought.
  (My automated pad-run detector mis-fired on this render — different line
  weights — and I did NOT paper over it: the verification above is a read of the
  dimension TEXT, which is what a footprint is authored from, and it is reported
  as such rather than as a pixel measurement it is not.)
- also corrected a CHANGELOG overstatement: the v1.11-vs-stock diff is the
  footprint NAME line PLUS three removed fp_line records, not three lines alone.
  Every pad record is identical, which is the part that matters.

## 2026-07-28 16:10 — iterate 10 (two of my own overclaims, caught before the reviewer did)
- did: proofread the CHANGELOG and MANIFEST against the measurements rather than
  against my memory of them. Two claims did not survive:
  1. "the alignment holes and four shell slots are one rigid set, internally
     self-consistent and CORRECT". Not true — the slots carried errors too
     (front offset 0.53 vs 0.50, span 8.64 vs 8.65, wrong pad/drill heights) and
     I corrected them in the same change. Rewritten to say what is actually true:
     the HOLES are the datum and their positions are right; the slot errors are
     0.03 mm / 0.005 mm / pad-size on 0.60-wide slots taking ~0.3 mm legs, an
     order of magnitude below the pad row's 0.375 mm. Scale is the argument, not
     flawlessness.
  2. "the land-pattern error and the placement error push in the same direction
     and add". WORKED IT THROUGH and it is wrong. The land error mis-registers a
     SEATED part. The placement error commands a position the part cannot occupy
     (0.2025 mm offset against 0.075 mm of radial post clearance). Which one you
     actually get depends on whether the posts or the pick-head win, and I cannot
     determine that from here — a chamfered post may simply be dragged into the
     hole, absorbing the placement error and leaving only the land error at the
     joint. Rewritten as two INDEPENDENT failure modes, with the uncertainty
     stated. Stacking the two numbers would have read better and been false.
- next: the fresh lens.

## 2026-07-28 16:20 — iterate 11 (the fresh lens landed, and its headline finding is RIGHT)
- did: read `08_reviews/2026-07-28_v1.12_fresh-lens_staged.md`. Verdict
  **ORDER-WITH-CONDITIONS, no P0**. It re-derived every dimension by methods
  sharing no code with mine — `pdftocairo -svg` VECTOR extraction of the sheet
  (5036 paths, 0 images, so it never touched a raster), its own EasyEDA parser,
  its own `.kicad_mod` re-parse — and confirmed the corrected copper on every
  number. It also independently reproduced 5.600000 mm alignment-to-edge on both
  boards and the 0.2194 / 0.1944 / 0.4200 clearance figures.
- **F-1, P1, AND IT IS CORRECT — MY SEVERITY NARRATIVE WAS A TAUTOLOGY.**
  `lead_overlap.txt` opened by DEFINING the contact tail as the vendor's
  recommended LAND. A land is lead PLUS fillets, so every number below it was a
  land-vs-land ratio wearing a lead's name: "0.220 mm inboard", "80.7% overlap",
  "0.530 mm past the lead". Its own output should have exposed it — it graded the
  CORRECTED pattern as "100% overlap, heel 0.0000, toe 0.0000", a land with no
  fillet at either end, which would be a BAD land pattern rather than the vendor's.
  I VERIFIED THE FINDING MYSELF before acting, with my own mesh parse:
      posts measured dia 0.5000 at x = +/-2.8900, span **5.7800** (sheet: 5.78)
      alignment line at mesh y 2.5050; mouth face -3.7750; +7.35 housing -> 3.5750
      mesh max y 3.9750 -> tail projects **0.4000** (sheet top view: 0.40 +/- 0.10)
      => SOLDERABLE TAIL = 1.0700 .. 1.4700 from the alignment line, 0.400 long
                     land            tail          heel     toe    paste
      v1.11    0.720..2.170    1.070..1.470      0.350    0.700   +27.2%
      v1.12    0.500..1.640    1.070..1.470      0.570    0.170     spec
  **The tail is 100% on its land in v1.11 too. Nothing hung off.** The real
  defect is an INVERTED FILLET BALANCE (heel starved to 61% of intended, toe
  doubled) plus 27% excess paste and 0.700 mm of open aperture past the tail tip
  on a 0.5 mm-pitch row. Real, correctly signed, and sufficient — but not what I
  said. Corrected in: lead_overlap.txt (rewritten from the mesh), the footprint
  `descr` (which forced a REBUILD — it lives inside the board file and every
  future release's source/), MANIFEST, CHANGELOG, ORDER_README, part.yaml,
  SUPERSEDED.md, the render note, and the targeted-confirmation review.
- **D-1 amendment, verified and REFINED rather than accepted.** The lens said the
  by-name correspondence set jlc_twin builds is EMPTY. Measured: it is not empty,
  it is **eight names** (A5..A8, B5..B8) — and **all eight are COLLINEAR** (their
  y = 2982.205, ours = -3.670, one value each). So after `centered()` removes the
  centroid the fit is eight points on a line against eight points on a line: it
  carries NO information about where that line sits on the only axis this defect
  lives on. Worse than it sounds, better than "empty". Recorded as measured.
- other conditions actioned: F-4 (gerber_payload_delta contradicted its own 494
  count — reworded to defer to the XOR area bound), F-6 (EasyEDA gives all FOUR
  shell slots at 2.00/1.70 and disagrees with both sheets on the REAR pair — the
  footprint follows the sheet and the disagreement is now stated), F-7 (the
  F.Courtyard is stale: north margin 0.500 -> 1.030 mm — declared, not changed,
  because changing it would perturb the graphics the mating-face proof rests on),
  F-8 (J5's twin metrics moved: MODEL-REG 0.05 -> 0.33, overlay excursion 0.049
  -> 0.326 — consequences of F-7, declared), F-9 ("deliberate" -> "untouched"
  everywhere; no decision record exists for the 5.600), F-10 (the base -12 sheet
  row now says it is NOT in the archive and carries its sha256).
  F-2 (policy_audit.md) and F-3 (A-POP re-run) are the post-stamp steps and are
  in the stamp script by construction. F-5 (min_hole_clearance has no fab
  provenance in fab_tiers.yaml) is accepted: the decision stands on the vendor's
  nominal and JLC's own 0.59999, not on the DRC number.
- next: rebuild finishes -> re-stage -> re-gate -> stamp -> seal.

## 2026-07-28 16:35 — finish (v1.12 SEALED)
- did: the 2-commit seal, following v1.11's split on this board so `git_dirty:
  false` is a MEASURED fact rather than a caveated one — S carries every input
  AND the staged release except the MANIFEST (which cannot exist before S, since
  it must carry S's hash); the seal commit adds only the release directory.
      S    4222c3bd3a1c9a8914dc00da5a779dfd229ab3e1  [1/2 source], 95 files
      SEAL d2586d7                                    release dir only, 6 files
  `release_git_dirty.py usb-hub-3s-v3` at S: **git_dirty: false, exit 0** — it
  excuses the untracked seal-output path itself, so no manual judgement was
  needed.
- STAMP ORDERING, which cost two attempts and is worth recording: the sha256
  table must be computed LAST, after every file that goes in the directory is
  final — but `policy_audit`'s M-REL leg needs the table to exist, and its output
  goes IN the directory. Circular. Resolved by iterating to a FIXED POINT
  (policy_audit and release_freshness are both deterministic — verified by
  running policy_audit twice and comparing md5), reached at iteration 2. The
  self-check VERDICT then goes into MANIFEST.txt, the one file excluded from its
  own table and therefore the only one that can carry it.
  Two real defects the freshness gate caught on the way, both fixed not waived:
  `freshness_exceptions.txt` is MACHINE-PARSED (every non-'#' line is
  "<relpath> <reason>"), and my prose '====' rules were correctly rejected as
  eight waivers with no reason; and the word "draft" in an ORDER_README sentence
  tripped the draft-marker check.
- FINAL GATES, all against the staging directory:
      DRC 0/0/0, and 0/0/0 again re-measured STANDALONE from inside source/
      ERC 0 err / 221 warn · audit_board PASS 21/19/4/129
      netlist parity 0 by three tools (122 comp / 73 nets / 372 nodes)
      J5 geometry gate 127 checks / 0 failures, RED-verified twice
      policy_audit FAIL=0 — PASS=31 WAIVED=2 HUMAN=6 N-A=2; M-REL PASS
      FRESHNESS PASS with TWO measured exceptions (v1.11 needed seven)
      MANIFEST SELF-CHECK PASS 88/88 sha256, 0 unlisted — cross-checked with
        `sha256sum -c`, a different tool from the Python that wrote it
      A-ROT 119/119 · A-POS worst 0.00050 mm · A-BODY 119/119 · F-PAYLOAD OK
      F-LEGIBLE OK 48 · M-BOM PASS 46 lines · A-STOCK PASS (necessary, not sufficient)
      M-BEACON PASS after the beacon refresh
- beacon: `01_docs/STATUS.md` OVERWRITTEN (never appended) to name
  v1.12-2026-07-28, `state: sealed`, and `status_beacon_check.py` exits 0.
- OWED AND NAMED, not closed here: F-ECHO at upload (J5's C5337088 reads catalog
  84 and allocated ZERO at the last real upload — it is a PRE-ORDER line), the
  A-POL order-preview check, the In1/In2 viewer check; the `jlc_twin`
  numbering-free-fit gap; a `--copper-only-supersede` freshness mode; a
  hole-to-copper field in `fab_tiers.yaml`; the stale F.Courtyard; and the two
  sibling boards carrying the same defective footprint.
