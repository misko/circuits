# journal — v1.5 CPL-correction supersede (PCBA fix pass)

## 2026-07-25 10:30 — start

- did: took ownership of v1.5. Read CLAUDE.md, pcb-design SKILL.md ("PCBA is the
  deliverable"), 07_releases/contracts.md, and the three source-fix post-mortems
  (`e0d735c` handedness, `9078ad9` C2982822, `95a8180` C98732).
- result: blocking dependency CHECKED FIRST — `jlc_twin.xform()` still read
  `(x*c - y*s, x*s + y*c)` at 10:32. Did the non-export work while re-checking.
  Re-checked at ~10:36: fix landed (`1b69760`), `(x*c + y*s, -x*s + y*c)`,
  matching `local_to_board()`. Unblocked.
- next: export against the UNCHANGED 04_kicad board, enforce the 4-cell gate.

## 2026-07-25 10:37 — iterate 1 (export + the acceptance gate)

- did: re-exported the fab package from `04_kicad/usb_hub_3s_v2.kicad_pcb` into
  `06_build/fab/` and diffed the CPL against sealed v1.4.
- result: **EXACTLY FOUR changed cells, as predicted** — C1 270.0→90.0,
  C2 270.0→90.0, J1 90.0→0.0, Q7 270.0→180.0. No fifth row. 108 placements both
  sides, 0 added/removed. Gerbers re-plotted and compared member-for-member
  against v1.4's zip: 13/13 identical once the plot's own `TF.CreationDate` /
  "Created by KiCad" comment lines are stripped (4 diff lines per file, all
  timestamp). So the copper is PROVEN unchanged, and v1.5 ships v1.4's gerber
  and drill BYTES verbatim — making the MANIFEST's sha256-identity claim literal.
- next: MPN column, twin, stock, then the documents.

## 2026-07-25 10:45 — iterate 2 (MPN column + stock)

- did: ran `jlc_stock_check` at `--min-stock 5`, harvested the MPN attributes
  into `06_build/fab/lcsc_mpn_map.csv`, re-exported so the BOM carries MPNs.
- result: **43/43 rows now carry an MPN** (v1.4: 0/43). BOM otherwise identical
  to v1.4 row-for-row. CPL gate re-verified after the second export: still
  exactly four cells. Stock **PASS, 43/43 OK, 0 uncoded**; split **12 Basic /
  31 Extended**; tightest ceilings C473910 = 37 boards (stock 75, 2/board),
  C5337088 = 90, C408523 = 225.
- next: cross-check the MPNs against something that did not produce them.

## 2026-07-25 10:52 — iterate 3 (M1 cross-check on the MPN map)

- did: compared the 43 harvested MPNs against the `02_parts/` directory names,
  which were authored independently from datasheets at parts stage.
- result: **26/26 hard-part MPNs matched; 0 `02_parts` directories unaccounted
  for.** The 17 unmatched are generic passives with no `02_parts` entry, as
  expected. Checker and checked do not share a source.
- next: assembly.yaml, then the twin.

## 2026-07-25 11:00 — iterate 4 (assembly.yaml + twin)

- did: measured the population set with pcbnew, wrote
  `03_src/rules/assembly.yaml` from the skill template, ran `jlc_twin` with the
  FIXED operator plus `--assembly` and `--adjudications`.
- result: 114 footprints − 4 mounting holes = 110 BOM parts = 108 CPL + 2
  declared not-assembled (F1, SW1); board `excl_pos` agrees; 108/108 `top`,
  zero bottom-side footprints; min pad-to-pad centre distance **0.600 mm**
  (> 0.4 mm ⇒ no local fiducials owed). **[CORRECTED at iterate 9: the true
  minimum is 0.500 mm at J5 — my measuring pass missed the USB-C pads. The
  conclusion survives; the number did not.]** Twin **exit 0, 231 checks, 102 OK,
  0 ROT-DB-SUGGEST** — with the fixed operator every fitted offset now agrees
  with the resolved value for every part on the board. One `MODEL-REG` on J1.
- next: adjudicate the MODEL-REG properly rather than waving it through.

## 2026-07-25 11:10 — iterate 5 (J1 MODEL-REG: verified, not assumed)

- did: the tool says "VERIFY leads sit on pads visually". Did that
  quantitatively: calibrated `twin_top.png` against the four mounting holes and
  overlaid J1's four pad centres.
- result: **false alarm.** All four pads land on the model's own features (two
  blade bores on pads 1/2, two housing posts on the anchors). The metric is
  meaningless here because the XT60 housing extends back from the blade face —
  WRL bbox centre is 4.10 mm off its own model origin. Separately re-derived
  the rotation by hand from JLC's `kicad_mod`: our 4 pads vs theirs, rms
  **0.0000 mm @270**, and **12.0 mm out at 90**. That is a stronger result than
  the rotation table's own caveat claims: the two anchor holes sit 6.0 mm
  perpendicular to the blade axis, so 90-vs-270 is broken by GEOMETRY, with no
  reliance on JLC's pad numbering. Recorded as PCBA-11/PCBA-2.
- next: the P0's own polarity evidence — do not inherit it either.

## 2026-07-25 11:15 — iterate 6 (C1/C2 polarity re-derived independently)

- did: rather than inherit the chamfer argument from `9078ad9`, read JLC's own
  `CAP-SMD_BD6.3-L6.5-W6.5-LS7.2-FD.kicad_mod` silk primitives.
- result: **decisive and better than the chamfer.** Three filled silk polygons:
  two crossed at (−2.706, 1.518) forming a **“+” over JLC's pad 1**, and a lone
  bar at (+2.706, 1.518) forming a **“−” over pad 2**. JLC's library states
  pad 1 = positive; our pad 1 is on VIN. They agree, so the pad fit is sound and
  CPL 90.0 is right. Q7 re-derived too: 3-pad asymmetric fit, rms 0.062 mm @180
  vs 1.95 mm @270.
- next: documents.

## 2026-07-25 11:25 — iterate 7 (the documents that did not exist)

- did: wrote `01_docs/DETAIL_DESIGN.md` — MISSING since 2026-07-21 although
  three sealed `part.yaml` files cite sec.1/2/5 as authority. Derivations taken
  from the datasheets directly: LM5116 SNVS499I §7.2.2 eq. (7)-(24) and USBLC6-2
  Doc ID 11265 Rev 5 Tables 1-2.
- result: four values re-derived for this board's envelope rather than copied
  from TI's 60 V example — L ripple **1.774 A**, `R_S ≤ 13.3 mΩ` (chose 10 mΩ,
  `I_LIM` 11.0 A), `C_RAMP` **340 pF → 330 pF** (TI's 270 pF is for 6 µH), and
  UVLO **9.652 V rise / 8.836 V fall** including the 5 µA pull-up — matching
  `part.yaml`'s 9.65/8.84 exactly, where naive divider arithmetic gives 9.90 V.
  Found and recorded a NEW P2 while writing sec.5: **U12's V_BUS transil V_BR is
  6.0 V min, BELOW D5's 6.67-8.15 V window**, so on a slow rise the ESD array
  conducts before the TVS (PCBA-15). Bounded — the window only opens in the
  already-best-effort fail-high case.
- next: ORDER_README, MANIFEST, gates, seal.

## 2026-07-25 11:40 — iterate 8 (order paperwork)

- did: rewrote ORDER_README around the finding that v1.4 mentioned the JLC order
  preview **zero times** (`grep -ci preview` = 0) while **12** twin findings are
  waived on exactly that gate — including C1/C2, the P0.
- result: added an **ORDER-PREVIEW HUMAN GATE** (P1-P7, each with what to see and
  what rejects it), the assembly-side / build-quantity / THT declarations, the
  stack-up and finish, and a **panel-rail policy** backed by a measurement:
  three of the four board edges cannot take a rail because J1 (−6.82 mm),
  J2-J4 (−4.29 mm) and J5 (−2.90 mm) physically OVERHANG the outline; only the
  edge opposite the USB-C connector is usable (C12 at +1.43 mm). Stock table
  rebuilt sorted by `floor(stock/qty)` with a named alternate per row.
- next: gates + 2-commit seal.
