# journal — usb-hub-3s-v3 v1.3 FIX PASS

## 2026-07-23 16:20 — gate (i): R12 + power_tree + D5 + OV-honesty (finish)

- did: fixed the v1.2 DO-NOT-ORDER blockers at SOURCE (canon M3), all from committed
  v1.2 source. Architecture = Option 2 (KEEP discrete Q6/Q7/F2/D5 as SECONDARY
  protection; NO new active OVP).
- result (MEASURED):
  - **R12 catalog verification (REAL JLC catalog lookup, NOT tscircuit auto-resolve):**
    - C2984354 (AR03BTCX4121, Viking) = 4.12kOhm +-0.1% +-25ppm 0603 100mW, stock 15353,
      extended. CHOSEN + BAKED (tsx fbtopMpn="C2984354").
    - C861436 (RT0603BRD074K12L, Yageo) = 4.12k 0.1% 25ppm, stock 4927 — verified alt.
    - C2933210 (FRC0603F3741TS) = 3.74k 1% 100ppm — CONFIRMED the v1.2 bug; do_not_use.
    - circuit.json now: R12 -> res 4120, jlc C2984354 (bug value-resolution eliminated).
  - **power_tree.yaml re-derived vs the ACTUAL Q6+F2 path** (eFuse 34-48 mOhm model
    REMOVED): 5VC = 1.215*(1+4.12/1.21) = 5.352V nom, 5.27V @Vref-1.5% (vout_min),
    5.43V @Vref+1.5% no-load (vout_max, = D5 Vwm-6.0V standoff check). ir_budget_mohm=88
    (Q6 4.3 + F2 R1max 18 catalog-verified/hot ~31 + board 3 + connector 5 + short 5A
    cable 45). E-MARGIN PASS: headroom 640mV vs 88mOhm*5A*1.2 = 528mV need (112mV slack).
    E-TOPO PASS (2 bucks). E-OFF PASS.
  - **D5 directionality FIX:** C140903 is listed BIDIRECTIONAL by the JLC catalog (LRC
    SMB-FL) — fails the uni-directional cathode=VBUSC design assumption. Replaced with
    C113976 (SMBJ6.0A UNIDIRECTIONAL DO-214AA/SMB, Vwm 6.0V, Vbr 7.37V, Vclamp 10.3V,
    stock 74758), baked in tsx + twin_adjudications + part.yaml + ADR-0002.
  - **OV honesty:** ADR-0002 + ARCHITECTURE + E-INV why-string now state D5/F2 are
    SECONDARY/best-effort (crowbar), NOT a guaranteed fail-high cutoff; supervised-
    prototype / replaceable-Pi context; escalation boundary recorded VERBATIM in
    BRIEF.md A3/D3 + ADR-0002. No doc claims "Pi-protected against fail-high".
  - **Schematic gate:** tsci build OK; converter 110/110 components w/ FPID; ERC 0.
  - ARCHITECTURE.md un-stale'd: was still describing the removed TPS26631 eFuse.
- files: tsx (R12 fbtopMpn, D5 code), 02_parts/AR03BTCX4121/ (renamed from
  RT0603BRD074K12L), 02_parts/SMBJ6.0A, power_tree.yaml, twin_adjudications.yaml,
  electrical_invariants.yaml, BRIEF.md, ARCHITECTURE.md, decisions/0002.
- next: STOP + report gate (i) to orchestrator (verify + schematic/BOM checkpoint
  commit). Remaining v1.3 work: SW1 off-CPL (fix #5), ORDER_README bench-qual gate
  (#3), FULL board+artifact regen distinct from v1.2 (#6, gate ii), fresh red-team
  (#7, gate iii, under the new semantic-M-BOM + freshness gates).

## 2026-07-23 17:25 — gate (ii): board regen + SW1/F1 off-CPL (iterate)

- did: grid-mode converter regen (ERC 0 err, 110/110 FPID) -> netlist export ->
  E-INV 24/24 -> rebuild_fast #1 (promoted chain) -> DRC 0/0/0 incl parity.
  SW1 marked exclude_from_pos_files via new floorplan `patterns:` block (BRIEF A3/D3
  pitch-unconfirmed); found F1 holder ALSO still in the CPL despite "not in the CPL
  placement set" twin adjudication + hand-solder docs since v1.1 -> F1 marked
  off-CPL too (the "F1-holder CPL status correct" item). rebuild_fast #2 running.
- result: rebuild #1 DRC 0/0/0; SW1 attrs verified (EXCLUDE_FROM_POS_FILES=True,
  in BOM, pos unchanged 66,58). ORDER_README v1.3 FINAL written to
  06_build/verification/ (bench-qual Q1-Q5 pre-Pi gate, 3-part stock recheck table
  incl R12, SW1 hand-solder + header+shunt fallback, honest OV posture).
- next: re-export fab, PDFs, policy_audit, bom_source_check, twin (D5 C113976 new
  CAD fetch), stage v1.3 release dir, release_freshness_check.

## 2026-07-23 18:50 — gate (ii): board + artifact regen (finish)

- did: rebuild_fast x2 (promoted chain; #2 after F1 off-CPL fix) -> full fab/PDF/
  STEP/audit/twin regen -> staged 07_releases/v1.3-2026-07-23 (37 files) ->
  self-ran release_freshness_check.
- result (MEASURED):
  - DRC 0/0/0 (severity-all + refill-zones + schematic-parity), ERC 0, E-INV 24/24.
  - CPL 108 placements: SW1 AND F1 excluded (FP_EXCLUDE_FROM_POS_FILES via new
    floorplan patterns block; F1 was erroneously machine-placed in v1.1/v1.2
    despite hand-solder docs). Both verified on the rebuilt board. BOM 44 lines,
    0 uncoded; delta vs v1.2 = EXACTLY {R12: C2933210->C2984354, D5: C140903->
    C113976} (diff-verified).
  - M-BOM bom_source_check PASS (staged BOM vs circuit.json).
  - jlc_twin 87 OK / 233 checked, exit 0, ZERO unadjudicated criticals. D5
    C113976 fit 0.21mm + MODEL-REG-OK. R12 C2984354 = EasyEDA 404 (genuinely
    absent, probed directly) -> evidence-backed FETCH-FAILED adjudication
    (standard 0603 land; same-spec alt C861436 has R0603 CAD).
  - policy_audit FAIL=0 (PASS=28, WAIVED=2, HUMAN=6, N-A=2) -- REGENERATED with
    the staged release so M-BOM grades v1.3's own BOM; shipped audit == MANIFEST
    claim (the v1.2 audit/manifest-disagreement bug cannot recur).
  - release_freshness_check: PASS exit 0 (every fab/pdf artifact sha256-distinct
    from v1.1+v1.2; ORDER_README FINAL no draft markers; audit==manifest).
  - MANIFEST git_sha = ecd82c0 (gate-i commit) -- ORCHESTRATOR MUST RE-STAMP at
    the gate-(ii)/seal commit + policy_audit --skip-drc re-clear (v1.2 precedent).
- next: orchestrator verify/commit -> gate (iii) fresh red-team -> 2-commit seal.

## 2026-07-23 20:15 — gate (ii-b): R30 wrong-part fix (start)

- did: resumed as board lead post-quota (all prior state committed: gate-ii 52273a4,
  re-stamp 747f9d2). Re-ran bom_source_check on the staged v1.3 BOM — reproduced the
  ONE blocker: VALUE-MISMATCH row '100kΩ [R30]' (C2933195): ledger C2933195 =
  FRC0603F3091TS = 3.09kΩ but the label says 100kΩ.
- part chosen (VETTED LEDGER FIRST): **C25803** = UNI-ROYAL 0603WAF1003T5E = 100kΩ
  ±1% 0603, ledger-verified 2026-07-23 (JLC catalog selectSmtComponentList describe).
  Offline MPN decode (the R12/ecd82c0 method): `1003` E96 4-digit = 100 x 10^3 =
  100kΩ; `F` = ±1%; family cross-checks in the same ledger: 0603WAF1002T5E=10k
  (C25804), 0603WAF1004T5E=1M (C22935), 0603WAF3091T5E=3.09k (C22992 — the same
  encoding family as the WRONG part FRC0603F3091TS, decoding consistently).
  CONVERGENCE EVIDENCE: R1/R8/R17 — the board's OTHER three 100k 0603s — already
  auto-resolved to C25803 in the staged v1.3 BOM (row '100kΩ [R1,R17,R8]' C25803);
  only R30's candidate list came back wrong (C2933195). Fix = bake C25803 so R30
  joins the same catalog-verified row. No second wrong-part class found.
- next: tsx supplierPartNumbers bake (R12 pattern) + Q6 gate-drive margin re-check,
  then full gate-(ii) regen.

## 2026-07-23 20:35 — gate (ii-b): R30 baked + Q6 margins re-checked (iterate 1)

- did: tsx R30 <- supplierPartNumbers jlcpcb C25803 (the R12 pattern) + DO-NOT-USE
  comment. Q6 gate-drive margin re-derived from the AON6403 datasheet STATIC table
  (02_parts/AON6403/AON6403_rev.pdf): Vgs(th) -1.2/-1.7/-2.2 V min/typ/max
  (Id=-250uA); IDSS -1uA max 25C; IGSS +-100nA.
- result (MEASURED, R30=100k real part):
  - OFF/back-feed: |Vgs| = (Q7 BSS138 Idss 0.5uA + Q6 IGSS 0.1uA) x 100k ~= 60mV,
    20x below |Vgs(th)|min 1.2V -> Q6 channel OFF; body diode (A=5VC K=PMID)
    reverse-biased under back-feed -> blocks (RT-T4 intent holds with real part).
  - ON: QG ~= 0 (Q7 on, 54uA x ~3.5R = 0.2mV); Vgs = -5.35V, past the -4.5V/4.3mOhm
    Rds spec point and > |Vgs(th)max| 2.2V -> fully enhanced (0.11W @5A per part.yaml).
  - R30 ON-waste: 5.352V/100k = 53.5uA (0.29mW) vs 1.6-1.7mA at the wrong 3.09k
    (v1.2 addendum figure) -> waste note RESOLVED (32x). Master-off no-back-feed:
    PMID dead, R30 ~0 -> E-OFF 270uA quiescent unaffected.
- next: gen_tscircuit bridge regen -> E-INV -> rebuild_fast -> artifacts.

## 2026-07-23 18:30 — gate (ii-b): full regen + restage (iterate 2)

- did: resumed post-handoff (Q6 margins already journaled 20:35 entry — verified,
  not redone). Prior tsci build outputs were killed-mid-run-suspect -> re-ran the
  FULL bridge chain from scratch: gen_tscircuit.sh -> grid-mode converter regen
  (110/110 FPID, ERC 0 errors) -> netlist export (the 15:54 netlist WAS stale,
  pre-R30) -> E-INV 24/24 -> rebuild_fast (promoted chain).
- NUANCE (measured): rebuild_fast run #1 after a converter regen gave DRC 3/0/0
  (HO_A via vs SW_A track clearance + hole-to-hole, buck-A); run #2 gave 0/0/0 —
  the taps/stitch pass is stochastic on the first run. Prior agent also ran x2
  (17:25 entry). Final board: DRC 0/0/0 (severity-all + refill-zones + parity).
- artifact regen (MEASURED): fab re-export -> BOM 43 grouped lines (R30 merges
  into the R1/R17/R8 C25803 row, 44->43), 0 uncoded; CPL 108, SW1+F1 absent
  (grep-verified). BOM delta vs sealed v1.2 = EXACTLY {R12 C2933210->C2984354,
  D5 C140903->C113976, R30 C2933195->C25803} (diff-verified, sorted). PDFs x4,
  STEP, twin re-run: 88 OK / 235 checked exit 0, R30 C25803 fit=0.08mm +
  MODEL-REG-OK; only R12 C2984354 FETCH-FAILED (known EasyEDA-404, adjudicated).
- restaged 07_releases/v1.3-2026-07-23 (fab/pdf/source/3d/verification):
  bom_source_check PASS (incl. semantic ledger leg — the R30 VALUE-MISMATCH is
  GONE); policy_audit FAIL=1 = M-REL only (stale MANIFEST sha table — expected
  pre-stamp); freshness: only finding = audit/manifest disagreement (same cause).
  MANIFEST + ORDER_README prose updated for R30 (NEVER C2933195; C25803 Basic).
- next: scoped verification (targeted delta confirm + ONE zero-context lens) ->
  source commit S -> stamp -> seal.

## 2026-07-23 19:05 — gate (iii): scoped verification COMPLETE (iterate 3)

- did: targeted delta confirmation (08_reviews/2026-07-23_v1.3_fix_confirmation.md,
  PASS — R12/D5/R30 each traced tsx->circuit.json->sch->netlist->BOM->twin with
  measured numbers) + ONE integrated zero-context fresh lens (claude -p headless,
  clean-room over curated inputs per the verification-scoping rule: staging
  archive + BRIEF/ARCHITECTURE/decisions + 02_parts + 03_src config; journals/
  STATUS/08_reviews EXCLUDED). Archived VERBATIM:
  08_reviews/2026-07-23_v1.3_redteam_fresh-lens.md; both copied to release
  verification/.
- result (MEASURED, lens independent re-derivation): VERDICT **ORDER** — NO P0 /
  NO P1, 6 P2s. Lens re-derived: D5 K=VBUSC/A=GND at COPPER level; Q6 OFF
  Vgs -60mV (20x under min th), ON -5.27..-5.43V; setpoint 5.352V nom / 5.272V
  worst-low, headroom 642mV vs 440mV drop (528mV derated) — and counter-checked
  the v1.2 3.74k bug WOULD FAIL (266mV < 440mV). Forbidden codes absent
  everywhere. P2 dispositions: #1 = pre-stamp MANIFEST staleness (the stamp step
  fixes, by design); #2/#3/#4/#5 already in ORDER_README/MANIFEST open items;
  #6 doc nits FIXED pre-seal (ORDER_README fab/bom.csv naming; D1 adjudication
  SMA->SMB label; twin re-run exit 0 after the label fix, 88 OK / 235).
- next: 2-commit seal (source S -> stamp -> seal), check-ignore sweep LAST.
