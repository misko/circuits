# STATUS beacon — usb-hub-3s-v3 (live head of the journal)

<!-- reader parses from here down -->
stage:   v1.6-COPPER-DONE-BLOCKED-ON-A-ROT
step:    "v1.6 (COPPER revision): ALL source, footprint, netclass, copper and docs work is LANDED and the board rebuilds DETERMINISTICALLY to DRC 0/0/0. Landed this pass — the 5-LED status cell (D8 amber PACK, FET-gated by Q8 off ENKILL; D9/D10/D11 green PER-PORT on VBUSA1/2/3; D12 green on VBUSC post-protection) at +11 placements and exactly +2 BOM lines; the H3 outer-copper short fixed by a 5VA pour notch; H4 VBUSA3 chamfer; In2 VIN pushed off all four drills; VBUS ampacity floor 0.5->0.8mm with a measured scoped relaxation; PowerPAK EP paste 100% -> a 65% 2x2 window-pane in a project-vendored footprint; USB-C delivery-corner via density; THREE FIDUCIALS (the board had NONE through v1.5); and the D5/U12 clamp ordering DECIDED in ADR-0003 rather than deferred a fourth time. Shared-skill fixes committed standalone: 375c0de (board.fiducials) and 0d354b1 (P-SILK-REF exempts by attribute, not refdes prefix). THE ONLY REMAINING BLOCKER IS EXTERNAL — A-ROT refuses the fab export until per-LCSC rows for C2296/C2297 land in skills/jlcpcb-fab/scripts/jlc_lcsc_rotations.csv, which this board deliberately does NOT own (canon M1: the measurer must not install the authority)."
measure: "LED ROTATION — THE DELIVERABLE: C2296 = 0 and C2297 = 0, polarity two-channel. The pad-NUMBER fit says 180 at a 17.7x margin (rms 0.1125 vs 1.9875mm) and is WRONG. Channel 1, unlabelled size-matched PAD CLOUD: 0.1125mm at BOTH 0 and 180, 1.4076mm at 90 and 270 — ADMITS {0,180}, REFUTES {90,270} at 12.5x. Channel 2, POLARITY MARKS matched cathode-to-cathode, reading no pad number and no pin-1 marker on either side: OURS draws the cathode WEST (F.SilkS full-height band at x=-1.685; F.Fab chamfer removed-corner at (-1.000,-0.600); KiCad Device:LED pin 1 = K, read from /usr/share/kicad/symbols/Device.kicad_sym). JLC draws the cathode WEST too (silk diode-glyph base at x=+0.200 with its APEX at x=-0.300 — a diode triangle points at its cathode — and the silk body outline chamfered at the WEST end, mean x=-2.025, square at the EAST). 0 is the ONLY angle where both cathode marks land on the same side. TOOL DEFECT FOUND WHILE MEASURING: jlc_rotation_measure.py --row PROPOSES 90 for both codes — refuted by its own PAD-CLOUD channel at 12.5x and physically impossible on a 2-pad chip land — because its PIN-1-MARK channel matches our F.Fab pin-1 chamfer against JLC's fp_circle pin-1 dot, and BOTH of those marks are defined BY PAD NUMBER, so the 'numbering-free' channel inherits the exact disagreement it exists to detect (0.5449 vs 1.2300mm = 2.26x, inside its 1.5mm absolute bar, so it wins and overrides the pad fit). || COPPER, before -> after, measured on FILLED zones both times: H3 (106,24) F.Cu carried GND 1.850 AND 5VA 1.850mm and B.Cu 5VA 1.850 AND GND 1.851 -> 5VA now stops at 4.500mm on both faces, GND alone inside r<=4.00 (the smallest M3 cap head, r 2.75, previously bridged both nets). H4 VBUSA3 4.152 -> 5.008mm. In2.Cu VIN 1.850 -> 2.077mm on ALL FOUR holes. VBUSA1/2/3 port feed 13.554mm of 0.500mm -> 0.800mm each; the 0.650mm intra-chip OUT1<->OUT2 bond keeps 0.500 under a scoped_floors rule area (TPS2557 pitch IS 0.650mm — an 0.8mm track leaves 0.075mm against a 0.150mm clearance, i.e. wider than the pitch). PowerPAK EP paste 1 aperture x 14.8971mm2 at 100% -> 4 x 1.5359 x 1.5762 = 9.683mm2 = 65.0%, webs 0.369/0.379mm (65.0% is KiCad's OWN ratio for this family: HTSSOP-20-1EP_...Mask2.75x3.43mm is 4 x 1.11 x 1.38 over 2.75 x 3.43). USB-C corner: F2 pads 0 -> 4 vias each, PMID 2 -> 13, VBUSC 5 -> 15, J5 VBUS pairs 0 -> 3 each. || GATES: KRT race 6/6 chains CLEAN (0 unconnected / 0 violations); taps 28/28 including all three 0.8mm VBUSA feeds; DRC 0 violations / 0 unconnected / 0 parity at --severity-all --refill-zones --schematic-parity; audit_board PASS (21 polarity, 19 proximity, 4 edge, 128 silk); E-INV 36 invariants + E-ADR OK; contracts_audit 187 files 0 violations; P-COLLIDE 0 pad shorts, 0 courtyard overlaps; 121 parts + 4 holes + 3 fiducials = 128 footprints, placement asserts 48/48."
state:   blocked
next:    "1. LAND THE A-ROT ROWS — owner is the rotation-table maintainer, NOT this board: C2296,0,\"...\",two-channel and C2297,0,\"...\",two-channel, with the two numbering-free channels above as evidence, and BOTH codes onto the JLC ORDER-PREVIEW human gate. Until they exist export_jlc_package exits 2 and deletes stale artifacts, by design. || 2. THEN, in order: fab export and CONFIRM every row is on JLC's pad-centre datum (the A-POS defect that made v1.5 DO-NOT-ORDER put 11 of 108 rows off it — J1 4.6861mm, J2/J3/J4 3.7346mm, J5 1.4975mm, Q4/Q5/Q6 0.0625mm — because the exporter emitted the footprint ANCHOR) -> re-run policy_audit (R-DRC/M-BOM/A-POP/A-BODY all currently read a stale or pre-export state) -> jlc_twin REGENERATED, never reused, bodies mounted N/N -> re-do the pin-1 visual check for the twelve refs at jlc_offset 270 (J1, U2-U12) whose prior verification is void, plus the newly-bodied -> E-MARGIN + E-OFF at quiescent_ua 271 -> tests/run_tests.sh -> fresh zero-context lens over the STAGED archive -> 2-commit seal + SUPERSEDED.md on v1.5 naming the A-POS datum defect as the reason. || 3. OPEN AND REPORTED, not hidden: user decision D5 ('reselect D5 to clamp below U12') CANNOT BE IMPLEMENTED. ST's USBLC6-2SC6 (doc ID 11265 rev 5, Table 2) specifies VBUS-GND breakdown as MIN 6.0V @1mA with NO typ and NO max; D5's breakdown MINIMUM is 6.67V. A replacement needs Vwm >= 5.479V AND Vbr(max) < 6.00V at once and that window is EMPTY — the SMBJ family has no step between Vwm 5.0 and 6.0V, and the tightest SMB part found at any qualifying standoff (SM6T6V8A, Vwm 5.80V, Vbr 6.45-7.14V) is still 450mV above U12's floor and returns 0 hits on jlcpcb.com. ADR-0003 records the accepted residual, corrects the part.yaml numbers (VBR was 6.67-8.15V @1mA; it is 6.67-7.37V @10mA, Ppk 10/1000us not 8/20us) and names ACTIVE OVP as the escalation. THE USER SHOULD CHOOSE: accept the residual, escalate to active OVP, or bench-qualify what U12 actually does under sustained conduction. Lowering 5VC is refused — it spends the 15mV E-MARGIN slack."
op_pid:
updated: 2026-07-25T22:40:00-07:00
