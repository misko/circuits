PAYLOAD IDENTITY - v1.11 vs v1.10 (canon: a fix-claim needs the measurement)
==========================================================================

CLAIM: v1.11 changes TWO CELLS of fab/bom.csv (R28/R29's MPN and LCSC) plus the
one line of source/usb_hub_3s_v2.tsx that produces them. NOTHING ELSE. The
copper is untouched.

This board's v1.6-v1.8 shipped 44287.91 mm2 of MISSING COPPER with every gate
green, so on THIS board "the copper is untouched" is the claim that most needs a
measurement rather than an assertion. And a part SUBSTITUTION is the one BOM
edit that CAN reach the copper - a different land pattern would - so the
footprint identity is measured too, not argued from the datasheet.

METHOD (falsifiable, not a copy-check). Four independent measurements:

  (1) BOARD IDENTITY (md5). One file, three places.
  (2) THE BOARD STILL GENERATES THESE BYTES. Gerbers + drills RE-PLOTTED from
      this release's own source board with export_jlc_package.py --layers 4 into
      a scratch dir, then compared member-by-member to v1.10's SEALED zip and
      its loose drills, after stripping ONLY the plot's own timestamps
      (%TF.CreationDate, "G04 Created by", the .drl "DRILL file KiCad ... date"
      line and its "; #@! TF.CreationDate" twin). A COPY would prove nothing; a
      RE-PLOT proves the BOARD - including its 36 zones / 106 filled_polygon of
      pour, which is inside those re-plotted bytes.
  (3) sha256 over EVERY payload file (fab/, pdf/, source/, 3d/) in either
      release. verification/ is deliberately EXCLUDED: it is this release's own
      fresh evidence and is expected to differ.
  (4) BOM CELL-LEVEL DIFF, parsed as CSV rather than as text: row count,
      designator list, and every cell of every row.

BOARD IDENTITY (md5):
  04_kicad/usb_hub_3s_v2.kicad_pcb        83af8e5a5596a51cf139dd06e8903d47
  v1.10/source/usb_hub_3s_v2.kicad_pcb    83af8e5a5596a51cf139dd06e8903d47
  v1.11/source/usb_hub_3s_v2.kicad_pcb    83af8e5a5596a51cf139dd06e8903d47
  -> the sealed board, the working board and this release's board are ONE FILE.
  Re-measured AFTER `kicad-cli pcb drc --refill-zones` ran on it: unchanged.
  (--refill-zones refills IN MEMORY; it must not, and did not, write.)

RESULT (2): 15/15 re-plotted gerber+drill artifacts BYTE-IDENTICAL after the
timestamp strip (13 zip members + 2 loose drills), 0 differ.
  NOTE ON METHOD: the first pass reported 11/15 because the strip list inherited
  from v1.10 did not cover the Excellon header's own date line. The four
  "failures" were the two drill files counted twice (zip member + loose copy)
  and differed ONLY in
      ; DRILL file KiCad 10.0.4-... date 2026-07-27T11:39:17
                                    vs date 2026-07-27T18:16:18
      ; #@! TF.CreationDate,2026-07-27T11:39:17-07:00
                           vs 2026-07-27T18:16:18-07:00
  Recorded here rather than silently fixed: the strip list is part of the
  method, and a method that grows a new pattern mid-run has to say so.

RESULT (3):
  CHANGED    fab/bom.csv                     3636288a8fcc5d7f -> 1dfc50426d09cb6a
  CHANGED    source/usb_hub_3s_v2.tsx        056bbf0b70677641 -> 01cbff1ae8406328
  payload files identical : 20
  payload files changed   : 2

  The .tsx is in the CHANGED column ON PURPOSE and it is the point of canon M3:
  the BOM row moved because the SOURCE moved. A release whose bom.csv changed
  and whose .tsx did not would be a hand-edited BOM, which is the defect
  crow-mic-pod-v2 paid for on 2026-07-27.

  fab/cpl.csv is BYTE-IDENTICAL (cmp, 0 differences, 119 rows): every A-ROT
  rotation and A-POS coordinate is carried forward untouched. R28/R29 keep the
  same package, so no placement datum moved and the five A-POL single-channel
  codes still carry the same order-preview obligation.

RESULT (4) - THE BOM DELTA, CELL BY CELL:
  rows                46 -> 46
  designator list     IDENTICAL, in order
  cells changed       2, both on the row "R28,R29":
                        MPN   0402WGF1002TCE  ->  RC0402FR-0710KL
                        LCSC  C25744          ->  C60490
  cells changed on any OTHER row                              0
  Footprint changed anywhere                                  0
  Comment changed anywhere                                    0
  rows added / removed                                        0 / 0

  The Footprint column reads R_0402_1005Metric on that row before and after, and
  the Comment reads 10kOhm before and after, which is what makes this a
  substitution rather than a redesign.

WHY THE TWO PARTS ARE INTERCHANGEABLE, MEASURED RATHER THAN ASSERTED. Both
catalog records were read live from JLC's parts API on 2026-07-27
(selectSmtComponentList, exact componentCode match) and their `describe` strings
were compared AS STRINGS:

  C25744  -55C~+155C 10kOhm 50V 62.5mW Thick Film Resistor +-1% +-100ppm/C
          0402 Chip Resistor - Surface Mount ROHS
  C60490  -55C~+155C 10kOhm 50V 62.5mW Thick Film Resistor +-1% +-100ppm/C
          0402 Chip Resistor - Surface Mount ROHS

  CHARACTER-IDENTICAL. componentSpecificationEn is "0402" on both.
  leastPatchNumber is 20 on both, so the minimum-order step did not move.
  What DID change: stockCount 0 -> 8,220,334 and componentLibraryType
  base -> expand (the one-time Extended feeder fee, accepted).
