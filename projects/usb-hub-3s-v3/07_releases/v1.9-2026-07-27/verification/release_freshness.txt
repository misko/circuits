== release-freshness: v1.9-2026-07-27 ==
  note: fab/bom.csv identical to v1.6-2026-07-26/fab/bom.csv — WAIVED (COPPER-ONLY supersede of v1.8-2026-07-26 — identical BY DESIGN. 46 rows / 122 refdes, 0 blank LCSC, and {BOM}-{CPL} = {F1, R42, SW1} exactly as 03_src/rules/assembly.yaml declares. No part was added, removed, substituted or revalued; only the pour changed. A DIFFERENT BOM here would mean something moved that this release does not claim to have moved.)
  note: fab/bom.csv identical to v1.7-2026-07-26/fab/bom.csv — WAIVED (COPPER-ONLY supersede of v1.8-2026-07-26 — identical BY DESIGN. 46 rows / 122 refdes, 0 blank LCSC, and {BOM}-{CPL} = {F1, R42, SW1} exactly as 03_src/rules/assembly.yaml declares. No part was added, removed, substituted or revalued; only the pour changed. A DIFFERENT BOM here would mean something moved that this release does not claim to have moved.)
  note: fab/bom.csv identical to v1.8-2026-07-26/fab/bom.csv — WAIVED (COPPER-ONLY supersede of v1.8-2026-07-26 — identical BY DESIGN. 46 rows / 122 refdes, 0 blank LCSC, and {BOM}-{CPL} = {F1, R42, SW1} exactly as 03_src/rules/assembly.yaml declares. No part was added, removed, substituted or revalued; only the pour changed. A DIFFERENT BOM here would mean something moved that this release does not claim to have moved.)
  note: fab/cpl.csv identical to v1.6-2026-07-26/fab/cpl.csv — WAIVED (COPPER-ONLY supersede of v1.8-2026-07-26 — identical BY DESIGN. 119 placements, all top side, every rotation from a MEASURED per-LCSC row (A-ROT 119/119) and every position on JLC's pad-array datum (A-POS worst residual 0.00050 mm). Placement did not change because the fix was a zone refill, and netlist parity against v1.8 is 0 differences.)
  note: fab/cpl.csv identical to v1.7-2026-07-26/fab/cpl.csv — WAIVED (COPPER-ONLY supersede of v1.8-2026-07-26 — identical BY DESIGN. 119 placements, all top side, every rotation from a MEASURED per-LCSC row (A-ROT 119/119) and every position on JLC's pad-array datum (A-POS worst residual 0.00050 mm). Placement did not change because the fix was a zone refill, and netlist parity against v1.8 is 0 differences.)
  note: fab/cpl.csv identical to v1.8-2026-07-26/fab/cpl.csv — WAIVED (COPPER-ONLY supersede of v1.8-2026-07-26 — identical BY DESIGN. 119 placements, all top side, every rotation from a MEASURED per-LCSC row (A-ROT 119/119) and every position on JLC's pad-array datum (A-POS worst residual 0.00050 mm). Placement did not change because the fix was a zone refill, and netlist parity against v1.8 is 0 differences.)
  note: A-STOCK: grading verification/stock_check.json (46 graded line(s), verdict=PASS) against 43 coded+placed BOM line(s) x 5 boards
FRESHNESS: PASS
