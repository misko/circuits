# STATUS beacon — pluto-cal-switch (live head of the journal)

<!-- reader parses from here down -->
stage:   00-COMMISSION
step:    "Scaffold + BRIEF landed (verbatim prompt sha256 0529613...). Fact-lock complete except TX drive level. Two commission findings recorded rather than guessed: (1) the brief and a follow-up answer DISAGREED on control polarity — asked, user confirmed ON = loopback (T2); (2) the length-match 'relaxation' is really a documentation requirement — build symmetric, publish the measured per-run electrical length and delta, because a software offset is only as good as the number it is given (D4). D-SPEC sourcing spike RUNNING: 6 functions fanned out, each with an adversarial refutation pass."
measure: "proven-parts ledger consulted first per D-ESC: 31 entries, ZERO RF parts — no switch/splitter/attenuator/SMA/USB-MCU. New part class for this repo; nothing copyable. 15 contracts.md placed from the skill's own templates."
state:   working
op_pid:  workflow wzrhwlc7o (sourcing spike, 6 functions x 2 stages)
next:    "Join the spike -> classify each function sourceable/costlier_tier/not_sourceable -> ARCHITECTURE.md + DETAIL_DESIGN.md (derive the attenuator pad from the MEASURED splitter and switch losses, not assumed) -> ADRs (splitter topology, control-polarity + power-on default, USB-vs-GPIO control, length-match publication) -> 02_parts."
updated: 2026-07-27T00:00:00
