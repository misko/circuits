# STATUS beacon — pluto-cal-switch (live head of the journal)

<!-- reader parses from here down -->
stage:   06-ROUTING-CHAIN-RUNS-END-TO-END-NOT-YET-CLEAN
step:    "THE BLOCKER IS GONE AND THE PIPELINE NOW REACHES seed_stubs BY ITSELF: prep -> route(race 4) -> import(--no-fill) -> taps -> stitch runs to EXIT 0 and `seed_stubs: 21 pin(s) served, 32 segments placed, 0 refused` with NO hand unfill anywhere. That is the thing that was impossible yesterday, and it is what makes the recipe expressible in route.yaml (canon M3). THE CHAIN IS STILL NOT PROMOTED, DELIBERATELY: the gate is 16 violations / 8 unconnected / 0 parity, and promoting a dirty chain would seal it. That is the same refusal the predecessor made, but the residue is now CLASSIFIED rather than inherited, and it is 5 named classes instead of a story."
measure: "ALL MEASURED 2026-07-30, UNPIPED, REAL EXIT CODES. FULL DRC (kicad-cli pcb drc --severity-all --refill-zones --schematic-parity) = 16 / 8 / 0, DOWN FROM 83 / 8 / 0 on the first full run this board has ever had. THE 67 THAT WENT AWAY WERE ONE DEFECT, NOT 67: every solder_mask_bridge finding (12 distinct pairs) was inside U_SW1/U_SW2 and was the SAME single mask aperture the BGS12WN6 footprint deliberately carries — the datasheet land (Rev 2.9 Fig 5) has a 0.15 mm land-to-land gap, below JLCPCB's 0.2 mm mask-dam minimum, so per-pad openings are UNMANUFACTURABLE. Declared to KiCad as `allow_soldermask_bridges` in the footprint that OWNS the fact, not waived downstream. VIOLATIONS REMAINING, CLASSIFIED: track_width 8 (4x 3V3_SW taps at 0.25 vs PWR 0.4; 3x USB_DP_MCU/USB_DM_MCU B.Cu hop at 0.25/0.20 vs USB_DP 0.33; 1x inside scoped_taper_u_mcu at 0.20 vs 0.25) — every one is a DECLARED tap width with no rule area covering it, the exact `wave_track_width does not read scoped_floors` gap this route.yaml already documents; starved_thermal 3 (J_USB.6, U_HDR_ESD.4, C_CTRL1.2 — 1 spoke against a 2-spoke minimum); via_dangling 3 (2 GND + 1 HDR_STATE_GPIO — prune_stitch_dangling was ADDED and ran, and did NOT clear them, so they are ROUTER vias, not stitcher vias, and the pass is correctly scoped); clearance 1 (VREG_VOUT vs 3V3 on In2.Cu at 0.0727 mm against 0.15); track_dangling 1 (HDR_STATE_GPIO). UNCONNECTED 8, AND THEY ARE FOUR DIFFERENT DEFECTS, NOT ONE STORY (this is the pluto-rx2-8way lesson landing on this board): 4x GND pad -> GND zone / pad (U_SW1.2, U_SW2.2, C_DV1.2, C_IO3.2 — these are pad_rescue's 4 unserved of 92); 1x RF_CTRL_SW2 U_SW2.6 to a 0.0200 mm stub; 2x VREG_VOUT (U_MCU.50 to a 0.1414 mm In2 stub, U_MCU.45 to U_MCU.50) — the ONLY two that are the inherited D-BACK; 1x HDR_STATE_GPIO track-to-track, which pairs with the track_dangling. HOLE_TO_HOLE 0.5 -> 0.25 AND THAT UNBLOCKED THE STITCH GATE: 5 escape vias walled in at 0.5 (gaps 0.282/0.350/0.373/0.404/0.450) and ALL are legal at the declared tier's published 0.25. It is arithmetic, not density: two adjacent escape vias on the RP2040's 0.400 mm pitch at the tier's 0.15 mm drill are pitch-drill = 0.250 mm apart EXACTLY, so at 0.5 every adjacent-pin QFN-56 escape via was a violation BY CONSTRUCTION — while ADR-0010 raised the tier to advanced with that very escape as justification #1. floorplan.yaml's own note pre-authorised this ('if a future escape genuinely walls in at 0.5 ... that is the moment to spend it'); ORDER-TIME OBLIGATION recorded. RACE: 4 concurrent chains, ALL FOUR identical at unconnected=3 violations=10, so the residue is STRUCTURAL, not stochastic — do not re-race it hoping. WIDTH 0.36 mm holds: rf_copper VERIFIES 14 paths / 32 segments / 18 nets / 0 vias and D4 SPREAD = 0.000000 mm = 0.0000 ps BY CONSTRUCTION. Schematic side green: E-INV 42/42, E-ADR 10/10, S-NETMERGE 48/48, S-COUNT 4/4, parity 0/49 nets over 263 nodes. audit_board still EXIT 1 on I9 (23 refdes silk labels), PRE-EXISTING and not waived."
state:   blocked-on-drc-residue
op_pid:  none
next:    "SIX NAMED PIECES, IN COST ORDER, AND NONE OF THEM IS A RE-RACE (all four candidates were identical). (1) track_width x8: give the declared tap widths a rule area — 3V3_SW at the two BGS12WN6 lands (the pin draws 125 uA, four orders under a 0.4 mm PWR floor) and the USB_DP_MCU/USB_DM_MCU B.Cu hop, which sits OUTSIDE taper_u_mcu; this is authoring in nets.yaml scoped_floors, not a router problem. (2) via_dangling x3 + track_dangling x1 + the HDR_STATE_GPIO unconnected are ONE cluster on one net — inspect that net's chain before treating them separately. (3) pad_rescue 4 unserved of 92 (U_SW1.2, U_SW2.2, C_DV1.2, C_IO3.2): U_SW1.2/U_SW2.2 are the BGS12WN6 SOLE ground pin whose outward via ARCHITECTURE sec.10.7 already specifies, so they want an explicit seed stub with a via, not a rescue ring. (4) starved_thermal x3: raise spoke count or solid-connect those three pads. (5) clearance x1 on In2.Cu. (6) U_MCU.45/.50 VREG_VOUT — the LAST thing to touch, and the untried remedy is a tap through the UPPER corridor y[30.880,31.740] with a B.Cu hop, NOT a placement change: the named x-slot IS the documented load-bearing USB pair escape corridor (floorplan.yaml records why, and the working USB_DP tap waypoint sits inside it). ONLY THEN promote the chain to 03_src/route/, set route.final:, and commit."
updated: 2026-07-30T00:00:00
