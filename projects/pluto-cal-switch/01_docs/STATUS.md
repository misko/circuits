# STATUS beacon — pluto-cal-switch (live head of the journal)

<!-- reader parses from here down -->
stage:   06-ROUTING-STOPPED-ON-SCOPE
step:    "STAGE 6 ROUTED BUT DELIBERATELY NOT PROMOTED AND NOT SEALED, on a SCOPE question that outranks it: THE 50 OHM WIDTH IS NOT 50 OHM. Verified independently from the stackup (Hammerstad + 1 oz thickness correction, h 0.2104, t 0.035): w=0.35 mm is 51.37 ohm at Dk 4.3 and 50.84 ohm at Dk 4.4, and 50.00 ohm needs 0.3680 / 0.3610 mm. The result does NOT depend on the Dk dispute, and my 50.84 reproduces this board's own nets.yaml table ('0.35 -> 51.0 ohm') to 0.16 ohm — so the RULES FILE IS RIGHT and ADR-0010's headline '0.35 mm = 50 ohm' is the number that is wrong, at either permittivity. Changing an RF class width is a change to ADR-0010's published impedance constant and ADR-0011's published conversion constant, i.e. a SCOPE change, so it is REPORTED not decided. ALSO REPORTED, not fixed (stage-4 source, outside this stage): the GPIO header divider is INVERTED — R_HDR_S 2.2k over R_HDR_G 3.3k gives 0.600*Vin, not the published 0.400, so the 5.0 V case is 3.000 V against a 3.3 V ADC full scale (0.30 V of headroom, ~0.23 V at a -2% ADC_AVDD) instead of the claimed 1.30 V; the published table is exactly the two values SWAPPED. Nothing gates the ratio."
measure: "ALL MEASURED 2026-07-29, UNPIPED, REAL EXIT CODES. WHAT IS GREEN: generate_board EXIT 0 (73/73 anchored, 24 asserts, 0 legalized, P-COLLIDE 0/0 over 306 pads, silk 65/79 owned / 14 degraded / 0 captions — IDENTICAL to stage 5, so three new rule areas moved nothing). placement_gates EXIT 0 PASS 0 fails 0 warns. generate_rules EXIT 0: 6 netclasses + 41 patterns + 9 width rules (was 6). count_parity EXIT 0 4/4 over 73 refdes. tier_preflight EXIT 0, 0 FAIL / 1 WARN (was 2 WARN — PF-HTC closed). audit_board EXIT 1 on I9 23/73 only, unchanged and not waived; A-SYM 11/11 at 0.0 um, A-PLANE 1 GND pour 0 keepouts on In1.Cu, A-PROX 46/46 0 over. KRT 7/7 waves route, 495 segments + 45 vias. THE D4 ARTIFACT, PLANNED AND VERIFIED BY 03_src/rf_copper.py (landing / clearance / width / 1 um grid / congruence, refuse-whole): ARM1 = LOOP_ARM1 + PAD_A2A_1 + LOOP_ARM1_SW = 16.080266 mm, ARM2 = 16.080266 mm, SPREAD 0.000000 mm = 0.0000 ps = 0.0000 deg at 6 GHz, by ARITHMETIC not by search — and because the spread is exactly zero, ADR-0011's 2.0%-low conversion constant cannot touch the published DELTA at all (0.000000 mm x any constant = 0.000 ps); it moves only the per-arm ABSOLUTE, 96.48 ps at 6.0 vs 98.17 at 6.105. ELEVEN PADS CANNOT ACCEPT THEIR OWN NETCLASS MINIMUM at the 0.15 mm clearance floor (48 approach directions x a 30 um landing grid): U_SW1.5/U_SW2.5 need RF50's 0.350 and take 0.250; U_MCU.46/.47 need USB_DP's 0.330 and take 0.300; U_MCU.10/22/26/33 (3V3) and .23/.45/.50 (VREG_VOUT) need PWR's 0.400 and take 0.300. hole_clearance 0.25 -> the DERIVED 0.20 took hole_clearance DRC findings from 57 to 0. WHAT IS RED AND WHY: (a) 2 OPENS, U_MCU.45 and U_MCU.50 to VREG_VOUT, after FOUR bounded tap attempts (F.Cu, B.Cu hop, nearest-copper point, waypoint split) — D-BACK declared, causal edge is placement, 8 escapes on the RP2040's north 0.400 mm side into a 3.2 mm band that is exactly 8 x (0.25+0.15) and both VREG_VOUT targets on the far side of the row, with no escape_corridors: declared; (b) stitch.seed_stubs is UNREACHABLE through the documented pipeline — p_seed_stubs dies on any filled zone and import_krt.py:93 fills unconditionally although it ALREADY HAS --no-fill and cmd_import never passes it, so the 21 planned RF runs were only placed after a hand unfill, which is a DIAGNOSTIC and not a shippable path; (c) unify_zone_priorities FAILED a LEGAL nested plane (In2.Cu 3V3 at priority 2 inside In2.Cu GND at priority 0) as 'a SHORT' and is removed with the measurement. NO FULL DRC IS CLAIMED: the run was stopped before the final generate_rules + DRC gate because the width question would invalidate it."
state:   stuck
op_pid:  none
next:    "OWNER DECISIONS NEEDED BEFORE STAGE 6 CAN CLOSE, in this order. (1) THE RF WIDTH: adopt 0.361 mm (Dk 4.4) or 0.368 mm (Dk 4.3) for RF50 and RF_LOOP_MATCH, which also settles which permittivity this board's documents use — ADR-0010's table says 4.4 and DETAIL_DESIGN sec.1 says 4.3, and the two give different answers. Cost of the current 0.35: |Gamma| 0.0086 = 41.3 dB return loss, VSWR 1.017, two orders below the SMA launch, so this is a documentation-consistency defect first. Re-authoring is cheap: nets.yaml min_width, re-run rf_copper.py --emit (both blocks are emitted, nothing is hand-typed), re-prep, re-race. All eleven landable-width measurements were taken against 0.35 and U_SW.1/U_SW.3 measure 0.460, so 0.36 still lands. (2) ADR-0011's CONVERSION CONSTANT: 6.0 ps/mm measures 6.125 at Dk 4.4 / w 0.35. The DELTA is immune (spread is 0.000000) but the per-arm published picoseconds are 2.0% low. (3) THE HEADER DIVIDER INVERSION — a stage-4 source fix (tsx + electrical_invariants + the ADR-0008 table) plus a node_level assert on the ratio, which is expressible because the divider is 2-resistor and the receiver has thresholds. (4) THE VREG_VOUT ESCAPE — a stage-5 placement change: the x-slot between C_USBV (109.1) and C_ADCV (112.8) at y=32.5 is EMPTY, so moving C_VREGO into it puts a VREG_VOUT land directly north of pin 45's own escape; re-grade P-ADJ (currently 4.30 of 5.0 mm). THREE SHARED-BACKEND PATCHES REPORTED, NOT APPLIED (siblings are live in skills/): cmd_import must pass import_krt's existing --no-fill or stitch.seed_stubs can never run; unify_zone_priorities must compare zone PRIORITIES before calling a cross-net overlap a short; and escape_check should grade min(landable track width) per pad against the netclass floor — pure pad geometry, no router, and it is the gate that would have caught all eleven pads before placement was signed off."
updated: 2026-07-29T00:00:00
