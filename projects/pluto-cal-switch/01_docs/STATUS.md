# STATUS beacon — pluto-cal-switch (live head of the journal)

<!-- reader parses from here down -->
stage:   03-RULES-DONE
step:    "Stages 1-3 COMPLETE, stopped deliberately BEFORE the schematic. ARCHITECTURE.md + DETAIL_DESIGN.md + 14 ADRs + 11 02_parts entries + nets/power_tree/electrical_invariants/assembly yaml. THREE PART CHANGES came out of re-reading the sourcing spike's own adversarial passes (5 of 6 returned WEAKENED, the SMA pass returned REFUTED) rather than its summary: switch BGS12P2L6 -> BGS12WN6 (same land, but it is the only one of the two with ANY published guarantee at EITHER end of 70 MHz-6 GHz); SMA BWSMA-KE23 -> KH-SMA-KE-Z (6.4x stock, 28% cheaper, 53% shorter, and the only one with a VSWR spec); attenuator 23 dB all-pre-split -> 10 dB pre + 12 dB per arm. TWO derived-engineering rules were found REVERSED and are now corrected: the SMA bottom-plane antipad is >=O3.5 mm not O2.6 (worth ~9 dB of RL at 6 GHz), and 'thinner dielectric makes the launch worse' is refuted once the barrel is modelled."
measure: "E-ADR OK (14 ADRs, every protection/topology ADR cited by >=1 invariant, 39 invariants). E-MARGIN N-A, E-OFF N-A (source_type usb). E-INV exit 2 = no netlist yet, expected pre-schematic. E-TOPO exit 2 = GATE GAP, not a design defect: power_topology.py normalize_type() accepts only buck/boost/buck_boost, so an LDO-only board cannot be expressed; declared truthfully rather than mislabelled. Chain loss re-derived from datasheet terms: 6.54 dB @70 MHz / 9.63 dB @6 GHz = 3.09 dB tilt (the spike said 1.64 dB - its microstrip term assumed a 1.6 mm substrate at 0.013 dB/mm where the 0.2104 mm prepreg the parts force costs 0.036). Pad 21.90 -> 21.74 dB, 30 dB met at ~3.0 GHz, span 30.0 -1.6/+1.4 dB, guaranteed envelope 27.2-32.9 dB. RX level -21.4 to -24.4 dBm, 22.7 dB below the AD936x abs max worst case."
state:   blocked
op_pid:  none
next:    "ESCALATED TO THE USER, three D# assumptions taken in their absence, each cheap to reverse: D5 30 dB referenced by minimax to ~3.0 GHz (one BOM line to change); D6 built to the 34.88 mm PlutoPlus midpoint (one number in floorplan.yaml); D7 control = header OR usb + a 10 s watchdog (firmware only). ALSO OWED BEFORE STAGE 4: three five-minute physical measurements on a real PlutoPlus (RF axis height above its PCB, SMA gender - $101 of adapters rides on it, port span); a JLC DFM check on the edge-launch SMP; and three part.yaml pin maps declared OWED rather than fabricated (W25Q16JVSSIQ, USBLC6-2SC6, and the micro-USB signal names, which the vendor drawing genuinely does not print)."
updated: 2026-07-27T12:10:00
