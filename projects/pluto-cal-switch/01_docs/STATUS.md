# STATUS beacon — pluto-cal-switch (live head of the journal)

<!-- reader parses from here down -->
stage:   04-SCHEMATIC-DONE
step:    "Stage 4 COMPLETE and STOPPED at the schematic gate, which is the declared handoff boundary. 03_tscircuit/ authored: 73 components driving the SHARED GENERIC BACKEND with ZERO board-specific generation Python (src/pluto_cal_switch.tsx + manifest.yaml + net_aliases.txt + parity_padmap.txt + package.json + README + GENERATE.md). rebuild_all.sh filled (BOARD/TSX = pluto_cal_switch); floorplan.yaml and route.yaml RE-IDENTIFIED to this board because both were still the cook-loadcell template VERBATIM and rebuild_reuse.sh derives the board NAME from floorplan.yaml — a rebuild would have emitted 04_kicad/cook_loadcell.kicad_pcb from this board's netlist. FIVE UPSTREAM DEFECTS FOUND BY AUTHORING AND FIXED AT SOURCE: (1) electrical_invariants.yaml was UNSATISFIABLE — U_SW1.1/U_SW2.1 asserted on LOOP_ARM1/2 while the ADR-0004 chain puts the arm pad between that net and the switch; (2) three names for two nodes on each antenna path (RX_ANTn_EXT was in no netclass, no part.yaml, no architecture table); (3) LOOP_IN and LOOP_SPLIT were two names for ONE node; (4) the pad chains still named ONE part each, three re-specs after A9 made PAD_A1 five chips and each arm pad two — E-INV would have FAILED a correct netlist; (5) nets.yaml never declared the PAD_A1 cascade internals or the arm-pad internals, so six 50-ohm RF segments would have routed at the 0.25 mm default (~55 ohm). Also: the two OWED pin maps (W25Q16JVSSIQ, USBLC6-2SC6) are CLOSED from primary datasheets with PDFs vendored, and ADR-0012's deferred crystal is CLOSED by resolution (a) — ABM8-272-T3, CL 10 pF / ESR 50 ohm max, the vendor-approved RP2040 part, so the reference 15 pF caps ship unmodified and no start-up test enters the release gate."
measure: "GATE NUMBERS, ALL MEASURED 2026-07-28. TSX-PRE PASS 20/20 part.yaml (13 multi-pin) BEFORE the first tsci build. ERC `kicad-cli sch erc --severity-all` = 0 ERRORS / 542 warnings, classified not counted: 284 endpoint_off_grid + 185 lib_symbol_issues + 73 footprint_link_issues, all three parametric and baselined by the 03_tscircuit contract (the 73 footprint links are the honest signal that 03_src/lib/pluto_cal_switch.pretty/ is stage-5 work). NETLIST PARITY vs a sealed 04_kicad = N/A, and it is stated as N/A rather than reported as a pass: this is the board's FIRST schematic and no sealed board exists. Converter: 73 components (73 with FPID) — the first build read 72, and the miss was F1, whose PPTC had no dossier at all; an unresolved FPID is invisible in ERC, parity and every render and hard-errors generate_board three stages later. SEMANTIC BATTERY: S-NETMERGE PASS 48/48 labels survive; E-INV OK 40/40; E-TOPO OK 1/1 rail 1/1 converter (required=BUCK, declared=LINEAR ME6211C33M5G-N, headroom 1034 mV vs dropout 120 mV, PD 202 mW vs 300 mW = 67%); E-MARGIN N-A; E-OFF N-A (source_type usb); S-COUNT PASS 3/3 source pairs vs manifest over 73 refdes; bom_source_check --circuit-only PASS. contracts_audit 243 files 0 violations. import_provenance 18/18. policy_audit FAIL=2 HUMAN=6 N-A=23 PASS=9 (S-VER went 11/13 -> 13/13). TWO GATES ARE RED AND NEITHER IS WAIVED, DELIBERATELY: E-ADR 11/12 is O8b, the declared gap where protection_adrs() does not read status: and still demands invariants from the SUPERSEDED ADR-0006 — one line would make it green and that is exactly why it is left red; S-OCCL 25 is label-vs-label overlap in the CONVERTER kicad_sch, the machine artifact ADR-0002 says need not be pretty, and three fleet boards carry a waiver for that class, but the waiver's EVIDENCE is a fresh-context render review of the shipped tscircuit PDF and no such review exists at the schematic gate — a waiver copied without its evidence is an inherited defect (M4), so it belongs at stage 7. O8 IS CLOSED AND CORRECTED: power_topology.py gained LINEAR on 2026-07-27 and names this board in its own docstring as the case that forced it."
state:   done
op_pid:  none
next:    "STAGE 5 — PLACEMENT. Its first task is NOT placement: 03_src/lib/pluto_cal_switch.pretty/ does not exist and NINE non-stock land patterns are named by 02_parts (SMA 5-D1.4 @ 5.08 THT, MCLP-6-EP 2x2, TSNP-6 0.7x1.1 @ 0.4, QFN-56 0.4, the XKB micro-B with its two OVAL PLATED SLOTS, SOT-553, SOT-23-5/-6, and two RF-minimum-area 0402 patterns) — without them generate_board_generic has nothing to place. Then floorplan.yaml placement, which carries this board's identity but no anchors or seeds, deliberately. Constraints are already derived, not open: ARCHITECTURE sec.10's eight critical geometries (mirror-symmetric loopback arms at IDENTICAL not mirrored rotation, inter-arm separation >= 3x dielectric height, RP2040+USB far from all five SMA jacks) and DETAIL_DESIGN sec.2's target segment lengths. STILL OPEN, none of which gates stage 5: D7 control arbitration (FIRMWARE ONLY, the last open D#, authored to HEADER OR USB + a 10 s watchdog which consumes no hardware); J_USB's pin map is still the INFERRED USB Micro-B standard map and its resolution is a fresh-context pin review against JLC's own footprint at the twin stage; O5c is now MEASURED rather than unverified — YAT-15A+ 79 in stock is the ONLY buyable mid value (12A+ 4, 5A+ 10) and collapsing PAD_A1 would lift the ceiling from 20 to ~51 boards, but it is NOT taken because ADR-0016's guarantee is built on MIN columns and YAT-15A+'s has not been read, so it is escalated to the user as a costed option; active gain between the AD936x TX and the Pluto's TX SMA (OWED in spf/); standard-vs-RP-SMA on the PlutoPlus; are the PlutoPlus RF ports DC-free. TWO PROPOSED skills/ PATCHES ARE IN THE STAGE-4 REPORT (E-ADR status: awareness; mpn_capacitance decoder + policy_audit S-VER YAML-parse) — neither was applied, because skills/ is out of scope for this session."
updated: 2026-07-28T11:40:00
