# STATUS beacon — pluto-cal-switch (live head of the journal)

<!-- reader parses from here down -->
stage:   06-ROUTING
step:    "STAGE 6 IN PROGRESS. Two things landed before any track was laid, in this order, and the order is the point. (1) `length_match: RF_LOOP_D4` AUTHORED in 03_src/rules/nets.yaml — without it copper_length_audit reports R-LEN-UNREACHED and the board would have been routed with NO gate on the property it exists to publish. (2) A D-BACK to stage 3: I measured, for every pad on a class with a width floor, THE WIDEST TRACK THAT CAN LAND ON IT at this board's 0.15 mm clearance floor, and ELEVEN PADS CANNOT ACCEPT THEIR OWN NETCLASS MINIMUM. U_SW1.5/U_SW2.5 (the BGS12WN6 RFC port, middle pad of a 0.400 mm row) need RF50's 0.350 and can take 0.250; U_MCU.46/.47 need USB_DP's 0.330 and can take 0.300; U_MCU.10/22/26/33 (3V3) and 23/45/50 (VREG_VOUT) need PWR's 0.400 and can take 0.300. The pitch is the part, so no routing or placement change fixes it. Fixed upstream as three PERMISSIVE rule areas in floorplan.yaml + scoped_floors in nets.yaml (land tapers, bounded to lambda_g/61..lambda_g/27 at 6 GHz)."
measure: "MEASURED 2026-07-29, UNPIPED. The arm-congruence transform is a REFLECTION about y=55.000 mm, NOT the +14.5 mm translation A-SYM publishes: the two maps coincide for every arm part at y=47.750/62.250 only because 62.250 = 2*55.000-47.750, and they disagree at the splitter resistors, where +14.5 sends R_DELTA1.2 (64.000,53.670) to y=68.170 and there is no pad. y -> 110.0-y lands on R_DELTA2.2 (64.000,56.330) exactly. BUT THE SWITCHES ARE TRANSLATED, NOT REFLECTED (U_SW1 centre y 47.950 -> U_SW2 62.450 = +14.5; a reflection would give 62.050), so NEITHER single isometry maps the whole arm: the transform is chosen PER NET, each one an isometry, so the summed length is equal to the nanometre either way. Pad-landing escape widths measured over 48 approach directions x a 30 um landing grid, every other-net F.Cu pad within 2.5 mm as an obstacle."
state:   working
op_pid:  none
next:    "Regenerate board + rules with the three rule areas, re-verify audit_board/placement_gates/count_parity unchanged, then 03_src/arm_copper.py (deterministic RF chain, per-net isometry, collision-verified with refusal), then KRT for the digital/power remainder, stitch, generate_rules LAST, DRC 0/0/0, copper_length_audit, promote + commit."
updated: 2026-07-29T00:00:00
