06_build/*
!06_build/contracts.md
!06_build/checkpoints/
!06_build/checkpoints/*.json

# Preserved exploratory boards, logs, and superseded build snapshots. They
# remain in the repository filesystem for forensic recovery but are not source
# inputs and must not make a release-scoped cleanliness check fail.
recovery/

# Guard against tools accidentally resolving a repo-relative project path from
# inside the project and recreating `projects/<name>/...` beneath itself.
/projects/

*.kicad_pcb-bak
*.kicad_sch-bak
*-backups/
_autosave-*
fp-info-cache

__pycache__/
*.kicad_prl
