# STATUS beacon — the live head of the journal

This file is the ONE place a coordinator reads to know, in a glance, where this
board is between gates. OVERWRITTEN (not appended) at every transition; history
lives in `journal/<stage>.md`. Read by `skills/kicad-pcb/scripts/pcb_status.py`,
graded by `skills/kicad-pcb/scripts/status_beacon_check.py` (canon M-BEACON).

<!-- reader parses from here down -->
stage:   seal
step:    "LIVE RELEASE IS STILL crow-mic-pod-v2-v1.3-2026-07-27 (no SUPERSEDED.md, byte-unchanged). A v1.4 was commissioned 2026-07-28 to fix CAL-1 with R4=33k/R5=18k as a BOM-ONLY supersede and was NOT SEALED — both of the commission's own pre-conditions fired. (1) NOT BOM-ONLY, measured: export_jlc_package.py:349 reads the BOM Comment AND the CPL Val column from the BOARD's fp.GetValue(), and the schematic carries the same value, so a value change moves fab/cpl.csv, fab/bom.csv, .kicad_pcb, .kicad_sch and .net — all three supersede modes in the 07_releases contract FAIL this shape, and the commissioned proof (md5 unchanged + CPL byte-identical) is unsatisfiable without hand-editing the CSV, which canon M3 forbids. (2) THE FIX'S MARGIN IS REFUTED: the audit's +3.4 dB worst-case recovers an implied 0.100 V op-amp output headroom while its own ceiling claim recovers the datasheet's guaranteed 0.800 V; at 0.800 V the proposed pair still clips by 1.65 dB. NOTHING was written to 07_releases/ or 04_kicad/. Awaiting a user decision on the value pair and on spending a source revision."
measure: "re-derived independently 2026-07-28 (pcbnew on a COPY of the sealed board, SBOS855E re-read with pdftotext, transfer function from the .tsx — no code shared with the audit): |LS1-MK1| 45.61798 mm; burst 106.817 dB SPL; mic load correction +2.135 dB -> 80.68 mV/Pa; v1.3 ceiling 106.81 / 103.81 / 101.31 dB, reproducing the audit to the second decimal; 33k/18k worst-case 105.17 dB at the datasheet limit (-1.65 dB vs the burst), 109.70 dB at 0.2 V (+2.88), 110.27 dB at 0.1 V (+3.45). Copper experiment: gerbers+drills 11/11 byte-identical base-vs-mod AND 11/11 vs the sealed v1.3 zip after the timestamp strip; fab/cpl.csv 2 Val cells differ; DRC --severity-all --refill-zones --schematic-parity base 0/0/0, mod 0/0/2 footprint_symbol_mismatch. Sealed board md5 c7b8512ccf0810997116c8c2e59dcad9 == 04_kicad's, 62 files in the release dir before and after, no .kicad_prl. Parts, live 2026-07-28: 33k C4216 JLC Basic 747998, 18k C25810 JLC Basic 1357175."
state:   blocked
next:    "USER DECISION, two questions. (a) WHICH VALUE PAIR: 33k/18k is a strict improvement under every output-swing model (+2.2..+4.2 dB of worst-case ceiling) but proven sufficient under none; at the datasheet's guaranteed (V-)+0.8 limit the optimum divider is VMID ~2.07 V, the OPPOSITE direction from the proposal. Answering it needs an output-swing model cited to a load condition — this board draws 0.22 mA, 3.2% of the spec's 2k test current. (b) WHETHER TO SPEND A SOURCE REVISION: .tsx -> tsci build -> schematic bridge -> board regeneration, copper identity then proven GEOMETRICALLY plus the 11/11 gerber re-plot (md5 equality is gone), sealed behind documented freshness exceptions (the usb-hub-3s-v3 v1.11 pattern) or behind a new --value-change-supersede mode, PROPOSED not applied (this pass may not edit skills/). Also still OPEN and untouched: POE-1, PSR-1, DC-1, MECH-1 — all recorded in 08_reviews/DISPOSITIONS.md, which had no rows for the 2026-07-27 audit at all."
op_pid:
updated: 2026-07-28T10:32:00
