# crow-mic-pod-v2 — consolidated red-team / review dispositions (2026-07-23)

Four zero-context lenses (verbatim archives alongside): `2026-07-23_v1.0_redteam_topology.md`,
`2026-07-23_v1.0_redteam_layout.md`, `2026-07-23_v1.0_pin-review_full.md`, `2026-07-23_v1.0_render-review_twin.md`.

**CONSOLIDATED VERDICT: DO NOT ORDER — 3 P0 blockers.**
Lens verdicts: topology **DO NOT ORDER**; layout **DO NOT ORDER** (assembly); pin **FAIL (J1)**;
render/twin **HOLD (BOM)**. The board is DRC 0/0/0 / parity 0 and the *documented* pinout +
gain topology are internally correct — the blockers are protection, a footprint-mirror
correctness question, and an assembly-file DNP defect.

Severity | ID | Finding (lens) | Disposition
---|---|---|---
**P0** | A | **PoE Alternative-B power injection destroys U1.** 5V_AUDIO is ONE net = {J1.4, J1.5, U1.8 (V+), R1, R4, C7-C10} — zero series impedance between RJ45 contacts 4/5 and the op-amp V+. PoE 802.3af/at Alt-B (+V on 4,5 / −V on 7,8) aliases EXACTLY onto this board's 5V_AUDIO/GND. A PoE switch drives U1 V+ to 44-57V > OPA1678 abs-max `vs_max=40V`. (topology #1) | **ACCEPT-AND-FIX — USER DIRECTIVE-DECISION.** VERIFIED against netlist + part.yaml. Root: the RJ45-everywhere directive (ADR-0003) + protection-delegation (ADR-0001); ADR-0003's hazard analysis covered PHY-signal + backfeed but NEVER power injection. Fix is a PoE-defeat network (clamp <40V + PPTC fuse on 5V_AUDIO, or break the PoE-alias by moving 5V_AUDIO off 4/5/7/8) + re-layout, OR explicit user sign-off that the risk is accepted for this deployment. **Not auto-fixed** (directive tension — the user's call). SEAL-BLOCKER.
**P0** | B | **PoE Mode-A sustained conduction frets D1.** Mode-A (+V on 1,2 / −V on 3,6) forces the TPD2E2U06 (pulse-rated only) into continuous conduction — ≈(48−9.7)×0.35A ≈ 13W into a <2mm SOT-553 → thermal failure/burn hazard in an outdoor enclosure. (topology #2) | **ACCEPT-AND-FIX** — same PoE-defeat fix as A resolves it. Secondary path of the same root cause.
**P0** | C | **J1 RJHSE-5384 footprint may be a full contact MIRROR.** The stock KiCad `RJ45_Amphenol_RJHSE538X` LED-hole numbering is REVERSED vs the Amphenol dwg P-RJHSE-538X Rev K "RECOMMENDED P.C.B. LAYOUT (COMPONENT SIDE)": datasheet LED row = `12 11 … 10 9` (11,12 LEFT / 9,10 RIGHT); the KiCad footprint places 9,10 LEFT / 11,12 RIGHT. Both are component-side views, so a reversal implies a full 1↔8 contact mirror — which would swap EVERY custom-pinout net onto the wrong physical contact (field hazard). (pin FAIL) | **ACCEPT-AND-FIX → REQUIRED PHYSICAL-CONTINUITY CHECK BEFORE ORDER.** The LED-numbering reversal is CONFIRMED against the datasheet (orchestrator re-read); the contact mapping CANNOT be certified from the 2D drawing alone (the layout figure does not label contacts 1-8, and I have no part/STEP to probe). Before ordering, multimeter pad-1 copper → the physical contact-1 blade on a real RJHSE-5384 (the exact continuity discipline the design already prescribes for the LED-polarity ambiguity + the first-power ritual). If mirrored, vendor a corrected footprint. SEAL-BLOCKER until verified.
**P0** | D | **D3 (DNP) BOM/CPL/DNP inconsistency — assembly-file defect.** D3 is in `bom_jlc.csv` with a real code (C559105) but ABSENT from `cpl_jlc.csv`, with NO machine-readable DNP marker (`dnp no` on symbol; footprint has `exclude_from_pos_files` only). A BOM line with no CPL placement → JLC validator bounce or bill-but-never-place. (layout P0 / render P1 / topology P2) | **ACCEPT-AND-FIX (data-hygiene, path pending coordinator go).** RE-ADJUDICATED honestly — my earlier "handled on parity grounds" did NOT address the assembly-order consequence. EVIDENCE from a live test: setting D3's footprint value to `…DNP` DOES drop it from BOTH BOM+CPL, but `--schematic-parity` **checks value** and flags a mismatch unless the SYMBOL value also carries "DNP" — which the tscircuit converter cannot emit (it hardcodes symbol `in_bom=yes/dnp=no` and derives value from the supplier code). So a parity-clean end-to-end DNP needs EITHER (a) a converter enhancement to emit `dnp`/`in_bom`/value-with-DNP from source, OR (b) POPULATE D3 (parity-clean, +~$0.05 extended-tier, and electrically fine — SS14 flyback + SMAJ6.0A over-clamp don't conflict), amending ADR-0001. BOM-without-CPL is NOT a proven-clean JLC path (no evidence found; JLC's DNP mechanism is "DNP in value → out of both"). Recommend (b) populate-D3 as the clean minimal-risk resolution, or (a) for a future converter capability. NO auto-apply — awaiting path decision.
**P1** | E | **Reverse-polarity on the hand-crimped custom pinout is not covered by the keying mitigation.** ADR-0001 relies on "keyed RJ45 + labeling + controlled install", but the custom hand-crimped Cat5e cable can have 4/5 swapped with 7/8, putting V+ below V− on U1 (abs-max ordering violation). (topology #3) | **ACCEPT-AND-FIX** — folds with the PoE decision (A). Either document as an OPEN risk distinct from "wrong device plugged in" (ADR-0001 conflates them), or add a cheap reverse-polarity clamp. User/coordinator path decision.
**P1** | F | **RJ45 GND tail pads 7,8 use THERMAL relief, not SOLID** — contradicts the RJHSE-5384 part.yaml gotcha ("set zone connection SOLID for the jack GND tails"); only 2-of-4 spokes reach the pour on the 1.02mm-pitch dense field. (layout #2) | **ACCEPT-AND-FIX** — add a floorplan `pad_overrides: [{pads:[7,8], zone_connection: full}]` on J1, refill, re-DRC. Clean, in-pipeline. (Fold into the fix pass.)
**P1** | G | **Mandatory "NOT ETHERNET" warning + full pinout legend are NOT adjacent to J1** (they sit ~15-20mm NE with the title; only a short differently-worded label is on the connector) — ADR-0003 requires "adjacent to J1 and legible". (layout #3) | **ACCEPT-AND-FIX** — reposition the banner + legend into J1's silk envelope (floorplan captions). Clean.
**P1** | H | **RJ45 mouth sits 1.05mm behind the PCB west edge; no enclosure CAD in-repo to verify fit** — the placement-journal "plug clears the edge" claim is asserted, not measured against an enclosure. (layout #4) | **ACCEPT-AND-VERIFY** — open mechanical dependency: confirm against the enclosure panel-cutout (16.89×13.46mm) before order, or notch the board edge at the mouth. Documented as an order-day mechanical check.
**P1** | I | **MK1 LCSC field carries the MPN string `AOM-5024L-HD-R`, not blank** — will confuse/fail JLC's BOM parser. (render/twin P1) | **ACCEPT-AND-FIX (data-hygiene, path pending).** MK1 is hand-solder (no JLC part); its LCSC must be blank. Cleanest: an export improvement that blanks a non-`C`-prefixed supplier code (it is not an LCSC), or a curation. NO auto-apply — awaiting go.
**P2** | J | **AUDIO_P/AUDIO_N copper length asymmetric ~2× (95.7 vs 46.1mm, 49.6mm skew)** — contradicts the CMRR intent. (layout #5) | **ACCEPT-WITH-EVIDENCE** — both legs are low-Z op-amp OUTPUTS (not sensitive inputs) at audio frequencies; practical CMRR impact negligible. Note for next spin (route as a matched pair). Not order-blocking.
**P2** | K | **Beep flyback loop D2↔LS1 is 12.3mm, not "HARD against"** as the part.yaml documents. (layout #6) | **ACCEPT-WITH-EVIDENCE** — 4kHz burst with a slow BJT low-side switch at CENTRAL; 12mm loop inductance is functionally irrelevant. Annotate the journal "satisfied at this drive speed". Not blocking.
**P2** | L | **D1 protection stub ~13mm** to J1. (topology #5) | **ACCEPT-WITH-EVIDENCE** — already documented in TPD2E2U06 part.yaml (AUDIO_P/N are driven low-Z outputs; ESD-scale threat only). Folds with A/B.
**P2** | M | **IR-drop math error in ARCHITECTURE.md** (counted one leg, not round-trip). (topology #4) | **FIXED** — 2026-07-23: corrected to round-trip (5V_AUDIO 0.64Ω→3.2mV; 5V_BEEP 1.28Ω→0.19V). Functionally harmless either way (vast margin).
**P2** | N | **tscircuit schematic PDF renders `N5V_BEEP`/`N5V_AUDIO`** (spurious leading N on digit-leading rails) — reads like active-low. (render/twin P2) | **ACCEPT-WITH-EVIDENCE** — cosmetic tscircuit-render artifact of the N-prefix canonical-net convention; the canonical `.kicad_sch` + netlist use the correct `5V_BEEP`/`5V_AUDIO`. Non-blocking; note in the release.
**tool** | O | **jlc_twin `pad_alias` sequential pop/extend corrupts a true 2-way pad swap** (turned a clean PAD-GEOM into PAD-MISMATCH best=none). (render/twin) | **NOTED — already spun off** as background task `task_d26971dc`. Not patched mid-review (shared tool). Does not affect this board's twin result (adjudicated).

## Verified clean (no defect — from all lenses)
- DRC **0/0/0** (`--severity-all --refill-zones --schematic-parity`) — re-run independently by BOTH the layout red-team and the orchestrator, not trusting the cache.
- **jlc_twin PASS** (exit 0, 25 OK / 63 checked, zero unadjudicated criticals; 5 evidence-backed adjudications in `03_src/rules/twin_adjudications.yaml`: MK1/J1 NO-CAD, LS1 pad-numbering, U1 + D3 PAD-GEOM).
- Active-balanced driver gain independently recomputed = **3.0 V/V differential** (1.5 / −1); VMID = 2.5V; DC-bias/AC-coupling correct.
- **Beep-loop galvanic isolation** confirmed (5V_BEEP/BEEP_RET carry only D2/D3/LS1/J1/TP — zero GND ties).
- **Pin review: 6/7 PASS** (U1, D1, D2, D3, MK1, LS1) — flyback/clamp polarity all correct (pad1=cathode→5V_BEEP); only J1 open (finding C).
- Vendored footprints (CMT-8504, AOM-5024) match datasheet land patterns; no silk-over-pad.
- Silk legend + TP labels match actual nets; pad-1 rect marker; NPTH-post copper clearance ≥0.88mm; all placeable parts top-side; MK1/J1 hand-solder-reachable.

## FIX-PASS RESOLUTION (2026-07-23) — all P0 closed, fresh red-team ORDER/SHIP 4/4

The DO-NOT-ORDER verdict above is SUPERSEDED by the fix pass. Fresh independent
4-lens red-team on the FIXED board: `2026-07-23_v1.0_redteam_fixpass.md` — topology
**ORDER**, layout **ORDER**, pin **7 PASS/0 FAIL**, render/twin **SHIP**, NO new
P0/P1. Resolutions of the original findings:

| ID | Original | Resolution |
|---|---|---|
| A/B | PoE injection destroys U1 / frets D1 | **ACCEPTED WAIVER — USER sign-off (BRIEF A1, ADR-0005).** Controlled deployment (never plugged into PoE infra); NOT-ETHERNET labeling + keying. ADR-0001 + ADR-0003 amended to cover power injection. No protection net, no re-pin. ORDER_README carries the warning. |
| C | J1 footprint may be a contact MIRROR | **CERTIFIED CORRECT — not mirrored.** Row-parity + chirality analysis vs the datasheet COMPONENT-SIDE layout (pure 180° rotation, no reflection); the 2.79mm post dim is symmetric (non-discriminating). Documented in ADR-0003 + J1 part.yaml verified-note; INDEPENDENTLY re-confirmed by the fresh pin lens. Pad-1→contact-1 continuity kept as an ORDER_README defense-in-depth backstop (not a blocker). |
| D | D3 DNP → BOM-without-CPL assembly defect | **D3 POPULATED** (BRIEF D5, ADR-0001 amended). Now in BOTH bom_jlc.csv + cpl_jlc.csv; twin-verified (fit 0.19mm). Redundant beep over-clamp, no conflict with D2. |
| E | reverse-crimp not covered by keying | Folded into ADR-0005 (same accepted-deployment-constraint waiver; ADR-0001 dec. 5 amended to stop conflating it with "wrong device"). |
| F | J1 GND tails 7,8 THERMAL not SOLID | **FIXED** — floorplan `pad_overrides {pads:[7,8], zone_connection: full}`; verified on board J1.7=J1.8=FULL. |
| G | banner/legend not adjacent to J1 | **FIXED** — relocated into the clear strip over J1 (0 crowded captions); render + layout lenses confirm adjacent & legible. |
| H | RJ45 mouth 1.05mm overhang, no enclosure CAD | **DOCUMENTED** — open mechanical dependency; ORDER_README order-day check. |
| I | MK1 LCSC = MPN string | **FIXED** — refdes_codes_from_circuit + export carry-over blank non-`C\d+` codes (RED-verified test in tests/t1_bom_source.py); MK1 BOM LCSC now blank. |
| J/K/L | audio-pair asym / beep loop / D1 stub | ACCEPT-WITH-EVIDENCE (unchanged; low-Z outputs, slow beep). |
| M | IR-drop math | FIXED (round-trip). |
| N | schematic N-prefix render | ACCEPT (cosmetic tscircuit artifact). |
| — | (route reproducibility) | **FIXED** — rebuild_all.sh now reuses the promoted r3 chain (was re-racing stochastically; the boxed-in J1.1 escape dropped on some race candidates). DRC 0/0/0 now reproducible. |

New P2s from the fresh pass (FP-1..FP-6) are in `2026-07-23_v1.0_redteam_fixpass.md`;
FP-1/2/3 fixed this pass, FP-4/5/6 accepted.

## Prioritized fix list (for the coordinator's path decision)
1. **[P0-A/B, USER]** PoE-injection protection on 5V_AUDIO (clamp+fuse + re-layout) OR user risk-acceptance sign-off. — *directive decision, not auto-fixed.*
2. **[P0-C]** J1 footprint-mirror: required physical-continuity check on a real RJHSE-5384 (pad1→contact1) before order; vendor a corrected footprint if mirrored.
3. **[P0-D]** D3 end-to-end DNP: populate D3 (parity-clean, +~$0.05, amend ADR-0001) OR converter enhancement. — *data-hygiene, pending go.*
4. **[P1-I]** Blank MK1's LCSC (hand-solder). — *data-hygiene, pending go.*
5. **[P1-F]** J1 GND tails 7,8 → SOLID zone connection.
6. **[P1-G]** Move the NOT-ETHERNET banner + full pinout legend adjacent to J1.
7. **[P1-H]** Confirm RJ45 mouth vs enclosure panel cutout (open mechanical dependency).
8. **[P2]** (M fixed) — J re-match audio pair, K annotate flyback loop, N note the schematic N-prefix.

## Independent audit 2026-07-26 (`2026-07-26_independent-audit.md`, target: v1.1 staging)

| id | review file | finding (one line) | severity | verification | disposition |
|---|---|---|---|---|---|
| IA-1 | 2026-07-26_independent-audit.md | A-POL human-gate documents contradict: MANIFEST + rotation_human_gate.txt named D2/D3 single-channel where the landed f9eee3f rows say U1 | P1 | confirmed (three-document table in the review; §3b's stale closing paragraph re-verified on disk) | fixed — pre-seal: gate file is the exporter's generated output (names C192421/U1 only), MANIFEST A-ROT/A-POL rewritten to the f9eee3f classification, §3b stale paragraph replaced; all three now agree |
| IA-2 | 2026-07-26_independent-audit.md | verification/ missing or mis-naming 9 items REQUIRED by 07_releases/contracts.md (parity.md, twin_report.txt, stock_check.{txt,csv} absent; six mis-named) | P1 | confirmed (hand-run REQUIRED pass reproduced all 9) | fixed — pre-seal: parity.md generated (board_netlist_parity 78/78 nodes PASS + DRC parity 0), twin_report.txt rendered from the same run's csv, stock_check.txt written, six files renamed to contract names; assembly_coverage.txt added per the re-synced contract; hand-run REQUIRED pass now finds 0 missing |
| IA-3 | 2026-07-26_independent-audit.md | board contracts are a stale template revision (rules contract predates assembly.yaml; C-ALLOW fails own files; root strays RESUME.md + drc.rpt) | P2 | confirmed (contracts_audit --projects reproduced every listed violation) | fixed — pre-seal: 5 contracts re-synced from skills/pcb-design/templates/contracts, root strays removed, five 08_reviews files renamed to the contract pattern; contracts_audit --projects: 0 violations for this board |
| IA-4 | 2026-07-26_independent-audit.md | MANIFEST cites "37/38 byte-identical" where payload_identity.txt states 19/18/1 | P2 | confirmed | fixed — pre-seal: MANIFEST now quotes the artifact (19 payload files: 18 identical, 1 differs = fab/bom.csv) |
| IA-5 | 2026-07-26_independent-audit.md | NOTE: missing_models.txt ran without --cpl (denominator = check count, not population) | P2 | confirmed | fixed — twin re-run 2026-07-26 with --cpl fab/cpl.csv; header now names the 26 CPL placements as the population; bodies 26/26 |

## Order-readiness audit 2026-07-27 (`2026-07-27_v1.3_external-regrade_order-readiness.md`, target: SEALED v1.3-2026-07-27)

External regrade run because `fleet_regrade.py` reports v1.3 as
`never-graded-by: FAB-PAYLOAD, RENDER`. `07_releases/` is immutable, so the two
missing verdicts are recorded in `08_reviews/` rather than retro-filled.
**Audit verdict: ORDER** — no copper/BOM/CPL change, no new release required.

| id | review file | finding (one line) | severity | verification | disposition |
|---|---|---|---|---|---|
| AUD-1 | 2026-07-27_v1.3_external-regrade_order-readiness.md | `kicad-cli pcb drc` on a sealed board WRITES `source/<board>.kicad_prl` into the immutable `07_releases/` directory | P2 | confirmed (integrity sweep found 62 files / 61 hashed with mtime == the DRC run's second; `git status --ignored` reports it `!!`, so no MANIFEST-hashed byte moves — this independently CONFIRMS the v1.3 MANIFEST `source_note` that the .prl is a dropping, not a shipped artifact) | recorded — dropping deleted, sealed archive re-verified 61/61 sha256 with 0 unhashed strays; this audit's DRC evidence re-produced from a COPY at `06_build/proof/audit-2026-07-27/src_copy/`. Rule for the next auditor: copy sealed `source/` into `06_build/proof/` before running kicad-cli. Filed against tooling, not this board |
| AUD-2 | 2026-07-27_v1.3_external-regrade_order-readiness.md | A-RENDER (`twin_overlay.py`) FAILS C1 at centre delta 1.620 mm — but `extract_body()` classifies a body pixel as `saturation < 0.12` and C1's JLC ochre mesh renders at saturation **1.000**, so 771/1209 px (63.8%) inside its expected box are classified as BACKGROUND and the flood returns a 208-px termination sliver | P2 | **refuted as a board/render defect, confirmed as a tool defect.** Outward excursion is only 0.059 mm (under-capture, not displacement). Re-measured with a classifier admitting both the ochre body and the grey terminations, everything else identical: **centre delta 0.020 mm, outward 0.447 mm, 1024 px — inside the 1.00 mm tol.** Passing refs all have unsaturated bodies (U1 (74,74,74), D2 (84,84,84), LS1 (70,70,70), sat 0.000) | recorded — **A-RENDER PASS on the board**; all 5 measurable bodies faithful, coverage 5/26 with 21 named sub-2.0 mm unresolvable and 0 resolvable-but-unmeasured; `--side bottom` correctly REFUSED (0 B.CrtYd courtyards). Filed against `twin_overlay.py`: a saturated body is neither measured nor declared unresolvable — it is silently mis-measured and fails |
| AUD-3 | 2026-07-27_v1.3_external-regrade_order-readiness.md | `fab/cpl.csv` `Val` column carries LCSC codes, not values, on 5 of 26 rows (D1, D2, D3, LS1, U1) — the F-WORDS class F-LEGIBLE fixed in the BOM, still present in the CPL because the checker grades `fab/bom.csv` only | P2 | confirmed (mechanical BOM/CPL reconciliation) | deferred — non-blocking (JLC places on Designator/X/Y/Layer/Rotation; `Val` is a human label). NOT worth a v1.4 alone; fold into the respin already owed for the R-RULES A-FIRE fix. Gate-coverage gap: F-LEGIBLE's denominator should include the CPL |
| AUD-4 | 2026-07-27_v1.3_external-regrade_order-readiness.md | LS1 (C22359707) stock trend — ORDER_README extrapolates "-62%/7d, reaches 8 in ~2 weeks" | P2 | **refuted by measurement** — 69 on 2026-07-25, 07-26, 07-27 (seal) and 07-27 19:50 (live). Flat for four readings across three days; the draw-down was a one-off, not a trend line | recorded — order-day re-check STANDS (single-source Extended, no qualified alternate) but it is no longer a race. Fallback if gone: consign separately, or omit from the CPL and hand-place (beep loop, not the signal path). Never substitute a transducer at the cart |
| AUD-5 | 2026-07-27_v1.3_external-regrade_order-readiness.md | FAB-PAYLOAD and RENDER families had NO archived verdict in any release of this board | P1 | confirmed (`fleet_regrade --project crow-mic-pod-v2`: v1.3 `never-graded-by: FAB-PAYLOAD, RENDER`) | fixed — both run read-only against sealed v1.3 and recorded here: **F-PAYLOAD OK** (F.Cu 1 zone -> 8 G36, B.Cu 1 -> 1, 2/2 gerbers distinct, 3 checks) and **A-RENDER PASS** (see AUD-2). An absent verdict is not a pass; the honest fix is an external record, not a retro-fill |

## Adversarial design audit 2026-07-27 (`2026-07-27_v1.3_adversarial-audit_first-principles.md`, target: SEALED v1.3-2026-07-27)

First-principles lens, 0 P0 / 5 P1 / 9 P2. **Audit verdict: PASS-WITH-NOTES** —
no gate fails; the findings are the things a gate cannot see. CAL-1 was
commissioned for a v1.4 on 2026-07-28 and independently re-verified in
`2026-07-28_v1.3_fix-verification_cal1.md`; **no v1.4 was sealed** (see FIX-1).
The four remaining P1s were NOT re-verified in that pass — it was scoped to
CAL-1 alone — and are carried forward OPEN so they are not lost.

| id | review file | finding (one line) | severity | verification | disposition |
|---|---|---|---|---|---|
| CAL-1 | 2026-07-27_v1.3_adversarial-audit_first-principles.md | The calibration transducer clips the preamp it exists to calibrate: LS1 ≥100 dB @10 cm at 45.62 mm = **106.82 dB SPL** at MK1, against a U1 input-common-mode ceiling of **106.81 dB nominal / 101.31 dB worst-case**; `part.yaml`'s headroom is 2.14 dB optimistic because the AOM-5024's −24 dB is spec'd at RL = 2.2k and this board loads it with 3.9k | P1 | **confirmed, independently re-derived 2026-07-28** — geometry re-measured with pcbnew from a copy of the sealed board (45.61798 mm), device limits re-read from SBOS855E §6.7, transfer function re-derived from the .tsx. Stimulus 106.817 dB, load correction +2.135 dB → 80.68 mV/Pa, ceilings 106.81 / 103.81 / 101.31 dB — every figure reproduces to the second decimal | **deferred — OPEN P1, no fix released.** Real defect, correctly filed P1 not P0 (OPA167x phase-reversal protection means it clips gracefully). The proposed fix is NOT released; see FIX-1 for why. v1.3 remains orderable with the limitation documented |
| FIX-1 | 2026-07-28_v1.3_fix-verification_cal1.md | CAL-1's proposed fix (**R4 = 33k / R5 = 18k**, "clears the burst by 3.4 dB worst-case") is computed with an op-amp output-swing headroom of **0.100 V** while the same review cites the datasheet's **0.800 V** eight paragraphs earlier | P1 | **refuted (in part), by re-derivation** — the audit's 113.7/110.2 dB recover an implied headroom of exactly 0.1000 V both times; its own "2.3×" ceiling claim recovers 0.800 V. SBOS855E §6.7 `VO` row guarantees only `(V−)+0.8 … (V+)−0.8` at RL = 2 kΩ. Under 0.800 V the fix reaches **105.17 dB worst-case = −1.65 dB, still clipping**; under 0.200 V (part.yaml's implicit model) +2.88 dB; under 0.100 V +3.45 dB. The defect is confirmed; only the fix's SUFFICIENCY is refuted | **deferred — user decision owed.** 33k/18k is a strict improvement under every model (+2.2…+4.2 dB of worst-case ceiling) but is proven sufficient under none of them, and under the datasheet limit the optimum divider sits at VMID ≈ 2.07 V (the OPPOSITE direction). Root cause: no output-swing model anywhere in this tree is cited to a load condition — three places state three different values. `02_parts/OPA1678IDR/part.yaml` corrected 2026-07-28 to carry the sourced number and the measured load current (0.22 mA = 3.2 % of the spec's test current) |
| FIX-2 | 2026-07-28_v1.3_fix-verification_cal1.md | A resistor VALUE change on this pipeline is **not BOM-only**: `export_jlc_package.py:349` reads `val = fp.GetValue()` from the BOARD and feeds it to both the BOM Comment and the CPL `Val` column, and the schematic carries the same value | P1 | **confirmed by experiment** — two exports differing only in 2 `(property "Value" …)` strings: gerbers+drills **11/11 byte-identical** (copper truly unmoved, and the sealed v1.3 zip re-plots 11/11 from its own source), but `fab/cpl.csv` moves 2 `Val` cells, `fab/bom.csv` gains a row and loses a row, `.kicad_pcb` md5 moves, and DRC `--schematic-parity` returns **2 `footprint_symbol_mismatch`** against the sealed schematic (base run: 0/0/0) | **recorded — v1.4 NOT sealed; returned to the user.** All three supersede modes in `07_releases/contracts.md` FAIL this shape (`--cpl-only` names a `Val` change as its own exclusion). The commissioned copper-identity proof (md5 unchanged + CPL byte-identical) is unsatisfiable without hand-editing the CSV, which canon M3 forbids. Proposed and NOT applied (this pass may not edit `skills/`): a `--value-change-supersede` mode asserting *gerbers/drills byte-identical, CPL delta confined to `Val`, BOM delta confined to the named designators*. Parts side is clean: 33k = **C4216** and 18k = **C25810**, both JLC **Basic**, same 0603 land, stock 748k/1.36M live 2026-07-28 |
| POE-1 | 2026-07-27_v1.3_adversarial-audit_first-principles.md | Nothing clamps 48 V — contacts 4/5 reach U1.8 with ZERO series impedance and D1 covers only 1/2; ADR-0005's stated mechanism is wrong for the realistic carrier (a compliant PSE will NOT power this pod — detection signature ≈1 kΩ vs the 23.75–26.25 kΩ window; a PASSIVE injector will), and the residual-risk list omits the MLCC ignition path (C1 at 7.6× rating → short → ~23 W in an 0603) | P1 | not re-verified in the 2026-07-28 pass (scope: CAL-1 only) — **carried forward OPEN** | **deferred — OPEN P1, needs copper, explicitly out of scope for the CAL-1 pass.** ADR-0005's accepted waiver stands; the ADR + ORDER_README wording correction is owed at the next revision. NOT addressed by any 2026-07-28 change |
| PSR-1 | 2026-07-27_v1.3_adversarial-audit_first-principles.md | PSRR never computed; VMID gets ZERO differential rejection (measured VMID→Vdiff 2.92 vs signal gain 2.97), so 1 mV of 60 Hz on 5V_AUDIO ≡ a 34 dB SPL tone COHERENT across all six pods; no ripple spec exists on the cable contract | P1 | not re-verified in the 2026-07-28 pass (scope: CAL-1 only) — **carried forward OPEN** | **deferred — OPEN P1, needs copper and an interface-contract number.** Explicitly out of scope for the CAL-1 pass. Recorded there that CAL-1's divider change would move the rail→VMID ripple divider 0.5000 → 0.3529 (−3.04 dB) while leaving the DOMINANT path (the 16 Hz R1·C1 mic-bias corner, −11.4 dB at 60 Hz) untouched — the two partially cancel, net effect a few tenths of a dB in an unknown direction. **PSR-1 must not be recorded as resolved by CAL-1's fix** |
| DC-1 | 2026-07-27_v1.3_adversarial-audit_first-principles.md | AUDIO_P/AUDIO_N have no DC return path on the pod (each net is only C5/C6 + D1 + J1 + a testpoint); DETAIL_DESIGN's "fc into ~10k central = 1.6 Hz" assumes a DC path the interface contract never requires | P1 | not re-verified in the 2026-07-28 pass (scope: CAL-1 only) — **carried forward OPEN** | **deferred — OPEN P1.** Answerable against the sibling CENTRAL board rather than by changing this pod; belongs in ARCHITECTURE.md's cable-contract table either way |
| MECH-1 | 2026-07-27_v1.3_adversarial-audit_first-principles.md | ORDER_README's RJ45 overhang (1.05 mm) is courtyard-derived; the F.Fab body outline gives a larger number (understated by 0.55 mm) | P1 | not re-verified in the 2026-07-28 pass (scope: CAL-1 only) — **carried forward OPEN** | **deferred — OPEN P1, documentation.** Folds into the existing order-day enclosure check (finding H) |
| (P2 ×9) | 2026-07-27_v1.3_adversarial-audit_first-principles.md | GAIN-1, CAP-1, PWR-1, POUR-1, SEP-1, MK1-1, ESD-1, MLCC-1, STRUCT-1 — plus two paperwork traps the audit corrected in itself (the `polarity=single-channel` misreading, and nets.yaml declaring BEEP at 0.4 mm while all 43 shipped segments are 0.5 mm because R-POUR reads `.kicad_pro` and never the copper) | P2 | not re-verified in the 2026-07-28 pass | **recorded — next revision.** The R-POUR gate gap is fleet-wide and filed against tooling, not this board |
