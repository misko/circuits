# Generate

From the repository root:

```sh
bash skills/kicad-pcb/scripts/gen_tscircuit.sh projects/pi-usb-port-switch
```

The canonical full pipeline remains `projects/pi-usb-port-switch/03_src/rebuild_all.sh`.
