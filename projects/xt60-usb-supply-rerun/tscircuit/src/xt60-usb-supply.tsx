// xt60-usb-supply-rerun — tscircuit render (SCAFFOLD / placeholder)
// Fab-of-record: ../../04_kicad/xt60-usb-supply.kicad_pcb (~51 parts). Author this
// board node-for-node from that netlist, then `gen_tscircuit.sh` renders it and
// writes verification/parity.md. Canon S-DSL: KiCad stays authoritative.
export default () => (
  <board width="10mm" height="10mm">
    {/* TODO: author xt60-usb-supply from the KiCad netlist (components, nets, footprints, JLC parts) */}
  </board>
)
