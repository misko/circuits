# STATUS beacon — cooksense (MAIN board; live head, OVERWRITTEN each transition)

History is append-only in `journal/<stage>_cooksense.md`; this is only the
current frame. Read by `skills/kicad-pcb/scripts/pcb_status.py`, graded against
the tree by `skills/kicad-pcb/scripts/status_beacon_check.py` (canon M-BEACON).

<!-- reader parses from here down -->
stage:   V1.7-CANDIDATE--REBUILD-DONE--PAPERWORK-FIXED--NOT-SEALED--TOPOLOGY-RE-GATE-RETURNED-DO-NOT-ORDER
step:    "**THE ATOMIC REBUILD LANDED AND THE SEAL DID NOT.** This pass did the ONE rebuild the plan called for — B30-11's comment cleanup plus the `rev \"dev\"` schematic regeneration, folded together and graded ONCE — wrote the A-STOCK M4 waiver on the argument, wrote a buyer-facing ORDER_README §5-0, and then **re-gated the topology lens on its own finding and stood down when it returned `verdict: DO-NOT-ORDER` / `P0-1: NOT RESOLVED`.** The `08_reviews` contract is unambiguous, so `07_releases/` was left untouched and the staged `cooksense-v1.7-2026-07-30/` directory was **REMOVED** rather than left implying a seal. **THIS IS THE EIGHTH DECLINE AND THE FIRST WHERE THE BLOCKER IS NEITHER THE BOARD NOR THE PAPERWORK** — the lens wrote that it *would accept the seal* (the M4 argument is sound) but that *\"sealing is not the question this verdict field asks\"*. The gate reads `verdict:`, `verdict:` means ORDERABLE, and this release is not orderable today. Nothing physical is in dispute."
measure: "**MEASURED BY ME, UNPIPED, RAW EXIT CODES.** DRC `--severity-all --refill-zones --schematic-parity` **0/0/0 EXIT 0**; policy_audit **EXIT 0, FAIL=0** (PASS=28 WAIVED=6 HUMAN=6 N-A=5); ERC **0 errors / 411 warnings**; S-COUNT **4/4 over 239 refdes**; E-INV **167/167**; E-ADR **11/11**; M-BOM PASS; M-DEPEND PASS; contracts_audit **0 violations**; A-STOCK **EXIT 1** (waived with evidence). **THE REBUILD MOVED THE NETLIST md5 AND NOT THE NETLIST, PROVEN NOT ASSERTED:** 198 nets / 239 components / 806 nodes both sides, **0** nets with a differing node set, **0** components with a differing (value, footprint), normalised md5 identical `900941caafe43eb6de7347171a8eb443`; board `9f4fd5fa…` **UNCHANGED**; sheet now `(rev \"v1.7\")`, fixed in SOURCE (`rebuild_schematic.sh --rev`), never by hand-editing 04_kicad. The `v1.8` comment count is **13, not 12** — verified by a comment-span PARSER, not by eye. **LIVE CATALOG 2026-07-30T21:33:59Z, through the gate AND independently:** `C265111` stock **5** / MOQ **21** — MOQ EXCEEDS STOCK, so it is **unbuyable at any quantity**, threshold to watch is **21 not 10**; `C42376901` 6030/1; control `C5620` 5212. **LAND-PATTERN EQUIVALENCE RE-DERIVED BY A METHOD THAT IS NOT jlc_twin** (raw EasyEDA `PAD~` + pcbnew, translation-only fit): genuine **0.0002 mm**, clone **0.0100 mm** signal / **0.0399 mm** tabs, non-mirrored — and the re-gate lens reproduced all of it to four decimals. **H4 CONFIRMED BY A SECOND CONSTRUCTION**: 105 drilled holes swept, H4 smallest, `J_ISOLOOP.1` next at 1.1500; the raw sweep reads 0.8000 to the stroke EDGE and every Edge.Cuts graphic is 0.1000 mm wide (56/56), so **0.8500 mm to the cut centreline** — the published figure. `J_ESTOP` pads re-read: 1=GND / 2=**3V3** / 3=ESTOP_RAW_IN at **1.0000 mm** pitch."
state:   blocked
op_pid:  none   # everything EXITED. Nothing is running.

next:    "**THE BOARD IS DONE AND THE PAPERWORK IS NOW CORRECT; WHAT IS OWED IS NOT WORK ON THIS BOARD.** Three ways forward, and only the third is ours: (1) **stock `C265111` >= 21** (not 10 — MOQ is the binding threshold); (2) a **mate-verified** `C42376901` substitution, which needs PHYSICAL PARTS and cannot be done from here; (3) **skill patches P1/P10**, so a seal can say *this design is correct* without also having to claim *and you can buy it today*. **THE RE-GATE EARNED ITS VERDICT — it found FIVE things this pass got wrong**, all now FIXED in staging: **RG-P1-1 (P1, the decisive one) — §5-0 told a buyer to 'edit one cell of `fab/bom.csv`' and `fab/bom.csv` IS NOT THE FILE JLC RECEIVES**; the assembly step takes `bom_jlc.csv`/`cpl_jlc.csv`, and the CPL `Val` column carries the LCSC code because `fp.GetValue()` on these footprints IS `C265111`, so following the instruction exactly would have ordered the unbuyable part (6 cells across 4 files; the correct remedy is to change `03_tscircuit/src/cooksense.tsx` 1216/1218 and REGENERATE, canon M3). **RG-P2-1** — 'zero bytes of the fab set' is false; the surviving form is 'zero bytes of the gerbers, drill and CPL GEOMETRY'. **RG-P2-2** — the fit table omitted tab pad SIZE: board 1.000x2.700, genuine JLC land 1.210x2.700, clone 1.000x2.500, so the board's retention tab matches the CLONE and 'the board IS the genuine part's land' holds on the eight signal pads only. **RG-P2-3** — §5-0 stated a safety consequence BACKWARDS: a dropped pod does not remove `TEMP_OK`, it ASSERTS it (margin 0.07022 of rail = 231.7 mV at 3.300 V, rail-independent), so the cost is nuisance latched stops, not a defeated interlock. **RG-P2-4** — an H2 'ORDERABLE' heading 693 lines above the section saying it is not. **ALSO FOUND AND FIXED BY THE STAGING RE-MEASURE, BEFORE THE LENS SAW IT:** the archive did not stand alone — `source/fp-lib-table` pointed OUTSIDE the archive (`${KIPRJMOD}/../03_src/lib/…`) and a standalone DRC returned **14 `lib_footprint_issues`**; one URI rewritten -> **0/0/0**. **A REGRESSION against this board's own sealed v1.6**, and a fleet sweep finds **5 of 33** sealed archives with the same defect (cooksense-v1.1, interposer-v1.0, usb-hub-3s-v3 v1.3/v1.4/v1.6) — immutable, recorded, not repaired. **THE INHERITED `0.01 mm` WAS NOT EVIDENCE:** its whole triple is verbatim the GENUINE part's rows in `twin_run.log:440-441`, and no jlc_twin artifact for `C42376901` exists anywhere in `06_build/` — `fit=` prints `0.01` for the genuine part too and cannot discriminate the two. **SIX SKILL PATCHES FILED, `skills/` UNTOUCHED** (`06_build/staging/cooksense-v1.7/verification/owed_skill_patches.md`): P1 + P10 (the two-claims split, gate side and review side), P3 (`--rev` defaults to `dev` and **33 of 33** sealed schematics say so), P4 (A-STOCK has no MOQ term — the lens's sweep proves `C265111` is the ONLY line with MOQ > stock, while `C25076`/`C11702`/`C25105` carry reel MOQs 837/914/887 against needs 10/45/10), P9 (nothing checks an archive can find its own vendored library), P2 (`jlc_twin` emits no parseable verdict line). **REPO SUITE STATE, RE-RUN PER FILE BY ME AND RECORDED BY NAME — NONE OF IT IS THIS BOARD'S AND NONE OF IT BLOCKED THIS PASS:** exit 1 with SEVEN failures — `t1_escape_tier` exit 1 / 2 FAIL (P-LAND known-bad `got 5, want 11`, on `pluto_cal_switch.kicad_pcb`), `t1_layout_precedent` **exit 0** / 1 FAIL (`PREC_GRADED_FLOOR`), `t1_adr_bounds` exit 1 / 2 FAIL (`CITED_FLOOR`, 37 of 38 ADRs OWED fleet-wide), `t1_schema_reader` exit 1 / 2 FAIL (`G-ORPHAN`, 1 orphan = `pluto-rx2-8way-v2/02_parts/RP2040-Zero` `mechanical`). Attribution CHECKED: every failing assertion names its own subject and none names a cooksense artifact, and nothing this pass changed declares a schema key or publishes an ADR bound. **AND ONE OF THE SEVEN IS INVISIBLE TO AN EXIT CODE:** `t1_layout_precedent.py` prints `10 passed, 1 failed` and **EXITS 0**, reproduced twice — the `jlc_twin` exit-0 shape the testing contract forbids. Not fixed here; a diagnosing agent is assigned. Full record: `06_build/staging/cooksense-v1.7/verification/gates_not_run.md`."

updated: 2026-07-30T22:15:00
