# STATUS beacon — cooksense (MAIN board; live head, OVERWRITTEN each transition)

History is append-only in `journal/<stage>_cooksense.md`; this is only the
current frame. Read by `skills/kicad-pcb/scripts/pcb_status.py`, graded against
the tree by `skills/kicad-pcb/scripts/status_beacon_check.py` (canon M-BEACON).

<!-- reader parses from here down -->
stage:   V1.8-2-OF-3-BLOCKERS-CLOSED-BOARD-REBUILD-IN-FLIGHT
step:    "**v1.8 IN PROGRESS — the three v1.7 order-blockers are being fixed in ONE revision so the four-lens battery is spent once.** BLOCKER 1 (SAFETY) LANDED IN SOURCE: J_DOOR.4 DOOR_RAW -> GND (the primary fix, removes the injection path rather than attenuating it), R_DOORPD/R_ESTOPPD 10k -> 470R (C25117, base, stock 1834632 live), new R_DOORS/R_ESTOPS 680R (existing C137948 line), D_DOOR/D_ESTOP moved to the connector-side node. **THE 680R FROM ADR-0018 DOES NOT TRANSFER AND THAT IS THE FINDING**: at 686.8R worst case the injected 2.2k gives 0.791 V against SCLS085L sec.5.5s V_T+(MIN) 0.700 V from the 2.0 V row, the only bound TI publishes that holds at 3.3 V without interpolation. 470R gives 0.591 V (0.608 at the rail ceiling), +92 mV. SCLS085L COMMITTED with sha256 (the pin lens F-EV1). BLOCKER 2 LANDED IN SOURCE: C_EFIN 100nF on 5V_RPP (SLVSE57C sec.10 Fig.67/68, Eq.18) and the keep_short budget re-pointed 5V_SELV -> 5V_RPP plus EN_OVLO_N/ILM/dVdt -> EF_OVLO/EF_ILM/EF_DVDT. RELAY COUNT corrected 13 -> 12 in all three files. NEXT: ADR-0024, E-INV asserts, floorplan anchors for 3 new refs, then BLOCKER 3 (label ownership, needs a floorplan change + re-race)."
measure: "**DRC 0 violations / 0 unconnected / 0 schematic parity** on a full from-source rebuild (`06_build/proof/drc_v21.json`, exit 0). E-INV **151/151** (+11 ADR-0023 coil asserts, RED-verified at 0.714 V and 0.895 V -> 11 FAILs each, restored byte-identical: `06_build/proof/einv_red_verification_coil_margin.txt`); E-ADR **9/9**; audit_board PASS (18 polarity / 28 proximity / 13 edge, I-ISO **6.22 mm**, I-OUT 0.35 mm, 245 silk); placement_gates PASS 0 fails 0 warns (P-OUT 0.30 mm, P-CAP 0.29); count_parity S-COUNT **4/4 over 241**; board_netlist_parity **817/817 nodes identical**; ERC **0 errors**. FAB: export exit 0, **A-ROT OK 208/208**, **F-LEGIBLE OK 59/59** (58 checks on the staged bytes), A-POL single-channel **10 codes** -> order-preview human gate; bom_source_check **PASS** (leg C 27/27 over 59 rows); part_facts **P-FACT OK 5/5 graded, 0 unreached**; rotation table **OK 64 rows**; jlc_twin **209 OK / 471 rows, bodies mounted 208/208**, exit 0; **A-RENDER exit 0 at 15.3961 px/mm — 53 measured / 210, ZERO resolvable-but-unmeasured** (its two FAILs at jlc_twin's own 8.34 px/mm render were extraction artifacts, proven by re-measuring at 1.85x: U_LDO 1.248 -> **0.111 mm**, Q_SWDRVRHA 13 px -> 872 px / 0.086 mm); jlc_stock_check **PASS 56/56**, thinnest line **C2653844 TPS259573DSGR at 103** on a fresh read, and `stock_check.csv` is a REAL map (56 rows, `mpn` column, **zero blank MPNs on coded rows**); waiver_provenance scoped **PASS, 12/12 independently reasoned**; contracts_audit **0 violations** in its graded scope. **THREE GATES ARE NOT GREEN AND EACH IS NAMED WITH ITS NUMBER.** policy_audit **FAIL=1 / WAIVED=6 / PASS=22 / HUMAN=6 / N-A=9**: the FAIL is M-BOM grading SEALED v1.6's BOM against current source (it names C506653 for U_EXP, which is v1.6's code) and resolves when v1.7 becomes the resolved release — TO BE PROVEN AT THE SEAL, NOT PREDICTED. A-POP **FAIL=1 MANIFEST-UNDECLARED** — release paperwork, the 16-ref `not_assembled:` line is computed and waiting. kicad_sch_parity **FAIL 1/169** on the single `('J_KEY_MATRIX','MP')` no-connect, INHERITED (identical 1/161 against sealed v1.6) and dispositioned: those two mechanical tabs are netless BY DESIGN because J_KEY_MATRIX is the only connector on the isolated side of the reed barrier, and they measure **19.407 mm** from the nearest SELV copper against a >= 6.000 mm requirement. **SILK, and naming the emitter as asked: 249 texts, 173 at h 0.600 / stroke 0.150 (all 11 safety texts among them) and 71 at h 0.450 / stroke 0.1125 — the REFDES de-collision emitter's floor at that height.** Also measured: 4 text-to-pad gaps below JLC's published 0.150 mm, three of them 0.0000 (two PINNED captions over `Q_SWDRVA` pads 1/2/3 and over `TP_RKEY.1`). REPRODUCIBILITY, including my own wrong first reading: two exports of the SAME board are **byte-identical on all eleven gerbers** once BOTH timestamps are stripped (I had stripped only `Created by KiCad`, not `%TF.CreationDate`); what is real is that a rebuild with a SILK-ONLY source delta moved copper by **11 D01 vertices on F.Cu and 4 on B.Cu** (In1/In2 order-only; masks, pastes, Edge_Cuts, CPL and BOM identical) — a zone-fill tie-break, so the pipeline is semantically and not byte reproducible."
state:   working
op_pid:  575988 (rebuild_all.sh --reroute, ATTEMPT 3 -> 06_build/rebuild_v18c.log; launched under setsid so a watcher cleanup cannot kill it). ATTEMPT 1 FAILED at stitch 6/8 and the diagnosis is in floorplan.yaml next to the C_EFIN anchor: the race returned unconnected=1 on ALL THREE candidates (5V_PROTECTED at U_EFUSE pad 5 OUT) where the three preceding re-routes were CLEAN 0/0, then the stitch gate REFUSED 2 seed stubs (R_AND1PD.2, C_LATCHB.1) against `gate: clean` on every prior run -- C_EFIN seeded due WEST of the eFuse IN pads forced 5V_RPP out east on B.Cu along y=58.000, through the only lane pad 5 has. ATTEMPT 2 was KILLED MID-SILK-PASS BY MY OWN WATCHER CLEANUP, not by a defect, so it carries no verdict. DO NOT poll with `pgrep -f rebuild_all` -- an until-loop whose own command line contains that string matches ITSELF and never exits; use `while kill -0 <pid>`.

next:    "**DO NOT SEAL. The next revision is a PLACEMENT change, not a paperwork pass.** (1) **THE P0: give the SE-corner connectors owned identity.** The four `SM05B-GHS-TB` headers are mutually cross-mateable and their designators are tied or stolen; `J_ISOLOOP`'s designator sits on `J_RH_EXHAUST`. Space has to be created, so this is a floorplan edit -> re-race -> full re-gate, and the four-lens battery must then be run AGAIN on the moved board (that is the whole reason it was not spent early here). Consider whether the honest fix is partly a PART change: four identical 5-pin GH headers on one board, two of them safety inputs, is the defect underneath the silk. (2) **7 topology P1s, each with its arithmetic**: `D_ESD_IN` (V_BR 5.5 V min) is upstream of F1 and is the only clamp left in circuit once the eFuse opens (630 mW at 9 V, 1.56 W at 12 V); the 3V3 rail declares `iout_max_A: 0.3` for LOGIC ONLY while its own declared children draw 0.45 A from it, so at the true 0.75 A **PD = 1.537 W against a 1.200 W budget (128%)** and dropout headroom **FAILS by 24 mV** — I re-derived both from the file, and E-TOPO reports PASS on the understated denominator, which makes it a GATE defect as well as a rail defect; the contactor opto delivers ~2.4 mA against a claimed 50 mA and the 30 V inductive loop has no snubber; the brief's Ioff buffers and 22-100 ohm series resistors DO NOT EXIST on the BOM while ARCHITECTURE.md says they do; the TPS3823 watchdog is 0.9/1.6/2.5 s fixed against a commissioned 300-500 ms with no ADR; door EOL supervision is unimplementable as built (`J_DOOR` pins 2 and 4 are ONE net, and the receiver is a single HC14 threshold, not a window); the external I2C runs have no ESD and no series damping. (3) **Two evidence-grade FAILs from the pin lens, both mine to fix**: 26 of 54 dossiers point at NO committed datasheet PDF (and `SN74LVC1G00DCKR`'s `doc_id` SCES214 contradicts the real SCES212AB, which the same file's `layout.source` gets right); and **`pin_audit.py` silently emits content-free dossiers for 16 of 54 parts including ALL TWELVE RELAYS** — the one land pattern whose predecessor was drawn against the wrong sub-figure is exactly the one the dossier generator blanks. That is a CHECKER defect and is reported upstream. (4) The pin lens's CONNECTOR group is still owed and has been requested. (5) Then the cheap items in the same pass: the two pinned captions off the pads; the LDO tab's **13.96 mm2** against the ~645 mm2 its 55-65 C/W wants; the 2N7002 datasheet DEVIATION declared (LCSC serves HTML, not the PDF — K_STOP's margin is +0.454 V at the estimated 0.10 V V_DS and still **+0.054 V** at 0.50 V, re-derived from 5V_STOP vout_min 4.754); the west-end comb slot's **1.00 mm web** as a JLC DFM query (their own Q&A asking the remaining-wall minimum is UNANSWERED). CARRIED AND CLOSED THIS PASS, do not re-open: the comb slots (now 1.000 mm, measured free), the coil-margin gate (E-INV 151/151), `J_PI`/`J_LOADCELL` (already `process_incompatible` in assembly.yaml with measured evidence, correctly OFF the CPL, and their MANIFEST line is computed), and the `GND_ISO` ghost in both the silk and the padmap."

updated: 2026-07-29T18:58:18
