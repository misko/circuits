# STATUS beacon — cooksense (MAIN board; live head, OVERWRITTEN each transition)

History is append-only in `journal/<stage>_cooksense.md`; this is only the
current frame. Read by `skills/kicad-pcb/scripts/pcb_status.py`, graded against
the tree by `skills/kicad-pcb/scripts/status_beacon_check.py` (canon M-BEACON).

<!-- reader parses from here down -->
stage:   routing
step:    "v1.7 ELECTRICAL REVISION — REROUTE #3 RUNNING, closing the last two plane pads. Reroute #2 gave 0 violations / 3 unconnected / 0 parity. C_WD.1 (3V3) took a deterministic seed-stub bond; R_WDOKPD.2 and R_OVB.2 (both GND) REFUSED theirs — `via collides foreign copper`, the stitch pass refusing rather than guessing, which means the ROUTER had already crossed the only on-pad site. That can only be fixed at PREP, so both now carry a +-0.55mm User.2 reservation rect (the third-wave pattern) and the route is being redone. A-ROT for the new LCSC C485354 is MEASURED: offset 180, two-channel (PAD-NUMBER rms 0.0000 vs 2.3717 next; numbering-free PAD-CLOUD 0.0707 vs 2.2395) -> CPL rotation for J_MODE = (90+180) = 270. NOTE THE DISSENT: the PIN-1-MARK sub-channel points at 270 offset (0.9463mm) rather than 180, so J_MODE should go on the JLC order-preview HUMAN GATE regardless of the two-channel verdict — a 180 error on this housing solders perfectly and reverses the harness, which is exactly how interposer v1.0 shipped DO-NOT-ORDER."
measure: "DRC --severity-all --refill-zones --schematic-parity: violations 0 / unconnected 3 / parity 0 on the pre-stub board. Race: 3/3 candidates unconnected=1 violations=0. Placement 235 parts / 107 anchored / 95 legalized, P-COLLIDE 0/0. Stitch: 315 vias emitted, 29 dangling pruned, post-refill prune 0. generate_rules LAST re-applied 8 netclasses + 40 patterns + 9 width rules; apply_drc_policy applied min_resolved_spokes=1 + the keypad_isolation_6mm / opto_isolation_2mm DRU rules. Schematic gate carried: E-INV 109/109 (RED-VERIFIED x4), net_label_survival 162/162 over 192 nets, ERC 0 err / 411 warn, circuit.json == manifest 235/235, placement_gates PASS, audit_board PASS. contracts_audit 243 files / 0 violations. JLC model for the NEW LCSC C485354 fetched into 06_build/twin/easyeda (CONN-SMD_ZH1.5-WS-4P) so A-ROT can be MEASURED."
state:   working
next:    "KRT race-3 reroute -> import -> stitch/fill -> generate_rules LAST -> DRC --severity-all --refill-zones --schematic-parity 0/0/0. Then promote the winning chain to 03_src/cooksense/route/final_chain.kicad_pcb and commit it. Then A-ROT MEASURE for the NEW LCSC C485354 (no per-LCSC row exists; export_jlc_package exits 2 without one), jlc_twin, stock, F-LEGIBLE, A-POP/A-POS/A-BODY, part_facts, policy_audit, the scoped review lenses, then seal cooksense-v1.7 + SUPERSEDED.md on v1.6."
op_pid:  1261532 (rebuild_all.sh --reroute #3; log scratchpad/reroute4.log)
updated: 2026-07-28T13:20:00
