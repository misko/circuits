# STATUS beacon — cooksense (MAIN board; live head, OVERWRITTEN each transition)

History is append-only in `journal/<stage>_cooksense.md`; this is only the
current frame. Read by `skills/kicad-pcb/scripts/pcb_status.py`, graded against
the tree by `skills/kicad-pcb/scripts/status_beacon_check.py` (canon M-BEACON).

<!-- reader parses from here down -->
stage:   verify
step:    "v1.7 NOT SEALED — BLOCKED ON TWO CONFIRMED P0s FROM THE REVIEW BATTERY. The LIVE sealed release is still cooksense-v1.6-2026-07-27 and its fab set is unchanged and still orderable. The complete pre-seal staging archive sits at `06_build/tmp/cooksense-v1.7-BLOCKED-2026-07-28/` and NOT under 07_releases/, deliberately: a directory under 07_releases/ IS a release to release_index.py, and leaving it there made t1_fleet_regrade and t1_release_index FAIL for the WHOLE REPO (it became 'the LIVE release', then demanded a SUPERSEDED.md on the release it had not superseded). 06_build is gitignored and disposable, so the PERMANENT record is what is committed: the four review verdicts verbatim in 08_reviews/, the DISPOSITIONS ledger, the CHANGELOG entry, this beacon, the journal, and 03_src + 04_kicad. The archive regenerates from those in one export run. Everything up to the review gate is DONE and committed: A-ROT cleared (C485354 J_MODE resolves 90+180 -> CPL 270 from the landed measured row), fab package exported EXIT=0, the whole fab/assembly battery green, all four review lenses run CONCURRENTLY against the staging archive with curated independent input, all four verdicts archived VERBATIM in 08_reviews/ + verification/, every finding dispositioned with the lead's own re-derivation. ALSO LANDED THIS PASS: the board silk revision string, which read `sidecar v1.3` and was OWED a bump because v1.7 moves copper (v1.4/v1.5/v1.6 were docs-only over a v1.3-identical board, so it was honest until now) — bumped at source and REBUILT, and the rebuild is a copper IDENTITY."
measure: "BLOCKERS, both CONFIRMED by the lead from primary sources, both ESCALATED not applied. **P0-A (found INDEPENDENTLY by BOTH red-team lenses): the eFuse OV cutoff is at 9.200 V nominal / 8.492-9.933 V worst case.** R_OVT 100k / R_OVB 15k = ratio 0.130435 against SLVSE57C's V_OVLO(R) 1.13/1.20/1.27 V (02_parts/TPS259573DSGR/SLVSE57C.pdf, EC table 'ENABLE / OVERVOLTAGE LOCKOUT (EN/OVLO) - TPS2595x3'). Three documents say the intent is 5.5-6 V. 5V_PROTECTED feeds 13 DIP05-1A72-12L reed coils rated **7.5 V max** and D_TVS SMBJ5.0A whose V_BR starts at **6.40 V**. BOTH lenses proposed a fix and NEITHER is correct: R_OVB 22k breaks the TVS constraint (top corner 7.159 V), R_OVT 57.6k nuisance-trips at the declared vin_max 5.5 V (V_pin 1.1545 > 1.13). At vin_max 5.5 the admissible ratio window is 1.0354x wide against a 1.0404x +-1% spread -- NO +-1% DIVIDER FITS. Needs a supply-tolerance decision, a higher-standoff TVS, or 0.5% parts. **P0-B (topology lens): one I2C write defeats the watchdog in BOTH chains AND the reset ADR-0020 just added.** WD_OK carries U_EXP.8 (GPB7, BIDIRECTIONAL, 25 mA) alongside U_EXP.18 (RESET_N) and U_WD.1 (specified to 1.2 mA). IODIRB.7=0 + OLATB.7=1 forces it high; recovery needs <0.66 V so the contention is self-sustaining. v1.6 had U_EXP.18 on EXP_RST_N -- ADR-0020 moved it onto WD_OK, so ADR-0020 Decision B's own claim is falsified in its own case. Fix is ONE 0402 on an existing line: 10k (C60490) in series to U_EXP.8 ONLY. GATES, all green and all recorded, which is the point: DRC --severity-all --refill-zones --schematic-parity 0/0/0; ERC 0 errors/411 warnings; E-INV 109/109 RED-VERIFIED with 4 NEW mutations one per new invariant family, each restored byte-identically; E-ADR 8/8; S-COUNT 4/4 over 235 refdes (shadow root); net_label_survival 162/162; A-ROT 202/202; A-POS worst 0.00000mm; A-POP 239 board/202 CPL/37 unpopulated; A-BODY 204/204; A-RENDER OK 53 measurable bodies within 1.00mm; A-STOCK PASS 55/55; F-LEGIBLE OK 0 findings; M-BOM PASS; P-FACT OK; jlc_twin exit 0; audit_board PASS (I-ISO 6.12mm); placement_gates PASS. policy_audit FAIL=3: E-TOPO (deliberate, ORDER_README section 0, unchanged) + M-REL/A-POP (both only because MANIFEST is deliberately unwritten). Copper delta v1.6->v1.7 MEASURED: footprints 226->239, tracks 3925->4166, vias 1047->1104, GND F.Cu +241.135 mm2, total track length -56.154 mm. Silk-bump rebuild identity: tracks 16d81d6b2d1634d6, vias 5cc95d962b455a39, footprints a4cfa2956c816c70 -- all three byte-identical by VALUE before and after, DRC still 0/0/0."
state:   blocked
next:    "DO NOT SEAL. The two P0s need a USER/coordinator decision, not an agent patch -- P0-A's correct value depends on a supply tolerance the BRIEF has never stated (BRIEF.md:77 '  5V SELV >=2A' with no tolerance, which is the SAME root cause as the E-TOPO dropout gap), and both are electrical setpoint changes on protection devices (the R_OPENT precedent: escalate, do not silently fix). WHEN APPROVED, one respin closes both P0s plus nine P1s with ZERO new BOM lines except the OVLO leg: (1) R_OVT/R_OVB per the supply decision + part_value asserts on BOTH; (2) 10k in series to U_EXP.8 only; (3) 680R into U_SCHM.1/.11/.5 and a PESD5V0S1BA on MODE_RAW -- but NEVER 680R on J_MODE.3, which would give 1.543V and brick the coil rail (use <=47R); (4) 100k->10k on the four expander-readback permissions (1.650V is indeterminate against a GPPU write); (5) 2.2k in series with the 3V3 feed to J_MODE.1 (a same-connector pin1->pin4 short still arms the rail); (6) R_PG.2 off 5V_PROTECTED onto 3V3; (7) a 0.1uF at U_EFUSE.3/4; (8) move the J_MODE and D_COILEN refdes OUT of the east-edge notch -- they currently print into a milled slot and will not exist on the board; (9) re-centre R_TH2 for the as-built 22k-clamped thermistor chain (trip is 72.81C, not the published 74.89C, and BOTH edges of the 70/75C brief window are breached). THEN re-run the whole battery -- a respin invalidates every gate above. ALSO OWED TO skills/, all reported not applied: pin_audit.py:130's literal MPN->dir join (16 of 54 dossiers blank, INCLUDING U_EXP); jlc_twin.py PAD_GEOM_TOL knife edge (identical parts split on a float's last bit) and marker_side()'s derotated-pads-vs-board-frame-graphics bug (same class as 772b152, still live); A-RENDER's absolute 20px floor against a hard-coded 1600x1000 render; count_parity.py's first-glob multi-board mis-targeting; jlc_twin --adjudications on a missing path running silently with zero adjudications; and P-ADJ needing a per-INSTANCE budget form (25 of 37 keep_short budgets name nets that do not exist here)."
op_pid:
updated: 2026-07-28T15:05:00
