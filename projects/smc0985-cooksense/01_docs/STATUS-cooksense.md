board: cooksense
stage: placement
state: blocked
git_sha: 0c89ffe
measure: THREE P0-class defects found in the v1.3 placement by running the DRC nobody had run. kicad-cli DRC on the committed 04_kicad board (95db1d2) classifies 112 violations / 423 unconnected / 0 parity as: 6 shorting_items + 6 solder_mask_bridge + 2 courtyards_overlap + 2 pth_inside_courtyard + 2 clearance(0.0576mm) + 1 invalid_outline + 79 cosmetic silk/text + 12 starved_thermal + 2 silk_edge_clearance. Board is track-free, so 423 unconnected is expected. After the two fixes landed here, an equivalent PROOF build (06_build/proof/cooksense_p0proof.kicad_pcb, J_ESTOPLOOP parked off the east edge purely to let the build complete) has 0 shorting_items / 0 courtyards_overlap / 0 pth_inside_courtyard / 0 invalid_outline, and generate_board reports "P-COLLIDE: 0 pad shorts, 0 anchored courtyard overlaps (795 copper pads, 80 anchored parts)". refdes on silk improved 215/223 -> 217/223 (R_SER3/R_SER7 recovered when U_COMP2 moved).
p0_open: ONE, and it needs a DECISION, not a nudge. **P0-A: J_ESTOPLOOP is anchored INSIDE J_DOOR.** [196.0,84.0,90] vs [197.0,84.0,90] — the OPTO-ISOLATED 30V contactor loop is shorted to 3V3, GND and DOOR_RAW (4 kicad shorting_items; pad copper overlapping 1.300 x 0.600 mm; nearest non-overlapping ISO-to-SELV approach 0.044mm CONTACTOR_LOOP<->DOOR_RAW and 0.047mm CONTACTOR_LOOP<->GND; 12 ISO/SELV pad pairs sit under the ISO_CONTACTOR rule's 2.0mm). The connector added in v1.3 to FIX the opto-isolation P0-2 created a worse version of it. THE EAST EDGE HAS NO ROOM: measured budget x[193.7,200.2], available y 54.605 (H4 courtyard) to 101.845 (south edge minus pad) = 47.240mm; needed J_MODE 10.79 + J_ESTOP 10.79 + J_DOOR 10.79 + J_ESTOPLOOP 7.69 + J_CONTACTOR 7.69 = 47.750mm of courtyard PLUS a 0.660mm courtyard gap to buy the 2.0mm copper moat = 48.410mm. SHORT BY 1.170mm with ZERO body gap between five connectors, so no reshuffle of that column works. The south edge is also full (only free x-intervals >3mm are [19.5,24.7], [39.3,46.5], [83.9,92.2]). Three real options, all changing an external interface: (a) move a SELV connector (J_DOOR or J_MODE) off the east edge; (b) MERGE J_ESTOPLOOP+J_CONTACTOR into ONE isolated 4-way terminal block — isolation-neutral or better (one isolated housing, zero SELV neighbours) and the budget then fits at ~45.4mm, but it is a schematic/netlist/BOM change; (c) grow the board. NOT MY CALL — same class as the H4 decision.
p0_fixed_here: (1) **P0-B U_COMP2 anchored ON TOP OF Q_SWA** — [30.0,88.0,0] byte-identical to Q_SWA's, so the SOIC-8 swallowed the SOT-23: TH_CAM_A shorted to SWG_A, TCAM_OPEN shorted to 3V3 (pad copper overlap 0.175 x 0.285 mm, 2 shorting_items + 2 clearance at 0.0576mm). FIXED: U_COMP2 -> [24.0, 86.5, 0], directly south of U_COMP as its own comment always asked. MEASURED clearances to the FIXED neighbours: H3 courtyard 0.710mm W, Q_SWA 0.280mm E, U_COMP 1.010mm N, J_THERM_A 4.510mm S. (2) **P0-C the H4 notch produced an INVALID BOARD OUTLINE** — generate_board emitted every `board.cutouts` rect as a CLOSED rectangle, correct for an internal slot but not for the edge-reaching H4 notch (x[191.50,200.10] against a board edge at x=200.0): the east edge segment ran straight through it, so Edge.Cuts self-intersected and kicad reported invalid_outline "malformed outline (self-intersecting) (gap 38.7000 mm)". FIXED in generate_board_generic.py: an edge-reaching cutout now SPLITS the side it crosses and its three inner sides join the boundary. KiCad heals the old geometry to the same polygon, so the predecessor's clearance figures stand — but the raw outline was invalid and the DRC gate could never have reached 0.
root_cause: generate_board_generic.py legalized 108 FLOATING parts and never collision-tested the 80 ANCHORED ones against each other. Two anchors at the same coordinate placed one footprint inside the other, printed "placed 223 footprints", and exited 0. DRC does catch it, but nothing forced DRC to run — the predecessor's own 06_build/policy_audit.md recorded "R-DRC FAIL 87 violations" and the board was committed anyway.
new_gates: **P-COLLIDE** (design-policies.md P11) in generate_board_generic.py, after legalize: no two different-net pads may have overlapping copper, and anchored courtyard overlap WARNS (CALIBRATED: fatal-on-both refused usb-hub-3s-v2 RS3/Q6 0.470x3.950mm and the interposer TP array 18x 0.053x2.592mm — real overlaps with NO short, already owned by P1/P-CRT at DRC). Findings name both refs, both nets and the measured overlap. RED-VERIFIED — it reproduces all 6 kicad shorting_items by an independent method (canon M1) and refuses the build. NOTE the overlap test is a true segment-crossing + containment test: a nearest-point metric called the J_ESTOPLOOP/J_DOOR short "+0.250mm clear", because two rectangles crossing in an X have all four endpoint-to-edge distances positive. Tests: tests/t1_generate_board.py, 35 passed / 0 failed, 19 known-bad (8 new: t_collide_clean, t_kb_anchor_collision, t_collide_names_the_nets, t_edge_notch_outline, t_internal_cutout_still_island, t_kb_corner_cutout, t_kb_severing_cutout, t_collide_pinned_lap_warns). FLEET-MEASURED after calibration: all 7 other boards + the interposer generate, and cooksense v1.3 is the ONLY board in the repo with a placement pad short.
ihw: LANDED and RED-VERIFIED. audit_board.py --ihw models each fastener as a 3.0mm-radius conductive disc and measures the SURFACE PATH around outline cutouts, not a straight line — a straight-line metric measures the pre-notch (3f781da) and notched boards IDENTICALLY at H4 a=4.031mm, so it cannot see the notch and would fail the board the notch fixes. MEASURED current board: H1 a=2.305 s=13.631 -> 15.936 PASS; H2 a=3.129 s=13.000 -> 16.129 PASS; H3 a=40.933 s=-1.450 -> a alone PASS; H4 a=6.598 s=-1.450 -> a alone PASS. Pre-notch board exits 1 at H4 a=4.031 < 6.000. Pairing branch (ENCLOSURE_BONDS_HOLES=True) FAILS the current board at 2.305mm. **H4 IS 6.598mm, NOT THE 8.500 RECORDED AT 95db1d2** — that figure does not reproduce and no geometry recovers it. Verdict direction unchanged (PASS) but the real margin is 0.598mm, so H4 is the TIGHT hole: nothing may grow keypad copper near it or shrink the notch. ADR-0012 + ORDER_README corrected.
enclosure_assumption: NOW IN THE BYTES, not just a commit body — ADR-0012 (01_docs/decisions/0012-non-conductive-enclosure-is-load-bearing.md) with the measured per-hole table; SILK on the board (two caption lines in the north band + "NYLON HW" beside all four holes, all six MEASURED collision-free, crowded-caption count unchanged at the pre-existing 2); the ORDER_README mechanical section (06_build/verification/ORDER_README.md, rewritten to v1.3); and audit_board.py check I-HW, which encodes BOTH the per-hole and the pairing rule with the enclosure assumption as the switch — flip it and the board FAILS. Do NOT add the H3/H4 pour pullback: under the per-hole rule the fastener there is SELV-bonded so the requirement is `a` alone, and the pullback costs copper on three layers for zero margin.
route_unblocked: route.yaml could not even PREP. CONTACTOR_C sat in the `safety` wave at track_width 0.2 while the v1.3 ISO_CONTACTOR netclass declares min_width 0.3, so the netclass-floor validator killed the run before one KRT cycle. FIXED: the three isolated nets moved to their own `iso` group + wave at 0.3, routed FIRST. Raising `safety` to 0.3 would have widened 19 SELV logic nets through the densest escape field to fix three nets in a corner.
next: 1) COORDINATOR/USER decides P0-A (option a/b/c above); 2) then generate_board (P-COLLIDE must print 0/0) -> generate_rules BEFORE route (canon R1) -> full re-race -> stitch -> generate_rules LAST -> apply_drc_policy -> DRC 0/0/0 -> M-REPRO; 3) the ISO_CONTACTOR 2.0mm moat still needs a POUR KEEPOUT around whatever corner the isolated connectors end up in — it cannot be written until P0-A is decided, and the router cannot supply it; 4) fab export (expect 0 ROT-DB-SUGGEST), A-POP/A-STOCK/M-BOM/bom_source_check/policy_audit/contracts_audit; 5) both red-team lenses + a fresh zero-context lens over the STAGED archive; 6) 2-commit seal + SUPERSEDED.md on v1.1.
do_not: seal (P0-A open); regenerate 04_kicad (P-COLLIDE correctly refuses — 04_kicad is at 95db1d2 and is DEFECTIVE, treat it as stale, do not hand-edit it); add the H3/H4 pour pullback; assert the LMV393 is rail-to-rail on the input (VICR ceiling is VCC-0.7); delete R_CLMPA/R_CLMPB; normalise R_WDPETPD back to 100k (1k IS the fix); export a CPL or add rotation rows (the coordinator's operator-disagreement investigation on C125121/C189896/C2683602 is still open); trust a nearest-point distance as an overlap test.
top_v13_gate_item: E-INV `part_value` kind (an invariant pinning EXISTENCE does not pin VALUE — all three WD_PET assertions PASS on a 100k board). Still NOT implemented; when it lands, assert R_WDPETPD = 1k and R_OS = 510k.
