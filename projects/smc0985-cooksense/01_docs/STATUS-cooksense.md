# STATUS beacon — cooksense (MAIN board; live head, OVERWRITTEN each transition)

History is append-only in `journal/<stage>_cooksense.md`; this is only the
current frame. Read by `skills/kicad-pcb/scripts/pcb_status.py`, graded against
the tree by `skills/kicad-pcb/scripts/status_beacon_check.py` (canon M-BEACON).

<!-- reader parses from here down -->
stage:   seal
step:    "cooksense-v1.6-2026-07-27 is SEALED and LIVE — the only cooksense release with no SUPERSEDED.md. IT IS A DOCUMENTATION-ONLY SUPERSEDE OF v1.5 AND REGENERATES NOTHING: fab/ (19 files), source/ (11), 3d/ (2) and pdf/ (3) are BYTE-FOR-BYTE v1.5's — 35 files, 0 differing, 0 added, 0 missing — asserted by release_freshness_check.py --docs-only-supersede, so freshness_exceptions.txt lists ZERO paths. source/cooksense.kicad_pcb is still md5 420445b5141dd1111eccab038c68511b, the same file v1.3/v1.4/v1.5 and 04_kicad/ carry, and the F.Silkscreen still reads `cooksense SMC0985KS sidecar v1.3`. IF YOU HOLD v1.5, ITS FAB SET IS THESE BYTES — order it; what v1.6 replaces is ORDER_README.md. v1.6 closes the three P1s the 2026-07-27 adversarial audit left open (A12 was closed without a reseal in 539ecf0), each RE-VERIFIED against the archive's own source/cooksense.net by an independent s-expression set query, and TWO of the three re-verifications DISAGREED with the audit in detail. A1: ORDER_README section 10 said the unkeyed 5-pin JST-GH family is three connectors and that any single cross-plug is fail-safe — THERE ARE FIVE IDENTICAL C189896 HOUSINGS and an SHT45 pod harness in J_MODE ENERGISES THE RELAY COIL RAIL with all seven AND-chain terms AND the Manual rail-cut bypassed (COIL_EN = 3 nodes, sole hold R_COILENPD 100k, no ESD, no series R; pod 10k pull-up -> 3.000 V on the 2N7002 gate vs 2.5 V max VGS(th)); the claim is WITHDRAWN and section 10 now carries all five housings and a 20-cell matrix. A2: 11 of the 18 safety-chain nets carry NO restrictive default, four of them permissions — but the tsx's 'the other twelve are pulled restrictive' is wrong in SCOPE, not arithmetic (those twelve are BRIEF D10's Pi/expander authorization lines and all twelve ARE pulled restrictive); NEW: one MCP23017 GPPUB write makes the float DETERMINISTICALLY permissive. A3: REARM_N held low forces the NAND latch's forbidden state, and NEW — EXP_RST_N has no driver, so the defeat survives every Pi reboot. All three HARDWARE fixes are copper and are USER DECISIONS for the next electrical revision."
measure: "measured on THIS release's artifacts at seal, 2026-07-27. Payload identity: fab/ + source/ + 3d/ + pdf/ = 35 files byte-identical to cooksense-v1.5-2026-07-27, 0 differing / 0 added / 0 missing by directory sha256 in BOTH directions, ASSERTED by release_freshness_check.py --docs-only-supersede (PASS, zero exceptions). Carried and still valid because the board did not move: DRC --severity-all --refill-zones --schematic-parity 0/0/0; ERC 0 errors / 1303 warnings; A-ROT 189/189 sourced; A-POS 189/189 on datum, worst 0.00000 mm; A-POP 226 board / 189 CPL / 37 unpopulated; A-BODY 189/189; M-BOM PASS; F-LEGIBLE OK 0 findings / 56 checks; A-STOCK 54 coded lines, 53 clear at 5 boards, the one failure C42400616 unplaced by design; jlc_twin 190 OK / 431 rows adjudicated. RE-RUN FOR v1.6: E-INV 85/85 (was 83/83 — two part_value asserts now pin R_COILENPD = 100k and R_REARMPU = 100k, the two numbers ORDER_README section 10.2's R <= 175 kOhm cross-plug bound and section 7a-1's power-up-forced-SET property are COMPUTED FROM; RED-VERIFIED, substituting 10k gives E-INV FAIL: 2/85); A-EVID 32 required present, 0 missing; policy_audit --board cooksense FAIL=1 HUMAN=6 N-A=4 PASS=25 WAIVED=4. Board 188 x 92 mm, 4 layer, 222 components + 4 holes, 3925 tracks / 1047 vias, all 1047 at 0.25/0.15 (ADVANCED tier REQUIRED)."
state:   done
next:    "TWO THINGS ARE OWED, AND BOTH ARE USER DECISIONS. (1) AN ELECTRICAL REVISION IS NOW JUSTIFIED BY THREE FINDINGS, none of which a document can fix: a keyed or different housing for J_MODE (or COIL_EN moved off a field connector entirely — ORDER_README section 10.6 measures that the obvious R_COILENPD 100k->10k trim is NOT sufficient on its own, since 3.3*10/20 = 1.65 V still turns the FET on); four 0402 pull-downs on WD_OK / ESTOP_OK / MODE_AUTO_HW / DOOR_OK (eleven for the whole chain); and an edge-detect or one-shot on REARM_N so that only a TRANSITION clears the fault latch. Until then all three are mitigated by DISCIPLINE ONLY — ORDER_README sections 10.5, 7a-1 and 7a-2 — and 03_tscircuit/src/cooksense.tsx:551 still carries the falsified 'the other twelve are pulled restrictive' clause, deliberately: that file is inside the docs-only byte-identity set, and its correction is OWED to the same revision that adds the pull-downs. (2) THE ONE GATE THAT DOES NOT PASS, unchanged from v1.5 and a SUPPLY SPECIFICATION rather than a copper defect: E-TOPO. The AMS1117-3.3 3V3 rail has headroom 1101 mV (Vin_min 4.5 - Vout_max 3.399) against a 1300 mV dropout -> FAIL, short 199 mV. Both numbers CITED to AMS ds1117; the 0.3 A dropout is OWED (no datasheet figure at this load) and vin_min was left at 4.5 rather than raised to the 4.75 that makes the gate pass. TO RETIRE IT: measure VIN-VOUT at U_LDO at IOUT 0.3 A at bring-up and write it into 02_parts/AMS1117-3.3/part.yaml as dropout_mv (MEASURED). TO ORDER SAFELY MEANWHILE: buy a supply holding >= 4.85 V at J_PWR under load. ALSO ORDER-DAY, unchanged: the H4 REQUIRED FASTENER SPEC (max conductive OD 6.0 mm, HARD LIMIT 6.3 mm); the JLC order-preview rotation gate plus the three POLARITY-FIT-BLIND diodes; the 16 hand-solder / not_assembled refs including J_ISOLOOP (C42400616 stockCount 0 at JLC — a 2EDG-family substitute would be a FOOTPRINT CHANGE and would void every review verdict this board carries); the first-power ritual."
op_pid:
updated: 2026-07-27T23:59:00
