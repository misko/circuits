# STATUS beacon — cooksense (MAIN board; live head, OVERWRITTEN each transition)

History is append-only in `journal/<stage>_cooksense.md`; this is only the
current frame. Read by `skills/kicad-pcb/scripts/pcb_status.py`, graded against
the tree by `skills/kicad-pcb/scripts/status_beacon_check.py` (canon M-BEACON).

<!-- reader parses from here down -->
stage:   V1.7-CANDIDATE--NOT-SEALED--TENTH-DECLINE--THE-TWO-P0s-CLOSED-AND-A-THIRD-OPENED-ON-THE-SAME-RAIL
step:    "**THE TWO DESIGN P0s ARE CLOSED AND INDEPENDENTLY CONFIRMED CLOSED — AND THE SEAL STILL DID NOT HAPPEN, BECAUSE THE FRESH TOPOLOGY LENS FOUND A THIRD P0 ON THE OTHER HALF OF THE SAME RAIL.** Both lenses were re-gated fresh-context, neither handed a conclusion. **LAYOUT: `design_verdict: SOUND` / `order_verdict: BLOCKED-SOURCING`, ZERO P0s** — it re-derived the P0-2 closure independently instead of reading it, building its OWN lumped thermal model of the measured mounting (the two 0.150 mm in-pad vias in parallel with 0.2104 mm of dielectric under 7.3654 mm2 of tab) and getting **theta_JA ~ 87 C/W**, which makes the declared 90 C/W conservative. **TOPOLOGY: `design_verdict: DEFECTIVE` / `order_verdict: BLOCKED-SOURCING`, ONE P0** — and it is the SAME DEFECT CLASS as P0-1, a load-bearing sum with a term missing, on the DROPOUT half instead of the DISSIPATION half. **NOT SEALED. `07_releases/` UNTOUCHED, still ends at cooksense-v1.6-2026-07-27, still DO-NOT-ORDER. There is still no orderable cooksense board.** Source work IS committed — the fixes are real and must not be re-derived."
measure: "**THE NEW P0, RE-MEASURED BY ME BEFORE I ACCEPTED IT.** `power_tree.yaml` derives `vin_min: 4.754` from THREE NAMED COMPONENT resistances (F1 70.0 + Q_REV 73.5 + eFuse 47.0 = 190.5 mOhm) and counts **NONE of the board's own copper**. The lens measured the routed path `J_PWR.1 -> F1 -> Q_REV -> U_EFUSE -> U_LDO.3` at **109.0 mOhm** (25.87 + 7.06 + 9.00 + 67.09, Dijkstra min-resistance walk, 35 um at 70 C) — a **57 % underestimate** of the series resistance in the term the ENTIRE v1.7 supply specification (ADR-0021) exists to produce — and re-derived headroom **+24.0 mV, not the declared +55 mV**, with the file's own claimed-still-passing 0.60 A corner at **-0.8 mV, FAIL**, break-even board current 0.597 A. **I REPRODUCED THE ORDER OF MAGNITUDE INDEPENDENTLY** with my own per-net series-sum upper bounds at 35 um / 70 C: `5V_IN` **26.07 mOhm** over 22.120 mm at 0.500 mm (the lens's 25.87 for that segment, agreeing to 0.8 %), `5V_FUSED` 16.23, `5V_RPP` 28.27, `5V_PROTECTED` **306.63 mOhm** over 260.059 mm — every per-segment figure the lens reports falls inside its own net's bound. **AND THE ENABLING FACT IS ONE I MEASURED EARLIER FOR A DIFFERENT REASON: NO 5V NET HAS A ZONE ANYWHERE ON THIS BOARD.** The only four filled zones are GND on F.Cu / B.Cu / In1.Cu and 3V3 on In2.Cu. Tracks are the whole conductor on the 5 V path. **WHAT IS CLOSED, AND CONFIRMED CLOSED BY THE LAYOUT LENS:** `rails[].iout_max_A` is now an ITEMISED SUM over five loads at datasheet maxima (on-board logic 4.804 mA + `3V3_ANALOG` 4.214 + off-board 9.572 + **the four switched sensor rails 85.116** = 103.706 mA), and `rails[].pdiss_max_mw` is DERIVED at a declared ambient — `(125-75)/90 x 1000 - 5.250 x 11 = 497.8` — from ds1117's OWN formula, its OWN ABS-MAX theta_JA 90 C/W and its OWN Tj_max 125 C, at BRIEF.md line 117's HARD enclosure limit. E-TOPO: **PD 410 mW vs 497 mW = 82 %, RAW EXIT 0**; Tj 117.1 C, 7.9 C of margin. **THE THIRD UNDECLARED LOAD I FOUND BY ASKING THE QUESTION: the SHT4x on-package HEATER, IDD max 100 mA at its 200 mW level**, into a condensing exhaust duct the brief itself commissions — budgeted at Sensirion's own <=10 % duty limit. **AND THE LENS CAUGHT ME UNDER-DECLARING TOO, WHICH IS EXACTLY WHY IT WAS ASKED:** `R_ESTOPPD` is **470 ohm** on the staged BOM, not the 100k my first draft assumed — a **213x** error worth 7.232 mA, flowing whenever the ADR-0025 shorting plug is fitted, which is always; `R_MODEPD` is 10k not 100k; three of the twelve `3V3` pullups are 10k not 100k. All re-read off the BOM and corrected. The lens's own independent recount is 115.5 mA against my 103.706, so **0.200 A is declared** — the next admissible step above BOTH counts — and **the 11.8 mA delta is OWED, not closed**. **EVERY OTHER GATE RE-MEASURED BY ME UNPIPED, RAW EXIT CODES:** board md5 `9f4fd5fae810f40a52b1035df727243c` live == archive, NO COPPER MOVED; DRC `--severity-all --refill-zones --schematic-parity` **0/0/0 EXIT 0** and **0/0/0 EXIT 0 again on `source/` copied OUTSIDE the repo**; ERC **EXIT 0, 411 violations ALL `warning`, 0 errors**; policy_audit **EXIT 0 FAIL=0** (PASS=28 WAIVED=6 HUMAN=6 N-A=5); S-COUNT 4/4 over 239 refdes; E-INV 167/167; E-ADR 11/11; M-BOM PASS; F-LEGIBLE 60; P-FACT 6/6; M-DEPEND PASS; M4 PASS; placement_gates PASS; audit_board PASS; A-ROT/A-POL 64 rows; A-POP PASS; F-PAYLOAD 5 checks; R-LEN N-A 0/0; **jlc_twin EXIT 0 (207 OK / 465 rows, 206/206 bodies)**; **A-RENDER EXIT 0 on a FRESH 15.2259 px/mm render, 52 measured / 208, 0 resolvable-but-unmeasured**; **G-ORPHAN PASS 315/315, 0 orphan**; **M-BOUND PASS, ADR-0026's `LDO_IOUT_MAX` graded CITED — regenerated 0.24295049 vs published 0.2429, on its own budget edge at 124.97 C, nearest standard value 0.20 A at 117.08 C**; contracts_audit 258 files 0 violations; M-BEACON PASS. **THREE gates exit 1, all pre-existing, none copper: A-STOCK** (`C265111` stock **5** / MOQ **21**, MEASURED BY ME 2026-07-31T01:07:38Z with my own urllib client, controls C5620 5212 / C25741 2797150 live in the same session — unbuyable at ANY quantity; watch **21 not 10**), **E-NETREF** (21 ghost K7 refs), **A-AMP** (2 `nets.yaml` defects predating v1.6)."
state:   blocked
op_pid:  none   # everything EXITED. Both lens agents returned. Nothing is running.

next:    "**THE FIX IS THE SAME SHAPE AS THE ONE JUST LANDED, ON THE OTHER HALF OF THE RAIL, AND IT IS STILL NOT COPPER.** (1) Re-derive `vin_min` with the COPPER counted: measure the min-resistance path per segment with the segment's OWN current (whole-board current through F1/Q_REV/eFuse; only the LDO's own current through the last leg), not one lumped 109 mOhm at 0.50 A. **The re-derivation may well PASS** — the 0.50 A the incumbent `vin_min` assumes is now measurably ~1.5x the real worst case (LDO 0.20 + Iq 0.011 + coils 0.15 + 5V_STOP 0.02 + TVS 0.0013 = 0.382 A), and 0.382 A x 299.5 mOhm = 114 mV gives headroom 1337 mV vs 1300 = **+37 mV**. IT HAS NOT BEEN ASKED, and asking it is the whole job. (2) Close the 11.8 mA load delta against the topology lens's four named-absent loads. (3) Re-gate the TOPOLOGY lens only (the layout lens returned SOUND on a board whose copper cannot move). **DO NOT re-derive `vin_min` DOWNWARD without also counting the copper — the two errors point in opposite directions and cancelling them by accident is how this rail got here.** **ALSO OPEN, from the topology lens, unfixed:** six P1s — `5V_PROTECTED`'s 2.0 A ceiling above the eFuse ILIM ~1.6 A and F1's I_hold 1.37 A at 75 C; the watchdog window 0.9/1.6/**2.5 s** against the brief's 300-500 ms with no ADR and its named mitigating `SN74LVC1G123` absent from the BOM; the contactor opto at I_F 4.74 mA giving ~2.4 mA guaranteed I_C against a 30 V silk label, with no flyback on the isolated side; `D_ESD_IN` above stand-off at 5.25 V with no `limits:` block in its part.yaml at all; and the AMS1117's 10 mA MINIMUM-LOAD spec absent from a 545-line power tree whose declared safe state sits at ~7.2 mA. **NEXT REVISION, NOT THIS ONE:** `02_parts/AMS1117-3.3/part.yaml`'s `layout` block asks for a 3V3 flood under the tab and the board has 7.60 mm2 of pad instead — pour it (NOT touching the GND pour, that shorts the rail) or state the pad-to-plane path as the design and put a number on it. **SIX SKILL PATCHES OWED, FOUR OF THEM NEW THIS PASS AND FILED NOT IMPLEMENTED** (`06_build/staging/cooksense-v1.7/verification/owed_skill_patches.md` P13a-P13d): `power_topology.py` must REFUSE a `power_tree.yaml` carrying rail-like keys it does not read; G-ORPHAN needs a grade ABOVE `OWED` for a row whose gap is a CORRECTNESS gap, because **it already grades `linear_rails[].iout_max_A` as OWED with the words *'also absent from the trunk-current sum `rails[]` feeds'* — the exact defect, in the contract, in prose, EXITING 0**; `theta_ja`/`ambient_c` want to be first-class keys; and E-TOPO's PD product omits the regulator's own ground current, 19 % of this rail's graded PD."

updated: 2026-07-30T19:05:00
