# STATUS beacon — cooksense (MAIN board; live head, OVERWRITTEN each transition)

History is append-only in `journal/<stage>_cooksense.md`; this is only the
current frame. Read by `skills/kicad-pcb/scripts/pcb_status.py`, graded against
the tree by `skills/kicad-pcb/scripts/status_beacon_check.py` (canon M-BEACON).

<!-- reader parses from here down -->
stage:   BLOCKED-DO-NOT-ORDER
step:    "**cooksense IS DO-NOT-ORDER FOR EVERY SEALED RELEASE, v1.0 THROUGH v1.6, AND v1.7 IS NOT SEALED.** The MAIN board only — interposer-v1.1-2026-07-27 is unaffected and remains ORDERABLE. The reed-relay land pattern carries 4 pads at DIP 1/7/8/14, but datasheet sub-figure 12 (re-rendered at 300 dpi, read directly, cross-checked against the netlist) shows 8 leads and 4 nodes: 1<->14 CONTACT_A, 7<->8 CONTACT_B, 2<->13 COIL_A, 6 COIL_B, 9 NC. So 5V_KEY_RELAY is hard-shorted to U_SEL_BUS, every ULN2803 output is shorted to its keypad line, the coil has NO HOLES, and the 12-relay key-matrix array is non-functional. The old map describes sub-figure 13, the genuinely-4-lead variant — the wrong sub-figure was read. NOT A FOOTPRINT SWAP: coil and contact INTERLEAVE along the package, so the true coil-contact separation is 2.54 mm between adjacent pins, not the 7.62 mm row separation ADR-0002 asserts — which makes the v1.1 isolation-comb repack, i.e. the shape of the whole board, rest on a boundary that does not exist. No sealed directory was written; 07_releases/ is untouched and the verdict lives here and in the CHANGELOG, per the usb-hub-3s-v3 v1.6-v1.8 precedent. Mitigating: the relays are not_on_assembly_bom with exclude_from_pos_files, so JLC never placed them and no CPL row was ever wrong — the defect lands on the HOLES and the NETLIST, at hand assembly and bring-up."

measure: "GREEN ALREADY, and independent of the route: **E-TOPO PASSES — the first green E-TOPO since the gate learned LINEAR on 2026-07-27** — `rail '3V3' LINEAR (AMS1117-3.3): headroom 1355 mV (Vin_min 4.754 - Vout_max 3.399) vs dropout 1300 mV; PD 615 mW ((Vin_max 5.25 - Vout_min 3.201) x 0.3 A) vs 1200 mW (51%) -> PASS`, exit 0. vin_min 4.754 is DERIVED from the ADR-0021 J_PWR floor minus 95.2 mV of CITED worst-case series drop (F1 R1Max 70.0 + Q_REV 73.5 + eFuse 47.0 = 190.5 mOhm at 0.50 A); all three datasheets are now COMMITTED to 02_parts/ because none of the three had a cited maximum in this tree before. The 'passes by 59 mV' figure was NOT in ORDER_README s7 — it is the v1.6 redteam_adversarial s.E part 3, and its 92 mV was an ESTIMATE. E-INV **115/115** (was 109) with ALL SIX NEW invariants RED-VERIFIED, each mutation being the defect the assert exists to catch and each restored to md5 aa1115d218b9dee5d67fc3aa2ffefa55. E-ADR **9/9** (ADR-0021 closes its own loop). Schematic stage: 193 nets, safety-chain spot-check 11/11, ERC 0 errors / 648 warnings. **NOT GREEN YET: DRC is 1 violation / 4 unconnected / 0 parity** on the second reroute (was 1/6/3 on the first). The violation is DETERMINISTIC, not a race lottery: TWO independent races, SIX candidates, all landed the SAME 0.1191 mm gap between ADC_CH4 B.Cu (58.410,84.9687)->(58.021,85.358) and the TH_CAM_A B.Cu stub (59.300,85.400)->(58.500,85.400) into R_SER0.1, against a 0.1200 mm Default rule; the same two nets are 0.8500 mm apart on the v1.6 board. Root cause MEASURED: KRT routed to its own 0.15 mm target and produced 0.1191, i.e. ~0.031 mm optimistic on a 45-degree crossing, so route.common.clearance is raised 0.15 -> 0.18 (the ROUTER's search clearance; nets.yaml still declares 0.12 and DRC still grades 0.12). The 4 opens are the TRAPPED-PLANE-PAD class and get the remedy route.yaml already uses twenty times: a User.2 reservation rect plus a deterministic seed_stub via-in-pad on each of R_AND2PD.2 / R_MODEPD.2 (GND) and R_FAULTPU.2 / U_LATCHA.5 (3V3), sites measured with pcb_toolkit.via_site_ok over a 21x21 0.03 mm on-pad grid (135/125/284/183 legal points, three ARE the pad centre). seed_stubs 61 -> 65. PLACEMENT DELTA vs the blocked v1.7 is now EXACTLY one added part: R_WDOKSER is ANCHORED at (133.0, 82.325), 3.5 mm west of U_EXP pad 8 — left floating it legalized 24 mm away at (112.400,69.500) AND rippled TP_3V3 3.905 mm, so TP_3V3 is re-pinned at the coordinate v1.6 shipped."
state:   blocked
op_pid:  none
next:    "USER DECISION REQUIRED — three routes, none of them the agent's to choose: (1) re-author the land to sub-figure 12 (8 pads) AND re-derive the isolation comb against the true 2.54 mm boundary — a placement/route/architecture pass; (2) change to the -13 pin-out code, which is genuinely 4-lead, and re-derive against ITS geometry; (3) re-spec the isolation approach. THE OTHER THREE v1.7 BLOCKERS ARE FULLY SPECIFIED AND WAITING ON THAT CHOICE, because there is no point rerouting twice: TOPO P0-1 needs 10k (C60490) in series into U_EXP.2/.3/.4/.5/.6 with RED-verified pin_on_net asserts (v1.7 fixed only U_EXP.8 of six); PIN P0-b needs U_EXP.1 off the 5 V EFUSE_FLT_N pull-up (idles 5.0 V into a 3.6 V abs-max); RENDER P0-A + LAYOUT P1-b need a silk pass enforcing label OWNERSHIP (J_ESTOP/J_DOOR discriminate by 0.069 mm; J_ISOLOOP is labelled on its neighbour). Recorded for whenever the board next moves: the moat gate blind spot (KiCad DRC cannot see zone-fill-vs-pad, PROVED), the LDO tab copper ~104 K/W vs a claimed 45, the power tree declaring 0.45 A of children under a 0.30 A parent, and vin_min omitting 89.9 mOhm of the board's own copper."

updated: 2026-07-29T00:05:00
