# STATUS beacon — cooksense (MAIN board; live head, OVERWRITTEN each transition)

History is append-only in `journal/<stage>_cooksense.md`; this is only the
current frame. Read by `skills/kicad-pcb/scripts/pcb_status.py`, graded against
the tree by `skills/kicad-pcb/scripts/status_beacon_check.py` (canon M-BEACON).

<!-- reader parses from here down -->
stage:   V1.7-ENVELOPE-DECIDED-65C--BENCH-GATE-WRITTEN--FRESHNESS-CLEAR--M4-GRADED-FIRST-TIME--LENSES-RE-GATING
step:    "**THE USER'S ENVELOPE DECISION IS APPLIED AND THE BOARD IS UNTOUCHED** (`04_kicad/cooksense.kicad_pcb` md5 `9f4fd5fae810f40a52b1035df727243c`, DRC 0/0/0 re-run by me with `--exit-code-violations`, RAW_EXIT 0; a copy of `source/` taken OUTSIDE the repo also measures 0/0/0 with `fp-lib-table`, `.kicad_pro` and `.kicad_dru` all in-archive). ADR-0029 published: **declared operating ambient 65 C, survive corner 75 C, plus a MANDATORY six-measurement bench gate** (`ORDER_README` §7b B1-B6). BRIEF.md carries it as D12 + a `thermal envelope` fact-lock row; `power_tree.yaml` carries the declared-envelope block; `ORDER_README` gets §0-T. Options (b) tab-vias / (c) buck / (d) seal-at-75 were priced and NOT chosen; tab-vias (-18.0 C/W, +8.4 C, NO BOM change) recorded as the next copper revision's item. Committed 6f8cd600."
measure: "**AT 65 C THE JUNCTION MARGIN IS 17.9205 C CITABLE AND 13.27...16.37 C CARRYING THE BOARD'S OTHER 0.958 W.** MEASURED by me, `/usr/bin/python3`, from CITED constants only (ds1117 theta_JA 90 C/W, Tj_max 125 C, Iq_max 11 mA) plus this file's own graded keys: `PD_pass` 409.800 mW + `PD_q` 57.750 mW -> rise 42.0795 C -> Tj 107.0795 C. At 75 C the same two forms are **7.92 and 3.27...6.37**, so the published 7.92 was wrong REGARDLESS of the envelope, by a term worth 20-59 % of itself. Dropout at 65 C **+19.5 mV** worst case with the CITED 2x10 mOhm contacts (INHERITED from ADR-0028's ladder; slope -0.31 mV/K, so narrowing bought +1.4 mV). **`pdiss_max_mw` HELD at 497 — the 75 C derating — ON PURPOSE**: the gate was never red (PD 410 vs 497 = 82 %), so moving it to the arithmetically-correct 608 would only loosen a ceiling. GATES, UNPIPED, raw exit codes: DRC **0** (0 violations / 0 unconnected / 0 parity, BOTH halves) / standalone-archive DRC **0** / E-TOPO **0** / E-OFF N-A / E-MARGIN N-A / policy_audit FAIL=0 (HUMAN=6 N-A=5 PASS=28 WAIVED=6) **0** / M-BOUND **0** (7 CITED, 37 OWED vs a <=37 ceiling; the NEW `LDO_TJ_DECLARED_AMBIENT` RED-TESTED in THREE directions, all 999) / G-ORPHAN 315/315 **0** / release-freshness **0** (DESIGN PASS, SOURCING BLOCKED-1, FRESHNESS PASS). **M4 RAW_EXIT 0, PASS (0 fails, 1 ok) — 16 waivers graded (12 cooksense + 4 interposer), THE FIRST TIME THIS BOARD'S WAIVERS HAVE EVER BEEN GRADED**, after skills commit `1b70262a` (P19) landed the multi-board path; all 16 OWED-for-evidence under a 22 ceiling, 4 UNBACKED machine waivers under a 9 ceiling — named debt, not a green verdict. SOURCING re-measured at seal time 2026-07-30 21:29 local (2026-07-31T04:29Z), own urllib client, both controls in-run: `C265111` **5 / MOQ 21** (FIFTH reading; 0->5->0->5->5 in 48 h and **MOQ 21 at every one** — the blocker is the MOQ and it has never once been the stock), `C587657` **70 / MOQ 1** (130->80->70->70, single-source, no drop-in fallback)."
state:   working
op_pid:  none

next:    "**FOUR FRESH-CONTEXT LENSES ARE RE-GATING RIGHT NOW, EACH TO A DISTINCT FILENAME** (`08_reviews/2026-07-30_v1.7_{redteam_topology,redteam_layout,pin-review,render-review}_REGATE4-65C.md`) — the envelope change is MATERIAL, so every prior verdict is void including the two SOUND ones. Any `design_verdict: DEFECTIVE` STOPS the seal. Then: MANIFEST, the 2-commit seal (source S -> stamp `git_sha=S` -> seal commit adding only the release dir), and refresh THIS beacon as PART of the seal. **REMAINING SEAL BLOCKER, NOT MINE:** `git_dirty` is scoped to `projects/<board>/ + skills/` and `skills/kicad-pcb/scripts/tier_preflight.py` is another agent's uncommitted work — the seal cannot stamp `git_dirty: false` until it lands. My own partition is CLEAN (committed 6f8cd600). **TWO OF THE THREE DECLARED FRESHNESS BLOCKERS WERE NEVER REAL** and this is worth carrying forward: `release_freshness_check.py` resolves sibling releases against `release_dir.parent`, which for a STAGING dir is `06_build/staging/`, so both correctly-named real siblings read as foreign; the same defect manufactured a phantom `STOCK-INSUFFICIENT` + `ORDER-DECL-FALSE` pair by failing to find `03_src/rules/assembly.yaml` from a staging path. Run it with `--releases-root <proj>/07_releases --assembly <proj>/03_src/cooksense/rules/assembly.yaml` or it invents findings about the archive's own correct contents. Only `ORDER-DECL-UNDATED` was real (a UTC date in a project that DECLARES local dating) and it is fixed. **OWED SKILL PATCHES P15-P18, P20, P21 — FILED, NOT IMPLEMENTED** (`skills/` outside this partition); P19 LANDED as `1b70262a`. P20: a declared ENVELOPE needs a graded home that is not a derating (`rails[].ambient_c:` + `survive_ambient_c:` read by `power_topology.py`), because this pass had to choose between a correct declaration and an unrelaxed gate. P21: `release_freshness_check.py` must resolve the owning project when the release dir is not under a `07_releases/`, since running it pre-seal against staging is the MANDATED workflow. Two fleet gate gaps still owed and still not mine: `jlc_twin` PAD-GEOM compares pad CENTRES only — there is NO pad-SIZE check anywhere, for any part, on any board; and pcbnew `GetEffectiveShape()` returns a full disc for NPTH pads whose `size == drill`, inventing false findings."
updated: 2026-07-30T21:50:00
