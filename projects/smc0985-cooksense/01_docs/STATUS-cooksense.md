# STATUS beacon — cooksense (MAIN board; live head, OVERWRITTEN each transition)

History is append-only in `journal/<stage>_cooksense.md`; this is only the
current frame. Read by `skills/kicad-pcb/scripts/pcb_status.py`, graded against
the tree by `skills/kicad-pcb/scripts/status_beacon_check.py` (canon M-BEACON).

<!-- reader parses from here down -->
stage:   V1.7-RE-GATE-3--RAIL-CLOSED-AND-GREEN--SEAL-BLOCKED-ONLY-BY-A-STALE-LENS-VERDICT--BOTH-LENS-AGENTS-DIED
step:    "**THE DROPOUT HALF IS CLOSED, THE RAIL PASSES HONESTLY, AND THE SEAL DID NOT HAPPEN FOR A REASON THAT IS NOT ABOUT THIS BOARD.** Source committed `b2023e3c`, pathspec-scoped, no copper: `power_tree.yaml` + `electrical_invariants.yaml` + the new `01_docs/decisions/0027-the-dropout-margin-counts-the-boards-own-copper.md`. Board md5 `9f4fd5fae810f40a52b1035df727243c` UNCHANGED across all three rounds. Staging archive synced: power_tree, invariants, the ADR-0027 copy, ORDER_README first screen + the new **§7a-3 heater-stagger constraint**, `owed_skill_patches.md` **P14**, and FRESH `stock_check.{json,txt}`. **BOTH RED-TEAM LENS AGENTS WERE LAUNCHED AND BOTH DIED WITHOUT WRITING A BYTE** — transcripts 139 bytes, unchanged since 19:12:50 / 19:13:18, verified twice 45 s apart; ~25 min of blocking polls returned ABSENT for both REGATE3 files. **SO THE DESIGN CLAIM FOR RE-GATE 3 IS UNGRADED — NOT RED, UNGRADED.** `07_releases/` UNTOUCHED, still ends at cooksense-v1.6-2026-07-27, still DO-NOT-ORDER. There is still no orderable cooksense board. The source work IS committed and must NOT be re-derived."
measure: "**THE COPPER, MEASURED BY ME — an EXACT DC NODAL SOLVE of the whole 5 V chain, not a min-resistance path walk.** Every track an edge (R = rho.L/(w.t)), every via a plated barrel, every pad a node, the three cited component R as bridges (both eFuse IN pads tied as the package bonds them), real load currents injected at the REAL PADS, G.v = i solved; copper 1.72e-8 Ohm.m x1.216 at 75 C, outer 35 um, barrels 0.15 mm drill x 1.6 mm at 18 um. Per segment: `J_PWR.1->F1.1` **26.29** mOhm (5V_IN, 0 vias) / `F1.2->Q_REV.3` **7.17** / `Q_REV.2->U_EFUSE.3` **24.07** / `U_EFUSE.5->U_LDO.3` **80.25** = **137.79 mOhm of copper**, + 190.5 cited = **328.29 mOhm total**, of which **248.04 is TRUNK** shared by every 5V_PROTECTED load and 80.25 the LDO's own branch. **THE TWO OPPOSING ERRORS, CARRIED SEPARATELY AND NOT ALLOWED TO CANCEL: copper omitted = -52 mV; the fictitious 0.50 A = +26 mV. NET vin_min 4.754 -> 4.728, headroom +55 mV (claimed) -> +29.1 mV (MEASURED).** E-TOPO **RAW_EXIT 0**, `headroom 1329 mV vs dropout 1300 mV -> PASS`, `PD 410 mW vs 497 mW (82%)`. **AT THE FILE'S OWN LITERAL READING — 0.50 A LUMPED AT THE LDO — THE TRUE ANSWER IS -13.1 mV, A FAIL.** Break-even whole-board current **0.4910 A**; the deleted-because-false claim *'still passes at 0.60 A (+37 mV)'* measures **-35.8 mV**. **WORSE THAN THE LENS THAT FOUND IT: it measured 109.0 mOhm; this measures 137.79** — it took the two eFuse IN-pad routes as DISJOINT parallel branches (they share a trunk) and allowed 0.5 mOhm/via where a 0.15 mm barrel at 18 um is 3.5 mOhm. Its `5V_IN` figure — the ONE segment with no vias — is 25.87 and reproduces here at 25.87 at its own corner, which is what makes the rest credible. **THE 11.8 mA LOAD DELTA IS CLOSED, AND CLOSED UPWARD, BY ENUMERATING ALL 87 RESISTORS from the netlist with BOTH endpoint nets and tracing every pull-down's far node to its DRIVER.** Three terms genuinely absent, all the SAME SHAPE — current leaving a 3V3-POWERED OUTPUT rather than the 3V3 NET, invisible to a 'which pads are on net 3V3' sweep: A\" 17 pull-downs **0.884 mA**, C' `U_AND3.4->J_MODE.3->DPDT->R_COILENPD 680` **4.999 mA**, C\" `U_CAND2.4->R_OPTOLED 330->opto LED` **7.270 mA**. **The sweep found one NEITHER count had** (`R_GPB3PD` 10k on the expander's spare GPB3, 0.340 mA), so the closed sum **116.859 mA** sits ABOVE the lens's 115.5 rather than between the two. x1.5 = 175.3 mA, so **`iout_max_A` STAYS 0.20** and ADR-0026's dissipation half is UNTOUCHED (re-verified: `(125-75)/90*1000 - 5.250*11 = 497.806`; Tj 117.08 C, 7.92 C margin) — the load correction went UP 13.2 mA and did not move the declaration, so the dropout re-derivation **cannot have been paid for out of the thermal budget**. **A FOURTH FINDING FROM THE SWEEP: `J_LOADCELL.1` is on 5V_PROTECTED and NO revision ever carried it** — only J_LOADCELL.2 (3V3) was budgeted. Board D is not in this repo so it is BOUNDED at 20 mA, not claimed, and MEASURED insensitive (passes to 125.9 mA). **ONE NEW CONSTRAINT INSTEAD OF AN INVENTED NUMBER (ADR-0027 §3): the two SHT45 heaters never fire coincidentally at 200 mW.** Coincident = 0.4990 A = **-2.6 mV FAIL** with a junction excursion bounded by NO cited number (+3.0 C adiabatic to +17.8 C steady state, upper end Tj 134.9 C; ds1117 publishes no thermal time constant, so ADR-0026's +3.8 C is CORRECTED here). Staggered = **+27.0 mV PASS**, +0.2..1.2 C. **GATES RE-MEASURED SO FAR, UNPIPED, RAW EXIT CODES:** E-TOPO **0**; E-INV **0, 168/168** (was 167 — the new `U_LDO.3 on 5V_PROTECTED` assert holds); E-ADR **0, 12/12** (was 11 — ADR-0027 covered); **M-BOUND PASS, 3 CITED / 0 ESTIMATED / 0 UNVERIFIED, 37 OWED against a <=37 floor — UNCHANGED**, ADR-0027's `LDO_DROPOUT_IBOARD_MAX` regenerates 0.4910935989 vs published 0.4910 (delta 9.4e-05 <= 0.0001) and sits on its own budget edge at 1300.06 mV. **A-STOCK still exits 1** — RE-MEASURED AT SEAL TIME 2026-07-31T02:17:24Z with my own client and BOTH controls: **`C265111` stock 0 / MOQ 21** (it read 5 earlier the SAME pass, and 0 on 2026-07-29 — stock moved on a board whose md5 did not, which is the cleanest evidence there is for why DESIGN and SOURCING are two fields). Full re-run: 57/58 coded lines OK, verdict FAIL, exactly one problem line and it is the known BLOCKED one. Controls: C2040 63064, C99999999 no exact-code row, substitute C42376901 5993 / MOQ 1 — watch 21, not 10), **E-NETREF** (21 ghost K7 refs) and **A-AMP** (2 nets.yaml defects predating v1.6) unchanged and pre-existing."
state:   blocked
op_pid:  none   # BOTH LENS AGENTS DIED: transcripts 139 bytes, unchanged since 19:12:50/19:13:18, verified twice 45s apart. Nothing is running.

next:    "**THE ONLY THING BLOCKING THE SEAL IS THAT NEITHER LENS RAN. RE-GATE BOTH; NOTHING ELSE IS OPEN.** The three P0s are CLOSED IN SOURCE (commit `b2023e3c`) and every mechanical gate is re-measured green (see `measure:`). The blocking condition is M-REV: `release_freshness_check.py` grades the CONTRACT-NAMED `verification/redteam_topology.md`, which still carries the RE-GATE 2 `design_verdict: DEFECTIVE` — **a verdict written against the PRE-FIX `vin_min: 4.754`, i.e. STALE, not current. DO NOT ARGUE IT AWAY; RE-GATE IT.** Spawn both lenses zero-context on the two-key vocabulary, tell each that the prior round's verdicts are SUPERSEDED TEXT and not evidence, and have each write `verification/2026-07-30_v1.7_redteam_{topology,layout}_REGATE3.md`; then COPY each over the contract-named `redteam_topology.md` / `redteam_layout.md` (M-REV parses `design_verdict:`/`order_verdict:` from the header of THOSE EXACT NAMES). **A `DEFECTIVE` from either means STOP.** Then: stage into `07_releases/cooksense-v1.7-2026-07-30/` — the seal contract stages THERE, not in `06_build/staging`, and doing so ALSO clears the two EVIDENCE-PATH-MISMATCH findings and the `_load_assembly` miss, all three of which are artifacts of grading a staging dir; MANIFEST; 2-commit seal (source S -> stamp `git_sha=S` -> seal commit adding ONLY the release dir + CHANGELOG + `SUPERSEDED.md` on v1.6); refresh THIS beacon as PART of the seal. **INVOCATION TRAP THAT COST THIS PASS TIME: this project's rules live under `03_src/cooksense/rules/` (symlinked from `03_src/rules/`), so `release_freshness_check.py` and `assembly_coverage.py` REQUIRE an explicit `--assembly projects/smc0985-cooksense/03_src/cooksense/rules/assembly.yaml` — without it sourcing reads CLEAR instead of BLOCKED-1 and A-POP reads FAIL; `count_parity.py` / `policy_audit.py` need `--board cooksense`.** `git_dirty` scope is `projects/smc0985-cooksense/ + skills/` via `release_git_dirty.py smc0985-cooksense` — pluto-rx2-8way-v2 is dirty under a LIVE agent and does NOT block. Carry forward unchanged: **A-BUY loud** (`SOURCING: BLOCKED-1 (C265111; measured 2026-07-31, stock 0 / MOQ 21)` in BOTH the MANIFEST gate summary and the ORDER_README first screen); the corrected §5-0 remedy (edit `03_tscircuit/src/cooksense.tsx` lines **1216/1218** — VERIFIED by grep this pass — and REGENERATE, never `fab/bom.csv`); the **UNRESOLVED tab-pad disagreement** (board 1.000x2.700 = CLONE exactly, genuine 1.210x2.700, 17.4 % narrow); ADR-0026's `<!-- bound -->` PASSING (do not raise the ceiling). **STILL UN-DERIVED after my sweep, named so silence is not read as coverage:** Board D's 5 V draw (bounded 20 mA, insensitive to 125.9 mA); the AMS1117 dropout at 0.2 A (ds1117 has it only at 0.8 A, no curve); theta_JA on this mounting; the SOT-223 thermal time constant; the LTV-817 V_F (bounded at a 1.0 V floor, not cited). **ONE G4 BENCH SESSION RETIRES THREE OF THE FIVE, and at +29 mV instead of +55 it is the highest-value hour on this board.** Skill patch **P14 FILED NOT IMPLEMENTED** (skills/ outside this partition). (the previous frame's next-list is superseded and dropped.)"
updated: 2026-07-30T21:45:00
