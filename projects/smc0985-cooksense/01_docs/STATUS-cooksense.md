# STATUS beacon — cooksense (MAIN board; live head, OVERWRITTEN each transition)

History is append-only in `journal/<stage>_cooksense.md`; this is only the
current frame. Read by `skills/kicad-pcb/scripts/pcb_status.py`, graded against
the tree by `skills/kicad-pcb/scripts/status_beacon_check.py` (canon M-BEACON).

<!-- reader parses from here down -->
stage:   V1.8-BUILD-BLOCKED-AT-1b-SILK--CONNECTOR-DECISION-IS-THE-GATE
step:    "**REBUILD ATTEMPT 3 FAILED, AND IT FAILED EARLIER AND FOR A DIFFERENT REASON THAN ATTEMPT 1 — do not re-race.** `06_build/rebuild_v18c.log` exits **1 at stage 1b/7**, never reaching the router: `FATAL: no clear silk position for ['R_DOORPD'] - a refdes that cannot be placed must not be left in a milled void`. **ADR-0024 IS THE CAUSE AND THE MECHANISM IS FULLY TRACED.** (1) ADR-0024 added `R_DOORS`/`R_ESTOPS`; floorplan.yaml anchored them into the pocket x[189.6,193.7] y[73.2,79.3] whose own comment calls it `4.1 x 6.1mm and empty`. (2) `R_DOORS` falls into the SAFETY-TEXT CLASS (10 members this run - membership is GEOMETRIC, `within 8.0 mm of a safety housing`, so it changes run to run and R_ESTOPPD/R_STOPRAIL were in it last run instead), which forces its designator to h 0.600 and gives it priority. (3) At h 0.600 rot 90 it takes R_DOORPD's slot: `R_DOORS displaces R_DOORPD from x[189.436,190.664] y[75.392,79.808]`. (4) R_DOORPD re-places at (191.800,74.150), inside J_ESTOP's courtyard y-band (J_ESTOP bbox y[65.505,76.255], J_DOOR starts 76.385 - 0.130 mm apart, watershed y~76.32). (5) The CROSS-NAME rule correctly refuses it (`6.143 mm from J_ESTOP, 9.217 mm from J_DOOR`) and finds no slot at any rung of the 1.5/1.0/0.5/0.1 ladder. **THE REUSABLE FINDING: the pocket was measured EMPTY OF COURTYARDS and it was not empty - it was R_DOORPD's DESIGNATOR SLOT** (v1.7: x[189.038,190.162] y[75.223,80.177], squarely inside). Silk is a placement resource with its own occupancy; a floorplan comment that clears a region by checking courtyards has checked the wrong layer. Two 0402s whose COPPER fits broke the build because their DESIGNATORS did not. The lever for the fix is in the floorplan's own words: both series resistors `carry NO DC, so their span is electrically free`, and `U_SCHM` is at (174.000,60.000), ~25 mm west - **do not spend the SE corner on them.**"
measure: "**BLOCKED, and the numbers that ARE measured this pass.** DRC/parity/fab NOT re-run: grading a board that does not build would be a number about nothing, so the v1.7 figures in git history stand and are NOT restated here as if fresh. **ADR-0024's PUBLISHED SAFETY BOUND WAS WRONG AND IS CORRECTED IN THE ADR** (independent re-derivation, `0024-...md` Decision B): the bound shipped as `R_pd <= 592 ohm`, which is the NOMINAL corner (3.300 V / 2200 ohm / 0% tol) sitting as the takeaway of an ADR whose whole argument is worst-case. At the corner the rest of the section uses (rail **3.399 V**, injection **2178 ohm**, R_pd **+1%**) the bound is **559.3 ohm**. Why 33 ohm mattered: **560 ohm is the next standard value under a 592 ceiling and it produces 0.7007 V - FAILS by 0.7 mV** against V_T+(MIN) 0.700 V. Re-derived: 549 ohm +10.3 mV, 511 ohm +48.9 mV, 499 ohm +61.3 mV, **470 ohm (chosen) +91.7 mV - UNAFFECTED, the board is not wrong**. Also reconciled: the 680 ohm row's 0.791 V uses a NOMINAL rail; at the same 3.399 V ceiling 680 ohm gives **0.8149 V, FAIL by 115 mV not 91 mV** - conclusion unchanged and strictly stronger. Cost corner confirmed correct and OPPOSITE (R_pd LOW, 470-1% = 465.3 ohm -> 7.30 mA / 24.8 mW / 40% of a 62.5 mW 0402). **A bound stated at a corner the decision does not use is a remedy copied without re-deriving it - the exact failure this ADR is named after, reproduced in its own summary line.** SILK CENSUS from the dead run: `177/244 owned, 59 degraded, 8 unplaced`; J_ISOLOOP still `own 8.49 vs J_RH_EXHAUST 5.19` - **no owned site, unchanged.** 1b/7 TIMING, so nobody kills it again: it is a LEGITIMATELY MULTI-MINUTE SILENT pass - measured `State: R`, **331 s of CPU with zero log output for 9 minutes**, because `fix_silk_placement.py` lines 752-771 search 3 texts x 2 rotations x a 257x257 quarter-mm grid = **396 k positions**, and the `g >= best[0]` pruning CANNOT fire while `best is None`, so the one caption that never lands (`1C2L3L4E`) pays the full unpruned cost. Python stdout to a file is block-buffered, so 1b's output appears all at once; the only live signal is /proc/<pid>/stat utime."
state:   blocked
op_pid:  

next:    "**THE GATE IS A HUMAN DECISION, NOT A BUILD ITERATION. DO NOT re-race, do not run the review battery, do not seal.** A fresh four-lens battery would return P0 on the same two findings the last one did (render F1: the 30 V NOT-SELV legend on a sensor connector; pin C2: the J_ESTOP/J_DOOR transposition), because **ADR-0024 explicitly does not close either** - its own `What this does NOT do #1` says the remedy is a mechanical key, i.e. a part change. Spending the battery on a board that does not build, and whose SE corner is about to be repacked by that part change, would be spending it twice. **(1) THE DECISION, now fully sourced - see the UPDATE section of `01_docs/learnings/label_ownership_se_corner.md` for the full matrix.** Live LCSC reads 2026-07-29: **ZH-3 `S3B-ZR-SM4A-TF` = C72591, stock 38 697**, frees **1.700 mm** of east column, KiCad footprint in-tree, and NO new crimp system (SZH-002T-P0.5 / C246761 / 113 425 is already needed for J_MODE) - conditions: pin it 1=3V3 2=DOOR_RAW_IN 3=GND and build the harness with THREE wires (the GND on ckt 3 is what makes both J_MODE seat positions ACTIVELY restrictive), and depth grows +1.600 mm inboard which was NOT checked. **SH-3 `SM03B-SRSS-TB` = C160403, stock 52 328** is safer still - **ZERO possibly-mateable cells anywhere on the board**, frees 3.900 mm, depth +0.160 mm - and is rank 2 only on BUILDABILITY (SSH-003T-P0.2 is AWG#32-28 against a field harness spec'd #30-26). **THE DISQUALIFICATION TO READ FIRST: a 3-circuit GH (`SM03B-GHS-TB` C514175) IS FAIL-PERMISSIVE and worse than the defect it fixes** - a pin-count change INSIDE the GH family blocks only wide->narrow; narrow->wide is full pitch-matched engagement at 1.25 mm, so a GHR-03V-S door harness in J_ESTOP's GH-5 shroud bridges 3V3 -> ESTOP_RAW_IN and **the E-stop reads NOT-ASSERTED with no button attached.** ZH-2 C265329 is worse again (3 seat positions in J_MODE's ZH-4, one of them ckts 3-4, where the oven DOOR closes the physical MANUAL rail cut). WHICH connector to change is also open: the learning file proposes J_DOOR, but **J_ESTOP is the input whose transposition costs the LATCHING property**, so keying J_ESTOP means the higher-integrity input accepts no other harness on the board. **(2) THEN, and only in the same pass, the 1b wall:** move R_DOORS/R_ESTOPS off the SE corner toward U_SCHM (174,60) - their span is electrically free by the floorplan's and ADR-0024's own statements - rather than repacking the pocket, because the connector change repacks that corner anyway. **(3) Then re-race, and expect ATTEMPT 1's stitch failure next:** unconnected=1 on all three candidates at 5V_PROTECTED / U_EFUSE pad 5, traced to `C_EFIN` seeded due WEST of the eFuse IN pads forcing 5V_RPP east on B.Cu at y=58.000 through pad 5's only lane. Move C_EFIN's seed; do not re-race it blind. **(4) STILL OWED in the ORDER_README when a seal is finally reachable:** the 1.00 mm web DFM query (JLC Q&A unanswered), section 2a's now-stale `R_DOORPD 10k`, section 10's cross-mate matrix, and the A-POL order-preview human gate for C165895/C558584 - plus policy_audit M-BOM must be DEMONSTRATED at the seal, not predicted (it has been predicted twice). Do NOT use `refdes_waiver.json` - `generate_board` writes it, so it is self-certifying."

updated: 2026-07-29T19:14:03
