# STATUS beacon — cooksense (MAIN board; live head, OVERWRITTEN each transition)

History is append-only in `journal/<stage>_cooksense.md`; this is only the
current frame. Read by `skills/kicad-pcb/scripts/pcb_status.py`, graded against
the tree by `skills/kicad-pcb/scripts/status_beacon_check.py` (canon M-BEACON).

<!-- reader parses from here down -->
stage:   V1.7-CANDIDATE--PAPERWORK-CLOSED--NOT-SEALED--BLOCKED-ON-ONE-USER-DECISION-B30-01
step:    "**THE BOARD IS DONE; THE BLOCKER IS A PURCHASE, AND IT IS NOT MINE TO MAKE.** This pass re-read A-STOCK LIVE (the first instruction of the brief), re-verified the headline gates instead of inheriting them, closed the two paperwork items that could be closed WITHOUT moving a build input, and RULED on the release identity. **B30-01 IS STILL OPEN AND IS THE ONLY THING BETWEEN THIS BOARD AND A SEAL.** C265111 (SM08B-GHS-TB, J_THERM_A/B) reads stock **5** against a need of 10 — but the NEW measurement this pass is `minPurchaseNum` **21**, so the genuine JST part is **NOT MERELY SHORT, IT IS UNBUYABLE AT ANY QUANTITY TODAY**: you cannot order 21 pieces when 5 exist, and you cannot order 5. The threshold to watch is therefore **21, not 10**. **I DID NOT CHOOSE A SUBSTITUTE** — assembly.yaml calls C42376901's plug-mate compatibility the user's call in its own words, and a hand-fit revision is a scope change. **I DELIBERATELY DID NOT REGENERATE ANYTHING**, because a rebuild would change the artifact the four-lens battery graded and cost a battery re-run on a board that still cannot ship."
measure: "**RE-MEASURED BY ME, UNPIPED, RAW EXIT CODES — NOT INHERITED.** DRC `--severity-all --refill-zones --schematic-parity` **0 violations / 0 unconnected / 0 parity, EXIT 0**. policy_audit **EXIT 0, PASS=28 WAIVED=6 HUMAN=6 N-A=5, FAIL=0** (matches the beacon's prior numbers; the brief's PASS=27 does not survive, 28 does). A-STOCK **EXIT 1**, verdict line parsed verbatim: `FAIL: 57/58 coded BOM lines have stock >= 5 x qty (1 with problems)`. **LIVE CATALOG, 2026-07-30T21:02Z, queried BOTH through the gate and independently off selectSmtComponentList:** C265111 stock **5** / MOQ **21** / JST; C42376901 stock **6030** (was 6086) / MOQ **1** / SHOU HAN; C2653844 TPS259573DSGR stock **103**. **THREE INHERITED LOAD-BEARING NUMBERS RE-DERIVED FROM THE BOARD, ALL THREE CONFIRM THE BRIEF AND ONE CORRECTS THIS BEACON:** (a) J_ESTOP pads measured 1=GND / 2=**3V3** / 3=ESTOP_RAW_IN at **1.0000 mm** pitch — a 1-2 bridge IS a dead short of the main logic rail; (b) **the minimum web is 0.8500 mm at H4** (Ø2.700 mm NPTH wall to Edge_Cuts, the smallest of 105 drilled holes; next is J_ISOLOOP.1 at 1.150) — **THIS BEACON PREVIOUSLY SAID 1.000 mm AND THAT WAS A DIFFERENT FEATURE** (comb-slot webs); the 0.850 figure is correct and is already carried in ORDER_README §3a; (c) H4 sits **0.2005 mm** from FILLED copper on ALL FOUR layers — GND on F.Cu/B.Cu/In1.Cu and **3V3 on In2.Cu** — so a conductive fastener bridges the two planes, measured against filled polys, not zone outlines. **NETLIST DID NOT MOVE — PROVEN BY md5, NOT ASSERTED:** cooksense.net `6d83ebe7…`, cooksense.kicad_pcb `9f4fd5fa…`, cooksense.tsx `c42fada9…`, bom.csv `c491dd00…`, cpl.csv `38a332bd…` — every one byte-identical to what the four lenses recorded in their own headers, and 03_tscircuit/04_kicad are byte-identical to the archive copies. **THE BATTERY THEREFORE STANDS.**"
state:   blocked
op_pid:  none   # everything EXITED. Nothing is running.

next:    "**ONE DECISION UNBLOCKS THIS BOARD, AND IT BELONGS TO THE USER: B30-01.** Options, with live numbers: **(1) WAIT FOR RESTOCK** — watch C265111 for stock **>= 21** (the MOQ, not the gate's 10); 0 -> 5 in a day says restocking; the ONLY option with zero new risk and zero rework. **(2) SUBSTITUTE C42376901** — stock 6030, MOQ 1, land pattern MEASURED drop-in at 0.01 mm residual / non-mirrored / jlc_offset 0 on both refs, so no copper, no CPL rotation, no footprint change; **the UNVERIFIED part is whether a genuine GHR-08V pigtail from a third-party SHT45 pod seats and RETAINS in a clone shroud**, on the connectors carrying the thermistor pods — pad correspondence is not mate compatibility, and ADR-0024 measured the GH FAMILY, not clones. **(3) not_assembled + HAND-FIT** — needs exclude_from_pos_files plus a board regeneration, i.e. a copper revision's verification for a sourcing problem, on a 1.25 mm 8-circuit SMD header that is a harder hand-solder than any of the 14 refs already hand-fit. **CLOSED THIS PASS:** B30-17 FIXED (BRIEF.md fact-lock corrected DIP05-1A72-12L x13 -> **DIP05-1A72-13L x12**; both fields were wrong, and that row is the one a BUYER reads on a self-supplied DO-NOT-SUBSTITUTE part whose wrong code is the very land defect that makes v1.0-v1.6 DO-NOT-ORDER; correcting entry appended to the append-only commission journal; the newly-surfaced fact that **every distributor quote in this tree is keyed to -12L and does not transfer** is now IN the fact-lock, so distributor sourcing for -13L is explicitly OWED). B30-11 **DECIDED: THE RELEASE IS v1.7** — no release ever sealed as v1.7 so the number is unclaimed, and decisively, the four lens reviews are APPEND-ONLY VERBATIM EVIDENCE whose subject headers name v1.7 + md5 9f4fd5fa, so renumbering to v1.8 would strand the battery on a release that does not exist with no legal way to edit those headers. The **12 v1.8 strings in cooksense.tsx are ALL COMMENTS** (verified line by line, not one is a version field) and are hereby NON-NORMATIVE. **B30-11 EXECUTION IS DELIBERATELY DEFERRED TO THE SEALING PASS:** editing those comments changes the tsx md5 all four lenses recorded — a comment cleanup would invalidate the battery — and the `rev \"dev\"` title block needs a schematic regeneration (circuit_json_to_kicad_sch.py --rev defaults to \"dev\"), which the seal ritual does NOT do on its own. Run it as ONE atomic rebuild together with whatever B30-01 forces, and grade it ONCE. **B30-18 (MANIFEST.txt) cannot close before the seal by design.** **THE CHANGELOG BANNER IS ALREADY CORRECT AND MUST NOT BE TOUCHED WHILE v1.7 IS UNSEALED** — it names the six sealed DO-NOT-ORDER releases and states explicitly that v1.7 is not on the list because it has never been sealed; the brief's request to 'correct' it presumes a seal that did not happen. **A FIFTH SKILL PATCH IS OWED, MEASURED THIS PASS:** jlc_twin.py emits NO verdict line anywhere (grep finds the word only in a comment), so twin_run.log ships with nothing parseable to grade — exactly the failure mode the brief warns about, still live in the archive."

updated: 2026-07-30T21:35:00
