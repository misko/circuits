# STATUS beacon — cooksense (MAIN board; live head, OVERWRITTEN each transition)

History is append-only in `journal/<stage>_cooksense.md`; this is only the
current frame. Read by `skills/kicad-pcb/scripts/pcb_status.py`, graded against
the tree by `skills/kicad-pcb/scripts/status_beacon_check.py` (canon M-BEACON).

<!-- reader parses from here down -->
stage:   V1.7-CANDIDATE--NOT-SEALED--NINTH-DECLINE--AND-THE-FIRST-ONE-ON-A-DESIGN-P0
step:    "**THE SOURCING BLOCKER WAS REMOVED BY `217ea175` AND THE SEAL STILL DID NOT HAPPEN — BECAUSE THIS PASS FOUND TWO REAL DESIGN P0s.** The two-key vocabulary worked exactly as intended: the TOPOLOGY lens, re-gated fresh-context, returned `design_verdict: SOUND` + `order_verdict: BLOCKED-SOURCING`, which is the sentence eight passes had no field for, and no design P0. **But the LAYOUT lens was re-gated too — and THAT is where the seal died.** It had to be re-asked for what looked like a bookkeeping reason (its legacy `verdict: ORDER` retrofits to `order_verdict: ORDER`, and M-REV refuses that on a release measured `SOURCING: BLOCKED`); nobody suspected its design content. It returned **`design_verdict: DEFECTIVE` with TWO P0s**, and I re-measured both myself before standing down. **THE CANDIDATE IS BACK IN `06_build/staging/cooksense-v1.7/`, `07_releases/` IS UNTOUCHED, AND THE `SUPERSEDED.md` DRAFTED FOR v1.6 WAS REMOVED — nothing superseded it.** `cooksense-v1.6-2026-07-27` is still the newest sealed release and is still DO-NOT-ORDER; there is still no orderable cooksense board."
measure: "**THE TWO P0s, RE-MEASURED BY ME, NEITHER OF THEM COPPER.** **P0-1:** `power_tree.yaml` grades the AMS1117 at `iout_max_A: 0.3` while the SAME FILE declares four 0.1 A switched sensor rails under a `linear_rails:` key it labels *'Documentation-only (ignored by power_topology.py)'*. With `pcbnew` on the archive's own board — not the yaml, not the lens's word — `Q_SWA`/`Q_SWB`/`Q_SWRHA`/`Q_SWRHE` pad 2 = **`3V3` on all four**, drains = `J_THERM_A.1`/`J_THERM_B.1`/`J_RH_AMBIENT.1`/`J_RH_EXHAUST.1`. Declared load **0.70 A; the graded 0.3 is 43% of it**. At the file's own constants PD = 1.434 W = **120% of the 1200 mW ceiling** (graded 51%) and dropout headroom = **-15 mV** (graded +55 mV); even at a realistic 0.36 A, PD is 20% above the reported 0.615 W inside a 55 mV margin. **P0-2:** `pdiss_max_mw: 1200` is a **25 C** figure (theta_JA implied 83.3 C/W) applied with NO ambient term on a board BRIEF.md line 117 puts at `enclosure <=50/55/65/75`; re-derived ceilings 0.900/0.840/0.720/**0.600 W**, so the release's own 615 mW is **OVER at the 75 C hard limit**. I enumerated EVERY zone on the board: **the only zone on net `3V3` anywhere is on `In2.Cu` — there is NO F.Cu `3V3` zone at all**, so the file's 'the tab is flooded with 3V3 copper -> 55-65 C/W' is REFUTED as written. **EVERY OTHER GATE, MEASURED BY ME UNPIPED WITH RAW EXIT CODES:** DRC `--severity-all --refill-zones --schematic-parity` **0/0/0 EXIT 0**, and **0/0/0 EXIT 0 again on `source/` copied OUTSIDE the repo** (the archive now stands alone — `fp-lib-table` fixed to `${KIPRJMOD}/cooksense.pretty`; it did NOT at the start of this pass, returning 14 `lib_footprint_issues`); policy_audit **EXIT 0 FAIL=0** (PASS=28 WAIVED=6 HUMAN=6 N-A=5); ERC **EXIT 0, 411 violations ALL severity `warning`, 0 errors**; S-COUNT 4/4 over 239 refdes; E-INV 167/167; E-ADR 11/11; M-BOM PASS; F-LEGIBLE 60 checks; P-FACT 6/6 reached a comparison; M-DEPEND PASS; M4 PASS; placement_gates PASS; audit_board PASS; A-ROT/A-POL 64 measured rows; A-POP **PASS**; **jlc_twin EXIT 0, 207 OK / 465 rows, 206/206 bodies vs the CPL**; **A-RENDER EXIT 0 at 15.3907 px/mm**, 52 measured / 208, 0 unmeasured; contracts_audit 0 violations; M-BEACON PASS. **THREE gates exit 1 and none is copper: A-STOCK** (`C265111` stock **5** / MOQ **21**, MEASURED BY ME 2026-07-30T23:46:46Z with my own HTTP client, control `C5620` 5212 — unbuyable at ANY quantity, watch **21 not 10**), **E-NETREF** (21 ghosts, ALL kind K7 `keep_short`, all inside policy_audit's evidenced P-ADJ-UNREACHED waiver, none reaching copper/silk/netlist/BOM), **A-AMP** (2 `nets.yaml` declaration defects predating v1.6). Board `9f4fd5fae810f40a52b1035df727243c` == `source/` byte-identical; 243 footprints (239 + H1..H4), 4181 segments, **1097 vias ALL 0.25/0.15**, CPL 206, BOM 61."
state:   blocked
op_pid:  none   # everything EXITED. Both lens agents returned. Nothing is running.

next:    "**THE FIX IS `power_tree.yaml` + THE AMS1117 DOSSIER, AND IT IS NOT A LAYOUT CHANGE.** (1) State the LDO's REAL total load — `3V3` logic PLUS the four switched sensor rails, each with a CITED sensor current rather than a round 0.1 A (2x MLX90640 ~23 mA + 2x SHT45 ~1 mA + pull-ups puts the realistic total near **0.36 A**) — in the `rails:` key `power_topology.py` actually reads. (2) Add an ambient term: a `theta_ja` in `02_parts/AMS1117-3.3/part.yaml` and an enclosure ambient in `power_tree.yaml`, so `pdiss_max_mw` stops being a 25 C number used as a ceiling at 75 C. (3) Re-run E-TOPO. **IT MAY WELL STILL PASS at 0.36-0.40 A with a defensible ambient — it has simply never been asked.** If it does not pass, that is a real engineering decision (a better heat path, or a different regulator) and it belongs to a v1.8 revision, not to a seal. **ONE BENCH SESSION AT BRING-UP RETIRES BOTH P0s AND P1-B TOGETHER:** load `3V3` to its real total, measure the `U_LDO` tab against a known ambient (gives theta_JA), and measure `V_IN - V_OUT` at `U_LDO` (gives the 0.3 A dropout the file already owes). **ALSO OWED, OUTSIDE THIS BOARD'S PARTITION AND NOT TOUCHED:** `git_dirty` measured **true** at stand-down from FOUR `skills/` paths carrying another agent's in-flight M-FRESH work — `kicad-pcb/scripts/gen_tscircuit.sh`, `kicad-pcb/scripts/policy_audit.py`, `pcb-design/templates/03_src/rebuild_all.sh` (modified) and `kicad-pcb/scripts/build_provenance.py` (untracked). That would have blocked the seal on its own; the P0s reached it first. **AND THE PROCESS LESSON, which is the most transferable thing this pass produced:** the layout lens was re-asked only because a verdict FIELD would not validate, and it came back with two P0s that eight passes and four graded lenses had walked past. **When a review VOCABULARY changes, every lens's verdict is stale — not just the one whose answer you disliked.** Six skill patches remain owed (P2/P3/P4/P9 from the prior pass, plus P11 A-RENDER's verdict moving with its input resolution and P12 gates failing loudly-and-wrongly when auto-discovering a per-board input on a two-board project): `06_build/staging/cooksense-v1.7/verification/owed_skill_patches.md`."

updated: 2026-07-30T17:55:00
