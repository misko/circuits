# STATUS beacon — cooksense (MAIN board; live head, OVERWRITTEN each transition)

History is append-only in `journal/<stage>_cooksense.md`; this is only the
current frame. Read by `skills/kicad-pcb/scripts/pcb_status.py`, graded against
the tree by `skills/kicad-pcb/scripts/status_beacon_check.py` (canon M-BEACON).

<!-- reader parses from here down -->
stage:   PRE-SEAL-REVIEW-OWED
step:    "**THE FOURTH P0 AND THE SOURCING BLOCKER ARE BOTH CLOSED. THE NEXT REVISION IS BUILT, MEASURES 0/0/0, AND NOTHING IN 07_releases HAS BEEN TOUCHED — what remains is the fresh four-lens review battery, two rotation ledger rows, one manifest line, and the seal itself. NO DESIGN BLOCKER IS OPEN.** v1.0-v1.6 remain DO-NOT-ORDER (pin-out-12 relay land); interposer-v1.1-2026-07-27 unaffected and ORDERABLE. **(1) TOPO P0 — THE REED COILS NOW PULL IN AT EVERY CORNER, and it took a part change (ADR-0023).** The ULN2803A Darlington's 0.67-0.88 V drop at the ~7 mA coil WAS the entire deficit: margin crossed zero at 60.7 C typical and **45.7 C worst case, BELOW the brief's own <=50 C NORMAL band**. `U_ULNA`/`U_ULNB` are now **TBD62083AFWG (C165895)**, a PIN-IDENTICAL 8ch DMOS sink array, and three facts were READ rather than assumed because each decided drop-in-vs-redesign: the PIN MAP off the p.2 pin TABLE (1-8 in, 9 GND, 10 COMMON, 11-18 O8-O1 — identical, reverse-order outputs and all); the LAND off the p.9 P-SOP18-0812-1.27-001 drawing at 300 dpi (body 11.35-11.68 x 7.37-7.62, span 10.01-10.64, pitch 1.27 — TI's DW 11.50 x 7.50 sits DEAD CENTRE, and LCSC gives both codes the same 'SOIC-18-300mil' string), so `Package_SO:SOIC-18W_7.5x11.6mm_P1.27mm` is UNCHANGED; and the FREEWHEEL off the p.2 equivalent-circuit FIGURE (clamp diode anode at OUTPUT, cathode at COMMON) — the twelve coils have NO external flyback diode, so losing it would have made this a topology change. Input side checked too: V_IN(ON) MAX 2.5 V against this board's **3V3** '238 decoders, +0.8 V (TBD62084A needs 7.0 V — recorded DO-NOT-ORDER). ZERO geometric change: same footprint, placement, nets, isolation geometry and route. `02_parts/ULN2803ADWR/` is RETAINED, marked `superseded_by` / `on_live_board: false` — I deleted it, citing the contract's "rejected candidates never get a committed PDF", and `t1_fleet_regrade` went RED within the hour because **02_parts is the MPN authority for EVERY release, the already-shipped ones included** and C9683 is in six of them; same mistake orphaned C506653 until the MCP dossier's `alternates:` moved to the MAPPING form `bom_legibility_check` can actually read. Both fixed and both recorded in the dossiers themselves. **(2) SOURCING — C506653 (MCP23017-E/SS, `U_EXP`) CONFIRMED stock 0 on an independent live read and REPLACED by C558584 (MCP23017T-E/SS, stock 7490).** Not an alternate: DS20001952C's PRODUCT IDENTIFICATION SYSTEM lists (f) `-E/SS` and (g) `T-E/SS` as the SAME device, the T being the tape-and-reel identifier only. Same die, same SSOP-28 land, same register map, and cheaper. Both swaps are D-SPEC class (a) — sourceable at the declared cost tier, no D-TIER decision owed — and both parts are CHEAPER than the ones they replace ($1.2252 vs $6.22; $1.7105 vs $1.8749)."

measure: "**THE BOARD MEASURES DRC 0 violations / 0 unconnected / 0 schematic parity** on a full from-source rebuild (`--severity-all --refill-zones --schematic-parity`, exit 0, `06_build/proof/drc_v18.rpt`). E-INV **140/140** (was 136/136 — four new ADR-0023 asserts); E-ADR 9/9; E-TOPO PASS (headroom 1355 mV vs dropout 1300 mV; PD 615 mW / 1200 mW, 51%); audit_board PASS (18 polarity / 28 proximity / 13 edge / 245 silk, I-ISO 6.22 mm, I-OUT 0.35 mm); placement_gates PASS 0 fails 0 warns (P-OUT 0.30 mm, P-CAP 0.29); **count_parity S-COUNT PASS 4/4 over 241 refdes**; contracts_audit 0 violations in its graded scope AND zero findings on the two new paths even under `--projects`; M-BEACON 2/2. FAB: export_jlc_package exit 0 with **A-ROT OK all 208 CPL rotations sourced** and **F-LEGIBLE 59/59**; bom_legibility 58 checks OK on the fresh BOM **and 56/56 on the SEALED v1.6 BOM** (which my own dossier tidy-up had broken — see the verify journal); tests/run_tests.sh **755 passed / 1 failed -> re-run 5/5 after the fix**; bom_source_check **PASS** (leg C 27/27 over 210 coded refdes); jlc_twin **209 OK / 471 rows, bodies mounted 208/208**; ERC 0 errors / 658 warnings; A-POS 208 rows on the pad-array centre, worst **0.00000 mm**. **jlc_stock_check IS NOW PASS — the verdict line reads PASS, 56/56 coded BOM lines at >= 5x qty**, 3/59 uncoded and not graded (`06_build/proof/stock_v18.json`); C165895 = 2334, C558584 = 7490; THINNEST line is C2653844 TPS259573DSGR at **103** (qty 1), which clears the floor 20x but is the one to re-read before an order. **THE NEW COIL MARGIN**, on the pessimistic driver bound (7.0 mA x 6.50 ohm = 46 mV, where 6.50 is 2x the 25 C EC-table MAX because Toshiba publishes no R_on tempco): **-20 C +1.754 / +25 C +1.124 / +50 C +0.774 / +70 C +0.494 / +75 C +0.424 V** — POSITIVE AT EVERY CORNER, where the ULN2803A gave -0.060 V at 50 C and -0.410 V at 75 C worst case. Ampere-turn cross-check at +70 C: **7.815 mA delivered against 7.00 mA required, +11.6%** (was 6.81 mA, a -2.7% deficit). Insensitive to the one unpublished number: even at 10x the table max (32.5 ohm) the drop is 0.228 V against a 0.540 V budget. R_ON is a GUARANTEED EC-TABLE MAXIMUM identical at all three published current points (2.0 typ / 3.25 max ohm, Test Circuit 2 defines R_ON = V_DS/I_OUT), which is the second, independent win: a table row replaces the 900-dpi digitization of a TYPICAL curve. **TWO GATES ARE NOT GREEN AND BOTH ARE UNCHANGED PRE-SEAL STATES, NEITHER A v1.8 DEFECT.** policy_audit FAIL=1 / WAIVED=5 / PASS=25 / HUMAN=6 / N-A=4 — the FAIL is M-BOM grading `07_releases/cooksense-v1.6-2026-07-27/fab/bom.csv`, which still contains C9683; MEASURED, the fresh v1.8 BOM has ZERO occurrences of C9683 and bom_source_check on it PASSES, so it resolves at the seal. assembly_coverage A-POP FAIL=1 `MANIFEST-UNDECLARED` — the release MANIFEST needs a `not_assembled:` line; release paperwork, the same finding recorded 2026-07-28. **AND THE MARGIN IS PROVEN BUT NOT YET GATED**: the ADR-0007 `node_level` assert that pins it measures **0.056 V against a 0.540 V budget = PASS with the DMOS, 0.895 V = FAIL with the Darlington's worst case and 0.714 V = FAIL with its TYPICAL**, but it is NOT in `electrical_invariants.yaml` because `_load_part_electrical()` joins dossiers by LCSC CODE and the thirteen SELF-SUPPLIED relays carry an MPN (`DIP05-1A72-13L`) as their netlist value, so it returns UNREACHED — see the verify journal for the 4-line join patch it waits on and for why forcing the join through `sourcing.alternates:` was REJECTED."

state:   working
op_pid:  none

next:    "NO DESIGN BLOCKER IS OPEN. FIVE ITEMS STAND BETWEEN THIS BUILD AND A SEAL, IN ORDER. (1) **THE FRESH FOUR-LENS REVIEW BATTERY, STILL OWED** — it was deliberately not run at v1.7 because a confirmed P0 blocked, and it is deliberately not run here either: this session changed two parts and a battery run against a board that might change again is spent twice. It is now the FIRST thing to do, and the pin lens should re-read only the dossiers whose hash or citation changed (ADR-0007 item 3): `TBD62083AFWG` (new), `MCP23017-E-SS` (sourcing only, pin map unchanged), `DIP05-1A72-13L` (gotcha + derived electrical facts, datasheet unchanged). (2) **TWO ROTATION LEDGER ROWS, MEASURED AND NOT LANDED.** `06_build/proof/rotation_rows_v18.csv` holds paste-ready rows for C165895 (offset 270, pad-number rms 0.1500 mm vs 8.1344 next best = 54x) and C558584 (270, rms 0.0403 vs 6.1624 = 153x), both DECLARED `single-channel` and therefore both obliging the JLC order-preview human gate exactly as their predecessors C9683 and C506653 do. Their numbers are byte-identical to those predecessors' — which they must be, same board footprint and same JLC model file — and they were MEASURED rather than copied so that the identity is a result. They belong in `skills/jlcpcb-fab/scripts/jlc_lcsc_rotations.csv`; this session could not write there, so the clean export was proven through the resolver's OWN `JLC_LCSC_ROTATIONS` env override. **A-ROT WILL BLOCK AGAIN UNTIL THEY LAND**, and this is the ONE item outside the `projects/smc0985-cooksense/` pathspec. (3) **LAND THE 4-LINE `node_level` JOIN PATCH** (`electrical_invariants.py`, `_load_part_electrical`: also index `d['mpn']`), then move the coil-margin assert out of ADR-0023 into `03_src/cooksense/rules/electrical_invariants.yaml` and re-run the RED verify IN PLACE. ADR-0007's own worked example IS this margin, so the kind was designed for it and only the join cannot reach it; every self-supplied part in the fleet is outside `node_level`'s reach today. Second, unrelated patch owed: `bom_source_check.py` should read JSON with `utf-8-sig` (it crashes on `Unexpected UTF-8 BOM` when handed a project dir). (4) **ORDER_README + MANIFEST**: add the `not_assembled:` line A-POP wants, and carry into the paperwork that (a) the coil margin is proven but not gated, (b) C165895 and C558584 are BOTH single-channel rotations needing the order-preview gate, (c) the thinnest stock line is C2653844 at 103. (5) SEAL the revision + `SUPERSEDED.md` on the v1.0-v1.6 series. CARRIED, WITH NUMBERS, NONE BLOCKING: the 0.600 mm comb slots against JLC's published 1.0 mm minimum (FIFTH sighting; widening is geometrically free, escalates to P0 if the capability page confirms 1.0); the 2N7002 datasheet is STILL not committed, so K_STOP's 0.10 V V_DS is an estimate (not load-bearing — even at 0.50 V the +70 C margin is +0.054 V — but ADR-0023's `class_sweep` excludes K_STOP on that number and should not have to); the LDO tab's 3V3 copper measures 13.96 mm2 against the ~645 mm2 the power_tree's 55-65 C/W wants; J_PI (C35165) and J_LOADCELL (C157991) carry LCSC codes but appear on no CPL row; J_ISOLOOP's own designator sits 10.817 mm from its block and 4.011 mm from J_RH_EXHAUST, non-blocking only because the 'ISO 30V' caption now sits 0.085 mm away and a KF350 cannot be cross-plugged with a JST-GH; the J_ISOLOOP per-pole legend is GEOMETRICALLY IMPOSSIBLE (pads at the centre of the KF350 body in x) and rides the north-stack caption; J_ESTOP's label ownership landed at a DEGRADED 0.5 mm demand (+0.659 mm measured). NEW AND MINOR: the fresh CPL is **208 rows against the v1.7 STAGED package's 203** — the five extra are `R_DOOROKSER`/`R_ESTOPOKSER`/`R_FAULTSER`/`R_MODEHWSER`/`R_TEMPOKSER`, which exist in source and were missing from that staging snapshot, i.e. `06_build/staging/` was already stale before this session touched anything. Do not seal from it; re-stage from `06_build/fab_v18/`."

updated: 2026-07-29T16:40:00
