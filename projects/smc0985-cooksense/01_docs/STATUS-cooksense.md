board: cooksense
stage: v1.3 COPPER RESPIN — electrical design LANDED in source, build blocked on one part dossier + one mechanical decision
state: blocked
git_sha: (this commit)
measure: E-INV 74/74 on the v1.3 netlist (11 NEW assertions for the two fail-permissive fixes + the isolated-loop move, all GREEN); manifest 221 components; converter sch 221 components / 775 pins, ERC 0 errors; isolation DRU rules RED-VERIFIED against v1.2 copper -> keypad_isolation_6mm 0 violations (real 6mm IS held, audit's 6.12mm confirmed) / opto_isolation_2mm 82 violations worst 0.199mm (P0-2 is real and now machine-visible)
v13_done: (1) P1-2 DOOR fail-permissive FIXED IN HARDWARE - R_DOORPU (10k to 3V3) -> R_DOORPD (10k to GND); open circuit now reads DOOR-OPEN, matching R_ESTOPPD/R_MODEPD. Harness becomes 3V3 -> Form-A reed -> J_DOOR.2. (2) P1-3 open-thermistor FIXED IN HARDWARE - added U_COMP2 (2nd LMV393) as an OPEN-DETECT window half: trips when TH_CAM rises above 3.107V (R_OPENT 6.2k / R_OPENB 100k), a level no connected NTC can reach; TEMP_OK is now hot(A).hot(B).open(A).open(B). U_COMP ALSO moved from LM393@5V_PROTECTED to LMV393@3V3_ANALOG because the LM393's CM ceiling (VCC-2 = 2.93V) was violated by the 3.0-3.3V cold/open readings. PROVED FIRST that a bias resistor cannot work: in a 2-terminal divider an open sensor IS an infinitely cold sensor, so cold and open sit on the same side of any single threshold - the information is not in the node, hence a second threshold. (3) P0-2 part A - the isolated loop LEAVES the SELV connector: new J_ESTOPLOOP (KF350 3.5mm) carries CONTACTOR_C/CONTACTOR_LOOP; J_ESTOP pins 3/4 become GND so that housing is SELV-only (was ESTOP_RAW and CONTACTOR_C adjacent at 0.650mm on 1.25mm pitch). (4) P0-2 part B - `opto_barrier` keepout denies tracks/vias/pours on ALL FOUR layers across x[181.4,188.6] y[85.8,94.2], the strip between U_OPTO's LED-side and isolated-side pad columns. (5) CARRY-FORWARD - new ISO_CONTACTOR netclass + TWO conditional DRU clearance rules emitted by apply_drc_policy.py: keypad_isolation_6mm and opto_isolation_2mm (IEC 60664-1 basic insulation, 30V working, pollution degree 3). The board's headline 6mm is now a rule DRC can fail on, where before KEYPAD_ISO's clearance was 0.12mm, identical to Default.
blocker: TWO, both needing a decision I should not take alone. (A) PART DOSSIER - U_COMP/U_COMP2 now specify C7984 = LMV393IDR, which has NO 02_parts/ dossier, so the converter cannot resolve their FPID (221 components, 219 with FPID) and generate_board cannot run. Needs 02_parts/LMV393IDR/part.yaml to the repo standard: datasheet fetched + sha256 + pin table VERIFIED against the pinout figure (it is LM393-pin-compatible, but on a safety board that must be read, not assumed), limits, CM range, gotchas. (B) MECHANICAL - P0-1 cannot be fixed with copper. MEASURED: a 14x14mm square centred on H4 (193,52) contains BOTH K_STOP.3 [RSTOP_MID, keypad domain] AND J_MODE.4/.5 + U_ULNB.14-18 [SELV] pads, so no keepout can create a 6mm keypad->hardware->SELV path there; H4 sits between the keypad relay column and the SELV drive band. With hardware modelled as a 3.0mm-radius conductive disc (M2.5 head + DIN125 washer + nut), the requirement min_i(a_i) + min_j(s_j) >= 6.000mm needs keypad copper >= 9.0mm from every hole centre; today H1 is 5.311mm, H2 5.962mm, H4 7.029mm. THE HOLES MUST MOVE, which changes the enclosure interface - not my call.
next: 1) coordinator decides the enclosure constraint for H1/H2/H4 (or rules that hardware is all-nylon, which downgrades P0-1 to P1 with a board marking); 2) create 02_parts/LMV393IDR/part.yaml; 3) add the I-HW mounting-hardware assert to audit_board.py (the rule above, machine-checkable - DRC cannot see an NPTH); 4) generate_board -> rules BEFORE route (canon R1, the two new DRU rules are in apply_drc_policy which runs LAST, so also confirm they survive) -> full re-race -> stitch -> gates; 5) both red-team lenses + fresh zero-context lens over the STAGED archive; 6) 2-commit seal + SUPERSEDED.md on v1.1.
do_not: export a CPL or add rotation rows (coordinator has the operator-disagreement investigation open on C125121/C189896/C2683602); seal; treat the other six topology P1s or ten layout P1s as in scope; normalise R_WDPETPD back to 100k (1k, and the VALUE is the fix); assume the LMV393 pinout without reading the datasheet.
top_v13_gate_item: E-INV `part_value` kind. An invariant that pins a component's EXISTENCE does not pin its VALUE - all three WD_PET assertions PASS on a 100k board. NOT yet implemented (the v1.3 electrical work took the session); when it lands, assert R_WDPETPD = 1k and R_OS = 510k.
