board: cooksense
stage: v1.2 all mechanical gates GREEN — SEAL BLOCKED by two P0 ISOLATION defects from the layout lens
state: blocked
git_sha: 2589dff
measure: DRC 0 viol / 0 unconn / 0 parity (all three buckets empty; re-verified by a second party on the final bytes); M-REPRO IDENTICAL (f0ec9c4069445438, 5544 items, 1086 vias); ERC 0 errors; audit_board PASS (18 polarity, 25 proximity, 13 edge, I-OUT 0.35mm, I-ISO 6.12mm); E-INV 63/63 + E-ADR OK; S-COUNT 216 x4; net_label_survival 159/159; bom_source_check PASS; jlc_stock_check exit 0; jlc_twin EXIT 0 (173 OK / 410 checked, 0 unadjudicated CRITICALs) after the C22046,180 row + 4 new PAD-GEOM adjudications; contracts_audit 179 files 0 violations; CPL 186 / BOM 54 (2 uncoded = declared not_assembled)
blocker: TWO P0s from the layout/thermal red-team lens (08_reviews/2026-07-25_v1.2_redteam_layout.md), BOTH independently re-measured by the lead, BOTH reproduce exactly. (1) MOUNTING HARDWARE BRIDGES THE KEYPAD ISOLATION - H4 @(193,52) NPTH d2.7: hole wall 5.679mm from RSTOP_MID (K_STOP.3) and 0.200mm from the GND pour on all 4 layers, so keypad->screw->SELV = 5.879mm < the board's own 6.000mm WITH NO HARDWARE FITTED; an M2.5 head puts keypad copper 4.779mm from a conductor sitting on the GND plane, and H1/H2 are keypad-side while H3/H4 are SELV-side, so any chassis plate shorts the domains. Invisible to DRC (an NPTH is a hole, not a conductor). (2) THE OPTO BARRIER IS NULLIFIED - min isolated-to-SELV copper gap 0.175mm (CONTACTOR_C track <-> DOOR_RAW via), with GND fill on F.Cu/In1/B.Cu, 3V3 fill on In2, 5 GND vias and SDA_B/SCL_B/SHIELD_DRAIN/3V3_SW_RHE tracks ALL running beneath the U_OPTO body; J_ESTOP carries both domains on 1.25mm pitch with a 0.650mm gap between ESTOP_RAW and CONTACTOR_C. 02_parts/LTV-817S-TA1/part.yaml forbids exactly this in its own words. Neither is fixable after fab.
op_pid:
next: 1) v1.3 layout round for the two P0s - H4 relocation or a keepout ring (and a hardware constraint that is EVIDENCED, not assumed), plus pour keepouts under/around the opto secondary, the contactor loop off the shared J_ESTOP, and CONTACTOR_C/LOOP routed clear of DOOR_RAW/ESTOP_RAW; 2) re-run the full battery + BOTH red-team lenses; 3) then stage/seal. Everything else for the seal is READY - fab exported, pdf+step built, twin exit 0, ORDER_README drafted at scratchpad/ORDER_README.md with the two MANDATORY mitigations (P1-2 door harness, P1-3 thermistor cross-check) + the bring-up ORDER + the JLC order-preview human gate.
do_not: seal while either P0 is open (07_releases contract: a release carrying an unresolved P0 may not seal); treat the 12 milled slots as the isolation evidence (MEASURED: the keypad<->SELV boundary occurs INSIDE each relay footprint where a slot is impossible - the 6.120mm intra-relay pad gap governs and no slot lengthens it); normalise R_WDPETPD back to 100k (it is 1k and the VALUE is the fix - SLVS165O sec.7.3.4 + I_IL 190uA max); edit jlc_lcsc_rotations.csv (coordinator owns it); treat the 8 topology P1s or the 10 layout P1s as fixed (all DEFERRED to v1.3 with dispositions)
top_v13_gate_item: E-INV needs a `part_value` assertion kind. THE LESSON OF THIS BOARD - an invariant that pins a component's EXISTENCE does not pin its VALUE. All three new WD_PET assertions (net_has_part + both pin_on_net) PASS on a 100k board; they would have certified the broken one, and the P0 was caught only because a lens read the datasheet. Coordinator is tracking this fleet-wide.
