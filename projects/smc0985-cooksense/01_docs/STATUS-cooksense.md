# STATUS beacon — cooksense (MAIN board; live head, OVERWRITTEN each transition)

History is append-only in `journal/<stage>_cooksense.md`; this is only the
current frame. Read by `skills/kicad-pcb/scripts/pcb_status.py`, graded against
the tree by `skills/kicad-pcb/scripts/status_beacon_check.py` (canon M-BEACON).

<!-- reader parses from here down -->
stage:   V1.8-USER-DECISION-LANDED--HYBRID-O4+O5-ON-J_DOOR--J_ESTOP-KEYED-TO-SH3--IMPLEMENTING
step:    "**THE REAL P0 IS FIXED IN SOURCE AND THE FIX IS MEASURED, NOT ARGUED.** `keypad_isolation_6mm` carried `&& B.NetName != ''`, so UNNETTED copper was EXEMPT BY CONSTRUCTION and `0 DRC violations` was never evidence about the board's headline safety property - which, on a keypad-emulation board whose whole claim is isolation from an appliance still under OEM safety control, is the property that outranks everything else. The conjunct is HAND-AUTHORED at `03_src/cooksense/apply_drc_policy.py` and RE-APPLIED after `generate_rules` on every rebuild, so it could never have been fixed by regenerating. **Repaired: the conjunct is GONE from both iso rules; `keypad_isolation_6mm` now exempts by NAMED REFDES (`!B.memberOfFootprint('J_KEY_MATRIX')`) with the evidence carried in the rule file itself; `opto_isolation_2mm` gets NO exemption at all.** **SECOND, the scope reduction is WORKED, not patched: ADR-0025 is written and this pass STOPPED for a user decision** - `J_DOOR`/`J_ESTOP` not installed makes the AND chain provably INERT, and every viable resolution is either a reversal of the user's decision or a change to a brief-normative safety chain. **DO NOT re-race, do not run the battery, do not seal until that decision lands** - the answer changes which parts exist in the SE corner, which is the same corner the 1b/7 FATAL is in."
measure: "**THE KEYPAD BARRIER, CLASSIFIED NEVER COUNTED, on the SEALED v1.6 copper (read-only copy in `06_build/proof/keypad_iso_v18/`, kicad-cli 10.0.4 `--severity-all --refill-zones`, all runs unpiped, exit 0):** old rule **0** clearance violations -> conjunct DELETED with no exemption **67** -> **67 of 67 name `Pad MP of J_KEY_MATRIX`** (the SM10B-GHS-TB shell tabs) on one side, and the far side is **100% KEYPAD_ISO-class net** (KP_D1..D4, KP_U1..U4, U_SEL_BUS). **ZERO domain crossings.** Worst actual **0.5723 mm**, best 5.9760 mm, against the 6.000 mm constraint - so the barrier really does hold and the rule simply could not say so. Named-refdes exemption -> **0**. **The exemption is BOUNDED and ENUMERATED: the whole board carries exactly TWO unnetted copper items and both are `J_KEY_MATRIX.MP`** (0 unnetted tracks/vias, 0 unnetted copper fills; the other no-net zones are RULE AREAS). **PROOF THE PREDICATE EVALUATES:** pointing the same exemption at the wrong refdes (`J_RH_AMBIENT`) restores all 67 - the exemption is what silences it, not a dead rule. `gate_contract_audit --dru` on the generated file: **G-VACUOUS-DRU 11 predicates graded, 0 fails, G-CONTRACT OK**; on sealed v1.6 it still FAILS both rules, which is correct and is why `t1_gate_contract.py`'s fixture must STAY pointed at v1.6. **THE SAFETY CHAIN, UNFITTED, DERIVED FROM THE NETLIST: RESTRICTIVE - the board is INERT.** `U_SCHM` (SN74HC14) is TWO cascaded stages per input, i.e. NON-inverting: ESTOP_RAW .1->.2/.3->.4, DOOR_RAW .11->.10/.13->.12 (a single-inverter reading would have flipped the answer to PERMISSIVE). Unfitted node V <= (1uA x 470R)+(1uA x 680R) = **1.15 mV** against V_T-(min) 0.500 V, so ESTOP_OK=LOW and DOOR_OK=LOW. Three independent stops: (1) ESTOP_OK LOW -> AND1 LOW -> **KEY_RELAY_ALLOWED LOW**; (2) ESTOP_OK LOW -> FAULT_SET_N LOW -> the C8185 NAND SR latch sets FAULT and holds FAULT_LATCH_CLEAR LOW with the SET input CONTINUOUSLY asserted, so **no REARM can ever clear it** and U_AND3 pin 6 is LOW too; (3) DOOR_OK LOW -> OS_CLR_N LOW -> **U_ONESHOT R1_N held asserted, the PRESS one-shot can never fire**. Safe, LOUD (FAULT readable at TP_FAULT and through R_FAULTSER to the expander), and completely non-functional. **CASCADE MEASURED, and it CORRECTS the hypothesis it was given:** (a) **NOT-ASSEMBLED frees ZERO silk** - `fix_silk_placement.py` has no dnp/exclude_from_bom/population reference at all, so DNP does NOT dissolve the 1b/7 FATAL; only removal from the netlist or MOVING the parts does; (b) the `4.1 x 6.1mm and holding nothing` pocket held **`J_ESTOP`'s designator x[187.978,192.022] y[77.266,78.494] AND `J_DOOR`'s x[189.149,192.851] y[75.146,76.374]** on sealed v1.6 - triple-booked before ADR-0024 added anything; (c) **REMOVING `J_DOOR` makes `J_ISOLOOP`'s designator OWNABLE** - at the one candidate pocket centre (192.655,84.975) the nearest courtyard goes `J_DOOR` 1.100 mm -> `J_ISOLOOP` 2.680 mm with `J_ESTOP` 8.769 mm second, a **+6.089 mm margin against MIN_OWNERSHIP_MARGIN_MM 1.5**, so the 30 V NOT-SELV render P0 that no silk-only pass could fix closes as a SIDE EFFECT of the scope reduction. DRC/parity/fab NOT re-run on a live board: it still does not build, and grading it would be a number about nothing."
state:   working
op_pid:  1180550   # rebuild_all.sh --reroute, 06_build/rebuild_v18g.log (C_EFIN attempt 4). Stage 1b/7 is legitimately multi-minute and silent; whole run ~11 min.

next:    "**THE GATE IS A USER DECISION AND IT IS ONE QUESTION (ADR-0025, `proposed`).** With no door signal and no E-stop signal available, do `DOOR_OK`/`ESTOP_OK` STAY in the chain and get satisfied by a REMOVABLE SHORTING PLUG on a populated connector (**O2** - zero schematic change, fail direction stays RESTRICTIVE when the plug falls out, the transposition hazard evaporates by symmetry because two identical 1-2 shorts are interchangeable, and it is REVERSIBLE if a real signal ever appears), or do they LEAVE the chain with a brief amendment (**O4** - NOT reversible without another copper revision, and it deletes the LATCHING property ADR-0024 traced)? Independently: should the `J_DOOR`/`J_ESTOP` FOOTPRINTS be removed (**O5**)? O2+O5 is incoherent; **O4+O5 is the only combination that closes four things at once** - transposition, the `J_ISOLOOP` 30 V misidentification, the 1b/7 silk FATAL, and the east-column squeeze. **ALREADY REJECTED, do not re-propose: on-board 0R links from 3V3 (O3)** - unfitted safety inputs silently satisfied on the PCB is a NEW fail-permissive defect worse than the one removed. **DO NOT PURSUE** the ZH-3/SH-3 keyed-connector change (hazard closed by scope; the sourcing data is retained as a measured spike) or the ADR-0024 cross-plug matrix rows naming J_DOOR/J_ESTOP (unreachable). **THEN, in ONE pass after the decision:** add `J_ISOLOOP` to `OWNERSHIP_FIX` in `fix_silk_placement.py` (it is not in the list, which is why nothing even tried for the 30 V terminal); move or delete `R_DOORS`/`R_ESTOPS` per the decision - **`R_DOORPD`/`R_ESTOPPD` 470R MUST SURVIVE EVERY OPTION**, they are the only reason the unfitted node measures 1.15 mV instead of floating; then re-race `--reroute` and **expect ATTEMPT 1's stitch failure next** (unconnected=1 at 5V_PROTECTED / U_EFUSE pad 5, traced to `C_EFIN` seeded due WEST of the eFuse IN pads forcing 5V_RPP east on B.Cu at y=58.000 through pad 5's only lane - **move C_EFIN's seed, do not re-race blind**); stage 1b/7 is a LEGITIMATELY MULTI-MINUTE SILENT pass (331 s CPU, 9 min of no output, 396 k grid positions) so do not read the silence as a hang; then the fab battery, then a FRESH four-lens battery, then seal ONLY if no lens returns a P0. **STILL OWED, unchanged:** the 1.00 mm web DFM query, section 2a's stale `R_DOORPD 10k`, the cross-mate matrix rewrite for two fewer housings, the A-POL order-preview human gate for C165895/C558584, and policy_audit M-BOM DEMONSTRATED at the seal (predicted twice already). **OWED SKILL PATCHES (reported, `skills/` untouched):** `check_dru_vacuity`'s hard-coded `1.0672 mm / 106 pairs` is a forensic centre-measure and UNDERSTATES DRC's edge-measure **0.5723 mm / 67 violations**; and `t1_gate_contract.py`'s v1.6 fixture must NOT be repointed at v1.7 - v1.6 is immutable and is now the archived known-bad input."

updated: 2026-07-29T23:05:00
