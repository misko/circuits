# STATUS beacon — cooksense (MAIN board; live head, OVERWRITTEN each transition)

History is append-only in `journal/<stage>_cooksense.md`; this is only the
current frame. Read by `skills/kicad-pcb/scripts/pcb_status.py`, graded against
the tree by `skills/kicad-pcb/scripts/status_beacon_check.py` (canon M-BEACON).

<!-- reader parses from here down -->
stage:   BLOCKED-DO-NOT-ORDER
step:    "**THE RELAY DEFECT IS FIXED AND VERIFIED; v1.7 IS STILL NOT SEALED, ON TWO NEW P0s FROM A SECOND REVIEW BATTERY.** v1.0-v1.6 remain DO-NOT-ORDER and that is unchanged — every one carries the pin-out-12 land. The MAIN board only; interposer-v1.1-2026-07-27 is unaffected and remains ORDERABLE. FIXED: the part is now DIP05-1A72-13L (pin-out CODE 13, C1524853) on a new land Relay_StandexDIP_1A_pinout13.kicad_mod; the pinout12 file is DELETED. Two zero-context lenses independently re-rendered DS p.3 at 400 dpi, counted FOUR leads on sub-figure 13 (contact 14<->8 on one row, coil 2<->6 on the other, rows 7.62 mm apart) and confirmed from the netlist that the coil and contact node sets are now DISJOINT — 5V_KEY_RELAY on no contact pad, U_SEL_BUS on no coil pad, minimum coil-to-contact pad distance 8.032 mm over all 48 pads; the land is also CHIRAL (DIP 2/6/8/14 map to 13/9/7/1 under 180 deg) so a relay cannot be inserted backwards. A third lens measured the >=6.0 mm keypad<->SELV barrier independently WITH POURS FILLED — audit_board.py deliberately excludes them, so the method is genuinely independent (canon M1) — at 6.2200 mm over 10 322 copper shapes, with a closed-form re-derivation agreeing to 0.0000 mm, and the intra-package coil<->contact gap IMPROVED from 6.1200 to 6.3494 mm. ADR-0002's isolation claim is TRUE under code 13, where under code 12 it was false. THE TWO NEW P0s, neither of them the relay: (1) PIN-P0-1, also found independently as TOPO P1-1 by a second lens with no shared method — the U_EXP pad-1 divider that v1.7 ADDED is sized as if EFUSE_FLT_N were a stiff 5 V source, but its ONLY driver-high is R_PG = 100k (U_EFUSE.6 is OPEN-DRAIN), so the real chain is 100k+10k over 22k, ratio 22/132 not 22/32, and the tap sits at 0.833 V against MCP23017 V_IH(min) 2.640 V and V_IL(max) 0.660 V — the indeterminate band. The eFuse fault readback can NEVER report power-good: the pin is protected and DEAD. TP_PGOOD, deliberately left on the raw node so 'the instrument sees the real node', now rests at 1.212 V, so that rationale is false as built. At R_PG = 100k NO R_top > 0 solution exists. (2) RENDER-P0-1 — J_ISOLOOP, the NOT-SELV 30 V contactor terminal, has NO ARTWORK AT THE TERMINAL: no text inside its silk body, no pole legend (0 of 4), and its own designator printed 1.300 mm from J_RH_EXHAUST against 4.900 mm from itself, at h=0.45 versus the neighbour's 0.60. The recorded 'SE corner is saturated, nearest site 33.6/41.9 mm away' justification DID NOT REPRODUCE — a re-run of the sweep under the same stated constraint set found 'ISO 30V' fits at (189.05, 93.35), 6.46 mm from the block. That is the inherited-defect pattern exactly: a measured 'impossible' carried across sessions without ever being re-measured."

measure: "**THE BOARD ITSELF IS GREEN — DRC 0 violations / 0 unconnected / 0 schematic parity** on a full from-source rebuild, and the whole fab/assembly battery passes: export_jlc_package exit 0; A-ROT OK on all 210 CPL rotations; F-LEGIBLE 59/59 and bom_legibility OK (58 checks); bom_source_check PASS on the staged BOM (leg C 27/27); jlc_stock_check VERDICT **PASS**, 56/56 coded lines at stock >= 5x qty; jlc_twin 211 OK / 475 rows, bodies mounted 210/210; A-RENDER OK — 53 measurable bodies all within 1.00 mm, 0 unfaithful, 0 unmeasured, at 3200x2000 (at jlc_twin's hard-coded 1600x1000 it FALSE-fails U_LDO and Q_SWDRVRHA because MIN_BODY_PX=20 is an ABSOLUTE pixel floor against an mm tolerance — a GATE defect, reported upstream, re-verified not assumed); P-FACT OK; audit_board PASS (18 polarity / 28 proximity / 13 edge, I-ISO 6.22 mm, I-OUT 0.35 mm); placement_gates PASS; count_parity S-COUNT 4/4 over **243** refdes; A-POS worst 0.00000 mm over 210 rows; ERC 0 errors / 420 warnings. **E-INV 115 -> 136 and ALL 21 NEW INVARIANTS RED-VERIFIED**, each mutation being the defect the assert exists to catch, every one run on a scratch copy (source netlist md5 20707e29ab0bdf848f6e8e34603b1424 identical before and after). E-ADR 9/9. E-TOPO PASS (headroom 1355 mV vs dropout 1300 mV; PD 615 mW vs 1200 mW, 51%). policy_audit FAIL=1 / WAIVED=5 / PASS=25 / HUMAN=6 / N-A=4 — the single FAIL is M-BOM grading the STALE SEALED v1.6 BOM against new source, PROVEN by reading both files side by side (R_COILENPD C25741 -> C137948, R_OVT -> C270658, R_OVB C25756 -> C407739, J_MODE C189896 -> C485354, all four genuine v1.7 changes) and it resolves at the seal. P-SILK-FN went FAIL -> WAIVED over 23 NAMED refs WITH A MEASUREMENT: 23/23 carry a visible refdes on silk at >= 0.45 mm, NONE illegible, 11/23 literally name a net the part touches — the check reads only board-level PCB_TEXT and structurally cannot see footprint reference text, so its premise (opaque J1/TP7 designators) is inverted on this board. **AND THE REVIEWERS CAUGHT THIS SESSION'S OWN SILK PASS SHIPPING SIX SAFETY DESIGNATORS AT 0.130 mm STROKE** — J_ESTOP, J_DOOR, J_MODE, D_DOOR, R_DOORPD, D_COILEN — below the 0.150 mm floor, against 0.150 on the other 243 texts, caused by fix_silk_placement.py's own max(0.13, sz*0.2) at sz=0.45. The pass that repairs the safety labels made them the thinnest silk on the board, and the P-SILK-FN waiver written in the same session calls 0.13-0.15 'at or above the floor', which it is not. Uncaught because text_thickness is one of the four DRC classes this project deliberately sets to ignore."

state:   blocked
op_pid:  none

next:    "FIX LIST, ordered so it costs ONE rebuild instead of four. (1) **PIN-P0-1 needs a DESIGN CHOICE, not a value tweak.** Recommendation (a): move R_PG's top end from 5V_PROTECTED to 3V3 and DELETE R_FLTDIVT and R_FLTDIVB entirely — the TPS259573 /FLT is open-drain and indifferent to the pull-up rail, the node then rests at 3.3 V and needs no divider at all, and TP_PGOOD reads the real node again; this REMOVES two parts instead of adding a constraint. Option (b) keeps 5 V: delete R_FLTDIVT and set R_FLTDIVB ~150k (tap 3.000 V, admissible window 112-257k), or make R_PG <= 9.67k. **AND THE ASSERT MUST GROW A LEVEL TERM** — every part_value/pin_on_net assert goes GREEN on the broken board, because they check that the divider EXISTS, not that the level WORKS. E-INV was RED-verified 21/21 and still missed this: topology asserts structurally cannot catch an arithmetic error. (2) RENDER-P0-1: add an 'ISO 30V' / 'NOT SELV' caption at (189.05, 93.35) — MEASURED legal, 6.46 mm from the block — plus the 1/2/3/4 pole legend, and move J_ISOLOOP's own designator off J_RH_EXHAUST. (3) ONE LINE: floor fix_silk_placement.py's stroke at 0.15 (it is max(0.13, sz*0.2), around lines 289 and 338) and correct the P-SILK-FN waiver text that calls 0.13 acceptable. (4) **TOPO P1-2, the most consequential non-P0: the reed coil's pull-in margin is computed NOWHERE and fails the board's own envelope.** DS p.2 gives pull-in max 3.5 V plus a 0.4 %/K footnote nothing in this tree carries, so V_PI is 3.920 V at 50 C and 4.200 V at 70 C, against 5V_KEY_RELAY vout_min 4.740 V minus a ULN2803 V_CE(sat) that part.yaml DOES NOT RECORD and whose datasheet is NOT COMMITTED; at the Darlington floor the margin runs +220 to -80 mV at 50 C and -60 to -360 mV at 70 C. Commit the datasheet, get V_CE(sat) at ~10 mA, then re-derive or move the rail. K_STOP is NOT exposed (MOSFET-driven off the ungated 5V_STOP rail, +450 mV at 70 C). (5) PIN Q-1, cheap and urgent before anyone writes firmware: every document and invariant in the P0-b work calls U_EXP pad 1 'GPA0'. It is **GPB0**; GPA0 is pad 21, which here is RAIL_EN_A, an OUTPUT. Copper is right, paperwork is wrong. (6) The 0.600 mm comb slots against JLC's 1.0 mm published minimum — THIRD independent sighting. Widening to 1.000 mm is geometrically FREE (verified: stays inside the iso_gapN/iso_pktS keepouts, nearest copper > 2.4 mm). Escalates to P0 if the capability page confirms 1.0 mm. (7) J_PI (C35165) and J_LOADCELL (C157991) carry LCSC codes in bom_jlc.csv but appear on NO CPL row — JLC is told to buy two parts it is never told to place; A-POP only tests the opposite direction. (8) Recorded with numbers: the LDO tab's 3V3 copper MEASURES 13.96 mm2 on the tab layer and 0.00 mm2 on In1+B.Cu, against power_tree.yaml's claimed 55-65 C/W which needs ~645 mm2; H4's SELV creepage leg fell to 0.607 mm because v1.7 placed D_COILEN in ADR-0015's load-bearing corridor (total still passes at ~7.21 mm, but the fastener-OD derivation is now stale); 18 of 46 dossiers have NO committed datasheet, 7 of them pin-map-load-bearing (MAX31856, ULN2803, HC595, HC14, LVC1G00, LTV-817S, AMS1117). **SOURCING — OWED, AND DELIBERATELY NOT INHERITED:** the -13L not_in_catalog verdict was RE-READ fresh on 2026-07-28 (keyword DIP05-1A72-13L -> 1 hit C1524853, stockCount 0; all five DIP05-1A72 variants 0; controls C5620 = 5414 and C25741 = 465129 live the same minute), so the hand-solder posture stands on its OWN evidence — but every DISTRIBUTOR quote in 01_docs/sourcing/ is keyed to the -12L code and does NOT transfer, and no -13L distributor stock figure has been read. The DIP05-1A72-12D alternate is WITHDRAWN: it is pin-out CODE 12, a different lead arrangement, and fitting it to the v1.7 land would reproduce the exact short v1.7 exists to fix."

updated: 2026-07-29T01:05:00
