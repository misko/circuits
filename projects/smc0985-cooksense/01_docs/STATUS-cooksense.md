# STATUS beacon — cooksense (MAIN board; live head, OVERWRITTEN each transition)

History is append-only in `journal/<stage>_cooksense.md`; this is only the
current frame. Read by `skills/kicad-pcb/scripts/pcb_status.py`, graded against
the tree by `skills/kicad-pcb/scripts/status_beacon_check.py` (canon M-BEACON).

<!-- reader parses from here down -->
stage:   V1.7-CANDIDATE--DRC-GATE-0/0/0--CHAIN-FROZEN--4-TRAPPED-PADS-BONDED--PRE-SEAL-BATTERY-NEXT
step:    "**THE USER DECISION IS IMPLEMENTED AND ADR-0025 IS `accepted`.** The resolution is ASYMMETRIC and that is the design, not a split: `J_DOOR` is REMOVED FROM THE NETLIST (no door signal exists or will) together with R_DOORPD/R_DOORS/D_DOOR/R_DOOROKPD, while `J_ESTOP` STAYS POPULATED behind a REMOVABLE SHORTING PLUG so a real E-stop can be fitted later with no copper revision. **REMOVAL AND NOT DNP, MEASURED:** fix_silk_placement.py has no dnp/exclude_from_bom/population reference at all, so a DNP part still gets a designator placed and frees ZERO silk - DNP could never have dissolved the 1b/7 FATAL. **THE FREED AND INPUT GETS A REAL TERM, NOT A RAIL:** DOOR_OK was NEVER in KEY_RELAY_ALLOWED (BRIEF.md:82 has no door term) - its only consumer was U_OSCLR.1, so `OS_CLR_N = ESTOP_OK . STOP_REQ_N` now, which is what BRIEF.md:89's 'hardware key-relay inhibit' commissions and the board did NOT have. No 3V3 tie-off anywhere (the rejected O4'/O3 move). **J_ESTOP IS KEYED TO SH-3 BECAUSE THE PLUG IS ITSELF A NEW HAZARD** - a GH-5 shorting plug bridging circuits 1-2 lands on switched-3V3-to-GND on all four pod housings, a dead short behind an AO3401A; ADR-0024's pinout argument is blind to it because the PLUG travels, not the pod. SM03B-SRSS-TB/C160403, 1.00mm pitch UNIQUE on this board, sense on CIRCUIT 3 (the one circuit that cannot align in any foreign shroud) so the plug bridges 2-3 and its short cannot be COMPLETED elsewhere. **THE BRIEF'S 4-PIN ASK WAS CHECKED FIRST and does not bind:** v1.3 P0-2 moved contact B off this housing permanently, so 3 circuits is the FULL requirement; SM04B-SRSS-TB/C160404 stock 17438 exists and was rejected on column width, not stock. **THE 30V J_ISOLOOP LABEL P0 IS CLOSED IN SOURCE AND MEASURED ON THE BUILT BOARD.**"
measure: "**THE DRC GATE IS 0/0/0. LIVE, UNPIPED, THIS PASS**, on 04_kicad/cooksense.kicad_pcb built by rebuild_v18h.log (`kicad-cli pcb drc --severity-all --refill-zones --schematic-parity`): **0 violations / 0 unconnected / 0 schematic parity**, exit 0. It was 0/4/0 before, and the FOUR were CLASSIFIED rather than counted, by UNION-FIND over every piece of FILLED copper on each plane net (pads+tracks+vias+each zone island, joined by exact-shape collision, layers bridged only by through-items) — a method the DRC engine does not share: GND 626 items -> **4 clusters** {main 622} {R_ESTOPOKPD.2 + a 0.327mm2 F.Cu island} {C_LATCHB.2} {U_CAND2.2}; 3V3 168 items -> **2 clusters** {main} {TP_3V3.1}. FOUR defects, one-for-one onto the four DRC items — and the ZONE-TO-ZONE item was NOT a fifth shape: DRC names both its endpoints at the zone ORIGIN (12,53), and the island is R_ESTOPOKPD's own thermal pocket, so bonding the PAD closed the ZONE item. R_ESTOPOKPD is NEW in ADR-0025, which is why it had never been reserved. AFTER the four deterministic bonds: GND **590 -> 1 cluster**, 3V3 **168 -> 1 cluster**; `seed_stubs: 81 pin(s) served, 0 refused`. SITES by MAX GROWTH not proximity, measured with 03_src/cooksense/scan_bond_sites.py (pcb_toolkit.via_site_ok at seed_stubs' own via 0.25/0.15 clearance 0.13), and BARREL-INSIDE (whole annulus within the pad, 8 rays) which the earlier waves did not require: R_ESTOPOKPD.2 growth 0.05@169.640,60.000 / C_LATCHB.2 0.05@183.820,76.190 / U_CAND2.2 0.30@186.250,81.200 / TP_3V3.1 1.00@112.050,70.200. The scanner CARRIES CONTROLS and they PASS (3 committed route.yaml sites score LEGAL with slack; the same coordinates probed on a foreign netcode and a point on a foreign track centreline are REFUSED) — a scan that cannot say NO cannot say YES. **S-COUNT PASS 239/239 across 4 source pairs** (board/circuit.json/kicad_sch/netlist vs manifest) — it had not run since ADR-0024. **CHAIN PROMOTED FIRST (933d7e5)**: final_chain.kicad_pcb held md5 1160b580 while the built board came from the v18g winner c0/r9 e9476c1f; bonds measured against an unpromoted chain are bonds for a board you will not ship. **LIVE LCSC READS 2026-07-29:** C265111 (SM08B-GHS-TB, J_THERM_A/B) **stock 0** — CONFIRMED sourcing blocker; C268100 (SHR-03V-S-B) **stock 0** — confirmed; C160403 (SM03B-SRSS-TB, J_ESTOP header) 52323 OK; and every listed plug equivalent is stocked (C2909166/2153, C2962274/1625, C338906/515, APSHR-03V-S C392108/25405) as are the contacts C263995/167550. **M-BOM: the beacon's previous claim is CONFIRMED and SHARPENED. policy_audit grades `latest_sealed_release/fab/bom.csv` (v1.6), NOT 06_build — so it structurally cannot clear until a v1.7 archive seals with a fresh BOM. AND `06_build/fab_v21/bom_jlc.csv` (17:50 today) is PROVABLY STALE for this revision: it still carries a **J_DOOR** row, a part ADR-0025 DELETED from the netlist, and it merges J_ESTOP onto the SM05B-GHS-TB/C189896 row — i.e. the stale artifact would ORDER THE E-STOP CONNECTOR AS A GH-5 instead of the keyed SH-3, reversing ADR-0025 D5's whole point in the one artifact a fab reads. export_jlc_package MUST rerun before anything grades a BOM."
state:   working
op_pid:  none   # rebuild_v18h (--reuse-route) EXITED 0. NEXT LONG OP = export_jlc_package + the four review lenses.

next:    "**DO NOT RE-RACE BLIND AND DO NOT RE-DERIVE WHAT IS ALREADY MEASURED.** 1) FINISH THE DRC LOOP. rebuild_v18g.log carries C_EFIN attempt 4 (59.4,63.2,0 - due SOUTH of esc_U_EFUSE_S, using the corridor for 5V_RPP instead of fighting it, at a documented 5.45mm loop) and J_ESTOP at x196.6. Read its DRC. **THE REMAINING CLASS IS TRAPPED PLANE PADS AND THE BOARD'S OWN REMEDY IS DETERMINISTIC BONDS, NOT A RE-RACE** - route.yaml lines ~770-925 hold ~20 precedents and the method is stated there: score on-pad via sites by **max GROWTH, not proximity** (a site legal by 0.00mm is a site the next reroute takes away). A scanner implementing exactly that is at /tmp/claude-1000/-home-mouse9911-gits-circuits/2f553bbf-5f29-41dd-813a-141c6949dd15/scratchpad/viascan.py (usage: viascan.py BOARD REF.PAD ...). WORKFLOW: race ONCE, PROMOTE the winning chain to 03_src/cooksense/route/final_chain.kicad_pcb, THEN add the seed_stubs + their User.2 reservation rects, then rebuild with the DEFAULT (--reuse-route) so the trapped set cannot move - `re-racing moves WHICH pads are trapped, it does not stop pads being trapped` (route.yaml says so, and three consecutive races proved it). Candidates measured on v18e/v18f: TP_3V3.1[3V3], C_LATCHB.2[GND], U_CAND2.2[GND]; RE-MEASURE on the settled board, do not carry these forward. 2) **THEN THE FAB BATTERY, and export_jlc_package IS WHAT MAKES M-BOM GRADEABLE** - it has been predicted three times and never demonstrated because the BOM it grades was never exported. 3) THEN a FRESH four-lens review battery. 4) THEN seal v1.7 per the 2-commit ritual + SUPERSEDED.md on v1.6. **OWED IN THE ORDER_README, and one is LOUD: the board DOES NOT FUNCTION WITHOUT THE J_ESTOP SHORTING PLUG** (SHR-03V-S-B/C268100 reads LCSC stock 0; stocked equivalents HDGC1002H-3P C2909166/2153, HC-1.0-3Y C2962274/1625, A1002H-3P C338906/515; contacts SSH-003T-P0.2-H C263995/167600; bridge circuits **2-3**, NOT 1-2) - with J_ESTOP open the board is inert BY DESIGN and that is indistinguishable from a dead board to a technician. Also owed: the 1.00mm web DFM query, the A-POL order-preview human gate for C165895/C558584, and a #28->#24/26 in-enclosure splice note for any future real E-stop field run (SH takes AWG#32-28 only; the objection applies to the FIELD harness, not to the 5mm shorting bridge). **NEW A-STOCK ITEM, out of ADR-0025's scope and NOT fixed: C265111 (SM08B-GHS-TB, J_THERM_A/J_THERM_B) reads LCSC stockCount 0 on a fresh read 2026-07-29.** **OWED SKILL PATCHES (skills/ untouched):** (i) NEW - `node_level`'s `released` branch requires a series-RESISTIVE path to a supply RAIL, so it returns UNREACHED on any node whose only DC path is a pull-down to GND, i.e. EVERY restrictive-default node on this board (ESTOP_RAW, OS_CLR_N, GPB3_SPARE); the code already special-cases the mirror image (`no resistive path to GND -> pulled to the rail`), so this is a one-branch symmetry fix - it is why ADR-0025 could not carry a node_level assert and had to use netlist MUST-NOT-EXIST asserts instead; (ii) check_dru_vacuity's hard-coded 1.0672mm/106 pairs UNDERSTATES DRC's edge-measure 0.5723mm/67; (iii) t1_gate_contract.py's fixture must STAY pointed at sealed v1.6 even after v1.7 seals; (iv) R-LEN's own VACUITY: block; (v) per-board G-VACUOUS-DRU wiring; (vi) P-SILK-REF's self-certification loop; (vii) a waiver schema carrying command:+output:; (viii) an E-NETREF kind for pins.<n>.tie; (ix) the `node_level: injected` gap ADR-0024 already owes. **THE KEYPAD P0 EVIDENCE MUST NOT BE DISTURBED**: keypad_isolation_6mm measures 0 with a named-refdes exemption, 67/67 pre-fix violations had J_KEY_MATRIX.MP on one side with the far side 100% KEYPAD_ISO-class - zero domain crossings, the barrier holds, only the rule was blind."

updated: 2026-07-29T22:39:29
