# STATUS beacon — cooksense (MAIN board; live head, OVERWRITTEN each transition)

History is append-only in `journal/<stage>_cooksense.md`; this is only the
current frame. Read by `skills/kicad-pcb/scripts/pcb_status.py`, graded against
the tree by `skills/kicad-pcb/scripts/status_beacon_check.py` (canon M-BEACON).

<!-- reader parses from here down -->
stage:   V1.7-MARGIN-VS-AMBIENT-ESTABLISHED--DEFECTS-FIXED--ENVELOPE-IS-THE-USERS-CALL--NOT-SEALED
step:    "**THE HONEST ANSWER IS ESTABLISHED AND THE BOARD IS UNTOUCHED** (`04_kicad/cooksense.kicad_pcb` md5 `9f4fd5fae810f40a52b1035df727243c`, DRC 0/0/0 re-run by me with `--exit-code-violations`, RAW_EXIT 0). ADR-0028 published: both margins as CURVES over ambient with every term at its own basis, the two missing terms (Micro-Fit contact resistance; the temperature basis of 58 % of the series sum) carried SEPARATELY and graded CITABLE / ASSUMPTION-BOUNDED / UN-CITABLE, and four options priced with a recommendation. **NO ENVELOPE DECLARED — that is the user's call and the ADR says so in Decision 6.** Deliverable-3 fixes landed in source: the `power_tree.yaml` worked example (was computing at `linear_rails[5V_KEY_RELAY].iout_max_A` 0.150 A and publishing a margin **2.16x** the true one), the deleted `+3.8 C` excursion, `02_parts/43650-0224` + `02_parts/PESD5V0S1BA` `limits:`, the **DIP05 reed pull-in invariant that was 49 mV too loose in the PERMISSIVE direction**, two waivers whose prose had drifted off the board, and eight measured doc-drift diffs. FIVE reviews rescued from `06_build/` (throwaway) and a scratchpad into `08_reviews/`, verbatim, md5-checked."
measure: "**DROPOUT MARGIN MOVES -0.31 mV/K - IT IS NOT PRIMARILY AN AMBIENT PROBLEM.** Model MEASURED by me (own decomposition of ADR-0027's two transfer resistances into copper + per-component temperature bases; control: reproduces `V(U_LDO.3) = 4.7281 V` / `1329.1 mV` exactly). Worst case with 2 CITED 10 mOhm contacts: **+33.0 mV @ 23 C -> +18.1 mV @ 75 C -> +14.0 mV @ 85 C**; at the aged 2x30 mOhm allowance **+2.0 mV @ 75 C and -2.1 mV @ 85 C**, and the ADR-0027 staggered heater peak goes **-0.6 mV**. TYPICAL case (nominal 5.000 V, typ 1.100 V dropout, true worst continuous 0.3191 A): **+497 mV**. **THERMAL BINDS FIRST**: `Tj <= 125 C` gives Ta <= **82.9 C** citable / **78.3-80.1 C** with the layout lens's DERIVED board-rise term (+1.55...+4.65 C from the board's other 0.958 W, i.e. **20-59 % of the declared 7.92 C margin**); honest margin at Ta 75 C is **3.3-6.4 C**, not 7.92. F1's PTC tempco is UN-CITED (Bourns publishes none); inverting Bourns' OWN `Ihold` derating table (2.00 A @23 C -> 1.29 A @85 C) under a NAMED assumption gives R(85)/R(23) = **1.0-1.15**, and makes the x1.82 break-even imply a **278 C** polymer switching temperature. GATES, unpiped: DRC 0/0/0 **0** / E-TOPO **0** / E-INV 168/168 **0** / E-ADR 12/12 **0** / policy_audit FAIL=0 (HUMAN=6 N-A=5 PASS=28 WAIVED=6) **0** / M-BOUND **0** (5 CITED, 37 OWED against a <=37 floor - the three NEW bounds all regenerate and all were RED-TESTED) / G-ORPHAN 315/315 **0** / E-NETREF **1** (pre-existing 21 ghost K7 refs). Stock re-measured 2026-07-31T03:02Z with both controls: `C265111` **5 / MOQ 21** (fourth reading; sequence 0->5->0->5 in 48 h - **the blocker is MOQ 21, never the stock**), `C587657` **130->80->70**."
state:   blocked
op_pid:  none

next:    "**THE SEAL WAITS ON A USER DECISION, NOT ON A GATE.** ADR-0028 prices four options and RECOMMENDS **(a) narrow the declared ambient to 65 C - the BRIEF's own `stop` rung - PLUS (d) a MANDATORY bench gate in ORDER_README**; do the tab-via item at the next copper revision; do not spend (c) yet. Reasoning: the two margins bind in DIFFERENT places, so no single option fixes both. Thermal is 1 C per C of ambient and (a) solves it outright (15.1 C of margin at 65 C **with** the board term). Dropout moves -0.31 mV/K, so (a) buys it +1.4 mV - nothing - because **the dropout problem is an EVIDENCE problem, not a design problem**: the whole comparison is a 1300 mV figure ds1117 publishes only at 0.8 A, applied to a 0.2 A rail. ONE BENCH HOUR retires four of the six un-citables (F1 R-vs-T, the AMS1117 dropout at 0.2 A, theta_JA on this mounting, the board rise) and may make every other option unnecessary. **DO NOT SEAL BECAUSE THE CONTRACT-NAMED REVIEW FILES SAY SOUND** - 4 independent lenses split **2 SOUND / 2 DEFECTIVE**, and the two SOUND ones occupy the contract filenames having overwritten the DEFECTIVE ones, so M-REV's design-side red clears on a FILENAME TECHNICALITY. All five are now archived verbatim in `08_reviews/` with md5s and verdicts tabled in `DISPOSITIONS.md`; choosing what the contract name carries IS the seal decision. Remaining mechanical blockers: 2 x EVIDENCE PATH MISMATCH (ADR-0025 + DISPOSITIONS name prior-release paths) - NOT fixed here, and note the checker scans review BODIES for release-dir path strings so quoting one raises the finding against your own document. `ORDER-DECL-UNDATED` IS fixed: this project now DECLARES it dates in LOCAL time and `measured_on` is 2026-07-30. **M-FRESH is UNREACHED on this board** - no `build_provenance.json` stamp exists, so the driver predates the gate and must be reseeded from the template at the next revision, not retro-edited. F-RENDER checked by hand instead: staged `pdf/schematic.pdf` is byte-identical (md5 `74cbf7cb...`) to `03_tscircuit/build/schematic.pdf` and post-dates its producer `cooksense.tsx` - fresh, not inherited. **OWED SKILL PATCHES P15-P19, FILED NOT IMPLEMENTED** (`skills/` outside this partition): P15 a series sum must declare its NODE; P16 every term must declare a TEMPERATURE, not just the copper; P17 `Tj = Ta + PD*theta_JA` must account for the board's OTHER dissipation; P18 a worked example in a graded file may only read graded keys; **P19 `waiver_provenance.py` hardcodes `03_src/rules/policy_waivers.yaml` and this project's rules live under `03_src/cooksense/rules/`, so M4 has NEVER graded this board's 12 waivers - it exits 1 on a zero denominator and that has read as an invocation error all along.** Two fleet gate gaps to report, also not mine: `jlc_twin` PAD-GEOM compares pad CENTRES only - there is NO pad-SIZE check anywhere, for any part, on any board; and pcbnew `GetEffectiveShape()` returns a full disc for NPTH pads whose `size == drill`, inventing false findings."
updated: 2026-07-30T22:40:00
