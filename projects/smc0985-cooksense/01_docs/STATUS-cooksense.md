# STATUS beacon — cooksense (MAIN board; live head, OVERWRITTEN each transition)

History is append-only in `journal/<stage>_cooksense.md`; this is only the
current frame. Read by `skills/kicad-pcb/scripts/pcb_status.py`, graded against
the tree by `skills/kicad-pcb/scripts/status_beacon_check.py` (canon M-BEACON).

<!-- reader parses from here down -->
stage:   BLOCKED-DO-NOT-ORDER
step:    "**ALL THREE INHERITED P0s ARE CLOSED AND MACHINE-VERIFIED. v1.7 IS STILL NOT SEALED, ON A FOURTH P0 THAT WAS CARRIED AS A P1 AND TURNED OUT TO BE REAL.** v1.0-v1.6 remain DO-NOT-ORDER, unchanged — every one carries the pin-out-12 relay land. MAIN board only; interposer-v1.1-2026-07-27 is unaffected and remains ORDERABLE. CLOSED (1) PIN-P0-1/TOPO P1-1, the dead eFuse fault readback: ADR-0022 moves R_PG's pull-up from 5V_PROTECTED to 3V3 and DELETES R_FLTDIVT + R_FLTDIVB and the net EFUSE_FLT_DIV; U_EXP.1 (GPB0, NOT GPA0 — that is pad 21, RAIL_EN_A, an OUTPUT) and TP_PGOOD now sit directly on EFUSE_FLT_N. The gate, not the argument: node_level went from 'EFUSE_FLT_DIV reads 0.833 V against V_IH 2.640 V' to 'EFUSE_FLT_N reads 3.300 V via 3V3 through 100k [R_PG=100k], no resistive path to GND -> pulled to the rail'. E-INV 136/136, E-ADR 9/9, and FIVE new asserts RED-VERIFIED on a scratch netlist (md5 5bc8eaf9225b5b551fca3b4872dd21b2 unchanged before/after). Recorded from that work: node_level grades a LOGIC LEVEL, not an abs-max — moving R_PG back to 5 V leaves node_level PASSING at 5.000 V and is caught only by the pin_on_net R_PG.2 assert; they are different claims. AND A CHECKER DEFECT WAS FOUND WHILE LANDING IT: `supplies:` declared `N3V3` (the tsx author-prefix form) and no such net exists in the netlist, so the 3V3 rail was INVISIBLE to every node_level grade. (2) RENDER-P0-1, J_ISOLOOP unlabelled: 'ISO 30V' now prints **0.085 mm** from the NOT-SELV block body and 'NOT SELV' at 7.892 mm, both h 0.600 / stroke 0.150. (3) The six 0.130 mm safety designators: fix_silk_placement now sets EVERY silk stroke to 0.25 x height (KiCad's own clamp) and every SAFETY text to h 0.600 / stroke 0.150 — 250 texts re-measured, 0 below the 0.1125 mm tier floor, 0 storing a stroke KiCad would clamp away, 11/11 safety texts at the published floor. THE FOURTH P0, TOPO P1-2, IS WHY THERE IS NO SEAL: the reed-coil pull-in margin is NEGATIVE inside the board's own -20..+70 C envelope."

measure: "**THE BOARD MEASURES DRC 0 violations / 0 unconnected / 0 schematic parity** on a full from-source rebuild (`rebuild_all.sh --reroute`, sixth attempt, exit 0). E-INV 136/136; E-ADR 9/9; E-TOPO PASS (headroom 1355 mV vs dropout 1300 mV; PD 615 mW vs 1200 mW, 51%); audit_board PASS (18 polarity / 28 proximity / 13 edge / 245 silk, I-ISO 6.22 mm, I-OUT 0.35 mm); placement_gates PASS 0 fails 0 warns (P-OUT 0.30 mm, P-CAP ratio 0.29); **count_parity S-COUNT PASS 4/4 over 241 refdes** (243 -> 241; `--board cooksense`, and `03_tscircuit/manifest.yaml` was updated because it is the AUTHOR's declared intent); export_jlc_package exit 0 with A-ROT 208/208 sourced and F-LEGIBLE 59/59; bom_legibility 58 checks OK; bom_source_check **PASS** against the STAGED v1.7 BOM (210 coded refdes, leg C 27/27); jlc_twin 209 OK / 471 rows, **bodies mounted 210/210**; ERC 0 errors / 417 warnings. **TWO GATES ARE NOT GREEN AND NEITHER IS WAIVED.** policy_audit FAIL=1 / WAIVED=5 / PASS=25 / HUMAN=6 / N-A=4 — the FAIL is M-BOM grading the STALE SEALED v1.6 BOM against moved source, PROVEN a non-defect by running bom_source_check directly on the staged BOM (PASS), and it resolves at the seal. **jlc_stock_check FAIL, and this one is NEW AND REAL: C506653 MCP23017-E/SS (U_EXP) reads stockCount = 0**, 55/56 coded lines clear the 5x floor. Last session the same gate read 56/56; this is a live change, not an inherited number. It is a SOURCING blocker, not a design defect. **THE COIL MARGIN, COMPUTED FOR THE FIRST TIME IN THIS TREE:** ULN2803A SLRS049G is now COMMITTED (sha256 84ec37810a5cef7c352a320852a6b21bdbe709d2fb60b4d6e7ad6d93ec324a4d) with a registered provenance deviation — every ti.com URL for the part 404s while a sibling dossier's TI URL returned 200 the same minute, so it came from a mirror, verified TI-origin by PDF metadata and cross-checked byte-identical against a second independent host. Its EC table specifies V_CE(sat) only at 100/200/350 mA, two orders above the ~7 mA coil, so Figure 1 p.5 was digitized at 900 dpi: 0.629-0.674 V at 10 mA. Reference temperature is 20 C, NOT 25 C (the Standex footnote sits under a table headed 'Coil Data (at 20C)'), so V_PI(T) = 3.500 x (1 + 0.004 x (T - 20)) and margin(T) = 4.740 - V_CE(sat) - V_PI(T): **-20 C +1.130 V / +25 C +0.500 V / +50 C +0.150 V / +70 C -0.130 V** on the TYPICAL 0.67 V driver drop, and negative from +50 C on the 0.88 V worst case. The current view is the same fact, not a second derate — pull-in is an ampere-turn condition so I_PI = 3.5/500 = 7.00 mA is temperature-independent while delivered current falls to **6.81 mA at +70 C**. K_STOP is the one relay NOT exposed (2N7002, not a Darlington, off the ungated 5V_STOP): +1.714 / +1.084 / +0.734 / **+0.454** V, reproducing the review's '+450 mV at 70 C' exactly."

state:   blocked
op_pid:  none

next:    "**TOPO P0 (was P1-2) — THE COIL PULL-IN MARGIN IS A TOPOLOGY DECISION AND IS DELIBERATELY NOT MADE HERE**, the same way the R_PG rail choice was not. Twelve reed relays on a cooking appliance are not guaranteed to close at the top of the declared envelope, and -20 C is comfortable so a room-temperature bench test will NOT find it. Three remedies, quantified: (a) RAISE THE COIL RAIL — the margin needs +130 mV at 70 C on typ and +340 mV on worst case, so 5V_KEY_RELAY vout_min must reach ~5.08 V, which the 4.85-5.25 V ADR-0021 supply cannot deliver through Q_COIL's drop; this probably means a boost or a different rail spec. (b) DROP THE DRIVER SATURATION — replace the ULN2803A Darlington with a MOSFET array (R_on x 7 mA is millivolts, not 670), which buys the whole 0.67-1.10 V back and makes every corner positive; this is the K_STOP arrangement already proven on this board and is the cheapest fix in margin terms. (c) NARROW THE DECLARED ENVELOPE to <= +45 C, which is a specification change on a COOKING appliance and should be argued, not assumed. Whichever is chosen, ADD A node_level-CLASS ASSERT so the margin is a check and not a comment (ADR-0007) and RED-VERIFY it. **THE FRESH FOUR-LENS BATTERY IS OWED, NOT SKIPPED** — it was deliberately not run this session because a confirmed P0 already blocks and closing it will change the power tree or the coil driver, a MATERIAL change needing its own battery; running four lenses now would grade a board that must change again. **SOURCING, NEW THIS SESSION: C506653 (MCP23017-E/SS, U_EXP) is at ZERO LCSC stock.** It was 56/56 PASS last session. Re-read it live before any order, find an alternate (MCP23017-E/SO SOIC-28 is a different footprint; MCP23S17 is SPI, not a drop-in), or move U_EXP to the hand-solder posture the twelve relays already use. ALSO CARRIED, with numbers, none of them blocking: the 0.600 mm comb slots against JLC's published 1.0 mm minimum (FOURTH independent sighting; widening is geometrically free, escalates to P0 if the capability page confirms 1.0); J_PI (C35165) and J_LOADCELL (C157991) carry LCSC codes but appear on no CPL row; the LDO tab's 3V3 copper measures 13.96 mm2 against the ~645 mm2 power_tree.yaml's 55-65 C/W needs; the 2N7002 datasheet is NOT COMMITTED so K_STOP's 0.10 V V_DS is an estimate (not load-bearing: even at 0.50 V the +70 C margin is +0.054 V); J_ISOLOOP's own designator now sits 10.817 mm from its block and 4.011 mm from J_RH_EXHAUST (lead -6.805 mm) — WORSE than before, non-blocking only because the block's identity is now carried by the 'ISO 30V' caption 0.085 mm away, and a KF350 screw terminal cannot be cross-plugged with a JST-GH; the J_ISOLOOP pole legend does NOT fit at the terminal (nearest legal site is unbounded) and rides the north-stack caption as 'POLES 1=C 2=LOOP 3=LOOP 4=E' — a per-pole digit is GEOMETRICALLY IMPOSSIBLE because the pads sit at the CENTRE of the KF350 body in x, so anything printed either side of a pole is under the moulding once it is fitted; and J_ESTOP's label ownership landed at a DEGRADED 0.5 mm demand (measured lead +0.659 mm) because the 1.5 mm margin was set when safety text was 0.45 mm and a 0.60 mm box needs 78% more area."

updated: 2026-07-29T11:20:00
