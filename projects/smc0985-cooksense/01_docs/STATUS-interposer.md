# STATUS beacon — interposer Board C (live head; overwritten each transition)

`stage`/`state` use the contract vocabularies (the seven pipeline stages;
`working` | `blocked` | `done`) — a value outside them renders as `?` in
`pcb_status.py`, and the free-text `stage: v1.1-SEALED-ORDERABLE` this file
used to carry did exactly that. The release the seal produced is named in
`step:`, where `status_beacon_check.py` grades it against the tree (M-BEACON).

<!-- reader parses from here down -->
stage:   seal
step:    "interposer-v1.1-2026-07-27 is SEALED, LIVE (no SUPERSEDED.md) and ORDERABLE (seal commit 40a899b, source commit ee5632a). It clears v1.0's P0-1 (A-ROT) and P0-2 (A-POP) and folds in a legible BOM, a standalone archive and the assembly gate family that had NEVER RUN on this board; v1.0 is DO-NOT-ORDER. **P0-1 A-ROT CLEARED: J_KEY_MATRIX CPL rotation 90.0 -> 270.0.** Derived from the EXACT PAD-FIT path, not the footprint-name DB: the measured per-LCSC row for C2683602 (jlc_lcsc_rotations.csv:17) records offset 0 from a pad-by-number fit against JLC's own cached model at rms 0.0049mm vs 5.0792mm next best = 1037x separation, operator verified against pcbnew; board orientation -90 => board_rot 270, so CPL = 270+0 = 270.0. Independently re-fitted for THIS release by jlc_twin against JLC's model: fit=0.01mm jlc_offset=0 src=lcsc, and the sealed main board (cooksense-v1.4) ships the same code at the same board orientation at 270.0. The refuted rule ^JST_GH_SM,180 is not consulted (db=0.0 advisory, agrees). **P0-2 A-POP CLEARED**: both 10FDZ-BT carry exclude_from_pos_files (03_src/interposer/floorplan.yaml pattern) and a not_assembled entry with a DATED catalog query in the NEW 03_src/interposer/rules/assembly.yaml; CPL is 1 row; the MANIFEST not_assembled line is GENERATED. **CPL datum FIXED too**: Mid X 15.00 -> 15.25, the pad-array-centre datum JLC actually places to. **F-LEGIBLE FAIL -> OK.** The ASSEMBLY FAMILY NOW ACTUALLY GRADES THIS BOARD: A-POP/A-POS/A-BODY/A-EVID/A-RENDER/A-STOCK/F-PAYLOAD/F-LEGIBLE all present and green, where v1.0's policy_audit.md had no A-* row at all."
measure: "DRC 0/0/0 (--severity-all --refill-zones --schematic-parity) on 04_kicad AND, independently, 0/0/0 on source/ extracted ALONE into an empty directory — the same command on v1.0's archive returns 29 (its fp-lib-table pointed outside itself and no .kicad_pro/.kicad_dru shipped). ERC 0 errors / 79 warnings on the SHIPPED schematic with the SHIPPED fp-lib-table beside it (endpoint_off_grid 56 + lib_symbol_issues 23); v1.0 shipped 102 and the delta is EXACTLY its 23 footprint_link_issues, one per component, which were reporting the SAME broken library link as its 29 standalone-DRC violations. That also closes v1.0's M-CONS 102-vs-79 discrepancy, where the manifest said 102 and its own audit said 79. **COPPER PROVEN GEOMETRICALLY IDENTICAL to v1.0** by an aperture-resolved, order-independent gerber/Excellon multiset comparator that shares no method with the plotter: F_Cu 450 atoms, B_Cu 180, masks 84/52, pastes 12/0, PTH 55 holes, NPTH 6 — ALL IDENTICAL. Edge_Cuts differs by ONE segment's traversal DIRECTION (identical as an undirected set). F.Silkscreen differs by 50 of 5368 atoms, every one inside a 0.514x0.900mm cell at x44.286-44.800 y12.009-12.909 = the version digit. Independently reproduced by the red-team lens with its own comparator. Re-plotted from source/ alone: 11/11 IDENTICAL. policy_audit FAIL=0 (PASS=22 WAIVED=4 HUMAN=7 N-A=7), at a FIXED POINT: a fresh run against the stamped MANIFEST reproduces the shipped file byte-for-byte; MANIFEST self-check 65/65 by `sha256sum -c`, 0 orphans, 0 unlisted; freshness PASS with ONE measured exception (pdf/schematic.pdf, because the .tsx and .kicad_sch are md5-identical to v1.0's and the netlist is node-identical); A-POS worst datum residual 0.00000mm; A-BODY 1/1; A-RENDER 1 measured/1, centre delta 0.165mm; A-STOCK C2683602 = 8559 re-queried 2026-07-27 vs need 1x5; F-LEGIBLE OK 3 checks (v1.0: FAIL, 2 findings); F-PAYLOAD OK incl. F-POUR pourless BY DECLARATION; A-EVID 0 missing; parity 50/50 connected nodes, 1 documented no-connect (J_KEY_MATRIX.MP, netless by design). Red-team INTEGRATED lens (fix-pass breadth): 0 P0, ORDER/ORDER."
state:   done
next:    "USER-HELD, and NOT mine to waive: (1) the 10FDZ-BT POLARITY read — M9/M10 UNMEASURED, so 'pin 1 = the boss end' is DECLARED not CONFIRMED against the OEM's CN1; if reversed the board still works (all ten lines pass, the tail is self-keying) and only TP_M_*/TP_C_*/KP_* NAMING is wrong. Discriminator: boss end overhangs 5.30mm, far end 8.10mm. (2) the M3 boss offset — measured 2.35mm puts the worst pin 0.190mm out against 0.10 (boss play) + 0.13 (pin play) = 0.23mm; margin 0.04mm, 83% of budget consumed, and at the boss's 1.70mm NOMINAL it would INTERFERE by ~0.01mm — dry-fit EVERY connector. The boss is a LOCATOR: ream or nip, not scrap. (3) the JLC placement-preview eyeball on the GH rotation before ordering. (4) flex-jumper G1/G2 coupon discipline (task #13, separate part) — S2 is ANSWERED from photographs: the OEM tail is PLAIN, so build the jumper PLAIN. USER DECISION 2026-07-27: build with the current 10FDZ-BT footprint; it is NOT re-cut. || KNOWN FALSE FAIL ON THE FLEET DASHBOARD, raised as a background task, NOT a defect in the release: fleet_regrade.py invokes assembly_coverage.py <rel> with no --assembly, and assembly_coverage resolves root/03_src/rules/assembly.yaml — which on this ADR-0007 multi-board project is a SYMLINK to the MAIN board's file, so the interposer release is graded against COOKSENSE's population set and reports A-POP FAIL with 18 cooksense refdes. MEASURED both ways 2026-07-27: bare -> FAIL(18); the same command with --assembly 03_src/interposer/rules/assembly.yaml -> PASS. Second trap recorded with it: symlinking the release into the shadow does NOT scope it, because resolve() follows the link back out. || ALSO RAISED, out of scope here: F-LEGIBLE grades the MPN column on CODED rows only, so the uncoded 10FDZ-BT self-supply row ships a blank MPN and no gate sees it (exporter change, background task)."
op_pid:
updated: 2026-07-27T18:39:08
