# STATUS beacon — interposer Board C (live head; overwritten each transition)
<!-- reader parses from here down -->
stage:   v1.0-SEALED-BUT-DO-NOT-ORDER
step:    "interposer-v1.0-2026-07-24 is SEALED and its COPPER IS GOOD, but its ASSEMBLY PAYLOAD IS NOT ORDERABLE. Fleet audit 2026-07-27 found two P0s. **P0-1 A-ROT: J_KEY_MATRIX (C2683602) ships CPL rotation 90.0 while the board orientation is -90 (=270), i.e. 180 DEGREES REVERSED.** The measured authority (jlc_lcsc_rotations.csv:17) says offset 0 -> correct CPL 270.0, at rms 0.0049mm vs 5.0792mm next best = 1037x separation; cooksense v1.4 ships the SAME code at the SAME board orientation with CPL 270.0; the 90 came from name-DB rule ^JST_GH_SM,180, already #REFUTED in jlc_rotations_db.csv:52-53; and this release's OWN twin_report agrees (jlc_offset=0). IT FAILS SILENTLY: the pad array is symmetric about its own centre (signal pads centre 33.000, MP tabs centre 33.000), so at 180 every pad still lands on a pad and the part SOLDERS PERFECTLY — but pin1<->pin10 swap maps KP_U1<->KP_D4 and REVERSES THE WHOLE TEN-LINE KEYPAD RIBBON. That is verbatim the failure ORDER_README S3 exists to prevent, and S3's claim 'both boards carry the SAME part at the SAME rotation' is true of the boards and FALSE of the two CPLs. **P0-2 A-POP: J_MEMBRANE and J_CN1_JUMPER (blank-LCSC THT 10FDZ-BT, 10 plated drilled pads each, F.Paste on NONE) are ON the CPL with no exclude_from_pos_files, no assembly.yaml and no not_assembled MANIFEST line** — the only defence is ORDER_README prose telling a human to delete the rows. ROOT CAUSE: the ENTIRE ASSEMBLY GATE FAMILY NEVER RAN on this release — its policy_audit.md has no A-POP/A-POS/A-ROT/A-POL/A-BODY/A-STOCK row at all, sealed 2026-07-24, the same days that family was landing, never re-graded."
measure: "COPPER GOOD, re-measured independently: DRC 0/0/0 reproduces with the project's real .kicad_pro/.kicad_dru; floating keypad domain EXACT (net list enumerated from the board is precisely KP_U1..U6/KP_D1..D4 and nothing else, 0 zones, no GND or power net anywhere); J_KEY_MATRIX.MP in NO net; min same-layer cross-net gap 0.5081mm vs 0.5 netclass; MANIFEST 42/42 sha256. **10FDZ-BT LAND PATTERN VERIFIED IN THE SHIPPED DRILL FILES** — PTH T2 C0.900 = 20 holes in two rows of 10, pitch set {2.5400} EXACTLY, span 22.8600mm; NPTH T1 C1.800 = 2 bosses at x22.460, exactly 2.5400mm outside pin 1 and colinear; matches 02_parts/10FDZ-BT/part.yaml verified: word for word. Pin 1 is a RECT pad (copper pin-1 marker). BROKEN: CPL rotation 90.0 (want 270.0); all 3 CPL rows on the footprint ANCHOR not the pad-centre datum (J_MEMBRANE 10.1600mm, J_CN1_JUMPER 10.1600mm, J_KEY_MATRIX 0.2500mm; residuals exactly 0.0000 against the anchor, which identifies the emission); F-PAYLOAD F-POUR FAIL — the board is deliberately pourless (BRIEF S4/S7 forbid a plane in the keypad zone) but nothing DECLARES it, and a pourless board and one that LOST its zones are the same bytes. ARCHIVE NOT STANDALONE (source-only, fab payload unaffected): source/fp-lib-table points the cooksense lib OUTSIDE the archive while source/cooksense.pretty/ ships inside, and no .kicad_pro/.kicad_dru ship — from the archive alone 29 violations, + real project files 2, + repointed lib 0/0/0; the 2 unresolvable footprints are the two 10FDZ-BT ZIFs, the entire point of the board. M-CONS 102-vs-79 LOCALISED: MANIFEST 102, erc.json 102 and a fresh kicad-cli run 102 all AGREE; the outlier is verification/policy_audit.md titled 'shadow_root', run against a 06_build tree where the library resolved — delta 23 = exactly its footprint_link_issues count, one per component."
state:   blocked
op_pid:  none
next:    "v1.1 RE-SEAL (task #18), and DO NOT ORDER v1.0: (1) re-export the CPL at rotation 270.0 on the pad-centre datum; (2) exclude_from_pos_files on both 10FDZ-BT + an assembly.yaml with a generated not_assembled: MANIFEST line + a not_on_assembly_bom assert on the dossier; (3) declare pourless: '<reason>' in assembly.yaml so F-POUR passes for the right reason; (4) ship .kicad_pro + .kicad_dru and rewrite fp-lib-table to ${KIPRJMOD}/cooksense.pretty; (5) re-run policy_audit AGAINST THE SEALED DIR so the A-family actually grades it; (6) SUPERSEDED.md on v1.0 naming v1.1 — NOT before v1.1 exists, per 07_releases/contracts.md, which permits that file only when a successor directory exists to name. USER-HELD and UNCHANGED: buy 2x 10FDZ-BT(S)(LF)(SN) + spares and confirm the land pattern against the PHYSICAL part (ORDER_README S0 gate 1) — the drill payload matches the datasheet derivation, but nothing in this repo can verify the derivation matches the real connector. Flex jumper (task #13) still owns the G1/G2 coupon."
updated: 2026-07-27T00:00:00
