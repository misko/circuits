PAYLOAD IDENTITY — v1.7 vs v1.6 (canon: a fix-claim needs the measurement)
==========================================================================

CLAIM: v1.7 changes TWO CELLS of fab/bom.csv (R_vb1's MPN and LCSC) plus the one
line of source/crow_recorder_central_v2.tsx that produces them. NOTHING ELSE.
The copper is untouched.

A part SUBSTITUTION is the one BOM edit that CAN reach the copper — a different
land pattern would need different pads — so the footprint identity is MEASURED
here, not argued from the catalog page.

METHOD (falsifiable, not a copy-check). Four independent measurements:

  (1) BOARD IDENTITY (md5). One file, three places.
  (2) THE BOARD STILL GENERATES THESE BYTES. Gerbers + drills RE-PLOTTED from
      04_kicad (md5-identical to this release's source/) with
      export_jlc_package.py --layers 6 into 06_build/proof/fab_v17, then
      compared member-by-member against v1.6's SEALED zip and its loose drills,
      after stripping ONLY the plot's own timestamps (%TF.CreationDate,
      "G04 Created by", the .drl "; DRILL file {KiCad} date" line and its
      "; #@! TF.CreationDate" twin). A COPY would prove nothing; a RE-PLOT
      proves the BOARD — including its zones, which are inside those bytes.
  (3) CPL byte comparison (cmp).
  (4) BOM CELL-LEVEL DIFF, parsed as CSV rather than diffed as text: row count,
      designator list AND ORDER, and every cell of every row.

BOARD IDENTITY (md5):
  04_kicad/crow_recorder_central_v2.kicad_pcb        de39e145e856cb14d491770c77d1ec0a
  v1.6/source/crow_recorder_central_v2.kicad_pcb     de39e145e856cb14d491770c77d1ec0a
  v1.7/source/crow_recorder_central_v2.kicad_pcb     de39e145e856cb14d491770c77d1ec0a
  -> the sealed board, the working board and this release's board are ONE FILE.
  Re-measured AFTER `kicad-cli pcb drc --severity-all --refill-zones
  --schematic-parity` ran on a COPY of it: unchanged. (--refill-zones refills
  IN MEMORY; it must not, and did not, write.)

RESULT (2): 17/17 re-plotted gerber+drill artifacts BYTE-IDENTICAL to v1.6's
sealed payload after the timestamp strip (15 zip members + 2 loose drills),
0 differ. Member name sets identical.

RESULT (3): fab/cpl.csv byte-identical, 0 differences, 174 rows. C138030 is the
same 0402 land as C25767 (JLC componentSpecificationEn "0402" on both records,
read live 2026-07-27), so no placement, rotation or datum moved.

RESULT (4): fab/bom.csv
  rows                          49 -> 49
  designator list, IN ORDER     identical
  cells changed                 2, both on R_vb1:
                                  MPN   0402WGF2203TCE -> RC0402FR-07220KL
                                  LCSC  C25767         -> C138030
  Footprint changes             0
  Comment changes               0
  rows added / removed          0 / 0
  md5   v1.6  c6a8ff14c563bd0b8913a5259bfff72d
        v1.7  3cf4ea2bf252b7ac2146b9c5fd8fbc6b

SOURCE-SIDE EVIDENCE (canon M3 — the BOM row moved because the SOURCE moved):
  03_tscircuit/src/crow_recorder_central_v2.tsx  CHANGED: R_vb1 gains
    supplierPartNumbers={ jlcpcb: ["C138030"] }. Before the edit the resolver's
    own ordering was ["C25767", "C138030", "C2909335"] and the exporter takes
    the FIRST — so the out-of-stock code was not chosen by us, and pinning is
    the only way to make the choice reproducible.
  03_tscircuit/build/circuit.json  regenerated by `tsci build`. Element-level
    diff against the pre-edit file: ONE substantive change,
    source_component_116 (R_vb1) supplier_part_numbers
    ["C25767","C138030","C2909335"] -> ["C138030"], plus that component's own
    supplier_footprint_mismatch_warning restating the new code.
  CONTROL, because "the rest is churn" is a claim: a SECOND `tsci build` of the
    SAME (edited) source was diffed against the first — 130 changed elements,
    20 added, 20 removed, and ZERO of them a source_component or a
    supplier_part_numbers field. The churn is the generator's nondeterminism
    (random warning ids, and RJ45 fetch messages alternating between "Component
    not found" and "Failed to fetch"), not this change.
  03_tscircuit/kicad/crow_recorder_central_v2.kicad_sch  md5
    45ffd1dc0f6a11ecdcb49873ef700135 before AND after the rebuild: the
    schematic converter was deliberately not re-run, and the LCSC code appears
    nowhere in the schematic, so the sheet cannot change under this edit.

WHAT IS **NOT** BYTE-IDENTICAL, and why each is the point rather than a defect:
  fab/bom.csv                          CHANGED (the two cells above)
  source/crow_recorder_central_v2.tsx  CHANGED (canon M3)
  fab/*.zip, fab/*.drl                 re-plotted, so their embedded plot
                                       TIMESTAMPS differ from v1.6's bytes
                                       while the plot itself is identical
                                       (result 2). This release ships ITS OWN
                                       outputs rather than carrying forward a
                                       predecessor's.
  verification/**                      this release's own fresh evidence

NO FRESHNESS WAIVERS. There is no verification/freshness_exceptions.txt in this
release. Every identity above is ASSERTED by
`release_freshness_check.py --sourcing-supersede`, a mode promoted under canon
M8 because usb-hub-3s-v3 v1.11 had to state seven of these same claims as prose
waivers a human wrote. An assertion a gate makes beats a waiver a human writes.
