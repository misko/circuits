# STATUS beacon — the live head of the journal

This file is the ONE place a coordinator reads to know, in a glance, where this
board is between gates. It is OVERWRITTEN (not appended) at every transition —
the append-only history lives in `journal/<stage>.md`; this is only the current
frame.

Read by `skills/kicad-pcb/scripts/pcb_status.py` and graded against the tree by
`skills/kicad-pcb/scripts/status_beacon_check.py` (canon M-BEACON). Everything
below the fence is `key: value` (one per line); `#` lines and blanks are
ignored by the reader.

<!-- reader parses from here down -->
stage:   seal
step:    "v1.7 SEALED and LIVE: 07_releases/crow-recorder-central-v2-v1.7-2026-07-27 — the only release of this board with no SUPERSEDED.md. SOURCING supersede of v1.6, NO COPPER CHANGE: C25767 (UNI-ROYAL 0402WGF2203TCE, 220kOhm 0402, R_vb1) went to JLC stockCount 0 after v1.6 sealed. It is ON the CPL (1 x 5 boards), carried no sourcing_plan and no watch-list entry, and AT THE v1.6 SEAL IT MEASURED 16 AND GRADED OK — nothing distinguishes 'clears 5x need' from 'clears it by eleven units'. Substituted at SOURCE (canon M3) to C138030 / YAGEO RC0402FR-07220KL, stock 736704, same 0402 land, catalog describe string CHARACTER-IDENTICAL compared as strings; Q-IDENT exact against the vetted passives ledger's authoritative MPN. Carried in the same release: the two stale v1.4-era ORDER_README sentences corrected, verification/twin_top.png regenerated (v1.6's was byte-identical to v1.5's and FAILED A-RENDER on J2 while the same sealed BOARD passes), the first-ever F-PAYLOAD and A-RENDER reports for this board archived, and the three off-by-one page anchors in 02_parts/XC6227C331PR-G/part.yaml corrected."
measure: "quoted from the SEALED archive, not re-measured here — crow-recorder-central-v2-v1.7-2026-07-27/MANIFEST.txt `gates:`: DRC 0/0/0 (--severity-all --refill-zones --schematic-parity) on a BYTE-VERIFIED COPY, plus standalone-archive DRC on source/ alone 0/0/0; ERC 0 errors (1211 warnings, unchanged from v1.6); policy_audit FAIL=0 PASS=29 HUMAN=6 N-A=3 WAIVED=2; freshness PASS in --sourcing-supersede mode with ZERO waiver files; A-EVID 31 present / 1 conditional absent; F-PAYLOAD OK 6 checks on the shipped zip; A-RENDER OK, all 22 measurable bodies within 1.00mm; F-LEGIBLE OK 0 findings 49 checks; M-BOM PASS 49 lines; A-POP/A-POS PASS, worst datum residual 0.00050mm over 174 rows; A-STOCK PASS re-queried on the FINAL BOM (45/47 coded lines clear; C6938291 has a dated sourcing_plan, C9900035627 is off the CPL); jlc_twin 176 OK, bodies mounted 174/174. Copper identity measured four ways: .kicad_pcb md5 de39e145e856cb14d491770c77d1ec0a identical to v1.6's and to 04_kicad/'s, gerbers+drills RE-PLOTTED from this release's own board 17/17 byte-identical after the timestamp strip, fab/cpl.csv byte-identical (174 rows), fab/bom.csv 49->49 rows with EXACTLY TWO CELLS changed, both on R_vb1."
state:   done
next:    "ORDERABLE. The PRE-ORDER set, none of it waivable here (v1.7 MANIFEST `pre_order:` + `rework:` + `echo_u5:`): F-ECHO — after uploading fab/bom.csv, save JLC's OWN resolved part table and diff it back (bom_legibility_check.py <bom> --echo <saved> against verification/bom_echo_gate.txt, 47 lines) and READ THE DENOMINATOR, not just the verdict; expect exactly one finding, U5 C82317 -> C131025, whose adjudication is ACCEPT (same MPN W25Q16JVSSIQ, brand, package and character-identical spec string at 7.5x the stock). The A-POL JLC order-preview human gate and the BLOCKING U1 270-degree rotation gate. MSL-3 handling for the consigned XU316 (ship with the order). The order-day stock recheck: a stock PASS is NECESSARY AND NOT SUFFICIENT — jlc_stock_check grades LCSC CATALOG stock while JLC's assembly uploader allocates from a different pool (usb-hub v1.10 recorded OK/291 for a line JLC then refused). Expect three JLC messages that are EXPECTED, not errors: 13 designators 'don't exist in the CPL file' (J1, J3-J10, JP_INJ, J_DBG, R_inj1, R_inj2), C9900035627 unavailable, and the U5 redirect. AFTER DELIVERY: the BLOCKING 33pF C0G feedforward rework on all 5 boards (one across R_fb1a, one across R_fb2a — AP61102 DS42004 Rev6-2 Table 1) before the first-article gates."
op_pid:
updated: 2026-07-27T21:05:00
