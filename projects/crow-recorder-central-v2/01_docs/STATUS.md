# STATUS beacon — the live head of the journal

This file is the ONE place a coordinator reads to know, in a glance, where this
board is between gates. It is OVERWRITTEN (not appended) at every transition —
the append-only history lives in `journal/<stage>.md`; this is only the current
frame.

Read by `skills/kicad-pcb/scripts/pcb_status.py` and graded against the tree by
`skills/kicad-pcb/scripts/status_beacon_check.py` (canon M-BEACON). Everything
below the fence is `key: value` (one per line); `#` lines and blanks are
ignored by the reader.

<!-- reader parses from here down -->
stage:   seal
step:    "v1.7 SEALED, LIVE and ORDERABLE, UNCHANGED — crow-recorder-central-v2-v1.7-2026-07-27, still the only release of this board with no SUPERSEDED.md. Landed on top of it 2026-07-28: a FIRMWARE + DOCS ONLY revision (CHANGELOG entry, `Released: no` — NOT a release, and it deliberately names no release version here), fixing the SIBLING board's defect CAL-1, then RETUNED the same day when the USER CHANGED THE ACCEPTANCE CRITERION. crow-mic-pod-v2's calibration transducer saturates the preamp it exists to calibrate; the POD cannot fix it (its divider was measured unable to clear the guaranteed spec by ANY value, best +0.86 dB, optimum in the OPPOSITE direction from the audit's 33k/18k), so the drive is cut HERE. THE CRITERION MOVED: sizing against LS1's datasheet MINIMUM was the wrong tolerance end for a CLIPPING problem — the unit that clips is a LOUD one — so the binding case is now a unit on the datasheet TYPICAL curve (~104 dB @10cm at 3.9 kHz), i.e. 110.8173 dB SPL at the pod capsule against a 101.3144 dB ceiling, SHORTFALL 9.5028 dB (was 5.5028). Duty retuned 1/6 -> 1/12. Established first, from the SEALED v1.7 netlist rather than the source, that any of this is reachable: PLUS5V_BEEP is the 5V rail through a ferrite bead with NO series resistor and NO regulator (11 nodes); BEEP_RETURN is ONE AO3400A for all EIGHT ports (10 nodes, BRIEF D1); BEEP_GATE has exactly TWO nodes, U1.122 and R_bg1.1. There is NO analog level control on this board, so the XU316 GPIO waveform is the only lever and the level is a firmware constant by construction. NO copper, BOM, netlist or release moved on either board; 04_kicad/ and 07_releases/ were not written; the pod's crow-mic-pod-v2-v1.3-2026-07-27 stays live and carries no SUPERSEDED.md."
measure: "re-derived here, not inherited. Geometry by pcbnew on a COPY of the SEALED pod board (sha256 2f936fd8..., verified unchanged): LS1 (33.000, 46.000), MK1 (74.000, 26.000), |d| = 45.61798 mm -> +6.8173 dB of distance term; pod mic at ITS 3k9 load 80.680 mV/Pa; worst-case ceiling 101.3144 dB. Minimum-spec LS1 -> 106.8173 dB (shortfall 5.5028); TYPICAL-curve LS1 -> 110.8173 dB (shortfall 9.5028, THE BINDING NUMBER). FIX: 05_firmware/cal_burst.c, CAL_BURST_DUTY_NUM/DEN = 1/12 -> 20*log10(sin(pi/12)) = -11.7401 dB NOMINAL -> 99.0772 dB typical (clears +2.2372) / 95.0772 dB minimum-spec (clears +6.2372). **THE sin(pi*D) LAW IS NOT A CONSERVATIVE BOUND AT THIS DUTY** — it WAS at 1/6 and that conclusion is NOT carried forward. Re-verified with an exact per-step exponential integration: (a) L-R REGIME CHANGE, the 20.8 us pulse no longer lets current build and the L=3mH corner returns -10.905 dB vs the law -11.740, non-conservative by +0.835 dB; (b) GATE-RC DUTY BIAS, the DOMINANT and previously unnoticed term — turn-ON waits only for the gate to CLIMB to Vgs(th) (20-44% of 3.3V) but turn-OFF waits for it to FALL from ~3.26V DOWN to Vgs(th) (56-80%), stretching the conduction window +1.11..+6.47 us, which is 5% of the pulse at D=1/2 but 31% at D=1/12. COMBINED WORST CASE -8.71 dB, not -11.74; slack +3.03 dB; under it the TYPICAL unit MISSES by 0.79 dB while minimum-spec clears by +3.21 dB. Model sanity by a second method (canon M1): datasheet MEASURED 150 mA at 1/2 duty vs ANALYTIC volt-second balance 151.7 mA, 1.1% apart. Electrical->acoustic is ESTIMATED, not specified — no SPL-vs-drive curve exists. FAR END re-checked at the new duty and still fine: far-pod matched-filter SNR 42.95 dB at 25 dB(1/3-oct) ambient and 22.95 dB at 45 dB, timing sigma 0.28-2.83 us vs a 20.83 us sample; local path 95.077 dB SPL, 81.1 dB above the mic self-noise. GATES: `make test` PASS 0 failures; RED-VERIFIED against the NEW criterion by recompiling at DEN=2 and DEN=6 — 6 failures and exit 1 BOTH times — and both are also always-run inline known-bad fixtures. M-BEACON PASS both boards; policy_audit --skip-drc 0 FAIL both; contracts_audit holds both boards at their pre-existing 10-file baseline, 0 added."
state:   done
next:    "ONE OPEN ITEM, and it is the USER'S CALL: the margin is met NOMINALLY (+2.2372 dB) and MISSED under the WORST-CASE model (-0.79 dB). The structural finding is that the OPEN-LOOP UNCERTAINTY (~3 dB) EXCEEDS THE CRITERION (2.24 dB), so this level CANNOT be set open-loop to the accuracy the criterion demands. Two cheap ways forward: (a) TRIM AGAINST A MEASUREMENT at bring-up — raise CAL_BURST_DUTY_DEN until the measured capsule level is <= 101.3 dB SPL; scoping BEEP_RETURN at TP11 against the commanded pulse reads the gate-RC stretch directly and collapses most of the 3 dB on its own; instruments: a calibrated 1/4-inch mic at 10 cm on axis, or the pod MK1 with the recorder as the meter, D=1/2 vs the shipped duty in the 4 kHz band. (b) TAKE THE WORST CASE OPEN-LOOP at DEN=14, the first value clearing it (+0.11 dB; 16 -> +0.93, 20 -> +2.41) — a one-line change, ladder in cal_burst.c, trim floor now 24. ALSO OWED: the operator-facing copy of this constraint belongs in the NEXT release ORDER_README.md — all eight existing ones sit inside immutable sealed releases and cannot be retro-filled. UNCHANGED and still owed from v1.7: the F-ECHO / A-POL / U1-270-degree-rotation pre-order gates, MSL-3 handling for the consigned XU316, the order-day stock recheck (a stock PASS is necessary and not sufficient), and the BLOCKING 33pF C0G feedforward rework on all 5 boards after delivery. REPORTED AGAINST TOOLING, NOT APPLIED (this pass may not edit skills/, and the finding is queued for a dedicated gate agent): the 05_firmware contracts.md template Allowed cell `*.c|*.h|*.rs|*.py` is split on the markdown pipe by contracts_audit.parse_allowed, so only `*.c` survives and EVERY firmware .h in this repo fails C-ALLOW — six live failures in archived_projects/ prove it."
op_pid:
updated: 2026-07-28T13:55:00
