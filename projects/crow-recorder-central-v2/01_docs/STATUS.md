# STATUS beacon — the live head of the journal

This file is the ONE place a coordinator reads to know, in a glance, where this
board is between gates. It is OVERWRITTEN (not appended) at every transition —
the append-only history lives in `journal/<stage>.md`; this is only the current
frame.

Read by `skills/kicad-pcb/scripts/pcb_status.py` and graded against the tree by
`skills/kicad-pcb/scripts/status_beacon_check.py` (canon M-BEACON). Everything
below the fence is `key: value` (one per line); `#` lines and blanks are
ignored by the reader.

<!-- reader parses from here down -->
stage:   seal
step:    "v1.7 SEALED, LIVE and ORDERABLE, UNCHANGED — crow-recorder-central-v2-v1.7-2026-07-27, still the only release of this board with no SUPERSEDED.md. NOTE THE CHANGELOG BANNER: the three most recent CHANGELOG entries are FIRMWARE + DOCS ONLY, no 07_releases/ directory exists or ever will for them, and the board you would order today is the v1.7 named above — this beacon deliberately names no other release version. Those three entries fix the SIBLING board's defect CAL-1 — crow-mic-pod-v2's calibration transducer saturates the preamp it exists to calibrate, and the POD cannot fix it (its divider was measured unable to clear the guaranteed spec by ANY value), so the drive is cut HERE. The criterion moved to the datasheet TYPICAL curve (sizing a CLIPPING problem against the MINIMUM is the wrong tolerance end — the unit that clips is a LOUD one): 110.8173 dB SPL at the pod capsule against a 101.3144 dB ceiling, SHORTFALL 9.5028 dB. Duty settled at 1/20 after two retunes (1/6 -> 1/12 -> 1/20). Reachable at all only because of what the SEALED netlist shows: PLUS5V_BEEP is the 5V rail through a ferrite bead with NO series resistor and NO regulator (11 nodes); BEEP_RETURN is ONE AO3400A for all EIGHT ports (10 nodes, BRIEF D1); BEEP_GATE has exactly TWO nodes, U1.122 and R_bg1.1. There is NO analog level control on this board, so the GPIO waveform is the only lever. NO copper, BOM, netlist or release moved on either board; 04_kicad/ and 07_releases/ not written; the pod's crow-mic-pod-v2-v1.3-2026-07-27 stays live and carries no SUPERSEDED.md."
measure: "all re-derived, none inherited. Geometry by pcbnew on a COPY of the SEALED pod board (sha256 2f936fd8..., unchanged): |LS1-MK1| = 45.61798 mm -> +6.8173 dB distance term; pod mic at ITS 3k9 load 80.680 mV/Pa; worst-case ceiling 101.3144 dB. TYPICAL-curve LS1 -> 110.8173 dB, shortfall 9.5028 dB (the binding number). SHIPPED: 05_firmware/cal_burst.c CAL_BURST_DUTY_NUM/DEN = 1/20 -> NOMINAL -16.1134 dB (typical +6.6105, min-spec +10.6105) and WORST CASE -11.9165 dB (typical +2.4136, min-spec +6.4136) — THE CRITERION IS MET UNDER THE WORST-CASE MODEL, so the self-test FATAL assertion was promoted to that form. sin(pi*D) is NOT a bound and is re-verified FROM SCRATCH at every retune, never extrapolated, because the L-R term is NON-MONOTONIC IN DUTY: conservative at 1/6, +0.835 dB non-conservative at 1/12, CONSERVATIVE AGAIN at 1/20 (worst slack -0.087 dB over the full 10uH-3mH sweep). At 1/20 the GATE-RC DUTY BIAS dominates outright — stretch +6.19/+3.19/+0.77 us at Vgs(th) 0.65/1.05/1.45, worth +3.450/+1.954/+0.514 dB, and as a fraction of the commanded pulse 5.2% at 1/2, 31.1% at 1/12, 49.5% AT 1/20: nearly half the conduction window at the shipped duty is gate artefact, not commanded drive, and it has NEVER BEEN MEASURED. Combined slack +4.197 dB. FAR END STILL CLOSES at the extra 4.37 dB (quoted at NOMINAL, the pessimistic end since worst-case drive is LOUDER): SNR 48.57/38.57/28.57/18.57 dB at 15/25/35/45 dB(1/3-oct) ambient, timing sigma 0.148/0.469/1.483/4.688 us vs a 20.83 us sample; local path 90.704 dB SPL, 76.7 dB above the mic self-noise. Coil average current ~4-15 mA (deep DCM), shared Q2 ~25-90 mA, pod capsule headroom to its 110 dB THD limit 15.30/19.30 dB. Timer quantization now EXACT (25000/20 = 1250 ticks, 0.0000 dB; it was -0.0014 dB at 1/12). TRIM FLOOR re-derived 24 -> 36, and the earlier "worst case saturates" rationale RETRACTED by measurement (1/20->1/40: nominal gains 5.99 dB, worst case 5.24 dB — they track within ~0.75 dB per doubling); the real floor is the gate reaching the AO3400A 2.5 V Rdson spec point, which fails at den ~ 37.5. GATES: make test PASS 0 failures; RED-VERIFIED by recompilation at DEN = 2, 6 AND 12 — exit 1 each, worst-case typical margin -10.2236 / -5.3586 / -0.7897 dB — and all three also run inline every time. M-BEACON PASS both boards; policy_audit --skip-drc 0 FAIL both; contracts_audit holds both boards at their pre-existing 10-file baseline, 0 added."
state:   done
next:    "ONE THING OWED, and it is a BENCH MEASUREMENT, not a design decision: the TP11 gate-RC stretch measurement, now a NORMATIVE bring-up step in 01_docs/CHECKLIST.md (new Bring-up section), repeated in 05_firmware/README.md and the cal_burst.c header because a person doing bring-up does not read .c files. Scope CH1 on U1.122 (COMMANDED pulse) against CH2 on TP11 / BEEP_RETURN (ACTUAL conduction window, low while Q2 conducts), subtract, record the stretch with the board serial in 01_docs/journal/verify.md. Expect +0.8..+6.2 us. WHAT IT LICENSES: once the stretch is known for real parts the Vgs(th) sweep collapses to a single number, the +4.20 dB open-loop uncertainty collapses with it, and THE DUTY MAY BE TIGHTENED BACK TOWARD 1/14..1/16 WITH EVIDENCE, recovering ~2-4 dB of burst level and far-pod SNR. Until then 1/20 stands and the duty must NOT be tightened on the strength of the model alone. Paired acoustic cross-check: a pod MK1 with the recorder as the meter, acceptance measured capsule level <= 101.3 dB SPL. ALSO RECOMMENDED, FLAGGED NOT APPLIED: give firmware its own version series (fw-vN.N, or a 05_firmware/CHANGELOG.md) — one series currently means two things, and renumbering rewrites committed history other records cite by number, so it is the owner's call; a banner at the top of CHANGELOG.md makes the distinction unmissable in the meantime. UNCHANGED and still owed from v1.7: the F-ECHO / A-POL / U1-270-degree-rotation pre-order gates, MSL-3 handling for the consigned XU316, the order-day stock recheck, the BLOCKING 33pF C0G feedforward rework on all 5 boards after delivery, and the operator-facing copy of the CAL-1 constraint in the NEXT release ORDER_README.md. Reported against tooling, NOT applied (queued for a gate agent): the 05_firmware contracts.md template Allowed cell is split on the markdown pipe by contracts_audit, so every firmware .h in this repo fails C-ALLOW."
op_pid:
updated: 2026-07-28T15:45:00
