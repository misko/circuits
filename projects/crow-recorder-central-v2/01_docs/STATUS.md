# STATUS beacon — the live head of the journal

This file is the ONE place a coordinator reads to know, in a glance, where this
board is between gates. It is OVERWRITTEN (not appended) at every transition —
the append-only history lives in `journal/<stage>.md`; this is only the current
frame.

Read by `skills/kicad-pcb/scripts/pcb_status.py` and graded against the tree by
`skills/kicad-pcb/scripts/status_beacon_check.py` (canon M-BEACON). Everything
below the fence is `key: value` (one per line); `#` lines and blanks are
ignored by the reader.

<!-- reader parses from here down -->
stage:   seal
step:    "v1.7 SEALED, LIVE and ORDERABLE, UNCHANGED — 07_releases/crow-recorder-central-v2-v1.7-2026-07-27, still the only release of this board with no SUPERSEDED.md. Landed on top of it 2026-07-28: a FIRMWARE + DOCS ONLY revision (CHANGELOG entry, `Released: no` — NOT a release, and it deliberately names no release version here), fixing the SIBLING board's defect CAL-1. crow-mic-pod-v2's calibration transducer saturates the preamp it exists to calibrate — LS1 sits 45.61798 mm from MK1 and puts 106.8173 dB SPL on the capsule against a 101.3144 dB worst-case OPA1678 input-common-mode ceiling, shortfall 5.5028 dB — and the POD cannot fix it: its divider was measured unable to clear the guaranteed spec by ANY value (best +0.86 dB, and its optimum runs the OPPOSITE way from the audit's proposed 33k/18k). USER DECISION: cut the drive ~6 dB HERE instead of respinning the pod. Established FIRST, from the SEALED v1.7 netlist rather than the source, that this is reachable at all — PLUS5V_BEEP is the 5V rail through a ferrite bead with NO series resistor and NO regulator (11 nodes); BEEP_RETURN is ONE AO3400A for all EIGHT ports (10 nodes, BRIEF D1); BEEP_GATE has exactly TWO nodes, U1.122 and R_bg1.1. There is NO analog level control on this board, so the XU316 GPIO waveform is the only lever and the level is a firmware constant by construction. 05_firmware/ held nothing but its template contracts.md (as does every board in this tree), so the constant was landed there with its derivation, a trim ladder, a trim floor and a host test. NO copper, BOM, netlist or release moved on either board; 04_kicad/ and 07_releases/ were not written; the pod's crow-mic-pod-v2-v1.3-2026-07-27 stays live and carries no SUPERSEDED.md."
measure: "re-derived here, not inherited. Geometry by pcbnew on a COPY of the SEALED pod v1.3 board (sha256 2f936fd8..., verified unchanged): LS1 (33.000, 46.000), MK1 (74.000, 26.000), |d| = 45.61798 mm -> burst 106.8173 dB SPL; pod mic at ITS 3k9 load 80.680 mV/Pa; worst-case ceiling 101.3144 dB; SHORTFALL 5.5028 dB. FIX: 05_firmware/cal_burst.c, CAL_BURST_DUTY_NUM/DEN = 1/6 -> 20*log10(sin(pi/6)) = -6.0206 dB of 4 kHz fundamental -> 100.7967 dB at the capsule, CLEARS BY 0.5178 dB. MODEL, stated because it is not free: the output follows the 4 kHz FUNDAMENTAL, not the RMS — CMT-8504 ds rev 1.04 p.3 is a sharp resonance peaking ~104 dB at ~3.9 kHz and a resonator passes the component AT resonance, so a 1/6-duty square is -4.8 dB in RMS but exactly -6.02 dB in fundamental. sin(pi*D) holds at BOTH ends of the UNSPECIFIED coil inductance and a numerical L-R-diode integration over L = 20uH..3mH returns -6.02..-6.68 dB, so the law is a CONSERVATIVE bound. Cross-checked by a second method (canon M1): the datasheet's MEASURED 150 mA at 1/2 duty against an ANALYTIC volt-second balance of 151.7 mA, 1.1% apart. Electrical->acoustic is ESTIMATED, not specified — the datasheet has NO SPL-vs-drive curve, one trace at one level. FAR END CHECKED AND NOT BROKEN: all six pods fire from the one shared FET, so each pod's dominant path is its OWN LS1; the five others on the 7.62 m-radius array (separations 7.62/13.20/15.24 m) power-sum to 67.0 dB SPL, 39.8 dB BELOW the local path, and the far-pod matched-filter SNR after -6 dB is 48.7 dB at a 25 dB(1/3-octave) ambient and 28.7 dB at 45 dB, for a timing sigma of 0.15-1.5 us against a 20.83 us sample period. GATES: `make test` PASS, 0 failures, and RED-VERIFIED — rebuilt on a COPY at the pre-fix DEN=2 it reports 5 failures including MARGIN -5.5028 dB and exits 1. contracts_audit holds both boards at their pre-existing 10-file baseline, 0 added."
state:   done
next:    "TWO things are owed, neither blocking the order. (1) USER CALL — the -6 dB is sized from LS1's datasheet MINIMUM output. A unit on the datasheet's own TYPICAL curve (~104 dB @10cm at 3.9 kHz, rev 1.04 p.3) lands at 110.8173 dB and stays 3.48 dB OVER the ceiling even after -6.02 dB. The level must be TRIMMED AGAINST A MEASUREMENT at bring-up: ladder in cal_burst.c (den 8 = -8.34 dB, 10 = -10.20, 12 = -11.74 which clears a typical unit; floor at 16, where the 4.7 us gate RC stops fully enhancing Q2). To measure: a calibrated 1/4-inch mic at 10 cm on axis, or the pod's own MK1 with the recorder as the meter, D=1/2 vs D=1/6 in the 4 kHz band. (2) The OPERATOR-FACING copy of this constraint is owed to the NEXT release's ORDER_README.md — all eight existing ones sit inside immutable sealed releases and cannot be retro-filled. UNCHANGED and still owed from v1.7: the F-ECHO / A-POL / U1-270-degree-rotation pre-order gates, MSL-3 handling for the consigned XU316, the order-day stock recheck (a stock PASS is necessary and not sufficient), and the BLOCKING 33pF C0G feedforward rework on all 5 boards after delivery. REPORTED AGAINST TOOLING, NOT APPLIED (this pass may not edit skills/): the 05_firmware contracts.md template's Allowed cell `*.c|*.h|*.rs|*.py` is split on the markdown pipe by contracts_audit.parse_allowed, so only `*.c` survives and EVERY firmware .h in this repo fails C-ALLOW — six live failures in archived_projects/ prove it. That is why the constant shipped as a .c and not a .h."
op_pid:
updated: 2026-07-28T12:10:00
